diff --git a/dist/ai-sdk.d.ts b/dist/ai-sdk.d.ts
index 063270cc49ef037279ba1f11d81ed5b8932ae785..201cc5e97d6353c24ac614b4f09dc332bdcd8336 100644
--- a/dist/ai-sdk.d.ts
+++ b/dist/ai-sdk.d.ts
@@ -1,4 +1,4 @@
-import { Ln as AgentModelExecutionOptions, Qr as MaybePromise, f as AgentAttachmentExecutionOptions, i as AgentAdapter, ji as Message, l as AgentAdapterMetadataContext, lr as AgentRuntimeConfig, o as AgentAdapterInstructions, xr as AgentToolResolverWithWorkspace } from "./types-C4oyRXNr.js";
+import { $r as MaybePromise, Mi as Message, Rn as AgentModelExecutionOptions, Sr as AgentToolResolverWithWorkspace, f as AgentAttachmentExecutionOptions, i as AgentAdapter, l as AgentAdapterMetadataContext, o as AgentAdapterInstructions, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
 import { Agent, ModelMessage, ToolLoopAgentSettings, ToolSet } from "ai";
 import { WorkspaceName } from "@vite-hub/workspace";
 
diff --git a/dist/ai-sdk.js b/dist/ai-sdk.js
index 49b564a76442447d88e3813b5d806c7267dc3591..57bbcedd169963cd12cf82b856763f55350398ac 100644
--- a/dist/ai-sdk.js
+++ b/dist/ai-sdk.js
@@ -4,7 +4,7 @@ import { t as loadAiSdk } from "./ai-sdk-runtime-DSI5i7Mj.js";
 import { j as agentInvocationCallbackContextValues, st as resolveMessageChannelInstructions, t as applyCapabilityToolTransforms, tt as markMessageChannelInstructionConsumer } from "./capability-runtime-Be85oBbC.js";
 import { t as composeInstructionDocument } from "./instruction-composition-CBkhC2F8.js";
 import { l as synthesizedAgentOutputSymbol } from "./agent-output-CyEZfgrQ.js";
-import { d as toJsonCompatibleValue, f as withAgentToolStepReporting, l as applyAgentToolPolicies, m as agentOutputInstructions, n as hasAgentTraceLog, p as withJsonCompatibleToolOutputs, t as aiSdkTelemetryIntegration, u as reportWorkspaceMaterialization } from "./trace-DtP8Cj3K.js";
+import { _ as withAgentToolStepReporting, g as toJsonCompatibleValue, h as reportWorkspaceMaterialization, m as applyAgentToolPolicies, n as hasAgentTraceLog, t as aiSdkTelemetryIntegration, v as withJsonCompatibleToolOutputs, y as agentOutputInstructions } from "./trace-GIqXV-YM.js";
 //#region src/ai-sdk.ts
 function isDataMessagePart(part) {
 	return part.type === "data" || part.type.startsWith("data-");
diff --git a/dist/box-sandbox-BUw2Q0iH.js b/dist/box-sandbox-BUw2Q0iH.js
new file mode 100644
index 0000000000000000000000000000000000000000..ae685056ca86c129df85b7f3bb896c26fdf1b3fe
--- /dev/null
+++ b/dist/box-sandbox-BUw2Q0iH.js
@@ -0,0 +1,157 @@
+import { S as openHarnessBox } from "./src-CktB4unw.js";
+import { posix } from "node:path";
+//#region src/harness/box-sandbox.ts
+const harnessRemoveDirectory = Symbol.for("vitehub.harnessRemoveDirectory");
+function streamFromBytes(bytes) {
+	return new ReadableStream({ start(controller) {
+		controller.enqueue(bytes);
+		controller.close();
+	} });
+}
+async function bytesFromStream(stream) {
+	const chunks = [];
+	const reader = stream.getReader();
+	while (true) {
+		const { done, value } = await reader.read();
+		if (done) break;
+		chunks.push(value);
+	}
+	const bytes = new Uint8Array(chunks.reduce((size, chunk) => size + chunk.byteLength, 0));
+	let offset = 0;
+	for (const chunk of chunks) {
+		bytes.set(chunk, offset);
+		offset += chunk.byteLength;
+	}
+	return bytes;
+}
+function resolvePath(session, path) {
+	return path.startsWith("/") ? posix.normalize(path) : posix.resolve(session.cwd, path);
+}
+function adaptProcess(process) {
+	return {
+		pid: process.pid,
+		stderr: process.stderr,
+		stdout: process.stdout,
+		async kill(signal) {
+			await process.kill(signal);
+		},
+		async wait() {
+			return { exitCode: (await process.wait()).code };
+		}
+	};
+}
+function adaptBoxSession(session) {
+	if (!session.spawn) throw new Error("[vitehub] Harness Agent Drivers require a Box runtime with process spawning.");
+	return {
+		async [harnessRemoveDirectory](path) {
+			const directory = resolvePath(session, path);
+			await session.files.remove(directory, { recursive: true });
+			for (const parent of [posix.dirname(directory), posix.dirname(posix.dirname(directory))]) {
+				if (await session.files.list(parent).then((entries) => entries.length > 0).catch(() => true)) break;
+				await session.files.remove(parent);
+			}
+		},
+		defaultWorkingDirectory: session.cwd,
+		description: `ViteHub Box ${session.id}`,
+		id: session.id,
+		ports: session.ports?.values || [],
+		async destroy() {
+			await session.close();
+		},
+		async getPortUrl({ port, protocol = "http" }) {
+			if (!session.ports) throw new Error(`[vitehub] Box runtime does not expose ports.`);
+			return String(await session.ports.expose(port, { protocol }));
+		},
+		async readBinaryFile({ path }) {
+			return await session.files.read(resolvePath(session, path));
+		},
+		async readFile({ path }) {
+			const bytes = await this.readBinaryFile({ path });
+			return bytes ? streamFromBytes(bytes) : null;
+		},
+		async readTextFile({ encoding = "utf8", endLine, path, startLine }) {
+			const bytes = await this.readBinaryFile({ path });
+			if (!bytes) return null;
+			const text = Buffer.from(bytes).toString(encoding);
+			if (startLine === void 0 && endLine === void 0) return text;
+			return text.split(/\r?\n/).slice((startLine || 1) - 1, endLine).join("\n");
+		},
+		restricted() {
+			return this;
+		},
+		async run({ abortSignal, command, env, workingDirectory }) {
+			const result = await session.exec("sh", ["-lc", command], {
+				cwd: resolvePath(session, workingDirectory || session.cwd),
+				env,
+				signal: abortSignal
+			});
+			return {
+				exitCode: result.code,
+				stderr: result.stderr,
+				stdout: result.stdout
+			};
+		},
+		async spawn({ abortSignal, command, env, workingDirectory }) {
+			return adaptProcess(await session.spawn("sh", ["-lc", command], {
+				cwd: resolvePath(session, workingDirectory || session.cwd),
+				env,
+				signal: abortSignal
+			}));
+		},
+		async stop() {
+			await session.close();
+		},
+		async writeBinaryFile({ content, path }) {
+			const target = resolvePath(session, path);
+			await session.files.mkdir(posix.dirname(target), { recursive: true });
+			await session.files.write(target, content);
+		},
+		async writeFile({ content, path }) {
+			await this.writeBinaryFile({
+				content: await bytesFromStream(content),
+				path
+			});
+		},
+		async writeTextFile({ content, encoding = "utf8", path }) {
+			await this.writeBinaryFile({
+				content: Buffer.from(content, encoding),
+				path
+			});
+		}
+	};
+}
+async function adaptOrClose(session) {
+	try {
+		return adaptBoxSession(session);
+	} catch (error) {
+		await session.close().catch(() => void 0);
+		throw error;
+	}
+}
+function createBoxHarnessSandbox(box) {
+	return {
+		executionAuthority: box.plan.executionAuthority,
+		providerId: box.plan.runtime,
+		specificationVersion: "harness-sandbox-v1",
+		async createSession(options) {
+			let adapted;
+			const session = await openHarnessBox(box, {
+				id: options?.sessionId,
+				signal: options?.abortSignal,
+				initialize: options?.onFirstCreate ? async (boxSession, { signal }) => {
+					adapted = adaptBoxSession(boxSession);
+					await options.onFirstCreate(adapted, { abortSignal: signal });
+				} : void 0
+			});
+			return adapted || await adaptOrClose(session);
+		},
+		async resumeSession(options) {
+			return await adaptOrClose(await openHarnessBox(box, {
+				id: options.sessionId,
+				signal: options.abortSignal
+			}));
+		}
+	};
+}
+//#endregion
+export { createBoxHarnessSandbox };
diff --git a/dist/capabilities.d.ts b/dist/capabilities.d.ts
index 7e4c521a3300f7a33fa4bafe674ede806ce041f2..9785bb6577de83682285688b713f4341d4b98477 100644
--- a/dist/capabilities.d.ts
+++ b/dist/capabilities.d.ts
@@ -1,7 +1,7 @@
-import { $n as AgentRunCallbackContext, At as AgentChatStateResolver, B as AgentCapabilityRuntimeContext, Ct as AgentChatOptions, Dt as AgentChatSendMessage, Et as AgentChatPlatformsResolver, Ht as AgentFinishEvent, Ir as AgentUsageRecord, It as AgentDriver, M as AgentCapabilityContext, N as AgentCapabilityDefinition, Nr as AgentUsage, Ot as AgentChatSessionOptions, Pr as AgentUsageCost, Qr as MaybePromise, R as AgentCapabilityMode, St as AgentChatMessageHookArgs, Ti as AudioPart, Tt as AgentChatPlatformResolver, V as AgentCapabilityTypeContract, Vn as AgentModelResolver, _r as AgentToolDefinition, _t as AgentChatErrorHookArgs, ar as AgentRunMetadata, bn as AgentInvocationContextStore, bt as AgentChatFinishExtension, ft as AgentChannelWebhookRegistrationDefinition, gt as AgentChatCapabilityOptions, h as AgentCallbackContext, ht as AgentChatAgentHookArgs, ji as Message, jt as AgentChatTriggerHistory, kt as AgentChatStateContext, lr as AgentRuntimeConfig, mt as AgentChatAgentBindingOptions, n as AgentAccessWorkspaceScopeContext, on as AgentHostIdentity, r as AgentActor, rr as AgentRunInput, sn as AgentInput, t as AgentAccessInvocationContextValue, ur as AgentRuntimeContext, vr as AgentToolPolicyContext, vt as AgentChatEventHookArgs, wn as AgentInvoker, wr as AgentToolStandardSchema, wt as AgentChatPlatformAdapter, xt as AgentChatMessage, yr as AgentToolPolicyDecision, yt as AgentChatEventHooks, zr as AgentWebhookRegistrationDefinition } from "./types-C4oyRXNr.js";
-import { a as AgentChatRunContext, c as getAgentChatContext, i as AgentChatOptionsOrigin, l as AgentChatMessageTriggerInput, n as AgentChatCapabilityOrigin, o as agentChatContextKey, r as AgentChatContext, s as chat, t as AgentChannelContext } from "./chat-trigger-BXf37awy.js";
-import { A as RepositoryHostWriteRequest, C as RepositoryHostProvider, D as RepositoryHostTargetKind, E as RepositoryHostTarget, O as RepositoryHostToolPolicy, S as RepositoryHostOptions, T as RepositoryHostReadRequest, _ as RepositoryHostContextTargetValue, a as PullRequestContextComment, b as repositoryHostContext, c as PullRequestContextRef, d as RepositoryHostContextCapabilityFactory, f as RepositoryHostContextInput, g as RepositoryHostContextTargetResolver, h as RepositoryHostContextTarget, i as JsonValue, j as repositoryHost, k as RepositoryHostWriteOperation, l as PullRequestContextUser, m as RepositoryHostContextResolver, n as JsonObject, o as PullRequestContextFile, p as RepositoryHostContextOptions, r as JsonPrimitive, s as PullRequestContextMetadata, t as AsyncRecord, u as PullRequestContextValue, v as RepositoryHostContextValue, w as RepositoryHostReadOperation, x as RepositoryHostClient, y as RepositoryHostIssueContext } from "./repository-host-context-DAVFc6T_.js";
-import { a as McpToolFingerprints, i as McpServerConfig, n as McpClient, r as McpClientConfig, t as McpCapabilityOptions } from "./types-DPF3rS0I.js";
+import { $r as MaybePromise, At as AgentChatStateContext, B as AgentCapabilityRuntimeContext, Br as AgentWebhookRegistrationDefinition, Ct as AgentChatMessageHookArgs, Dt as AgentChatPlatformsResolver, Ei as AudioPart, Et as AgentChatPlatformResolver, Fr as AgentUsageCost, Hn as AgentModelResolver, Lr as AgentUsageRecord, Lt as AgentDriver, M as AgentCapabilityContext, Mi as Message, Mt as AgentChatTriggerHistory, N as AgentCapabilityDefinition, Ot as AgentChatSendMessage, Pr as AgentUsage, R as AgentCapabilityMode, St as AgentChatMessage, Tn as AgentInvoker, Tr as AgentToolStandardSchema, Tt as AgentChatPlatformAdapter, Ut as AgentFinishEvent, V as AgentCapabilityTypeContract, _t as AgentChatCapabilityOptions, br as AgentToolPolicyDecision, bt as AgentChatEventHooks, cn as AgentInput, dr as AgentRuntimeContext, er as AgentRunCallbackContext, gt as AgentChatAgentHookArgs, h as AgentCallbackContext, ht as AgentChatAgentBindingOptions, ir as AgentRunInput, jt as AgentChatStateResolver, kt as AgentChatSessionOptions, n as AgentAccessWorkspaceScopeContext, or as AgentRunMetadata, pt as AgentChannelWebhookRegistrationDefinition, r as AgentActor, sn as AgentHostIdentity, t as AgentAccessInvocationContextValue, ur as AgentRuntimeConfig, vr as AgentToolDefinition, vt as AgentChatErrorHookArgs, wt as AgentChatOptions, xn as AgentInvocationContextStore, xt as AgentChatFinishExtension, yr as AgentToolPolicyContext, yt as AgentChatEventHookArgs } from "./types-CW2XmfE7.js";
+import { a as AgentChatRunContext, c as getAgentChatContext, i as AgentChatOptionsOrigin, l as AgentChatMessageTriggerInput, n as AgentChatCapabilityOrigin, o as agentChatContextKey, r as AgentChatContext, s as chat, t as AgentChannelContext } from "./chat-trigger-mwxwRC8l.js";
+import { A as RepositoryHostWriteRequest, C as RepositoryHostProvider, D as RepositoryHostTargetKind, E as RepositoryHostTarget, O as RepositoryHostToolPolicy, S as RepositoryHostOptions, T as RepositoryHostReadRequest, _ as RepositoryHostContextTargetValue, a as PullRequestContextComment, b as repositoryHostContext, c as PullRequestContextRef, d as RepositoryHostContextCapabilityFactory, f as RepositoryHostContextInput, g as RepositoryHostContextTargetResolver, h as RepositoryHostContextTarget, i as JsonValue, j as repositoryHost, k as RepositoryHostWriteOperation, l as PullRequestContextUser, m as RepositoryHostContextResolver, n as JsonObject, o as PullRequestContextFile, p as RepositoryHostContextOptions, r as JsonPrimitive, s as PullRequestContextMetadata, t as AsyncRecord, u as PullRequestContextValue, v as RepositoryHostContextValue, w as RepositoryHostReadOperation, x as RepositoryHostClient, y as RepositoryHostIssueContext } from "./repository-host-context-DB2_VeVi.js";
+import { a as McpToolFingerprints, i as McpServerConfig, n as McpClient, r as McpClientConfig, t as McpCapabilityOptions } from "./types-BZoKvU1a.js";
 import { TraceContext, ViteHubError } from "@vite-hub/runtime";
 import { StandardSchemaV1 } from "@standard-schema/spec";
 import { WorkspaceContent, WorkspaceName, WorkspaceSourceInput } from "@vite-hub/workspace";
@@ -777,6 +777,7 @@ declare function vercelAiGatewayPricing(options?: VercelAiGatewayPricingOptions)
 //#endregion
 //#region src/capabilities/usage-cost.d.ts
 interface UsageCostOptions {
+  format?: "usd";
   pricing?: AgentUsagePricing;
 }
 declare global {
diff --git a/dist/capabilities.js b/dist/capabilities.js
index d221ddaba389735fa353d40d5f80cb81a779abfe..f5ae39c0dd022f3239b900e1a0b0029d373479cb 100644
--- a/dist/capabilities.js
+++ b/dist/capabilities.js
@@ -3,7 +3,7 @@ import { c as isAsyncIterable$1, d as withAsyncIterator, i as publishWorkspaceAr
 import { t as loadAiSdk } from "./ai-sdk-runtime-DSI5i7Mj.js";
 import { A as withResolvedAgentInvokerInput, B as parseStandardSchema, F as validateWorkspaceCommands, H as markTrustedWorkspaceAccessScope, I as workspaceCommandTools, J as createMessageChannelTitleEffectIntent, K as claimMessageChannelTitleDelivery, L as defineInternalTool, M as agentInvocationSourceContext, P as normalizeWorkspaceCommandTimeout, R as requirePrimitive, U as markTrustedWorkspaceSourceResolutionDefinition, W as workspaceOverrideSymbol, X as finishMessageChannelTitleDelivery, at as resetMessageChannelTitleDelivery, c as defineCapability, et as markAuxiliaryMessageChannelInstructionContext, f as normalizeMode, i as capabilityWorkspaceSources, l as eagerFinishExtensionSymbol, nt as messageChannelStateContextKey, r as capabilityInvocationStartSymbol, u as globalSkillsSymbol, y as workspaceMaterializationPathsSymbol, z as executeHttpRequest } from "./capability-runtime-Be85oBbC.js";
 import { a as streamAgentOutputToEvents, f as responseTitleFallbackContextKey, o as toAgentRunResult, s as toAgentStreamEvent } from "./agent-output-CyEZfgrQ.js";
-import { A as getAgentChatContext, C as selectStore, E as agentChatContextKey, O as chat, S as requirePrimitive$1, _ as repositoryHostContext, b as jsonObjectSchema$1, s as messageChannelTitleSupportContextKey, v as assertString, w as storageValue, x as method, y as createTool$2 } from "./channels-DNuHTrNn.js";
+import { A as getAgentChatContext, C as selectStore, E as agentChatContextKey, O as chat, S as requirePrimitive$1, _ as repositoryHostContext, b as jsonObjectSchema$1, s as messageChannelTitleSupportContextKey, v as assertString, w as storageValue, x as method, y as createTool$2 } from "./channels-D6wOMJN1.js";
 import { n as resolveNormalizedHarnessDriver, t as normalizeAgentDriver } from "./agent-driver-ktEa_Dj0.js";
 import { n as toAgentUiMessageStreamResponse } from "./http-response-BHcgRXcy.js";
 import { S as progressSummaryOutputContextKey, _ as normalizeUiMessageStreamChunk, a as parseScheduledAgentTurnInput, c as scheduledAgentTargetName, d as scheduledAgentTurnReplyEffect, i as createScheduledAgentTurnInput, o as scheduledAgentChannelIdsContextKey, s as scheduledAgentNameContextKey, t as awaitAgentInvocationResult, u as scheduledAgentTurnPrompt } from "./agent-invocation-D9tJRj_l.js";
@@ -1193,7 +1193,7 @@ async function generateTitleWithDriver(context, options, input, prompt) {
 	const runContext = titleAdapterRunContext(context, input, prompt);
 	if (driver.kind === "harness") {
 		const resolvedDriver = await resolveNormalizedHarnessDriver(driver);
-		const { createHarnessAgentAdapter } = await import("./harness-agent-KBnlg4pE.js");
+		const { createHarnessAgentAdapter } = await import("./harness-agent-CklIYSAq.js");
 		return await titleResultText(await createHarnessAgentAdapter(resolvedDriver).generate(runContext));
 	}
 	const { createAiSdkAdapter } = await import("./ai-sdk.js");
@@ -3383,7 +3383,7 @@ async function generateWithDriver(context, options, input, prompt) {
 	const runContext = progressSummaryAdapterRunContext(context, input, prompt);
 	if (driver.kind === "harness") {
 		const resolvedDriver = await resolveNormalizedHarnessDriver(driver);
-		const { createHarnessAgentAdapter } = await import("./harness-agent-KBnlg4pE.js");
+		const { createHarnessAgentAdapter } = await import("./harness-agent-CklIYSAq.js");
 		return await resultText(await createHarnessAgentAdapter({
 			...resolvedDriver,
 			instructions
@@ -5219,8 +5219,8 @@ function usageCost(options = {}) {
 		metadata: { kind: "usage-cost" },
 		async finish(event) {
 			const record = event.invocation.usage;
-			if (!record || record.cost || !record.usage) return record;
-			try {
+			if (!record) return record;
+			if (!record.cost && record.usage) try {
 				const cost = await pricing({
 					model: record.model,
 					response: record.response,
@@ -5231,10 +5231,27 @@ function usageCost(options = {}) {
 			} catch {
 				return record;
 			}
+			if (record.cost && options.format === "usd") {
+				const formatted = formatUsd(record.cost);
+				if (formatted) record.cost.formatted = formatted;
+			}
 			return record;
 		}
 	}), { [eagerFinishExtensionSymbol]: true });
 }
+function formatUsd(cost) {
+	if (!cost || cost.currency !== "USD") return;
+	const amount = Number(cost.amount);
+	if (!Number.isFinite(amount)) return;
+	const fractionDigits = amount > 0 && amount < .01 ? 6 : 2;
+	const formatted = new Intl.NumberFormat("en-US", {
+		currency: "USD",
+		maximumFractionDigits: fractionDigits,
+		minimumFractionDigits: fractionDigits,
+		style: "currency"
+	}).format(amount);
+	return `${cost.estimated ? "~" : ""}${formatted}`;
+}
 //#endregion
 //#region src/capabilities/workspace-shell.ts
 function workspaceShell(options = {}) {
diff --git a/dist/channels-D6wOMJN1.js b/dist/channels-D6wOMJN1.js
new file mode 100644
index 0000000000000000000000000000000000000000..3581f91ea36b667fa30d0db5c69db1d169b76e67
--- /dev/null
+++ b/dist/channels-D6wOMJN1.js
@@ -0,0 +1,2457 @@
+import { createMessage, isAttachmentData, isAttachmentPart } from "./messages.js";
+import { a as publishedDeliveryArtifactsFromUnknown, c as isAsyncIterable, i as publishWorkspaceArtifacts, n as deliveryArtifactMarkdownReferencePaths, o as rewriteDeliveryArtifactMarkdown, r as normalizeDeliveryArtifactPath, t as deliveryArtifactAttachments } from "./delivery-artifacts-BOKCLStf.js";
+import { E as normalizeAgentInvoker, Y as defineMessageChannelInstructions, c as defineCapability, y as workspaceMaterializationPathsSymbol } from "./capability-runtime-Be85oBbC.js";
+import { resolveRuntimeValue } from "@vite-hub/runtime";
+import { createHash, createSign } from "node:crypto";
+//#region src/chat-message-input.ts
+function uiMessageText(message) {
+	return (Array.isArray(message.parts) ? message.parts : []).filter((part) => typeof part === "object" && part !== null && part.type === "text" && typeof part.text === "string").map((part) => part.text).join("");
+}
+function firstString(...values) {
+	return values.find((value) => typeof value === "string" && value.length > 0);
+}
+function chatIdentity(user, run) {
+	const identity = firstString(user?.id, user?.sub, user?.email, user?.username)?.trim();
+	if (!identity) return;
+	return run?.origin ? `${run.origin}:${identity}` : identity;
+}
+function resolveChatTriggerInvoker(triggerInput) {
+	const userMeta = {};
+	for (const key of [
+		"id",
+		"sub",
+		"email",
+		"username",
+		"name",
+		"customer"
+	]) {
+		const value = firstString(triggerInput?.user?.[key])?.trim();
+		if (value) userMeta[key] = value;
+	}
+	const meta = Object.keys(userMeta).length || triggerInput?.meta ? {
+		...userMeta,
+		...triggerInput?.meta
+	} : void 0;
+	const invokerId = chatIdentity(triggerInput?.user, triggerInput?.run);
+	return triggerInput?.invoker ? normalizeAgentInvoker(triggerInput.invoker, "chat.message input.invoker") : invokerId ? normalizeAgentInvoker({
+		id: invokerId,
+		kind: "chat",
+		...meta ? { meta } : {}
+	}, "chat.message input.user") : void 0;
+}
+function uiToolName(part) {
+	if (part.type === "dynamic-tool") return firstString(part.toolName, part.name) || "tool";
+	return typeof part.type === "string" && part.type.startsWith("tool-") ? part.type.slice(5) : firstString(part.toolName, part.name) || "tool";
+}
+function uiToolId(part, name, index) {
+	return firstString(part.toolCallId, part.id) || `${name}-${index + 1}`;
+}
+function uiAttachmentPartToAgentPart(part) {
+	const mediaType = typeof part.mediaType === "string" && part.mediaType ? part.mediaType : void 0;
+	const inputType = isAttachmentPart(part) ? part.type : void 0;
+	const type = mediaType?.startsWith("audio/") ? "audio" : mediaType?.startsWith("image/") ? "image" : inputType;
+	if (!type || !mediaType) return [];
+	const id = firstString(part.id);
+	const data = isAttachmentData(part.data) ? part.data : void 0;
+	const fetchData = typeof part.fetchData === "function" ? part.fetchData : void 0;
+	const name = firstString(part.name, part.filename);
+	const url = typeof part.url === "string" && part.url ? part.url : void 0;
+	if (!data && !fetchData && !url) return [];
+	return [{
+		...data ? { data } : {},
+		...fetchData ? { fetchData } : {},
+		...id ? { id } : {},
+		...name ? { name } : {},
+		...typeof part.size === "number" && Number.isFinite(part.size) ? { size: part.size } : {},
+		...typeof part.fetchMetadata === "object" && part.fetchMetadata !== null ? { fetchMetadata: part.fetchMetadata } : {},
+		mediaType,
+		type,
+		...url ? { url } : {}
+	}];
+}
+function uiMessagePartsToAgentParts(message) {
+	return (Array.isArray(message.parts) ? message.parts : []).flatMap((part, index) => {
+		if (!part || typeof part !== "object") return [];
+		const record = part;
+		if (record.type === "text" && typeof record.text === "string") return [record.text];
+		if ((record.type === "data" || typeof record.type === "string" && record.type.startsWith("data-")) && "data" in record) return [{
+			data: record.data,
+			...typeof record.id === "string" ? { id: record.id } : {},
+			type: record.type
+		}];
+		if (record.type === "audio" || record.type === "file" || record.type === "image") return uiAttachmentPartToAgentPart(record);
+		if (record.type === "dynamic-tool" || typeof record.type === "string" && record.type.startsWith("tool-")) {
+			const state = typeof record.state === "string" ? record.state : void 0;
+			const errorText = typeof record.errorText === "string" ? record.errorText : void 0;
+			const hasToolError = errorText !== void 0;
+			const hasToolOutput = state === "output-available" || state === "output-denied" || state === "output-error" || record.output !== void 0 || hasToolError;
+			if (!hasToolOutput) return [];
+			const name = uiToolName(record);
+			const id = uiToolId(record, name, index);
+			return [{
+				id,
+				input: record.input,
+				name,
+				state: state === "input-available" || hasToolOutput ? "proposed" : "running",
+				type: "tool-call"
+			}, {
+				id,
+				name,
+				state: hasToolError ? "failed" : "completed",
+				type: "tool-result",
+				...hasToolError ? { error: errorText } : {},
+				...record.output !== void 0 ? { output: record.output } : {}
+			}];
+		}
+		return [];
+	});
+}
+function uiMessagesToAgentMessages(messages) {
+	return messages.map((message, index) => {
+		const role = message.role === "assistant" || message.role === "system" || message.role === "tool" || message.role === "user" ? message.role : "user";
+		return createMessage({
+			createdAt: message.createdAt,
+			id: message.id || `ui-${index}`,
+			metadata: typeof message.metadata === "object" && message.metadata !== null ? message.metadata : void 0,
+			parts: uiMessagePartsToAgentParts(message),
+			role
+		});
+	});
+}
+function metadataRecord(message) {
+	return typeof message?.metadata === "object" && message.metadata !== null ? message.metadata : void 0;
+}
+function nestedSessionId(metadata) {
+	const chat = typeof metadata?.chat === "object" && metadata.chat !== null ? metadata.chat : void 0;
+	const session = typeof metadata?.session === "object" && metadata.session !== null ? metadata.session : void 0;
+	return firstString(metadata?.sessionId, metadata?.chatSessionId, chat?.sessionId, session?.id);
+}
+function uiMessageSessionId(message, metadataKey) {
+	const metadata = metadataRecord(message);
+	return firstString(metadataKey ? metadata?.[metadataKey] : void 0, nestedSessionId(metadata));
+}
+function uiMessageTime(message) {
+	const metadata = metadataRecord(message);
+	const raw = message.createdAt || metadata?.createdAt || metadata?.updatedAt;
+	const time = raw instanceof Date ? raw.getTime() : typeof raw === "string" ? Date.parse(raw) : void 0;
+	return typeof time === "number" && Number.isFinite(time) ? time : void 0;
+}
+function normalizeSessionOptions(sessions) {
+	if (!sessions) return void 0;
+	return sessions === true ? { strategy: "manual" } : sessions;
+}
+function selectManualSession(messages, sessions, triggerSession) {
+	if (triggerSession?.action === "new") return messages.slice(-1);
+	const selectedId = triggerSession?.id || uiMessageSessionId(messages.at(-1) || {}, sessions.metadataKey);
+	if (!selectedId) return messages;
+	const filtered = messages.filter((message) => uiMessageSessionId(message, sessions.metadataKey) === selectedId);
+	return filtered.length ? filtered : messages;
+}
+function selectIdleSession(messages, sessions) {
+	const timeout = sessions.idleTimeoutMs;
+	if (!timeout || timeout <= 0 || messages.length < 2) return messages;
+	for (let index = messages.length - 1; index > 0; index--) {
+		const current = uiMessageTime(messages[index]);
+		const previous = uiMessageTime(messages[index - 1]);
+		if (current !== void 0 && previous !== void 0 && current - previous > timeout) return messages.slice(index);
+	}
+	return messages;
+}
+function selectChatSession(messages, sessions, triggerSession) {
+	const options = normalizeSessionOptions(sessions);
+	if (!options) return messages;
+	const strategy = options.strategy || (options.idleTimeoutMs ? "idle-timeout" : "manual");
+	if (strategy === "manual") return selectManualSession(messages, options, triggerSession);
+	if (strategy === "idle-timeout") return selectIdleSession(messages, options);
+	return selectIdleSession(selectManualSession(messages, options, triggerSession), options);
+}
+function normalizedMaxMessages(value) {
+	return typeof value === "number" && Number.isFinite(value) ? Math.max(1, Math.floor(value)) : void 0;
+}
+function threadHistoryMaxMessages(threadHistory) {
+	if (!threadHistory || typeof threadHistory !== "object" || Array.isArray(threadHistory)) return;
+	return normalizedMaxMessages(threadHistory.maxMessages);
+}
+function resolveChatTriggerHistory(options, triggerHistory) {
+	if (triggerHistory !== void 0) return triggerHistory;
+	if (options?.triggerHistory !== void 0) return options.triggerHistory;
+	const maxMessages = threadHistoryMaxMessages(options?.threadHistory);
+	return maxMessages === void 0 ? void 0 : {
+		maxMessages,
+		source: "thread"
+	};
+}
+function chatTriggerHistoryLimit(triggerHistory) {
+	if (triggerHistory === "none") return;
+	if (!triggerHistory || triggerHistory.source !== "thread") return;
+	return normalizedMaxMessages(triggerHistory.maxMessages) ?? 20;
+}
+function selectChatHistory(messages, triggerHistory, sessions, triggerSession) {
+	const sessionMessages = selectChatSession(messages, sessions, triggerSession);
+	if (triggerHistory === "none") return sessionMessages.slice(-1);
+	const limit = chatTriggerHistoryLimit(triggerHistory);
+	if (limit) return sessionMessages.slice(-limit);
+	return sessionMessages.slice(-20);
+}
+function createChatTriggerHookArgs(messages, run, session) {
+	const message = messages.at(-1);
+	const metadata = metadataRecord(message);
+	return {
+		history: uiMessagesToAgentMessages(messages),
+		message: {
+			id: message?.id,
+			...metadata ? { metadata } : {},
+			text: message ? uiMessageText(message) : ""
+		},
+		run,
+		session,
+		thread: { post: async () => void 0 }
+	};
+}
+function createChatMessageTriggerInput(options, triggerInput) {
+	const messages = Array.isArray(triggerInput?.messages) ? triggerInput.messages : [];
+	if (!messages.length) throw new TypeError("[vitehub] chat.message trigger requires at least one UI message.");
+	const selectedMessages = selectChatHistory(messages, resolveChatTriggerHistory(options, triggerInput?.triggerHistory), options.sessions, triggerInput?.session);
+	const hookArgs = createChatTriggerHookArgs(selectedMessages, triggerInput?.run, triggerInput?.session);
+	const invoker = resolveChatTriggerInvoker(triggerInput);
+	return {
+		hookArgs,
+		input: {
+			abortSignal: triggerInput?.abortSignal,
+			context: {
+				...invoker ? { invoker } : {},
+				...triggerInput?.invokerProfileId ? { invokerProfileId: triggerInput.invokerProfileId } : {},
+				channel: {
+					message: hookArgs.message,
+					run: triggerInput?.run,
+					session: triggerInput?.session,
+					meta: triggerInput?.meta,
+					user: triggerInput?.user
+				},
+				chat: {
+					message: hookArgs.message,
+					session: triggerInput?.session,
+					meta: triggerInput?.meta,
+					user: triggerInput?.user
+				}
+			},
+			messages: uiMessagesToAgentMessages(selectedMessages),
+			timeout: triggerInput?.timeout
+		},
+		selectedMessages
+	};
+}
+//#endregion
+//#region src/chat-trigger.ts
+const agentChatContextKey = "chat";
+const CHAT_WEBHOOK_DEFAULTS = { telegram: {
+	method: "POST",
+	provider: "telegram",
+	secretHeader: "x-telegram-bot-api-secret-token"
+} };
+const CHAT_FINISH_EXTENSION_CONTEXT_KEY = "chat.finish";
+function getAgentChatContext(input) {
+	return ("get" in input ? input : input.context).get(agentChatContextKey);
+}
+async function resolveChatThinkingFallback(options, args) {
+	const fallback = options.fallbackStreamingPlaceholderText;
+	if (fallback === null) return null;
+	if (typeof fallback === "function") {
+		const resolved = await fallback(args);
+		if (resolved === null) return null;
+		return typeof resolved === "string" ? resolved : void 0;
+	}
+	if (Array.isArray(fallback)) {
+		if (fallback.length === 0) return null;
+		return fallback[Math.floor(Math.random() * fallback.length)];
+	}
+	if (typeof fallback === "string") return fallback;
+}
+function isResolvableObject(value) {
+	return typeof value === "object" && value !== null && typeof value.resolve === "function";
+}
+function isStaticPlatformMap(value) {
+	return typeof value === "object" && value !== null && !Array.isArray(value) && !isResolvableObject(value);
+}
+function isKnownChatWebhookPlatform(platform) {
+	return platform in CHAT_WEBHOOK_DEFAULTS;
+}
+function hasExplicitChatWebhook(options, platform) {
+	return !!options.webhooks && Object.prototype.hasOwnProperty.call(options.webhooks, platform);
+}
+function normalizeChatWebhookRegistrations(platform, input) {
+	if (input === false) return [];
+	const defaults = isKnownChatWebhookPlatform(platform) ? CHAT_WEBHOOK_DEFAULTS[platform] : { provider: platform };
+	const registrations = Array.isArray(input) ? input : [input];
+	return registrations.map((registration, index) => ({
+		...registration,
+		id: registration.id || (registrations.length > 1 ? `${platform}-${index + 1}` : platform),
+		method: registration.method || defaults.method || "POST",
+		provider: registration.provider || defaults.provider || platform,
+		secretHeader: registration.secretHeader || defaults.secretHeader
+	}));
+}
+function inferredChatWebhookRegistrations(options) {
+	if (!isStaticPlatformMap(options.platforms)) return [];
+	return Object.keys(options.platforms).filter((platform) => !hasExplicitChatWebhook(options, platform)).flatMap((platform) => normalizeChatWebhookRegistrations(platform, {}));
+}
+function explicitChatWebhookRegistrations(options) {
+	return Object.entries(options.webhooks ?? {}).flatMap(([platform, input]) => normalizeChatWebhookRegistrations(platform, input));
+}
+function resolveChatWebhookRegistrations(options) {
+	const registrations = [...explicitChatWebhookRegistrations(options), ...inferredChatWebhookRegistrations(options)];
+	return registrations.length ? registrations : void 0;
+}
+function createChatMessageTrigger(options = {}) {
+	return {
+		input: "ui-message[]",
+		output: "ui-message-stream",
+		webhooks: resolveChatWebhookRegistrations(options),
+		async invoke(_context, triggerInput) {
+			const { hookArgs, input } = createChatMessageTriggerInput(options, triggerInput);
+			const thinkingFallback = await resolveChatThinkingFallback(options, hookArgs);
+			return {
+				input,
+				...thinkingFallback !== void 0 ? { metadata: { thinkingFallback } } : {},
+				run: triggerInput.run
+			};
+		}
+	};
+}
+function assertNoLegacyChatHistoryOption(options) {
+	if (Object.prototype.hasOwnProperty.call(options, "history")) throw new TypeError("[vitehub] messages.history was replaced by messages.triggerHistory. Use triggerHistory to configure the chat.message trigger input window.");
+}
+function assertChatDeliveryOptions(options) {
+	if (options.delivery === "manual" && (options.stream === true || options.commentary !== void 0)) throw new TypeError("[vitehub] messages.delivery \"manual\" cannot be combined with messages.stream or messages.commentary.");
+	if (options.timeout !== void 0 && (!Number.isFinite(options.timeout) || options.timeout <= 0)) throw new TypeError("[vitehub] messages.timeout must be a positive finite number.");
+}
+function getChatCapabilityOptions(capabilities) {
+	return capabilities.find((capability) => capability.id === "chat" && capability.metadata?.kind === "chat")?.metadata?.chat;
+}
+function defineChatCapability(options = {}) {
+	assertNoLegacyChatHistoryOption(options);
+	assertChatDeliveryOptions(options);
+	return defineCapability({
+		id: "chat",
+		metadata: {
+			chat: options,
+			kind: "chat"
+		},
+		prepare(context) {
+			context.state.require("chat-history", { optional: true });
+		},
+		output(context) {
+			context.finish.provide(() => context.context.get(CHAT_FINISH_EXTENSION_CONTEXT_KEY));
+		},
+		triggers: { message: createChatMessageTrigger(options) }
+	});
+}
+function chat(options = {}) {
+	if (Object.prototype.hasOwnProperty.call(options, "platforms")) throw new TypeError("[vitehub] chat({ platforms }) was removed. Use defineAgent({ channels }) with an adapter-backed Channel instead.");
+	if (Object.prototype.hasOwnProperty.call(options, "webhooks")) throw new TypeError("[vitehub] chat({ webhooks }) was removed. Use defineAgent({ channels }) with an adapter-backed Channel instead.");
+	return defineChatCapability(options);
+}
+//#endregion
+//#region src/capabilities/storage/shared.ts
+function createTool(tool) {
+	if (!tool || typeof tool !== "object") throw new TypeError("[vitehub] tool definitions must be objects.");
+	if (!tool.name || typeof tool.name !== "string") throw new TypeError("[vitehub] tool definitions require a tool name.");
+	return tool;
+}
+function jsonObjectSchema(properties, required = []) {
+	return {
+		additionalProperties: false,
+		properties,
+		...required.length ? { required } : {},
+		type: "object"
+	};
+}
+function requirePrimitive(context, name) {
+	const handle = context.capabilities?.[name];
+	const value = typeof handle === "object" && handle !== null && "value" in handle ? handle.value : handle;
+	if (!value) throw new Error(`[vitehub] Capability "${name}" requires the ${name} primitive to be configured.`);
+	return value;
+}
+function assertString(value, label) {
+	if (typeof value !== "string" || !value.trim()) throw new TypeError(`[vitehub] ${label} must be a non-empty string.`);
+	return value;
+}
+function method(handle, primitive, name) {
+	const fn = typeof handle === "object" && handle !== null ? handle[name] : void 0;
+	if (typeof fn !== "function") throw new Error(`[vitehub] ${primitive} primitive does not expose ${name}().`);
+	return fn.bind(handle);
+}
+async function storageValue(result) {
+	const resolved = await result;
+	if (!Array.isArray(resolved) || resolved.length !== 2 || resolved[0] !== null && !(resolved[0] instanceof Error)) return resolved;
+	if (resolved[0]) throw resolved[0];
+	return resolved[1];
+}
+function selectStore(handle, primitive, store) {
+	if (!store) return handle;
+	const storeFn = typeof handle === "object" && handle !== null ? handle.store : void 0;
+	if (typeof storeFn !== "function") throw new Error(`[vitehub] ${primitive} Capability store selection requires the ${primitive.toLowerCase()} primitive to expose store().`);
+	return storeFn.call(handle, store);
+}
+//#endregion
+//#region src/capabilities/repository-host-context.ts
+const defaultRepositoryHostContextId = "repository-host-context";
+const defaultRepositoryHostContextKey = "repositoryHost";
+const repositoryHostContextKeys = [
+	"issue",
+	"pullRequest",
+	"body",
+	"labels",
+	"comments",
+	"files"
+];
+async function resolveMaybeFunction(value, context) {
+	return typeof value === "function" ? await value(context) : value;
+}
+function isRecord$1(value) {
+	return !!value && typeof value === "object" && !Array.isArray(value);
+}
+function maybeString$1(value) {
+	return typeof value === "string" && value ? value : void 0;
+}
+function maybeContextValue(value) {
+	return typeof value === "number" || typeof value === "string" ? value : void 0;
+}
+function maybeStrings$1(value) {
+	if (!Array.isArray(value)) return;
+	const items = value.filter((item) => typeof item === "string" && !!item);
+	return items.length ? items : void 0;
+}
+function normalizedLabelNames(value) {
+	if (!Array.isArray(value)) return;
+	const labels = value.flatMap((item) => {
+		if (typeof item === "string" && item) return [item];
+		if (isRecord$1(item) && maybeString$1(item.name)) return [maybeString$1(item.name)];
+		return [];
+	});
+	return labels.length ? labels : value.length === 0 ? [] : void 0;
+}
+function isPresent(value) {
+	return value !== void 0;
+}
+function normalizedRepositoryFullName(value) {
+	if (typeof value === "string" && value) return value;
+	return isRecord$1(value) ? maybeString$1(value.fullName) || maybeString$1(value.full_name) : void 0;
+}
+function normalizedRef(value) {
+	if (!isRecord$1(value)) return;
+	const ref = maybeString$1(value.ref);
+	const repo = normalizedRepositoryFullName(value.repo) || maybeString$1(value.repo);
+	const sha = maybeString$1(value.sha);
+	if (!ref && !repo && !sha) return;
+	return {
+		...ref ? { ref } : {},
+		...repo ? { repo } : {},
+		...sha ? { sha } : {}
+	};
+}
+function normalizedFlatRef(value, fallbackRef) {
+	return normalizedRef(value) || (fallbackRef ? { ref: fallbackRef } : void 0);
+}
+function normalizedUser(value) {
+	if (!isRecord$1(value)) return;
+	const id = maybeContextValue(value.id);
+	const login = maybeString$1(value.login);
+	const type = maybeString$1(value.type);
+	if (id === void 0 && !login && !type) return;
+	return {
+		...id !== void 0 ? { id } : {},
+		...login ? { login } : {},
+		...type ? { type } : {}
+	};
+}
+function normalizedComment(value) {
+	if (!isRecord$1(value)) return;
+	const id = maybeContextValue(value.id);
+	const body = maybeString$1(value.body);
+	const user = normalizedUser(value.user);
+	if (id === void 0 && !body && !user) return;
+	return {
+		...maybeString$1(value.authorAssociation) ? { authorAssociation: maybeString$1(value.authorAssociation) } : {},
+		...maybeString$1(value.author_association) ? { authorAssociation: maybeString$1(value.author_association) } : {},
+		...body ? { body } : {},
+		...maybeString$1(value.createdAt) ? { createdAt: maybeString$1(value.createdAt) } : {},
+		...maybeString$1(value.created_at) ? { createdAt: maybeString$1(value.created_at) } : {},
+		...maybeString$1(value.htmlUrl) ? { htmlUrl: maybeString$1(value.htmlUrl) } : {},
+		...maybeString$1(value.html_url) ? { htmlUrl: maybeString$1(value.html_url) } : {},
+		...id !== void 0 ? { id } : {},
+		...maybeString$1(value.nodeId) ? { nodeId: maybeString$1(value.nodeId) } : {},
+		...maybeString$1(value.node_id) ? { nodeId: maybeString$1(value.node_id) } : {},
+		...maybeString$1(value.updatedAt) ? { updatedAt: maybeString$1(value.updatedAt) } : {},
+		...maybeString$1(value.updated_at) ? { updatedAt: maybeString$1(value.updated_at) } : {},
+		...user ? { user } : {}
+	};
+}
+function normalizedComments(value) {
+	if (!Array.isArray(value)) return;
+	const comments = value.map(normalizedComment).filter(isPresent);
+	return comments.length ? comments : [];
+}
+function normalizedFile(value) {
+	if (!isRecord$1(value)) return;
+	const filename = maybeString$1(value.filename);
+	if (!filename) return;
+	return {
+		...maybeContextValue(value.additions) !== void 0 ? { additions: maybeContextValue(value.additions) } : {},
+		...maybeContextValue(value.deletions) !== void 0 ? { deletions: maybeContextValue(value.deletions) } : {},
+		filename,
+		...maybeString$1(value.status) ? { status: maybeString$1(value.status) } : {}
+	};
+}
+function normalizedFiles(value) {
+	if (!Array.isArray(value)) return;
+	const files = value.map(normalizedFile).filter(isPresent);
+	return files.length ? files : [];
+}
+function normalizedMetadata(value) {
+	if (!isRecord$1(value)) return;
+	const omittedComments = maybeContextValue(value.omittedComments);
+	const omittedFiles = maybeContextValue(value.omittedFiles);
+	const unavailable = maybeString$1(value.unavailable);
+	if (omittedComments === void 0 && omittedFiles === void 0 && !unavailable) return;
+	return {
+		...omittedComments !== void 0 ? { omittedComments } : {},
+		...omittedFiles !== void 0 ? { omittedFiles } : {},
+		...unavailable ? { unavailable } : {}
+	};
+}
+function normalizedRun(value) {
+	if (!isRecord$1(value)) return;
+	const messageId = maybeString$1(value.messageId);
+	const origin = maybeString$1(value.origin);
+	const runId = maybeString$1(value.runId);
+	const threadId = maybeString$1(value.threadId);
+	if (!messageId && !origin && !runId && !threadId) return;
+	return {
+		...messageId ? { messageId } : {},
+		...origin ? { origin } : {},
+		...runId ? { runId } : {},
+		...threadId ? { threadId } : {}
+	};
+}
+function normalizedSource(value) {
+	if (!isRecord$1(value)) return;
+	const checkout = typeof value.checkout === "boolean" ? value.checkout : void 0;
+	const mount = typeof value.mount === "string" ? value.mount : void 0;
+	const ref = maybeString$1(value.ref);
+	const repo = maybeString$1(value.repo);
+	if (checkout === void 0 && mount === void 0 && !ref && !repo) return;
+	return {
+		...checkout !== void 0 ? { checkout } : {},
+		...mount !== void 0 ? { mount } : {},
+		...ref ? { ref } : {},
+		...repo ? { repo } : {}
+	};
+}
+function normalizedTrigger(value) {
+	if (!isRecord$1(value)) return;
+	const action = maybeString$1(value.action);
+	const actor = normalizedUser(value.actor);
+	const args = maybeString$1(value.args);
+	const command = maybeString$1(value.command);
+	const comment = normalizedComment(value.comment);
+	const deliveryId = maybeString$1(value.deliveryId);
+	const event = maybeString$1(value.event);
+	const installationId = maybeContextValue(value.installationId);
+	const sender = normalizedUser(value.sender);
+	if (!action && !actor && !args && !command && !comment && !deliveryId && !event && installationId === void 0 && !sender) return;
+	return {
+		...action ? { action } : {},
+		...actor ? { actor } : {},
+		...args ? { args } : {},
+		...command ? { command } : {},
+		...comment ? { comment } : {},
+		...deliveryId ? { deliveryId } : {},
+		...event ? { event } : {},
+		...installationId !== void 0 ? { installationId } : {},
+		...sender ? { sender } : {}
+	};
+}
+function normalizePullRequestContext(value) {
+	if (!isRecord$1(value)) return;
+	const pullRequest = isRecord$1(value.pullRequest) ? value.pullRequest : isRecord$1(value.pull_request) ? value.pull_request : void 0;
+	const repository = isRecord$1(value.repository) ? value.repository : void 0;
+	const number = maybeContextValue(pullRequest?.number);
+	const repositoryName = normalizedRepositoryFullName(repository);
+	if (pullRequest && number !== void 0 && repositoryName) {
+		const comments = normalizedComments(pullRequest.comments);
+		const files = normalizedFiles(pullRequest.files);
+		const labels = normalizedLabelNames(pullRequest.labels) || maybeStrings$1(pullRequest.labels);
+		const metadata = normalizedMetadata(pullRequest.metadata);
+		const base = normalizedRef(pullRequest.base);
+		const head = normalizedRef(pullRequest.head);
+		const source = normalizedSource(pullRequest.source);
+		const trigger = normalizedTrigger(value.trigger);
+		const actor = trigger?.actor?.login;
+		const deliveryId = trigger?.deliveryId;
+		return {
+			...actor ? { actor } : {},
+			...maybeString$1(pullRequest.apiUrl) ? { apiUrl: maybeString$1(pullRequest.apiUrl) } : {},
+			...maybeString$1(pullRequest.url) ? { apiUrl: maybeString$1(pullRequest.url) } : {},
+			...base ? { base } : {},
+			...base?.ref ? { baseRef: base.ref } : {},
+			...maybeString$1(pullRequest.body) ? { body: maybeString$1(pullRequest.body) } : {},
+			...comments !== void 0 ? { comments } : {},
+			...deliveryId ? { deliveryId } : {},
+			...files !== void 0 ? { files } : {},
+			...head ? { head } : {},
+			...source?.ref || head?.ref ? { headRef: source?.ref || head?.ref } : {},
+			...maybeString$1(pullRequest.htmlUrl) ? { htmlUrl: maybeString$1(pullRequest.htmlUrl) } : {},
+			...maybeString$1(pullRequest.html_url) ? { htmlUrl: maybeString$1(pullRequest.html_url) } : {},
+			...maybeContextValue(pullRequest.id) !== void 0 ? { id: maybeContextValue(pullRequest.id) } : {},
+			...labels ? { labels } : {},
+			...metadata ? { metadata } : {},
+			number,
+			provider: maybeString$1(value.provider) || "github",
+			repository: repositoryName,
+			...normalizedRun(value.run) ? { run: normalizedRun(value.run) } : {},
+			...source ? { source } : {},
+			...maybeString$1(pullRequest.title) ? { title: maybeString$1(pullRequest.title) } : {},
+			...trigger ? { trigger } : {}
+		};
+	}
+	const flatNumber = maybeContextValue(value.number);
+	const flatRepository = normalizedRepositoryFullName(value.repository);
+	if (flatNumber !== void 0 && flatRepository) {
+		const base = normalizedFlatRef(value.base, maybeString$1(value.baseRef));
+		const comments = normalizedComments(value.comments);
+		const files = normalizedFiles(value.files);
+		const head = normalizedFlatRef(value.head, maybeString$1(value.headRef));
+		const labels = normalizedLabelNames(value.labels) || maybeStrings$1(value.labels);
+		const metadata = normalizedMetadata(value.metadata);
+		const run = normalizedRun(value.run);
+		const source = normalizedSource(value.source);
+		const trigger = {
+			...normalizedTrigger(value.trigger) || {},
+			...maybeString$1(value.actor) ? { actor: { login: maybeString$1(value.actor) } } : {},
+			...maybeString$1(value.deliveryId) ? { deliveryId: maybeString$1(value.deliveryId) } : {}
+		};
+		const actor = trigger.actor?.login;
+		const deliveryId = trigger.deliveryId;
+		return {
+			...actor ? { actor } : {},
+			...maybeString$1(value.apiUrl) ? { apiUrl: maybeString$1(value.apiUrl) } : {},
+			...maybeString$1(value.url) ? { apiUrl: maybeString$1(value.url) } : {},
+			...base ? { base } : {},
+			...base?.ref ? { baseRef: base.ref } : {},
+			...maybeString$1(value.body) ? { body: maybeString$1(value.body) } : {},
+			...comments !== void 0 ? { comments } : {},
+			...deliveryId ? { deliveryId } : {},
+			...files !== void 0 ? { files } : {},
+			...head ? { head } : {},
+			...source?.ref || head?.ref ? { headRef: source?.ref || head?.ref } : {},
+			...maybeString$1(value.htmlUrl) ? { htmlUrl: maybeString$1(value.htmlUrl) } : {},
+			...maybeString$1(value.html_url) ? { htmlUrl: maybeString$1(value.html_url) } : {},
+			...maybeContextValue(value.id) !== void 0 ? { id: maybeContextValue(value.id) } : {},
+			...labels ? { labels } : {},
+			...metadata ? { metadata } : {},
+			number: flatNumber,
+			...maybeString$1(value.provider) ? { provider: maybeString$1(value.provider) } : {},
+			repository: flatRepository,
+			...run ? { run } : {},
+			...source ? { source } : {},
+			...maybeString$1(value.title) ? { title: maybeString$1(value.title) } : {},
+			...Object.keys(trigger).length ? { trigger } : {}
+		};
+	}
+}
+function normalizeHostIssue(value, fallback = {}) {
+	if (!isRecord$1(value)) return;
+	const number = maybeContextValue(value.number) ?? fallback.number;
+	if (number === void 0) return;
+	const pullRequest = isRecord$1(value.pullRequest) ? value.pullRequest : isRecord$1(value.pull_request) ? value.pull_request : void 0;
+	const repository = normalizedRepositoryFullName(value.repository) || fallback.repository;
+	return {
+		...maybeString$1(value.apiUrl) ? { apiUrl: maybeString$1(value.apiUrl) } : {},
+		...maybeString$1(value.url) ? { apiUrl: maybeString$1(value.url) } : {},
+		...maybeString$1(value.body) ? { body: maybeString$1(value.body) } : {},
+		...maybeString$1(value.htmlUrl) ? { htmlUrl: maybeString$1(value.htmlUrl) } : {},
+		...maybeString$1(value.html_url) ? { htmlUrl: maybeString$1(value.html_url) } : {},
+		...maybeContextValue(value.id) !== void 0 ? { id: maybeContextValue(value.id) } : {},
+		...normalizedLabelNames(value.labels) ? { labels: normalizedLabelNames(value.labels) } : {},
+		...maybeString$1(value.nodeId) ? { nodeId: maybeString$1(value.nodeId) } : {},
+		...maybeString$1(value.node_id) ? { nodeId: maybeString$1(value.node_id) } : {},
+		number,
+		...pullRequest ? { pullRequest: {
+			...maybeString$1(pullRequest.apiUrl) ? { apiUrl: maybeString$1(pullRequest.apiUrl) } : {},
+			...maybeString$1(pullRequest.url) ? { apiUrl: maybeString$1(pullRequest.url) } : {},
+			...maybeString$1(pullRequest.htmlUrl) ? { htmlUrl: maybeString$1(pullRequest.htmlUrl) } : {},
+			...maybeString$1(pullRequest.html_url) ? { htmlUrl: maybeString$1(pullRequest.html_url) } : {}
+		} } : {},
+		...repository ? { repository } : {},
+		...maybeString$1(value.state) ? { state: maybeString$1(value.state) } : {},
+		...maybeString$1(value.title) ? { title: maybeString$1(value.title) } : {},
+		...normalizedUser(value.user) ? { user: normalizedUser(value.user) } : {}
+	};
+}
+function normalizeHostPullRequest(value, fallback) {
+	const normalized = normalizePullRequestContext(value);
+	if (normalized) return normalized;
+	if (!isRecord$1(value)) {
+		if (!fallback.issue?.pullRequest || fallback.number === void 0 || !fallback.repository) return;
+		return {
+			...fallback.issue.pullRequest.apiUrl ? { apiUrl: fallback.issue.pullRequest.apiUrl } : {},
+			...fallback.issue.body ? { body: fallback.issue.body } : {},
+			...fallback.issue.pullRequest.htmlUrl || fallback.issue.htmlUrl ? { htmlUrl: fallback.issue.pullRequest.htmlUrl || fallback.issue.htmlUrl } : {},
+			...fallback.issue.labels ? { labels: fallback.issue.labels } : {},
+			number: fallback.number,
+			provider: "github",
+			repository: fallback.repository,
+			...fallback.issue.title ? { title: fallback.issue.title } : {}
+		};
+	}
+	const number = maybeContextValue(value.number) ?? fallback.number;
+	const repository = normalizedRepositoryFullName(value.repository) || fallback.repository;
+	if (number === void 0 || !repository) return;
+	const base = normalizedRef(value.base);
+	const head = normalizedRef(value.head);
+	const source = normalizedSource(value.source) || (head?.repo ? {
+		...head.ref ? { ref: head.ref } : {},
+		repo: head.repo
+	} : void 0);
+	const labels = normalizedLabelNames(value.labels) || fallback.issue?.labels;
+	return {
+		...maybeString$1(value.apiUrl) ? { apiUrl: maybeString$1(value.apiUrl) } : {},
+		...maybeString$1(value.url) ? { apiUrl: maybeString$1(value.url) } : {},
+		...!maybeString$1(value.apiUrl) && !maybeString$1(value.url) && fallback.issue?.pullRequest?.apiUrl ? { apiUrl: fallback.issue.pullRequest.apiUrl } : {},
+		...base ? { base } : {},
+		...base?.ref ? { baseRef: base.ref } : {},
+		...maybeString$1(value.body) || fallback.issue?.body ? { body: maybeString$1(value.body) || fallback.issue.body } : {},
+		...head ? { head } : {},
+		...source?.ref || head?.ref ? { headRef: source?.ref || head?.ref } : {},
+		...maybeString$1(value.htmlUrl) ? { htmlUrl: maybeString$1(value.htmlUrl) } : {},
+		...maybeString$1(value.html_url) || fallback.issue?.pullRequest?.htmlUrl ? { htmlUrl: maybeString$1(value.html_url) || fallback.issue.pullRequest.htmlUrl } : {},
+		...maybeContextValue(value.id) !== void 0 ? { id: maybeContextValue(value.id) } : {},
+		...labels ? { labels } : {},
+		number,
+		provider: "github",
+		repository,
+		...source ? { source } : {},
+		...maybeString$1(value.title) || fallback.issue?.title ? { title: maybeString$1(value.title) || fallback.issue.title } : {}
+	};
+}
+function readPullRequestContext(context, contextKey = "pullRequest") {
+	if (isRecord$1(context) && isRecord$1(context.context) && typeof context.context.get === "function") return normalizePullRequestContext(context.context.get(contextKey));
+	if (isRecord$1(context) && typeof context.get === "function") return normalizePullRequestContext(context.get(contextKey));
+	return normalizePullRequestContext(context);
+}
+function isAsyncRecord(value) {
+	return isRecord$1(value) && typeof value.keys === "function" && typeof value.get === "function" && typeof value.pick === "function" && typeof value.entries === "function" && typeof value.resolveAll === "function";
+}
+function createAsyncRecord(label, knownKeys, resolveKeys, loaders) {
+	const known = new Set(knownKeys);
+	const loads = /* @__PURE__ */ new Map();
+	let keysPromise;
+	function assertKey(key) {
+		if (!known.has(key)) throw new Error(`[vitehub] Unknown ${label} key "${key}". Known keys: ${knownKeys.join(", ")}.`);
+		return key;
+	}
+	async function load(key) {
+		let promise = loads.get(key);
+		if (!promise) {
+			promise = Promise.resolve().then(() => loaders[key]()).catch((error) => {
+				loads.delete(key);
+				throw error;
+			});
+			loads.set(key, promise);
+		}
+		return promise;
+	}
+	const record = {
+		async keys() {
+			keysPromise ??= Promise.resolve().then(resolveKeys).then((keys) => keys.map(assertKey)).catch((error) => {
+				keysPromise = void 0;
+				throw error;
+			});
+			return keysPromise;
+		},
+		async has(key) {
+			return await record.get(key) !== void 0;
+		},
+		async get(key) {
+			return load(assertKey(key));
+		},
+		async pick(keys) {
+			const result = {};
+			for (const key of keys) {
+				const knownKey = assertKey(key);
+				const value = await load(knownKey);
+				if (value !== void 0) result[knownKey] = value;
+			}
+			return result;
+		},
+		async *entries(keys) {
+			const selected = keys || await record.keys();
+			for (const key of selected) {
+				const knownKey = assertKey(key);
+				const value = await load(knownKey);
+				if (value !== void 0) yield [knownKey, value];
+			}
+		},
+		async resolveAll() {
+			return record.pick(await record.keys());
+		},
+		toJSON() {
+			throw new Error("[vitehub] AsyncRecord values are async. Call resolveAll() before JSON.stringify().");
+		}
+	};
+	return record;
+}
+function contextReader(input) {
+	if (isRecord$1(input) && isRecord$1(input.context) && typeof input.context.get === "function") return input.context;
+	if (isRecord$1(input) && typeof input.get === "function") return input;
+}
+function repositoryHostContextValues(input) {
+	const store = contextReader(input);
+	const record = isRecord$1(input) ? input : void 0;
+	const rootRepository = normalizedRepositoryFullName(record?.repository);
+	const rawIssue = record?.issue ?? store?.get("issue");
+	const rawPullRequest = record?.pullRequest ?? record?.pull_request ?? store?.get("pullRequest") ?? (record?.issue ? void 0 : input);
+	const directPullRequest = readPullRequestContext(rawPullRequest);
+	const issue = normalizeHostIssue(rawIssue, {
+		number: directPullRequest?.number,
+		repository: directPullRequest?.repository || rootRepository
+	}) || (directPullRequest ? normalizeHostIssue({
+		body: directPullRequest.body,
+		htmlUrl: directPullRequest.htmlUrl,
+		labels: directPullRequest.labels,
+		number: directPullRequest.number,
+		pullRequest: {
+			htmlUrl: directPullRequest.htmlUrl,
+			url: directPullRequest.apiUrl
+		},
+		title: directPullRequest.title
+	}, {
+		number: directPullRequest.number,
+		repository: directPullRequest.repository
+	}) : void 0);
+	const pullRequest = directPullRequest || normalizeHostPullRequest(rawPullRequest, {
+		issue,
+		number: issue?.number,
+		repository: issue?.repository || rootRepository
+	});
+	return {
+		...issue ? { issue } : {},
+		...pullRequest ? { pullRequest } : {},
+		...record?.body !== void 0 ? { body: maybeString$1(record.body) } : issue?.body || pullRequest?.body ? { body: issue?.body || pullRequest?.body } : {},
+		...record?.labels !== void 0 ? { labels: normalizedLabelNames(record.labels) || maybeStrings$1(record.labels) } : issue?.labels || pullRequest?.labels ? { labels: issue?.labels || pullRequest?.labels } : {},
+		...record?.comments !== void 0 ? { comments: normalizedComments(record.comments) } : pullRequest?.comments !== void 0 ? { comments: pullRequest.comments } : {},
+		...record?.files !== void 0 ? { files: normalizedFiles(record.files) } : pullRequest?.files !== void 0 ? { files: pullRequest.files } : {}
+	};
+}
+function staticRepositoryHostRecord(input) {
+	const values = repositoryHostContextValues(input);
+	return createAsyncRecord("repositoryHostContext()", repositoryHostContextKeys, () => repositoryHostContextKeys.filter((key) => values[key] !== void 0), {
+		body: () => values.body,
+		comments: () => values.comments,
+		files: () => values.files,
+		issue: () => values.issue,
+		labels: () => values.labels,
+		pullRequest: () => values.pullRequest
+	});
+}
+function repositoryParts(target, nested) {
+	const rawRepo = maybeString$1(nested?.repository) || maybeString$1(target.repository) || maybeString$1(target.repo);
+	const owner = maybeString$1(nested?.owner) || maybeString$1(target.owner);
+	if (!rawRepo) throw new TypeError("[vitehub] repositoryHostContext() target requires repo or repository.");
+	if (rawRepo.includes("/") && !owner) {
+		const [repoOwner, repoName] = rawRepo.split("/", 2);
+		return {
+			owner: repoOwner,
+			repository: repoName,
+			repositoryFullName: rawRepo
+		};
+	}
+	return {
+		...owner ? { owner } : {},
+		repository: rawRepo,
+		repositoryFullName: owner ? `${owner}/${rawRepo}` : rawRepo
+	};
+}
+function targetValueRecord(value) {
+	return isRecord$1(value) ? value : void 0;
+}
+function targetValueId(value) {
+	if (typeof value === "number" || typeof value === "string") return value;
+	if (!isRecord$1(value)) return;
+	return maybeContextValue(value.id) ?? maybeContextValue(value.number);
+}
+function normalizedTarget(input) {
+	const raw = input.pullRequest ?? input.issue ?? input.number;
+	const nested = targetValueRecord(raw);
+	const id = targetValueId(raw);
+	if (id === void 0) throw new TypeError("[vitehub] repositoryHostContext() target requires number, issue, or pullRequest.");
+	const { repositoryFullName, ...repositoryTarget } = repositoryParts(input, nested);
+	const base = {
+		...maybeString$1(nested?.host) || input.host ? { host: maybeString$1(nested?.host) || input.host } : {},
+		id,
+		...repositoryTarget
+	};
+	return {
+		explicitPullRequest: input.pullRequest !== void 0,
+		issue: {
+			...base,
+			kind: "issue"
+		},
+		number: id,
+		pullRequest: {
+			...base,
+			kind: "changeRequest"
+		},
+		repositoryFullName
+	};
+}
+async function resolveRepositoryHostContextClient(options, context) {
+	const client = options.client ? typeof options.client === "function" ? await options.client(context) : options.client : requirePrimitive(context, "repository-host");
+	if (!client || typeof client !== "object" || typeof client.read !== "function") throw new Error("[vitehub] repositoryHostContext() requires a Repository Host client with read().");
+	const provider = options.provider || client.provider;
+	if (provider && provider !== "github") throw new Error("[vitehub] repositoryHostContext() supports GitHub targets in V1.");
+	return client;
+}
+function targetRepositoryHostRecord(client, target) {
+	const normalized = normalizedTarget(target);
+	let issuePromise;
+	let pullRequestPromise;
+	function loadIssue() {
+		issuePromise ??= Promise.resolve(client.read({
+			operation: "issue",
+			target: normalized.issue
+		})).then((value) => normalizeHostIssue(value, {
+			number: normalized.number,
+			repository: normalized.repositoryFullName
+		})).catch((error) => {
+			issuePromise = void 0;
+			throw error;
+		});
+		return issuePromise;
+	}
+	async function loadPullRequest() {
+		if (!pullRequestPromise) pullRequestPromise = (async () => {
+			const issue = await loadIssue();
+			if (!normalized.explicitPullRequest && !issue?.pullRequest) return;
+			return normalizeHostPullRequest(await client.read({
+				operation: "changeRequest",
+				target: normalized.pullRequest
+			}), {
+				issue,
+				number: normalized.number,
+				repository: normalized.repositoryFullName
+			});
+		})().catch((error) => {
+			pullRequestPromise = void 0;
+			throw error;
+		});
+		return pullRequestPromise;
+	}
+	return createAsyncRecord("repositoryHostContext()", repositoryHostContextKeys, async () => {
+		const issue = await loadIssue();
+		return [
+			"issue",
+			"body",
+			"labels",
+			"comments",
+			...normalized.explicitPullRequest || issue?.pullRequest ? ["pullRequest", "files"] : []
+		];
+	}, {
+		body: async () => (await loadIssue())?.body ?? (await loadPullRequest())?.body,
+		comments: async () => normalizedComments(await client.read({
+			operation: "comments",
+			target: normalized.issue
+		})),
+		files: async () => {
+			if (!await loadPullRequest()) return;
+			return normalizedFiles(await client.read({
+				operation: "changeRequestFiles",
+				target: normalized.pullRequest
+			}));
+		},
+		issue: loadIssue,
+		labels: async () => (await loadIssue())?.labels ?? (await loadPullRequest())?.labels,
+		pullRequest: loadPullRequest
+	});
+}
+function readRepositoryHostContext(input, contextKey = defaultRepositoryHostContextKey) {
+	if (isAsyncRecord(input)) return input;
+	const store = contextReader(input);
+	if (store) {
+		const value = store.get(contextKey);
+		if (isAsyncRecord(value)) return value;
+		return staticRepositoryHostRecord(value ?? (contextKey === defaultRepositoryHostContextKey ? input : void 0));
+	}
+	const value = isRecord$1(input) ? input[contextKey] : void 0;
+	if (isAsyncRecord(value)) return value;
+	return staticRepositoryHostRecord(value ?? (contextKey === defaultRepositoryHostContextKey ? input : void 0));
+}
+function createRepositoryHostContext(options = {}) {
+	const materialization = options.materialize;
+	const capabilityId = options.id || defaultRepositoryHostContextId;
+	const contextKey = options.contextKey || defaultRepositoryHostContextKey;
+	const recordedContexts = /* @__PURE__ */ new WeakSet();
+	async function recordContext(context) {
+		if (recordedContexts.has(context.context)) return;
+		const hasContextOption = "context" in options;
+		const hasTargetOption = "target" in options;
+		const input = await resolveMaybeFunction(options.context, context);
+		const target = await resolveMaybeFunction(options.target, context);
+		const existing = context.context.get(contextKey);
+		if (existing !== void 0) {
+			if (!isAsyncRecord(existing)) context.context.set(contextKey, staticRepositoryHostRecord(existing), { overwrite: true });
+			recordedContexts.add(context.context);
+			return;
+		}
+		if (target === false || target === null || target === void 0) {
+			if ((hasContextOption || hasTargetOption) && (input === false || input === null || input === void 0)) return;
+		}
+		const value = target ? targetRepositoryHostRecord(await resolveRepositoryHostContextClient(options, context), target) : staticRepositoryHostRecord(input ?? context);
+		context.context.set(contextKey, value);
+		recordedContexts.add(context.context);
+	}
+	return Object.assign(defineCapability({
+		id: capabilityId,
+		metadata: {
+			contextKey,
+			kind: "repository-host-context"
+		},
+		prepare: recordContext,
+		requires: options.target && !options.client ? [{ primitive: "repository-host" }] : void 0,
+		triggers: options.triggers,
+		...materialization ? { async workspace(context) {
+			if (typeof materialization === "string") throw new TypeError("[vitehub] repositoryHostContext() materialize paths require the ViteHub Agent plugin.");
+			await recordContext(context);
+			const value = context.context.get(contextKey);
+			if (value === void 0) return;
+			if (!isAsyncRecord(value)) throw new Error("[vitehub] repositoryHostContext() could not resolve materialization data.");
+			return { sources: { [capabilityId]: {
+				content: await materialization.template(await value.resolveAll()),
+				materialize: "build",
+				mount: "",
+				workspacePath: materialization.path
+			} } };
+		} } : {}
+	}), typeof materialization === "object" ? { [workspaceMaterializationPathsSymbol]: [materialization.path] } : {});
+}
+const repositoryHostContext = Object.assign(createRepositoryHostContext, { read: readRepositoryHostContext });
+//#endregion
+//#region src/delivery-effects.ts
+function intent(kind, payload, options = {}) {
+	return {
+		kind,
+		...options.artifacts?.length ? { artifacts: options.artifacts } : {},
+		...options.intent ? { intent: options.intent } : {},
+		...options.metadata ? { metadata: options.metadata } : {},
+		...payload !== void 0 ? { payload } : {}
+	};
+}
+function defineFinishEffect(effect) {
+	return effect;
+}
+function createReplyDeliveryEffectIntent(input, options = {}) {
+	if (typeof input === "string" || isAsyncIterable(input)) return intent("reply", input, options);
+	const { artifacts, ...payload } = input;
+	return intent("reply", payload, {
+		...options,
+		artifacts: options.artifacts ?? artifacts
+	});
+}
+function createReactionDeliveryEffectIntent(input, options = {}) {
+	return intent("reaction", input, options);
+}
+function createStatusDeliveryEffectIntent(input, options = {}) {
+	return intent("status", typeof input === "string" ? { state: input } : input, options);
+}
+//#endregion
+//#region src/channels.ts
+const messageChannelTitleSupportContextKey = "channel.delivery.supportsTitle";
+const customTitleEffectChannels = /* @__PURE__ */ new WeakSet();
+function githubPullRequestRunContextFromUnknown(input) {
+	if (!isRecord(input)) return;
+	const value = isRecord(input.pullRequest) && isRecord(input.pullRequest.pullRequest) ? input.pullRequest : input;
+	if (!isRecord(value.pullRequest) || !isRecord(value.repository) || !isRecord(value.run) || !isRecord(value.trigger)) return;
+	return value;
+}
+const pullRequest = { read(invocation) {
+	const context = readPullRequestContext(invocation);
+	if (!context) throw new Error("[vitehub] pullRequest.read() requires pull request invocation context.");
+	return context;
+} };
+function isRecord(value) {
+	return typeof value === "object" && value !== null;
+}
+function maybeString(value) {
+	return typeof value === "string" && value ? value : void 0;
+}
+function maybeNumber(value) {
+	return typeof value === "number" && Number.isFinite(value) ? value : void 0;
+}
+function normalizeGitHubPullRequestWorkspaceMount(mount) {
+	const parts = mount.replace(/\\/g, "/").replace(/^\/+|\/+$/g, "").split("/").filter(Boolean);
+	if (mount.startsWith("/") || /^[A-Za-z]:[\\/]/.test(mount) || parts.some((part) => part === "." || part === "..") || parts[0] === ".git" || parts[0] === ".vitehub") throw new TypeError("[vitehub] GitHub pull request workspace mount must stay inside the Workspace.");
+	return parts.join("/");
+}
+function githubPullRequestWorkspacePolicy(options) {
+	if (options.workspace === false) return {
+		enabled: false,
+		mount: options.sourceMount ?? ""
+	};
+	return {
+		enabled: true,
+		mount: normalizeGitHubPullRequestWorkspaceMount(options.workspace === true ? "" : typeof options.workspace === "object" ? options.workspace.mount ?? "" : options.sourceMount ?? "portal")
+	};
+}
+function maybeStrings(value) {
+	if (!Array.isArray(value)) return;
+	const strings = value.filter((item) => typeof item === "string" && Boolean(item));
+	return strings.length ? strings : void 0;
+}
+function githubUserMetadata(value) {
+	if (!isRecord(value)) return;
+	const login = maybeString(value.login);
+	const id = maybeNumber(value.id);
+	const type = maybeString(value.type);
+	if (!login && !id && !type) return;
+	return {
+		...id !== void 0 ? { id } : {},
+		...login ? { login } : {},
+		...type ? { type } : {}
+	};
+}
+function githubCommentMetadata(value) {
+	if (!isRecord(value)) return;
+	const id = maybeNumber(value.id);
+	if (id === void 0) return;
+	const user = githubUserMetadata(value.user);
+	return {
+		...maybeString(value.author_association) ? { authorAssociation: maybeString(value.author_association) } : {},
+		...maybeString(value.body) ? { body: maybeString(value.body) } : {},
+		...maybeString(value.created_at) ? { createdAt: maybeString(value.created_at) } : {},
+		...maybeString(value.html_url) ? { htmlUrl: maybeString(value.html_url) } : {},
+		id,
+		...maybeString(value.node_id) ? { nodeId: maybeString(value.node_id) } : {},
+		...maybeString(value.updated_at) ? { updatedAt: maybeString(value.updated_at) } : {},
+		...user ? { user } : {}
+	};
+}
+function githubFileMetadata(value) {
+	if (!isRecord(value)) return;
+	const filename = maybeString(value.filename);
+	if (!filename) return;
+	const additions = maybeNumber(value.additions);
+	const changes = maybeNumber(value.changes);
+	const deletions = maybeNumber(value.deletions);
+	return {
+		...additions !== void 0 ? { additions } : {},
+		...maybeString(value.blob_url) ? { blobUrl: maybeString(value.blob_url) } : {},
+		...changes !== void 0 ? { changes } : {},
+		...maybeString(value.contents_url) ? { contentsUrl: maybeString(value.contents_url) } : {},
+		...deletions !== void 0 ? { deletions } : {},
+		filename,
+		...maybeString(value.previous_filename) ? { previousFilename: maybeString(value.previous_filename) } : {},
+		...maybeString(value.raw_url) ? { rawUrl: maybeString(value.raw_url) } : {},
+		...maybeString(value.status) ? { status: maybeString(value.status) } : {}
+	};
+}
+function githubRefMetadata(value) {
+	if (!isRecord(value)) return;
+	const ref = maybeString(value.ref);
+	const repo = isRecord(value.repo) ? maybeString(value.repo.full_name) : void 0;
+	const sha = maybeString(value.sha);
+	if (!ref && !repo && !sha) return;
+	return {
+		...ref ? { ref } : {},
+		...repo ? { repo } : {},
+		...sha ? { sha } : {}
+	};
+}
+const defaultGitHubPullRequestMaxBodyLength = 12e3;
+const defaultGitHubPullRequestMaxCommentBodyLength = 2e3;
+const defaultGitHubPullRequestMaxComments = 30;
+const defaultGitHubPullRequestMaxFiles = 200;
+function githubPullRequestLimit(value, fallback) {
+	return typeof value === "number" && Number.isFinite(value) && value >= 0 ? Math.floor(value) : fallback;
+}
+function truncateGitHubPullRequestText(value, maxLength) {
+	if (!value || value.length <= maxLength) return value;
+	return maxLength > 0 ? `${value.slice(0, maxLength)}\n[truncated ${value.length - maxLength} characters]` : `[truncated ${value.length} characters]`;
+}
+function chatFinishExtension(input) {
+	return chatFinishExtensionFromUnknown(isRecord(input.context) ? input.context[CHAT_FINISH_EXTENSION_CONTEXT_KEY] : void 0);
+}
+function chatFinishExtensionFromUnknown(value) {
+	return isRecord(value) && typeof value.sendMessage === "function" ? value : void 0;
+}
+function inputPayload(input) {
+	if (!isRecord(input)) return;
+	return isRecord(input.payload) ? input.payload : void 0;
+}
+function inputPayloadOrBody(input) {
+	const payload = inputPayload(input);
+	if (payload) return payload;
+	if (!isRecord(input) || typeof input.body !== "string") return;
+	return JSON.parse(input.body || "{}");
+}
+function inputGithubFacts(input) {
+	if (!isRecord(input)) return;
+	return isRecord(input.github) ? input.github : void 0;
+}
+function parseSlashCommand(body) {
+	const text = body.trim();
+	const match = /^(\/[a-z][a-z0-9_-]*)(?:\s+([\s\S]*))?$/i.exec(text);
+	if (!match) return;
+	return {
+		args: match[2]?.trim() || "",
+		command: match[1]
+	};
+}
+function inputCommandsMetadata(value) {
+	if (!isRecord(value) || Array.isArray(value.commands) || !isRecord(value.commands) || typeof value.trigger !== "string") return;
+	return {
+		commands: value.commands,
+		trigger: value.trigger
+	};
+}
+function declaredInputCommand(context, command) {
+	let sawMatchingTrigger = false;
+	const channelId = context.trigger.channelId;
+	for (const capability of context.agentCapabilities || []) {
+		const metadata = inputCommandsMetadata(capability.metadata);
+		if (!metadata || !command.startsWith(metadata.trigger)) continue;
+		const name = command.slice(metadata.trigger.length);
+		if (!name) continue;
+		sawMatchingTrigger = true;
+		const configured = metadata.commands[name];
+		if (!configured) continue;
+		const channels = isRecord(configured) && Array.isArray(configured.channels) ? configured.channels : void 0;
+		if (!channels?.length || channels.includes(channelId || "")) return true;
+	}
+	return sawMatchingTrigger ? false : void 0;
+}
+function githubPullRequestCommandFromInput(input) {
+	const payload = inputPayload(input);
+	const facts = inputGithubFacts(input);
+	const event = maybeString(facts?.event);
+	if (!payload || event && event !== "issue_comment" || payload.action !== "created") return;
+	if (!payload.issue?.pull_request) return;
+	const body = maybeString(payload.comment?.body);
+	const parsed = body ? parseSlashCommand(body) : void 0;
+	if (!body || !parsed) return;
+	const repository = maybeString(payload.repository?.full_name);
+	const [owner, repo] = repository?.split("/") || [];
+	const login = maybeString(payload.comment?.user?.login);
+	const issueNumber = maybeNumber(payload.issue?.number);
+	const commentId = maybeNumber(payload.comment?.id);
+	const pullRequestUrl = maybeString(payload.issue.pull_request.url);
+	if (!repository || !owner || !repo || !login || !issueNumber || !commentId || !pullRequestUrl) return;
+	const association = maybeString(payload.comment?.author_association) || maybeString(payload.issue.author_association);
+	const installationId = maybeNumber(facts?.installationId) ?? maybeNumber(payload.installation?.id);
+	return {
+		action: "created",
+		actor: {
+			...association ? { association } : {},
+			...maybeNumber(payload.comment?.user?.id) ? { id: maybeNumber(payload.comment?.user?.id) } : {},
+			login,
+			...maybeString(payload.comment?.user?.type) ? { type: maybeString(payload.comment?.user?.type) } : {}
+		},
+		args: parsed.args,
+		body,
+		command: parsed.command,
+		commentId,
+		...maybeString(payload.comment?.node_id) ? { commentNodeId: maybeString(payload.comment?.node_id) } : {},
+		...maybeString(facts?.deliveryId) ? { deliveryId: maybeString(facts?.deliveryId) } : {},
+		...installationId ? { installationId } : {},
+		issueNumber,
+		owner,
+		pullRequestUrl,
+		repo,
+		repository
+	};
+}
+async function githubPullRequestMetadata(app, context, command, options, payload) {
+	const maxBodyLength = githubPullRequestLimit(options.maxBodyLength, defaultGitHubPullRequestMaxBodyLength);
+	const maxCommentBodyLength = githubPullRequestLimit(options.maxCommentBodyLength, defaultGitHubPullRequestMaxCommentBodyLength);
+	const maxComments = githubPullRequestLimit(options.maxComments, defaultGitHubPullRequestMaxComments);
+	const maxFiles = githubPullRequestLimit(options.maxFiles, defaultGitHubPullRequestMaxFiles);
+	const fallbackBody = truncateGitHubPullRequestText(maybeString(payload?.issue?.body), maxBodyLength);
+	const fallback = { ...fallbackBody ? { body: fallbackBody } : {} };
+	try {
+		const appOptions = app ? githubAppOptions(app) || {} : {};
+		const token = await githubPullRequestMetadataToken(app, context, command.installationId).catch(() => void 0);
+		const fetcher = appOptions.fetch || fetch;
+		const headers = githubApiHeaders(token, appOptions.userAgent);
+		const apiBaseUrl = appOptions.apiBaseUrl || "https://api.github.com";
+		const [pullRequest, comments, files] = await Promise.all([
+			githubApiJson(fetcher, command.pullRequestUrl, headers),
+			token ? githubApiJsonPages(fetcher, `${apiBaseUrl}/repos/${command.owner}/${command.repo}/issues/${command.issueNumber}/comments`, headers, maxComments + 1) : [],
+			token ? githubApiJsonPages(fetcher, `${apiBaseUrl}/repos/${command.owner}/${command.repo}/pulls/${command.issueNumber}/files`, headers, maxFiles + 1) : []
+		]);
+		const commentMetadata = Array.isArray(comments) ? comments.map(githubCommentMetadata).filter((comment) => Boolean(comment)) : [];
+		const fileMetadata = Array.isArray(files) ? files.map(githubFileMetadata).filter((file) => Boolean(file)) : [];
+		const metadata = {
+			...commentMetadata.length > maxComments ? { omittedComments: commentMetadata.length - maxComments } : {},
+			...fileMetadata.length > maxFiles ? { omittedFiles: fileMetadata.length - maxFiles } : {}
+		};
+		const body = isRecord(pullRequest) ? truncateGitHubPullRequestText(maybeString(pullRequest.body), maxBodyLength) : void 0;
+		return {
+			...fallback,
+			...body ? { body } : {},
+			...isRecord(pullRequest) && githubRefMetadata(pullRequest.base) ? { base: githubRefMetadata(pullRequest.base) } : {},
+			...isRecord(pullRequest) && githubRefMetadata(pullRequest.head) ? { head: githubRefMetadata(pullRequest.head) } : {},
+			...commentMetadata.length ? { comments: commentMetadata.slice(0, maxComments).map((comment) => ({
+				...comment,
+				...comment.body ? { body: truncateGitHubPullRequestText(comment.body, maxCommentBodyLength) } : {}
+			})) } : {},
+			...fileMetadata.length ? { files: fileMetadata.slice(0, maxFiles) } : {},
+			...Object.keys(metadata).length ? { metadata } : {}
+		};
+	} catch (error) {
+		return {
+			...fallback,
+			metadata: { unavailable: error instanceof Error && error.message ? error.message : "unknown" }
+		};
+	}
+}
+function githubPullRequestRunContext(command, options = {}, payload, metadata = {}) {
+	const runId = command.deliveryId || `github:${command.repository}#${command.issueNumber}:comment:${command.commentId}`;
+	const workspace = githubPullRequestWorkspacePolicy(options);
+	const labels = maybeStrings(Array.isArray(payload?.issue?.labels) ? payload.issue.labels.map((label) => isRecord(label) ? maybeString(label.name) : void 0) : void 0);
+	return {
+		pullRequest: {
+			apiUrl: command.pullRequestUrl,
+			...metadata,
+			...maybeString(payload?.issue?.pull_request?.html_url) || maybeString(payload?.issue?.html_url) ? { htmlUrl: maybeString(payload?.issue?.pull_request?.html_url) || maybeString(payload?.issue?.html_url) } : {},
+			...labels ? { labels } : {},
+			number: command.issueNumber,
+			source: {
+				...!workspace.enabled ? { checkout: false } : {},
+				mount: workspace.enabled ? workspace.mount : options.sourceMount ?? command.repo,
+				ref: `refs/pull/${command.issueNumber}/head`,
+				repo: command.repository
+			},
+			...maybeString(payload?.issue?.title) ? { title: maybeString(payload?.issue?.title) } : {}
+		},
+		repository: {
+			fullName: command.repository,
+			name: command.repo,
+			owner: command.owner
+		},
+		run: {
+			messageId: String(command.commentId),
+			origin: options.origin || "github-pull-request-comment",
+			runId,
+			threadId: options.threadId || command.pullRequestUrl
+		},
+		trigger: {
+			action: command.action,
+			actor: command.actor,
+			args: command.args,
+			command: command.command,
+			comment: {
+				...command.actor.association ? { authorAssociation: command.actor.association } : {},
+				...maybeString(payload?.comment?.body) ? { body: maybeString(payload?.comment?.body) } : {},
+				...maybeString(payload?.comment?.created_at) ? { createdAt: maybeString(payload?.comment?.created_at) } : {},
+				...maybeString(payload?.comment?.html_url) ? { htmlUrl: maybeString(payload?.comment?.html_url) } : {},
+				id: command.commentId,
+				...command.commentNodeId ? { nodeId: command.commentNodeId } : {},
+				...maybeString(payload?.comment?.updated_at) ? { updatedAt: maybeString(payload?.comment?.updated_at) } : {}
+			},
+			...command.deliveryId ? { deliveryId: command.deliveryId } : {},
+			event: "issue_comment",
+			...command.installationId ? { installationId: command.installationId } : {},
+			...payload?.sender ? { sender: {
+				...maybeNumber(payload.sender.id) ? { id: maybeNumber(payload.sender.id) } : {},
+				...maybeString(payload.sender.login) ? { login: maybeString(payload.sender.login) } : {},
+				...maybeString(payload.sender.type) ? { type: maybeString(payload.sender.type) } : {}
+			} } : {}
+		}
+	};
+}
+function pullRequestCommandInput(command, pullRequest) {
+	return {
+		context: {
+			github: command,
+			pullRequest
+		},
+		prompt: command.body
+	};
+}
+function githubCommandFromUnknown(value) {
+	if (!isRecord(value)) return;
+	const owner = maybeString(value.owner);
+	const repo = maybeString(value.repo);
+	const repository = maybeString(value.repository) || (owner && repo ? `${owner}/${repo}` : void 0);
+	const issueNumber = maybeNumber(value.issueNumber);
+	const commentId = maybeNumber(value.commentId);
+	const pullRequestUrl = maybeString(value.pullRequestUrl);
+	if (!owner || !repo || !repository || !issueNumber || !commentId || !pullRequestUrl) return;
+	return {
+		action: "created",
+		actor: isRecord(value.actor) && maybeString(value.actor.login) ? {
+			...maybeString(value.actor.association) ? { association: maybeString(value.actor.association) } : {},
+			...maybeNumber(value.actor.id) ? { id: maybeNumber(value.actor.id) } : {},
+			login: maybeString(value.actor.login),
+			...maybeString(value.actor.type) ? { type: maybeString(value.actor.type) } : {}
+		} : { login: "" },
+		args: maybeString(value.args) || "",
+		body: maybeString(value.body) || maybeString(value.command) || "",
+		command: maybeString(value.command) || "",
+		commentId,
+		...maybeString(value.commentNodeId) ? { commentNodeId: maybeString(value.commentNodeId) } : {},
+		...maybeString(value.deliveryId) ? { deliveryId: maybeString(value.deliveryId) } : {},
+		...maybeNumber(value.installationId) ? { installationId: maybeNumber(value.installationId) } : {},
+		issueNumber,
+		owner,
+		pullRequestUrl,
+		repo,
+		repository
+	};
+}
+function githubCommandFromEffect(context) {
+	const effectPayload = isRecord(context.effect.payload) ? context.effect.payload : void 0;
+	const effectMetadata = context.effect.metadata;
+	return githubCommandFromUnknown(effectPayload?.github) || githubCommandFromUnknown(effectMetadata?.github) || githubCommandFromUnknown(isRecord(context.input.context) ? context.input.context.github : void 0);
+}
+async function resolveEffectOption(value, context) {
+	if (typeof value === "function") return await value(context);
+	if (isRecord(value) && typeof value.resolve === "function") return await value.resolve(context);
+	return value;
+}
+async function resolveGithubAppOption(value, context) {
+	if (value === void 0) return void 0;
+	if (typeof value === "function") return await value(context);
+	if (isRecord(value) && typeof value.resolve === "function") return await value.resolve(context);
+	return value;
+}
+function unseal(value) {
+	return isRecord(value) && typeof value.unseal === "function" ? value.unseal() : value;
+}
+function cleanSecret(value) {
+	const secret = unseal(value);
+	return typeof secret === "string" && secret.trim() ? secret.trim() : void 0;
+}
+const serverEnvModuleId = "#vitehub/env/server";
+async function githubEnv(event) {
+	const fallback = typeof process === "object" && process?.env ? {
+		appId: process.env.GITHUB_APP_ID,
+		appInstallationId: process.env.GITHUB_APP_INSTALLATION_ID,
+		appPrivateKey: process.env.GITHUB_APP_PRIVATE_KEY,
+		appPrivateKeyPath: process.env.GITHUB_APP_PRIVATE_KEY_PATH,
+		token: process.env.VITEHUB_GITHUB_TOKEN || process.env.GH_TOKEN || process.env.GITHUB_TOKEN,
+		webhookSecret: process.env.GITHUB_WEBHOOK_SECRET
+	} : {};
+	try {
+		const env = (await import(
+			/* @vite-ignore */
+			/* @vitehub-env */
+			serverEnvModuleId
+)).useServerEnv?.(event);
+		const github = isRecord(env) && isRecord(env.github) ? Object.fromEntries(Object.entries(env.github).filter(([, value]) => value !== void 0)) : {};
+		return {
+			...fallback,
+			...github
+		};
+	} catch {
+		return fallback;
+	}
+}
+function githubAppOptions(app) {
+	return app === true ? {} : app;
+}
+async function githubAppSetting(options, env, key, envKey, context) {
+	return await resolveGithubAppOption(options[key], context) ?? unseal(env[envKey]);
+}
+function requiredString(value, name) {
+	const string = typeof value === "number" ? String(value) : cleanSecret(value);
+	if (!string) throw new Error(`[vitehub] Missing GitHub App ${name}.`);
+	return string;
+}
+function requiredNumber(value, name) {
+	const number = typeof value === "number" ? value : Number(cleanSecret(value));
+	if (!Number.isFinite(number)) throw new Error(`[vitehub] Missing GitHub App ${name}.`);
+	return number;
+}
+async function githubAppPrivateKey(options, env, context) {
+	const inline = cleanSecret(await githubAppSetting(options, env, "privateKey", "appPrivateKey", context));
+	if (inline) return inline.replace(/\\n/g, "\n");
+	const path = cleanSecret(await githubAppSetting(options, env, "privateKeyPath", "appPrivateKeyPath", context));
+	if (path) try {
+		const { readFileSync } = await import(
+			/* @vite-ignore */
+			"node:fs"
+);
+		const file = readFileSync(path, "utf8").trim();
+		if (file) return file.replace(/\\n/g, "\n");
+	} catch (error) {
+		throw new Error(`[vitehub] Failed to read GitHub App privateKeyPath: ${path}`, { cause: error });
+	}
+	throw new Error("[vitehub] Missing GitHub App privateKey. github.app.privateKey, github.app.privateKeyPath, GITHUB_APP_PRIVATE_KEY, or GITHUB_APP_PRIVATE_KEY_PATH is required.");
+}
+function base64url(value) {
+	return Buffer.from(value).toString("base64url");
+}
+function githubAppJwt(appId, privateKey) {
+	const now = Math.floor(Date.now() / 1e3);
+	const data = `${base64url(JSON.stringify({
+		alg: "RS256",
+		typ: "JWT"
+	}))}.${base64url(JSON.stringify({
+		exp: now + 540,
+		iat: now - 60,
+		iss: appId
+	}))}`;
+	return `${data}.${createSign("RSA-SHA256").update(data).sign(privateKey).toString("base64url")}`;
+}
+const githubAppTokenCache = /* @__PURE__ */ new Map();
+async function githubAppInstallationToken(app, context, installation) {
+	const options = githubAppOptions(app) || {};
+	const env = await githubEnv(context);
+	const appId = requiredString(await githubAppSetting(options, env, "appId", "appId", context), "appId");
+	const installationId = installation ?? ("effect" in context ? githubCommandFromEffect(context)?.installationId : void 0) ?? requiredNumber(await githubAppSetting(options, env, "installationId", "appInstallationId", context), "installationId");
+	const apiBaseUrl = options.apiBaseUrl || "https://api.github.com";
+	const cacheKey = `${apiBaseUrl}:${appId}:${installationId}`;
+	const cached = githubAppTokenCache.get(cacheKey);
+	if (cached && cached.expiresAt > Date.now() + 6e4) return cached.token;
+	const body = await (await githubApi(options.fetch || fetch, `${apiBaseUrl}/app/installations/${installationId}/access_tokens`, {
+		headers: githubApiHeaders(githubAppJwt(appId, await githubAppPrivateKey(options, env, context)), options.userAgent),
+		method: "POST"
+	})).json().catch(() => void 0);
+	const token = isRecord(body) && typeof body.token === "string" ? body.token : void 0;
+	if (!token) throw new Error("[vitehub] GitHub App installation token response did not include token.");
+	const expiresAt = isRecord(body) && typeof body.expires_at === "string" ? Date.parse(body.expires_at) : Date.now() + 9 * 6e4;
+	githubAppTokenCache.set(cacheKey, {
+		expiresAt,
+		token
+	});
+	return token;
+}
+async function githubPullRequestMetadataToken(app, context, installation) {
+	const token = cleanSecret((await githubEnv(context)).token);
+	if (!app) return token;
+	try {
+		return await githubAppInstallationToken(app, context, installation);
+	} catch (error) {
+		if (token) return token;
+		throw error;
+	}
+}
+async function githubAppWebhookSecret(app, context) {
+	const secret = await githubAppSetting(githubAppOptions(app) || {}, await githubEnv(context), "webhookSecret", "webhookSecret", context);
+	if (secret === false) return false;
+	return cleanSecret(secret) || "";
+}
+function staticGithubAppWebhookSecret(app) {
+	if (app === true) return;
+	const secret = app.webhookSecret;
+	if (secret === false) return false;
+	if (typeof secret === "function") return;
+	return cleanSecret(secret);
+}
+function githubAppWebhookSecretToken(app) {
+	const staticSecret = staticGithubAppWebhookSecret(app);
+	if (staticSecret !== void 0) return staticSecret;
+	return (context) => githubAppWebhookSecret(app, context);
+}
+function githubApiHeaders(token, userAgent) {
+	return {
+		accept: "application/vnd.github+json",
+		...token ? { authorization: `Bearer ${token}` } : {},
+		"content-type": "application/json",
+		...userAgent ? { "user-agent": userAgent } : {},
+		"x-github-api-version": "2022-11-28"
+	};
+}
+async function githubApi(fetcher, url, init) {
+	const response = await fetcher(url, init);
+	if (!response.ok) throw new Error(`[vitehub] GitHub delivery effect failed with ${response.status}.`);
+	return response;
+}
+async function githubApiJson(fetcher, url, headers) {
+	const response = await fetcher(url, {
+		headers,
+		method: "GET"
+	});
+	if (!response.ok) throw new Error(`[vitehub] GitHub metadata request failed with ${response.status}.`);
+	return await response.json().catch(() => void 0);
+}
+async function githubApiJsonPages(fetcher, url, headers, limit) {
+	const items = [];
+	let page = 1;
+	let nextUrl = githubApiPageUrl(url, page);
+	while (nextUrl && (limit <= 0 || items.length < limit)) {
+		const response = await fetcher(nextUrl, {
+			headers,
+			method: "GET"
+		});
+		if (!response.ok) throw new Error(`[vitehub] GitHub metadata request failed with ${response.status}.`);
+		const pageItems = await response.json().catch(() => void 0);
+		if (!Array.isArray(pageItems) || !pageItems.length) break;
+		items.push(...pageItems);
+		if (limit > 0 && items.length >= limit) break;
+		nextUrl = githubApiNextPageUrl(response.headers.get("link")) || githubApiPageUrl(url, ++page);
+	}
+	return limit > 0 ? items.slice(0, limit) : [];
+}
+function githubApiPageUrl(url, page) {
+	const parsed = new URL(url);
+	parsed.searchParams.set("per_page", "100");
+	if (page > 1) parsed.searchParams.set("page", String(page));
+	return parsed.toString();
+}
+function githubApiNextPageUrl(link) {
+	return link?.split(",").map((part) => part.trim()).find((part) => part.endsWith(`rel="next"`))?.match(/^<([^>]+)>/)?.[1];
+}
+function reactionContent(context) {
+	if (typeof context.effect.payload === "string") return context.effect.payload;
+	if (isRecord(context.effect.payload) && typeof context.effect.payload.content === "string") return context.effect.payload.content;
+	if (isRecord(context.effect.payload) && typeof context.effect.payload.emoji === "string") return context.effect.payload.emoji;
+	if (context.effect.intent === "completed") return "hooray";
+	if (context.effect.intent === "failed") return "confused";
+	return "eyes";
+}
+function reactionAction(context) {
+	return isRecord(context.effect.payload) ? maybeString(context.effect.payload.action) : void 0;
+}
+function transientReactionKey(context) {
+	return maybeString(context.effect.metadata?.transientKey);
+}
+function transientReactionStore(input) {
+	const context = input.context;
+	if (!context) return {};
+	const key = "github.delivery.transientReactions";
+	if (!isRecord(context[key])) context[key] = {};
+	return context[key];
+}
+function replyBody(context) {
+	if (typeof context.effect.payload === "string") return context.effect.payload;
+	if (isRecord(context.effect.payload) && typeof context.effect.payload.body === "string") return context.effect.payload.body;
+	if (isRecord(context.effect.payload) && typeof context.effect.payload.markdown === "string") return context.effect.payload.markdown;
+	if (typeof context.effect.metadata?.body === "string") return context.effect.metadata.body;
+	if (typeof context.effect.metadata?.markdown === "string") return context.effect.metadata.markdown;
+}
+function normalizedDeliveryArtifactPath(path) {
+	try {
+		return normalizeDeliveryArtifactPath(path);
+	} catch {
+		return;
+	}
+}
+function replyBodyWithLinkArtifacts(body, artifacts) {
+	const referencedPaths = new Set(deliveryArtifactMarkdownReferencePaths(body, artifacts));
+	const links = artifacts.flatMap((artifact) => {
+		const path = normalizedDeliveryArtifactPath(artifact.path);
+		if (artifact.placement !== "link" || !artifact.url || path && referencedPaths.has(path)) return [];
+		const label = (artifact.alt || artifact.path.split("/").pop() || artifact.path).replace(/[\r\n\[\]]+/g, " ").trim() || "Artifact";
+		const url = artifact.url.replace(/[\r\n<>]+/g, "");
+		return url ? [`[${label}](<${url}>)`] : [];
+	});
+	if (!links.length) return body;
+	return body ? `${body}\n\n${links.join("\n")}` : links.join("\n");
+}
+function deliveryArtifacts(context) {
+	return publishedDeliveryArtifactsFromUnknown([...Array.isArray(context.effect.artifacts) ? context.effect.artifacts : [], ...isRecord(context.effect.payload) && Array.isArray(context.effect.payload.artifacts) ? context.effect.payload.artifacts : []]);
+}
+function deliveryArtifactFilename(artifact) {
+	return artifact.path.split("/").filter(Boolean).at(-1) || "artifact";
+}
+function arrayBufferContent(value) {
+	if (value instanceof ArrayBuffer) return value;
+	const bytes = typeof value === "string" ? new TextEncoder().encode(value) : value;
+	const copy = new Uint8Array(bytes.byteLength);
+	copy.set(bytes);
+	return copy.buffer;
+}
+async function deliveryArtifactFiles(context, artifacts) {
+	if (!context.workspace) return [];
+	const files = [];
+	for (const artifact of artifacts) {
+		if (artifact.url || artifact.placement === "link") continue;
+		const path = normalizeDeliveryArtifactPath(artifact.path, "Delivery artifact path");
+		const stat = await context.workspace.fs.stat(path).catch(() => void 0);
+		if (stat && stat.type !== "file") continue;
+		const mediaType = artifact.mediaType || stat?.mediaType;
+		const content = await context.workspace.fs.readFile(path, { encoding: "binary" });
+		files.push({
+			data: arrayBufferContent(content),
+			filename: deliveryArtifactFilename(artifact),
+			...mediaType ? { mimeType: mediaType } : {}
+		});
+	}
+	return files;
+}
+async function messageChannelReplyEffect(context) {
+	const artifacts = deliveryArtifacts(context);
+	const attachments = deliveryArtifactAttachments(artifacts);
+	const files = await deliveryArtifactFiles(context, artifacts);
+	const stream = isAsyncIterable(context.effect.payload) ? context.effect.payload : void 0;
+	if (stream && !artifacts.length) {
+		const chat = context.finish ? chatFinishExtensionFromUnknown(context.finish.extensions.get("chat")) || chatFinishExtension(context.input) : void 0;
+		if (chat) {
+			await chat.sendMessage(stream);
+			return;
+		}
+		const adapter = context.channel.adapter ? await resolveEffectOption(context.channel.adapter, context) : void 0;
+		if (adapter && context.run?.threadId) {
+			const threadId = adapter.channelIdFromThreadId(context.run.threadId);
+			if (adapter.stream && await adapter.stream(threadId, stream) !== null) return;
+			let body = "";
+			for await (const chunk of stream) body += chunk;
+			if (body) await adapter.postMessage(threadId, { markdown: body });
+		}
+		return;
+	}
+	let body = replyBody(context);
+	if (stream) for await (const chunk of stream) body = `${body || ""}${chunk}`;
+	body = rewriteDeliveryArtifactMarkdown(replyBodyWithLinkArtifacts(body, artifacts), artifacts);
+	if (!body && !attachments.length && !files.length) return;
+	const message = {
+		markdown: body || "",
+		...attachments.length ? { attachments } : {},
+		...files.length ? { files } : {}
+	};
+	const chat = context.finish ? chatFinishExtensionFromUnknown(context.finish.extensions.get("chat")) || chatFinishExtension(context.input) : void 0;
+	if (chat) {
+		await chat.sendMessage(message);
+		return;
+	}
+	const adapter = context.channel.adapter ? await resolveEffectOption(context.channel.adapter, context) : void 0;
+	if (adapter && context.run?.threadId) await adapter.postMessage(adapter.channelIdFromThreadId(context.run.threadId), message);
+}
+function titleEffectPayloadTitle(value) {
+	const title = typeof value === "string" ? value : isRecord(value) ? value.title : void 0;
+	return typeof title === "string" ? maybeString(title.trim()) : void 0;
+}
+function adapterSetThreadTitle(adapter) {
+	const setThreadTitle = adapter?.setThreadTitle;
+	return typeof setThreadTitle === "function" ? setThreadTitle.bind(adapter) : void 0;
+}
+function adapterSetAssistantTitle(adapter) {
+	const setAssistantTitle = adapter?.setAssistantTitle;
+	return typeof setAssistantTitle === "function" ? setAssistantTitle.bind(adapter) : void 0;
+}
+async function messageChannelTitleAdapter(context) {
+	return context.channel.adapter ? await resolveEffectOption(context.channel.adapter, context) : void 0;
+}
+async function messageChannelSupportsTitleEffect(context) {
+	if (!context.run?.threadId) return false;
+	const adapter = await messageChannelTitleAdapter(context);
+	return Boolean(adapterSetThreadTitle(adapter) || adapterSetAssistantTitle(adapter));
+}
+function channelHasCustomTitleEffect(channel) {
+	const titleEffect = channel.effects?.title;
+	return customTitleEffectChannels.has(channel) || Boolean(titleEffect && titleEffect !== messageChannelTitleEffect);
+}
+async function messageChannelTitleEffect(context) {
+	const title = titleEffectPayloadTitle(context.effect.payload);
+	if (!title || !context.run?.threadId) return;
+	const adapter = await messageChannelTitleAdapter(context);
+	const setThreadTitle = adapterSetThreadTitle(adapter);
+	if (setThreadTitle) {
+		await setThreadTitle(context.run.threadId, title);
+		return;
+	}
+	const setAssistantTitle = adapterSetAssistantTitle(adapter);
+	if (adapter && setAssistantTitle) await setAssistantTitle(adapter.channelIdFromThreadId(context.run.threadId), context.run.threadId, title);
+}
+function messageChannelDeliveryEffects(effects) {
+	return {
+		...effects,
+		reply: effects?.reply ?? messageChannelReplyEffect,
+		title: effects?.title ?? messageChannelTitleEffect
+	};
+}
+function githubMarkdownText(value) {
+	return value.replace(/[\r\n\[\]]+/g, " ").trim() || "Artifact";
+}
+function githubMarkdownUrl(value) {
+	return value.replace(/[\r\n<>]+/g, "");
+}
+const imageArtifactExtensions = new Set([
+	"gif",
+	"jpeg",
+	"jpg",
+	"png",
+	"svg",
+	"webp"
+]);
+let githubArtifactPublishCounter = 0;
+function isImageArtifactPath(value) {
+	return imageArtifactExtensions.has(value.split(".").pop()?.toLowerCase() || "");
+}
+function githubBodyImagePath(value) {
+	if (!isImageArtifactPath(value)) return;
+	try {
+		return normalizeDeliveryArtifactPath(value, "GitHub delivery image path");
+	} catch {
+		return;
+	}
+}
+async function existingWorkspaceImagePath(context, path) {
+	const normalized = githubBodyImagePath(path);
+	if (!normalized || !context.workspace) return;
+	const stat = await context.workspace.fs.stat(normalized).catch(() => void 0);
+	if (!stat || stat.type !== "file") return;
+	return normalized;
+}
+async function githubBodyImageArtifacts(context, body, options, command, headers) {
+	if (!body || !context.workspace || options.artifacts === false) return [];
+	const paths = /* @__PURE__ */ new Set();
+	const collect = async (value) => {
+		const path = await existingWorkspaceImagePath(context, value);
+		if (path) paths.add(path);
+	};
+	for (const match of body.matchAll(/!\[[^\]\r\n]*\]\(\s*<?(\.?\/?(?:[\w.-]+\/)*[\w.-]+\.(?:gif|jpe?g|png|svg|webp))>?\s*\)/gi)) await collect(match[1]);
+	for (const match of body.matchAll(/(?<!!)\[[^\]\r\n]*\]\(\s*<?(\.?\/?(?:[\w.-]+\/)*[\w.-]+\.(?:gif|jpe?g|png|svg|webp))>?\s*\)/gi)) await collect(match[1]);
+	for (const match of body.matchAll(/(^|\s)((?:\.\/)?(?:[\w.-]+\/)*[\w.-]+\.(?:gif|jpe?g|png|svg|webp))(?![\w.-])/gi)) await collect(match[2]);
+	if (!paths.size) return [];
+	const branch = options.artifacts && options.artifacts.branch || "vitehub-agent-assets";
+	const pathPrefix = options.artifacts && options.artifacts.pathPrefix || "vitehub-agent-assets";
+	const fetcher = options.fetch || fetch;
+	await ensureGitHubArtifactBranch(fetcher, options.apiBaseUrl, command, headers, branch);
+	return await publishWorkspaceArtifacts(context, [...paths].map((path) => ({
+		path,
+		placement: "inline"
+	})), {
+		prefix: `${pathPrefix}/pr-${command.issueNumber}/${context.run?.runId || command.commentId}`,
+		publish: async (input) => await publishGitHubArtifact(fetcher, options.apiBaseUrl, command, headers, branch, input)
+	});
+}
+async function ensureGitHubArtifactBranch(fetcher, apiBaseUrl, command, headers, branch) {
+	const baseUrl = apiBaseUrl || "https://api.github.com";
+	const existing = await fetcher(`${baseUrl}/repos/${command.owner}/${command.repo}/git/ref/heads/${encodeURIComponent(branch)}`, {
+		headers,
+		method: "GET"
+	});
+	if (existing.ok) return;
+	if (existing.status !== 404) throw new Error(`[vitehub] GitHub delivery effect failed with ${existing.status}.`);
+	const sha = await githubPullRequestBaseSha(fetcher, command, headers);
+	if (!sha) throw new Error("[vitehub] GitHub delivery artifact publishing requires a pull request base SHA.");
+	const created = await fetcher(`${baseUrl}/repos/${command.owner}/${command.repo}/git/refs`, {
+		body: JSON.stringify({
+			ref: `refs/heads/${branch}`,
+			sha
+		}),
+		headers,
+		method: "POST"
+	});
+	if (!created.ok && created.status !== 422) throw new Error(`[vitehub] GitHub delivery effect failed with ${created.status}.`);
+}
+function githubWebBaseUrl(apiBaseUrl) {
+	if (!apiBaseUrl) return "https://github.com";
+	const url = new URL(apiBaseUrl);
+	if (url.hostname === "api.github.com") return "https://github.com";
+	if (url.pathname === "/api/v3" || url.pathname.startsWith("/api/v3/")) return url.origin;
+	if (url.hostname.startsWith("api.")) {
+		url.hostname = url.hostname.slice(4);
+		return url.origin;
+	}
+	return url.origin;
+}
+function githubRawUrl(apiBaseUrl, command, branch, pathname) {
+	return `${githubWebBaseUrl(apiBaseUrl)}/${command.owner}/${command.repo}/raw/${encodeURIComponent(branch)}/${pathname.split("/").map(encodeURIComponent).join("/")}`;
+}
+async function publishGitHubArtifact(fetcher, apiBaseUrl, command, headers, branch, input) {
+	const hash = createHash("sha256").update(input.content).digest("hex").slice(0, 12);
+	const parts = input.pathname.split("/");
+	const filename = parts.pop() || input.artifact.path.split("/").pop() || "artifact";
+	const pathname = [
+		...parts,
+		`${Date.now()}-${++githubArtifactPublishCounter}-${hash}`,
+		filename
+	].join("/");
+	await githubApi(fetcher, `${apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/contents/${pathname.split("/").map(encodeURIComponent).join("/")}`, {
+		body: JSON.stringify({
+			branch,
+			content: Buffer.from(input.content).toString("base64"),
+			message: `chore: publish agent delivery artifact ${input.artifact.path} [skip ci]`
+		}),
+		headers,
+		method: "PUT"
+	});
+	return { url: githubRawUrl(apiBaseUrl, command, branch, pathname) };
+}
+function githubArtifactMarkdown(artifact) {
+	if (!artifact.url) return;
+	const label = githubMarkdownText(artifact.alt || artifact.path.split("/").pop() || artifact.path);
+	const url = githubMarkdownUrl(artifact.url);
+	if (!url || artifact.placement === "attachment") return;
+	if ((artifact.placement || (artifact.mediaType?.startsWith("image/") ? "inline" : "link")) === "inline") return `![${label}](<${url}>)`;
+	return `[${label}](<${url}>)`;
+}
+async function githubBodyWithArtifacts(context, body, options, command, headers) {
+	const structuredArtifacts = deliveryArtifacts(context);
+	const referencedStructuredPaths = new Set(deliveryArtifactMarkdownReferencePaths(body, structuredArtifacts));
+	const structuredBody = rewriteDeliveryArtifactMarkdown(body || "", structuredArtifacts) || "";
+	const rewrittenBody = (await githubBodyImageArtifacts(context, structuredBody, options, command, headers)).reduce((text, artifact) => {
+		const markdown = githubArtifactMarkdown(artifact);
+		if (!markdown || !artifact.url) return text;
+		const path = artifact.path.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
+		const url = githubMarkdownUrl(artifact.url);
+		return text.replace(new RegExp(`!\\[([^\\]\\r\\n]*)\\]\\(\\s*<?(?:\\./)?${path}>?\\s*\\)`, "g"), (_match, alt) => `![${githubMarkdownText(alt || artifact.alt || artifact.path)}](<${url}>)`).replace(new RegExp(`(?<!!)\\[([^\\]\\r\\n]*)\\]\\(\\s*<?(?:\\./)?${path}>?\\s*\\)`, "g"), (_match, label) => `[${githubMarkdownText(label || artifact.alt || artifact.path)}](<${url}>)`).replace(new RegExp(`(^|\\s)(?:\\./)?${path}(?![\\w.-])`, "g"), (_match, prefix) => `${prefix}${markdown}`);
+	}, structuredBody);
+	const explicitArtifacts = structuredArtifacts.flatMap((artifact) => {
+		const path = normalizedDeliveryArtifactPath(artifact.path);
+		if (path && referencedStructuredPaths.has(path)) return [];
+		const markdown = githubArtifactMarkdown(artifact);
+		return markdown ? [markdown] : [];
+	});
+	if (!explicitArtifacts.length) return rewrittenBody || body;
+	return rewrittenBody ? `${rewrittenBody}\n\n${explicitArtifacts.join("\n")}` : explicitArtifacts.join("\n");
+}
+function githubPullRequestCommentReplyEffect(context) {
+	const text = context.result?.text ?? context.text;
+	if (!text) return;
+	const body = text.trim() || "_No reply generated._";
+	return context.reply(body);
+}
+function githubPullRequestCommentFinishEffects(options) {
+	if (options.reply === false) return;
+	return options.reply === true || options.reply === void 0 ? githubPullRequestCommentReplyEffect : options.reply;
+}
+function statusPayload(context, defaultContext) {
+	const payload = isRecord(context.effect.payload) ? context.effect.payload : typeof context.effect.payload === "string" ? { state: context.effect.payload } : {};
+	return {
+		context: payload.context || context.effect.metadata?.context || defaultContext,
+		description: payload.description || context.effect.metadata?.description,
+		sha: payload.sha || context.effect.metadata?.sha,
+		state: payload.state || context.effect.metadata?.state || (context.effect.intent === "failed" ? "failure" : context.effect.intent === "completed" ? "success" : "pending"),
+		target_url: payload.target_url || context.effect.metadata?.target_url
+	};
+}
+async function githubPullRequestSha(fetcher, command, headers, key) {
+	const payload = await (await githubApi(fetcher, command.pullRequestUrl, {
+		headers,
+		method: "GET"
+	})).json().catch(() => void 0);
+	return isRecord(payload) && isRecord(payload[key]) ? maybeString(payload[key].sha) : void 0;
+}
+async function githubPullRequestBaseSha(fetcher, command, headers) {
+	return await githubPullRequestSha(fetcher, command, headers, "base");
+}
+async function githubPullRequestHeadSha(fetcher, command, headers) {
+	return await githubPullRequestSha(fetcher, command, headers, "head");
+}
+function githubPullRequestEffects(options) {
+	return {
+		async reaction(context) {
+			const command = githubCommandFromEffect(context);
+			if (!command) return;
+			const fetcher = options.fetch || fetch;
+			const token = await resolveEffectOption(options.token, context);
+			const url = `${options.apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/issues/comments/${command.commentId}/reactions`;
+			const key = transientReactionKey(context);
+			if (reactionAction(context) === "remove") {
+				const id = key ? transientReactionStore(context.input)[key]?.id : void 0;
+				if (!id) return;
+				await githubApi(fetcher, `${url}/${id}`, {
+					headers: githubApiHeaders(token, options.userAgent),
+					method: "DELETE"
+				});
+				delete transientReactionStore(context.input)[key];
+				return;
+			}
+			const body = await (await githubApi(fetcher, url, {
+				body: JSON.stringify({ content: reactionContent(context) }),
+				headers: githubApiHeaders(token, options.userAgent),
+				method: "POST"
+			})).json().catch(() => void 0);
+			const id = isRecord(body) ? maybeNumber(body.id) : void 0;
+			if (key && id) transientReactionStore(context.input)[key] = { id };
+		},
+		async reply(context) {
+			const command = githubCommandFromEffect(context);
+			if (!command) return;
+			const fetcher = options.fetch || fetch;
+			const headers = githubApiHeaders(await resolveEffectOption(options.token, context), options.userAgent);
+			const body = await githubBodyWithArtifacts(context, replyBody(context), options, command, headers);
+			if (!body) return;
+			await githubApi(fetcher, `${options.apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/issues/${command.issueNumber}/comments`, {
+				body: JSON.stringify({ body }),
+				headers,
+				method: "POST"
+			});
+		},
+		async update(context) {
+			const command = githubCommandFromEffect(context);
+			if (!command) return;
+			const fetcher = options.fetch || fetch;
+			const headers = githubApiHeaders(await resolveEffectOption(options.token, context), options.userAgent);
+			const body = await githubBodyWithArtifacts(context, replyBody(context), options, command, headers);
+			if (!body) return;
+			await githubApi(fetcher, `${options.apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/issues/comments/${command.commentId}`, {
+				body: JSON.stringify({ body }),
+				headers,
+				method: "PATCH"
+			});
+		},
+		async review(context) {
+			const command = githubCommandFromEffect(context);
+			if (!command) return;
+			const payload = isRecord(context.effect.payload) ? context.effect.payload : {};
+			const fetcher = options.fetch || fetch;
+			const headers = githubApiHeaders(await resolveEffectOption(options.token, context), options.userAgent);
+			const body = await githubBodyWithArtifacts(context, replyBody(context), options, command, headers);
+			if (!body) return;
+			await githubApi(fetcher, `${options.apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/pulls/${command.issueNumber}/reviews`, {
+				body: JSON.stringify({
+					body,
+					event: maybeString(payload.event) || maybeString(context.effect.metadata?.event) || "COMMENT"
+				}),
+				headers,
+				method: "POST"
+			});
+		},
+		async status(context) {
+			const command = githubCommandFromEffect(context);
+			if (!command) return;
+			const fetcher = options.fetch || fetch;
+			const headers = githubApiHeaders(await resolveEffectOption(options.token, context), options.userAgent);
+			const payload = statusPayload(context, options.statusContext || "ViteHub Agent");
+			const sha = maybeString(payload.sha) || await githubPullRequestHeadSha(fetcher, command, headers);
+			if (!sha) return;
+			await githubApi(fetcher, `${options.apiBaseUrl || "https://api.github.com"}/repos/${command.owner}/${command.repo}/statuses/${sha}`, {
+				body: JSON.stringify({
+					...payload,
+					sha: void 0
+				}),
+				headers,
+				method: "POST"
+			});
+		}
+	};
+}
+function githubWebhookDefaults(webhooks, app) {
+	const defaults = {
+		secretHeader: "x-hub-signature-256",
+		...app ? { secretToken: githubAppWebhookSecretToken(app) } : {},
+		signature: "github-sha256"
+	};
+	if (webhooks === void 0 || webhooks === true) return defaults;
+	if (webhooks === false) return false;
+	const apply = (webhook) => ({
+		...defaults,
+		...webhook
+	});
+	return Array.isArray(webhooks) ? webhooks.map(apply) : apply(webhooks);
+}
+function telegramWebhookDefaults(webhooks) {
+	const defaults = { secretHeader: "x-telegram-bot-api-secret-token" };
+	if (webhooks === void 0 || webhooks === true) return defaults;
+	if (webhooks === false) return false;
+	const apply = (webhook) => ({
+		...defaults,
+		...webhook
+	});
+	return Array.isArray(webhooks) ? webhooks.map(apply) : apply(webhooks);
+}
+function telegramAdapterResolver(options) {
+	if (options.adapter) return options.adapter;
+	return async (context) => {
+		const [allowedUserIds, apiBaseUrl, apiUrl, botToken, longPolling, userName, webhookSecret] = await Promise.all([
+			options.allowedUserIds === void 0 ? void 0 : resolveRuntimeValue(options.allowedUserIds, context),
+			options.apiBaseUrl === void 0 ? void 0 : resolveRuntimeValue(options.apiBaseUrl, context),
+			options.apiUrl === void 0 ? void 0 : resolveRuntimeValue(options.apiUrl, context),
+			options.botToken === void 0 ? void 0 : resolveRuntimeValue(options.botToken, context),
+			options.longPolling === void 0 ? void 0 : resolveRuntimeValue(options.longPolling, context),
+			options.userName === void 0 ? void 0 : resolveRuntimeValue(options.userName, context),
+			options.webhookSecret === void 0 ? void 0 : resolveRuntimeValue(options.webhookSecret, context)
+		]);
+		const { createTelegramAdapter } = await import("@chat-adapter/telegram");
+		return createTelegramAdapter({
+			...allowedUserIds ? { allowedUserIds } : {},
+			...apiBaseUrl ? { apiBaseUrl } : {},
+			...apiUrl ? { apiUrl } : {},
+			...botToken ? { botToken: cleanSecret(botToken) } : {},
+			...longPolling ? { longPolling } : {},
+			...options.mode ? { mode: options.mode } : {},
+			...userName ? { userName } : {},
+			...webhookSecret ? { secretToken: cleanSecret(webhookSecret) } : {}
+		});
+	};
+}
+function telegramWebhookSecretToken(secret) {
+	return async (context) => {
+		const resolved = await resolveRuntimeValue(secret, context);
+		return resolved === void 0 || resolved === false ? false : cleanSecret(resolved) || false;
+	};
+}
+function telegramApiError(method, response, result, secrets) {
+	let description = result.description?.trim();
+	for (const secret of secrets) if (secret) description = description?.split(secret).join("[redacted]");
+	const suffix = description ? `: ${description}` : "";
+	return /* @__PURE__ */ new Error(`[vitehub] Telegram ${method} failed with HTTP ${response.status}${suffix}`);
+}
+function telegramWebhookManager(options) {
+	return async (context) => {
+		const [resolvedApiBaseUrl, resolvedApiUrl, resolvedBotToken, resolvedSecret] = await Promise.all([
+			options.apiBaseUrl === void 0 ? void 0 : resolveRuntimeValue(options.apiBaseUrl, context),
+			options.apiUrl === void 0 ? void 0 : resolveRuntimeValue(options.apiUrl, context),
+			options.botToken === void 0 ? void 0 : resolveRuntimeValue(options.botToken, context),
+			options.webhookSecret === void 0 ? void 0 : resolveRuntimeValue(options.webhookSecret, context)
+		]);
+		const botToken = cleanSecret(resolvedBotToken) || process.env.TELEGRAM_BOT_TOKEN;
+		if (!botToken) throw new Error("[vitehub] Telegram webhook management requires telegram({ botToken }) or TELEGRAM_BOT_TOKEN.");
+		const webhookSecret = resolvedSecret === false ? void 0 : cleanSecret(resolvedSecret) || process.env.TELEGRAM_WEBHOOK_SECRET_TOKEN;
+		const apiBaseUrl = (resolvedApiUrl || resolvedApiBaseUrl || process.env.TELEGRAM_API_BASE_URL || "https://api.telegram.org").replace(/\/+$/, "");
+		const request = async (method, body, signal) => {
+			let response;
+			try {
+				response = await fetch(`${apiBaseUrl}/bot${botToken}/${method}`, {
+					...body ? { body: JSON.stringify(body) } : {},
+					headers: { "content-type": "application/json" },
+					method: "POST",
+					signal
+				});
+			} catch {
+				throw new Error(`[vitehub] Telegram ${method} request failed.`);
+			}
+			const result = await response.json().catch(() => ({ ok: false }));
+			if (!response.ok || !result.ok || result.result === void 0) throw telegramApiError(method, response, result, [botToken, webhookSecret || ""]);
+			return result.result;
+		};
+		return {
+			async delete({ signal } = {}) {
+				await request("deleteWebhook", { drop_pending_updates: false }, signal);
+			},
+			async inspect({ signal } = {}) {
+				const info = await request("getWebhookInfo", void 0, signal);
+				return {
+					details: {
+						...info.allowed_updates ? { allowedUpdates: info.allowed_updates } : {},
+						customCertificate: Boolean(info.has_custom_certificate),
+						...info.ip_address ? { ipAddress: info.ip_address } : {},
+						...info.last_error_date ? { lastErrorAt: (/* @__PURE__ */ new Date(info.last_error_date * 1e3)).toISOString() } : {},
+						...info.last_error_message ? { lastErrorMessage: info.last_error_message } : {},
+						...typeof info.max_connections === "number" ? { maxConnections: info.max_connections } : {},
+						pendingUpdateCount: info.pending_update_count || 0,
+						unverifiable: ["secretToken"]
+					},
+					url: info.url || void 0
+				};
+			},
+			async set({ signal, url }) {
+				const target = new URL(url);
+				if (target.protocol !== "https:") throw new Error("[vitehub] Telegram webhooks require an HTTPS URL.");
+				if (target.port && ![
+					"80",
+					"88",
+					"443",
+					"8443"
+				].includes(target.port)) throw new Error("[vitehub] Telegram webhook URLs support ports 443, 80, 88, and 8443.");
+				if (webhookSecret && !/^[A-Za-z0-9_-]{1,256}$/.test(webhookSecret)) throw new Error("[vitehub] Telegram webhookSecret must be 1-256 letters, numbers, underscores, or hyphens.");
+				await request("setWebhook", {
+					allowed_updates: ["message"],
+					drop_pending_updates: false,
+					...webhookSecret ? { secret_token: webhookSecret } : {},
+					url
+				}, signal);
+			}
+		};
+	};
+}
+function discordAdapterResolver(input) {
+	if (input === void 0) return void 0;
+	if (input !== true && (typeof input === "function" || isAdapter(input) || isResolver(input))) return input;
+	const { longContent, ...adapterOptions } = input === true ? {} : input;
+	return async () => {
+		let createDiscordAdapter;
+		try {
+			({createDiscordAdapter} = await import("@chat-adapter/discord"));
+		} catch (error) {
+			throw new Error("[vitehub] discord({ adapter: true }) requires @chat-adapter/discord to be installed.", { cause: error });
+		}
+		const botToken = cleanSecret(adapterOptions.botToken);
+		const adapter = createDiscordAdapter({
+			...adapterOptions,
+			...botToken ? { botToken } : {},
+			...adapterOptions.publicKey ? { publicKey: cleanSecret(adapterOptions.publicKey) } : {}
+		});
+		addDiscordThreadTitleSupport(adapter, adapterOptions, botToken);
+		if (longContent?.mode === "split") Object.defineProperty(adapter, Symbol.for("vitehub.discord.longContent.mode"), {
+			configurable: true,
+			value: "split"
+		});
+		return adapter;
+	};
+}
+const discordApiBaseUrl = "https://discord.com/api/v10";
+function discordThreadIdFromAgentThreadId(threadId) {
+	const parts = threadId.split(":");
+	return parts[0] === "discord" ? maybeString(parts[3]) : void 0;
+}
+function addDiscordThreadTitleSupport(adapter, options, botToken) {
+	if (!botToken || adapterSetThreadTitle(adapter)) return;
+	Object.defineProperty(adapter, "setThreadTitle", {
+		configurable: true,
+		value: async (threadId, title) => {
+			const discordThreadId = discordThreadIdFromAgentThreadId(threadId);
+			const name = title.replace(/\s+/g, " ").trim().slice(0, 100).trim();
+			if (!discordThreadId || !name) return;
+			const response = await fetch(`${(options.apiUrl || discordApiBaseUrl).replace(/\/+$/, "")}/channels/${discordThreadId}`, {
+				body: JSON.stringify({ name }),
+				headers: {
+					Authorization: `Bot ${botToken}`,
+					"Content-Type": "application/json"
+				},
+				method: "PATCH"
+			});
+			if (!response.ok) {
+				const error = await response.text().catch(() => response.statusText);
+				throw new Error(`[vitehub] Discord thread title update failed: ${response.status}${error ? ` ${error}` : ""}`);
+			}
+		}
+	});
+}
+function isAdapter(value) {
+	return isRecord(value) && typeof value.postMessage === "function";
+}
+function isResolver(value) {
+	return isRecord(value) && typeof value.resolve === "function";
+}
+function ignored(reason) {
+	return Response.json({
+		accepted: false,
+		ok: true,
+		reason
+	});
+}
+function githubPullRequestRunContextFromInput(input) {
+	return githubPullRequestRunContextFromUnknown(input);
+}
+function githubCommandFromRunContext(value) {
+	const repository = maybeString(value.repository.fullName);
+	const [fallbackOwner, fallbackRepo] = repository?.split("/") || [];
+	const owner = maybeString(value.repository.owner) || fallbackOwner;
+	const repo = maybeString(value.repository.name) || fallbackRepo;
+	const issueNumber = maybeNumber(value.pullRequest.number);
+	const commentId = maybeNumber(value.trigger.comment.id);
+	const pullRequestUrl = maybeString(value.pullRequest.apiUrl);
+	const login = maybeString(value.trigger.actor.login);
+	if (!repository || !owner || !repo || !issueNumber || !commentId || !pullRequestUrl || !login) return;
+	return {
+		action: value.trigger.action,
+		actor: value.trigger.actor,
+		args: value.trigger.args,
+		body: maybeString(value.trigger.comment.body) || value.trigger.command,
+		command: value.trigger.command,
+		commentId,
+		...maybeString(value.trigger.comment.nodeId) ? { commentNodeId: maybeString(value.trigger.comment.nodeId) } : {},
+		...maybeString(value.trigger.deliveryId) ? { deliveryId: maybeString(value.trigger.deliveryId) } : {},
+		...maybeNumber(value.trigger.installationId) ? { installationId: maybeNumber(value.trigger.installationId) } : {},
+		issueNumber,
+		owner,
+		pullRequestUrl,
+		repo,
+		repository
+	};
+}
+function githubPullRequestDevPrompt(input, pullRequest) {
+	const command = maybeString(pullRequest.trigger.command);
+	const args = maybeString(pullRequest.trigger.args);
+	return maybeString(input.prompt) || maybeString(pullRequest.trigger.comment.body) || (command && args ? `${command} ${args}` : command) || "/review";
+}
+function githubDevPayload(input) {
+	const payload = inputPayloadOrBody(input);
+	if (payload) return payload;
+	if (!isRecord(input) || !isRecord(input.issue) || !isRecord(input.comment)) return;
+	return input;
+}
+function githubEventTriggers(pullRequest, app) {
+	if (!pullRequest) return void 0;
+	const options = pullRequest === true ? {} : pullRequest;
+	return {
+		webhook: { async invoke(context, input) {
+			const payload = inputPayloadOrBody(input);
+			const command = githubPullRequestCommandFromInput(isRecord(input) ? {
+				...input,
+				payload
+			} : { payload });
+			if (!payload && !command) return options.ignored?.("missing_payload") || ignored("missing_payload");
+			if (!command) return options.ignored?.("not_command") || ignored("not_command");
+			if (declaredInputCommand(context, command.command) === false) return options.ignored?.("not_command") || ignored("not_command");
+			const metadata = await githubPullRequestMetadata(app, context, command, options, payload);
+			const pullRequest = githubPullRequestRunContext(command, {
+				...options,
+				threadId: options.threadId || maybeString(payload?.issue?.pull_request?.html_url) || maybeString(payload?.issue?.html_url) || command.pullRequestUrl
+			}, payload, metadata);
+			const finishEffects = githubPullRequestCommentFinishEffects(options);
+			return {
+				...finishEffects ? { delivery: { finishEffects } } : {},
+				input: pullRequestCommandInput(command, pullRequest),
+				run: {
+					...pullRequest.run,
+					channelId: context.trigger.channelId
+				}
+			};
+		} },
+		dev: {
+			webhooks: [],
+			async invoke(context, input) {
+				const inputRecord = isRecord(input) ? input : {};
+				const finishEffects = githubPullRequestCommentFinishEffects(options);
+				const existingPullRequest = githubPullRequestRunContextFromInput(input);
+				const controls = {
+					...inputRecord.abortSignal ? { abortSignal: inputRecord.abortSignal } : {},
+					...typeof inputRecord.timeout === "number" ? { timeout: inputRecord.timeout } : {}
+				};
+				if (existingPullRequest) {
+					const command = githubCommandFromUnknown(inputRecord.github) || githubCommandFromRunContext(existingPullRequest);
+					if (command && declaredInputCommand(context, command.command) === false) return options.ignored?.("not_command") || ignored("not_command");
+					return {
+						...finishEffects ? { delivery: { finishEffects } } : {},
+						input: {
+							...controls,
+							context: {
+								...command ? { github: command } : {},
+								pullRequest: existingPullRequest
+							},
+							prompt: githubPullRequestDevPrompt(inputRecord, existingPullRequest)
+						},
+						run: {
+							...existingPullRequest.run,
+							channelId: context.trigger.channelId
+						}
+					};
+				}
+				const payload = githubDevPayload(input);
+				const command = githubPullRequestCommandFromInput(isRecord(input) ? {
+					...input,
+					payload
+				} : { payload });
+				if (!payload && !command) return options.ignored?.("missing_payload") || ignored("missing_payload");
+				if (!command) return options.ignored?.("not_command") || ignored("not_command");
+				if (declaredInputCommand(context, command.command) === false) return options.ignored?.("not_command") || ignored("not_command");
+				const metadata = await githubPullRequestMetadata(app, context, command, options, payload);
+				const pullRequest = githubPullRequestRunContext(command, {
+					...options,
+					threadId: options.threadId || maybeString(payload?.issue?.pull_request?.html_url) || maybeString(payload?.issue?.html_url) || command.pullRequestUrl
+				}, payload, metadata);
+				return {
+					...finishEffects ? { delivery: { finishEffects } } : {},
+					input: {
+						...controls,
+						context: {
+							github: command,
+							pullRequest
+						},
+						prompt: maybeString(inputRecord.prompt) || command.body
+					},
+					run: {
+						...pullRequest.run,
+						channelId: context.trigger.channelId
+					}
+				};
+			}
+		}
+	};
+}
+function githubPullRequestContextValue(input) {
+	const value = pullRequest.read(input);
+	if (!value.source?.repo) throw new Error("[vitehub] GitHub pull request workspace requires a repository source.");
+	if (!value.source.ref) throw new Error("[vitehub] GitHub pull request workspace requires a source ref.");
+	if (!value.head?.sha) throw new Error("[vitehub] GitHub pull request workspace requires the exact head SHA.");
+	return value;
+}
+function githubPullRequestInstallationId(value) {
+	const installationId = value.trigger?.installationId;
+	if (typeof installationId === "number") return installationId;
+	if (typeof installationId !== "string" || !installationId) return;
+	const parsed = Number(installationId);
+	return Number.isFinite(parsed) ? parsed : void 0;
+}
+function githubPullRequestWorkspaceCapability(workspace, app) {
+	if (!workspace?.enabled) return;
+	return defineCapability({
+		id: "github-pull-request-workspace",
+		async workspace(context) {
+			const value = githubPullRequestContextValue(context);
+			const token = await githubPullRequestMetadataToken(app, context, githubPullRequestInstallationId(value));
+			const { github: githubSource } = await import("@vite-hub/workspace");
+			return { sources: { vitehubGitHubPullRequest: githubSource({
+				...token ? { auth: token } : {},
+				materialize: "lazy",
+				mount: { path: workspace.mount },
+				ref: value.head.sha,
+				repo: value.source.repo
+			}) } };
+		}
+	});
+}
+function defineChannel(kind, options = {}) {
+	if (typeof kind !== "string" || !kind.trim()) throw new TypeError("[vitehub] defineChannel() requires a non-empty Channel kind.");
+	const messages = options.messages === void 0 ? {} : options.messages;
+	const effects = messages !== false && options.adapter ? messageChannelDeliveryEffects(options.effects) : options.effects;
+	const channel = {
+		...options,
+		...effects ? { effects } : {},
+		kind,
+		messages
+	};
+	if (options.effects?.title) customTitleEffectChannels.add(channel);
+	return channel;
+}
+function discord(options = {}) {
+	return defineChannel("discord", {
+		...options,
+		adapter: discordAdapterResolver(options.adapter)
+	});
+}
+function github(options = {}) {
+	const { app: appOptions, pullRequest, ...channelOptions } = options;
+	const app = githubAppOptions(appOptions);
+	const workspaceCapability = githubPullRequestWorkspaceCapability(pullRequest ? githubPullRequestWorkspacePolicy(pullRequest === true ? {} : pullRequest || {}) : void 0, appOptions);
+	const appEffects = appOptions ? githubPullRequestEffects({
+		apiBaseUrl: app?.apiBaseUrl,
+		artifacts: app?.artifacts,
+		fetch: app?.fetch,
+		statusContext: app?.statusContext,
+		token: (context) => githubAppInstallationToken(appOptions, context),
+		userAgent: app?.userAgent
+	}) : void 0;
+	return defineChannel("github", {
+		...channelOptions,
+		capabilities: [...workspaceCapability ? [workspaceCapability] : [], ...channelOptions.capabilities || []],
+		effects: appEffects ? {
+			...appEffects,
+			...options.effects
+		} : options.effects,
+		messages: false,
+		triggers: {
+			...githubEventTriggers(pullRequest, appOptions),
+			...options.triggers
+		},
+		webhooks: githubWebhookDefaults(options.webhooks, appOptions)
+	});
+}
+function http(options = {}) {
+	if ("path" in options) throw new TypeError("[vitehub] http({ path }) is not wired yet. Webhook routes are configured with webhooks.path.");
+	return defineChannel("http", options);
+}
+function slack(options = {}) {
+	return defineChannel("slack", options);
+}
+function teams(options = {}) {
+	return defineChannel("teams", options);
+}
+function telegram(options = {}) {
+	if (options.webhooks !== void 0 && options.webhookSecret !== void 0) throw new TypeError("[vitehub] telegram() accepts webhookSecret or webhooks, not both.");
+	const { adapter: _adapter, allowedUserIds: _allowedUserIds, apiBaseUrl: _apiBaseUrl, apiUrl: _apiUrl, botToken: _botToken, longPolling: _longPolling, mode, userName: _userName, webhooks, webhookSecret, ...channelOptions } = options;
+	const webhookOptions = webhookSecret !== void 0 ? { secretToken: telegramWebhookSecretToken(webhookSecret) } : webhooks;
+	return defineMessageChannelInstructions(defineChannel("telegram", {
+		...channelOptions,
+		adapter: telegramAdapterResolver(options),
+		...mode === "polling" ? { listener: { kind: "telegram-polling" } } : {},
+		webhookManager: mode === "polling" ? false : telegramWebhookManager(options),
+		webhooks: mode === "polling" ? false : telegramWebhookDefaults(webhookOptions)
+	}), "Write the final response for Telegram. Match the language of the user's latest message. Prefer short paragraphs or bullets and keep the answer concise. Do not use Markdown tables; express rows as bullets because Telegram fallback delivery exposes table syntax. Avoid decorative emoji, redundant restatement, and generic follow-up questions. Follow the Agent's own instructions when they require a different format.");
+}
+function webChat(options = {}) {
+	return defineChannel("web-chat", {
+		...options,
+		route: options.route ?? true
+	});
+}
+//#endregion
+export { getAgentChatContext as A, selectStore as C, assertChatDeliveryOptions as D, agentChatContextKey as E, uiMessagesToAgentMessages as F, chatTriggerHistoryLimit as M, createChatMessageTriggerInput as N, chat as O, resolveChatTriggerHistory as P, requirePrimitive as S, CHAT_FINISH_EXTENSION_CONTEXT_KEY as T, repositoryHostContext as _, http as a, jsonObjectSchema as b, pullRequest as c, telegram as d, webChat as f, defineFinishEffect as g, createStatusDeliveryEffectIntent as h, github as i, getChatCapabilityOptions as j, defineChatCapability as k, slack as l, createReplyDeliveryEffectIntent as m, defineChannel as n, messageChannelSupportsTitleEffect as o, createReactionDeliveryEffectIntent as p, discord as r, messageChannelTitleSupportContextKey as s, channelHasCustomTitleEffect as t, teams as u, assertString as v, storageValue as w, method as x, createTool as y };
diff --git a/dist/channels.d.ts b/dist/channels.d.ts
index 57bb9ccff756f8b5c8912aa7eb7958cdff324ec7..3701e0b4705fc86d45d6684346d7409685c95681 100644
--- a/dist/channels.d.ts
+++ b/dist/channels.d.ts
@@ -1,7 +1,7 @@
-import { $ as AgentChannelDeliveryFinishEffectResult, $r as MaybeResolvable, Ft as AgentDeliveryArtifactPlacement, G as AgentChannelDeliveryEffectIntent, H as AgentChannelDefinition, J as AgentChannelDeliveryEffectPayload, K as AgentChannelDeliveryEffectIntentOptions, Pt as AgentDeliveryArtifact, Q as AgentChannelDeliveryFinishEffectContext, Qr as MaybePromise, U as AgentChannelDeliveryEffectContext, W as AgentChannelDeliveryEffectHandler, X as AgentChannelDeliveryFinishEffect, Y as AgentChannelDeliveryEffects, Z as AgentChannelDeliveryFinishEffectCallback, at as AgentChannelDeliveryStatusInput, ct as AgentChannelFactory, ei as PublishedAgentDeliveryArtifact, et as AgentChannelDeliveryReactionInput, h as AgentCallbackContext, it as AgentChannelDeliveryReplyStream, kn as AgentMessageChannelSettings, lr as AgentRuntimeConfig, lt as AgentChannelInput, nt as AgentChannelDeliveryReplyInput, ot as AgentChannelDeliveryStatusPayload, pt as AgentChannels, q as AgentChannelDeliveryEffectKind, rt as AgentChannelDeliveryReplyPayload, st as AgentChannelDeliveryStatusState, tt as AgentChannelDeliveryReactionPayload, ut as AgentChannelInputs } from "./types-C4oyRXNr.js";
-import { u as PullRequestContextValue } from "./repository-host-context-DAVFc6T_.js";
-import { a as AgentChannelChatRouteHandlerOptions, r as AgentChannelChatRouteBody } from "./routes-DUlHQxPe.js";
-import { t as defineFinishEffect } from "./delivery-effects-B8D8m9OC.js";
+import { $ as AgentChannelDeliveryFinishEffectResult, $r as MaybePromise, An as AgentMessageChannelSettings, Ft as AgentDeliveryArtifact, G as AgentChannelDeliveryEffectIntent, H as AgentChannelDefinition, It as AgentDeliveryArtifactPlacement, J as AgentChannelDeliveryEffectPayload, K as AgentChannelDeliveryEffectIntentOptions, Q as AgentChannelDeliveryFinishEffectContext, U as AgentChannelDeliveryEffectContext, W as AgentChannelDeliveryEffectHandler, X as AgentChannelDeliveryFinishEffect, Y as AgentChannelDeliveryEffects, Z as AgentChannelDeliveryFinishEffectCallback, at as AgentChannelDeliveryStatusInput, ct as AgentChannelFactory, ei as MaybeResolvable, et as AgentChannelDeliveryReactionInput, h as AgentCallbackContext, it as AgentChannelDeliveryReplyStream, lt as AgentChannelInput, mt as AgentChannels, nt as AgentChannelDeliveryReplyInput, ot as AgentChannelDeliveryStatusPayload, q as AgentChannelDeliveryEffectKind, rt as AgentChannelDeliveryReplyPayload, st as AgentChannelDeliveryStatusState, ti as PublishedAgentDeliveryArtifact, tt as AgentChannelDeliveryReactionPayload, ur as AgentRuntimeConfig, ut as AgentChannelInputs } from "./types-CW2XmfE7.js";
+import { u as PullRequestContextValue } from "./repository-host-context-DB2_VeVi.js";
+import { a as AgentChannelChatRouteHandlerOptions, r as AgentChannelChatRouteBody } from "./routes-1Y1zW1HK.js";
+import { t as defineFinishEffect } from "./delivery-effects-Bol6CuhN.js";
 import { Attachment } from "chat";
 import { TelegramAdapterConfig } from "@chat-adapter/telegram";
 
diff --git a/dist/channels.js b/dist/channels.js
index bde4ae1805d43b9501ba6c0db7e9aca93ace922d..9e6673701429d943e7624d8303ba64eb25f43b84 100644
--- a/dist/channels.js
+++ b/dist/channels.js
@@ -1,3 +1,3 @@
 import { i as publishWorkspaceArtifacts, o as rewriteDeliveryArtifactMarkdown, t as deliveryArtifactAttachments } from "./delivery-artifacts-BOKCLStf.js";
-import { a as http, c as pullRequest, d as telegram, f as webChat, g as defineFinishEffect, i as github, l as slack, n as defineChannel, o as messageChannelSupportsTitleEffect, r as discord, s as messageChannelTitleSupportContextKey, t as channelHasCustomTitleEffect, u as teams } from "./channels-DNuHTrNn.js";
+import { a as http, c as pullRequest, d as telegram, f as webChat, g as defineFinishEffect, i as github, l as slack, n as defineChannel, o as messageChannelSupportsTitleEffect, r as discord, s as messageChannelTitleSupportContextKey, t as channelHasCustomTitleEffect, u as teams } from "./channels-D6wOMJN1.js";
 export { channelHasCustomTitleEffect, defineChannel, defineFinishEffect, deliveryArtifactAttachments, discord, github, http, messageChannelSupportsTitleEffect, messageChannelTitleSupportContextKey, publishWorkspaceArtifacts, pullRequest, rewriteDeliveryArtifactMarkdown, slack, teams, telegram, webChat };
diff --git a/dist/chat-trigger-mwxwRC8l.d.ts b/dist/chat-trigger-mwxwRC8l.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2b624e16edeee901c9f82bf2e46364eaebd140dd
--- /dev/null
+++ b/dist/chat-trigger-mwxwRC8l.d.ts
@@ -0,0 +1,76 @@
+import { Mt as AgentChatTriggerHistory, N as AgentCapabilityDefinition, Tn as AgentInvoker, V as AgentCapabilityTypeContract, _t as AgentChatCapabilityOptions, gt as AgentChatAgentHookArgs, or as AgentRunMetadata, ur as AgentRuntimeConfig, xn as AgentInvocationContextStore } from "./types-CW2XmfE7.js";
+import { WorkspaceName } from "@vite-hub/workspace";
+
+//#region src/chat-message-input.d.ts
+type UIMessageLike = {
+  createdAt?: Date | string;
+  id?: string;
+  metadata?: unknown;
+  parts?: Array<{
+    text?: string;
+    type?: string;
+  } | Record<string, unknown>>;
+  role?: string;
+};
+interface AgentChatMessageTriggerInput {
+  abortSignal?: AbortSignal;
+  invoker?: AgentInvoker;
+  invokerProfileId?: string;
+  meta?: Record<string, unknown>;
+  messages: UIMessageLike[];
+  run?: AgentRunMetadata;
+  session?: {
+    action?: "continue" | "new" | "switch";
+    id?: string;
+  };
+  timeout?: number;
+  triggerHistory?: AgentChatTriggerHistory;
+  user?: Record<string, unknown>;
+}
+//#endregion
+//#region src/chat-trigger.d.ts
+type ResolvedMaybeResolvable<T> = T extends ((...args: any[]) => infer TResult) ? Awaited<TResult> : T extends {
+  resolve: (...args: any[]) => infer TResult;
+} ? Awaited<TResult> : T;
+type AgentChatPlatformOrigin<TPlatforms> = Extract<keyof NonNullable<ResolvedMaybeResolvable<TPlatforms>>, string>;
+type AgentChatKnownOrigin<TOptions> = TOptions extends {
+  platforms?: infer TPlatforms;
+} ? AgentChatPlatformOrigin<TPlatforms> : never;
+type AgentChatOptionsOrigin<TOptions> = [AgentChatKnownOrigin<TOptions>] extends [never] ? string : AgentChatKnownOrigin<TOptions>;
+declare const agentChatContextKey = "chat";
+type ChatCapabilityTypeContract<TOrigin extends string = string> = AgentCapabilityTypeContract & {
+  chatOrigins: TOrigin;
+};
+type AgentChatCapabilityOrigin<TCapability> = TCapability extends AgentCapabilityDefinition<any, any, infer TTypeContract> ? TTypeContract extends {
+  chatOrigins: infer TOrigin;
+} ? Extract<TOrigin, string> : string : string;
+interface AgentChatRunContext<TMessageMetadata extends object = Record<string, unknown>, TUser extends object = Record<string, unknown>, TMeta extends object = Record<string, unknown>> {
+  chat?: {
+    message?: Omit<AgentChatAgentHookArgs["message"], "metadata"> & {
+      metadata?: TMessageMetadata;
+    };
+    meta?: TMeta;
+    session?: AgentChatMessageTriggerInput["session"];
+    user?: TUser;
+  };
+}
+type AgentChatContext<TMeta extends object = Record<string, unknown>, TUser extends object = Record<string, unknown>, TMessageMetadata extends object = Record<string, unknown>> = NonNullable<AgentChatRunContext<TMessageMetadata, TUser, TMeta>["chat"]>;
+type AgentChannelContext<TMeta extends object = Record<string, unknown>, TUser extends object = Record<string, unknown>, TMessageMetadata extends object = Record<string, unknown>> = AgentChatContext<TMeta, TUser, TMessageMetadata> & {
+  run?: AgentRunMetadata;
+};
+declare global {
+  interface ViteHubAgentChannelMeta {}
+  interface ViteHubAgentChannelUser {}
+  interface ViteHubAgentInvocationContextValues {
+    channel: AgentChannelContext<ViteHubAgentChannelMeta, ViteHubAgentChannelUser>;
+  }
+  interface ViteHubWorkspaceSourceResolutionContextMap {
+    channel: AgentChannelContext<ViteHubAgentChannelMeta, ViteHubAgentChannelUser>;
+  }
+}
+declare function getAgentChatContext<TMeta extends object = Record<string, unknown>, TUser extends object = Record<string, unknown>, TMessageMetadata extends object = Record<string, unknown>>(input: AgentInvocationContextStore | {
+  context: AgentInvocationContextStore;
+}): AgentChatContext<TMeta, TUser, TMessageMetadata> | undefined;
+declare function chat<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, const TOptions extends AgentChatCapabilityOptions<TRuntimeConfig> = AgentChatCapabilityOptions<TRuntimeConfig>>(options?: TOptions): AgentCapabilityDefinition<TRuntimeConfig, WorkspaceName, ChatCapabilityTypeContract<string>>;
+//#endregion
+export { AgentChatRunContext as a, getAgentChatContext as c, AgentChatOptionsOrigin as i, AgentChatMessageTriggerInput as l, AgentChatCapabilityOrigin as n, agentChatContextKey as o, AgentChatContext as r, chat as s, AgentChannelContext as t };
\ No newline at end of file
diff --git a/dist/cli.d.ts b/dist/cli.d.ts
index 947ac8a377ba8c7e431609f06af6129b0585ba3e..5dd9b302ddec0705f1be36827a4e41c264358199 100644
--- a/dist/cli.d.ts
+++ b/dist/cli.d.ts
@@ -1,4 +1,4 @@
-import { Bt as AgentEvalOptions } from "./types-C4oyRXNr.js";
+import { Vt as AgentEvalOptions, ft as AgentChannelWebhookManager } from "./types-CW2XmfE7.js";
 
 //#region src/internal/agent-info-cli.d.ts
 interface AgentInfoCliContext {
@@ -17,6 +17,35 @@ interface AgentInfoCliOptions {
 }
 declare function runAgentInfoCli<TContext extends AgentInfoCliContext>(args: string[], context: TContext, options?: AgentInfoCliOptions): Promise<number>;
 //#endregion
+//#region src/internal/webhook-cli.d.ts
+interface AgentWebhookCliContext {
+  env: NodeJS.ProcessEnv;
+  rootDir: string;
+  stderr: {
+    write: (chunk: string | Uint8Array) => unknown;
+  };
+  stdout: {
+    write: (chunk: string | Uint8Array) => unknown;
+  };
+}
+interface ChannelWebhookSyncTarget {
+  agent: string;
+  channel: string;
+  configuredUrl?: string;
+  manager: AgentChannelWebhookManager;
+  provider: string;
+  route?: string;
+  webhook: string;
+}
+interface RunChannelWebhookSyncCliOptions {
+  configFile?: false | string;
+  discover?: () => Promise<ChannelWebhookSyncTarget[]>;
+  fetch?: typeof fetch;
+  serverDirs?: string[];
+  webhookRoute?: false | string;
+}
+declare function runChannelWebhookSyncCli(args: string[], context: AgentWebhookCliContext, options?: RunChannelWebhookSyncCliOptions): Promise<number>;
+//#endregion
 //#region src/internal/evalite-config.d.ts
 type ResolvedAgentEvalOptions = AgentEvalOptions & {
   forceRerunTriggers: string[];
@@ -60,9 +89,11 @@ interface AgentCliContributor {
   }>;
 }
 interface AgentCliContributorOptions {
+  configFile?: false | string;
   eval?: AgentEvalOptions;
   rootDir?: string;
   serverDirs?: string[];
+  webhookRoute?: false | string;
 }
 interface AgentDevCliOptions {
   fetch?: typeof fetch;
@@ -71,4 +102,4 @@ declare function runAgentEvalCli(args: string[], context: AgentCliContext, optio
 declare function runAgentDevCli(args: string[], context: AgentCliContext, options?: AgentDevCliOptions): Promise<number>;
 declare function createAgentCliContributor(options?: false | AgentCliContributorOptions): AgentCliContributor | undefined;
 //#endregion
-export { createAgentCliContributor, runAgentDevCli, runAgentEvalCli, runAgentInfoCli };
\ No newline at end of file
+export { type ChannelWebhookSyncTarget, type RunChannelWebhookSyncCliOptions, createAgentCliContributor, runAgentDevCli, runAgentEvalCli, runAgentInfoCli, runChannelWebhookSyncCli };
\ No newline at end of file
diff --git a/dist/cli.js b/dist/cli.js
index 6456fd5bb5c4f7a2093da0e252c5616d1907da1e..a7d5623b11704d42e0fe115affd6e1c08cd98653 100644
--- a/dist/cli.js
+++ b/dist/cli.js
@@ -1,10 +1,10 @@
-import { r as formatAgentError } from "./agent-error-C0oO_ldm.js";
+import { d as formatAgentError } from "./trace-GIqXV-YM.js";
 import { t as vercelAiGatewayPricing } from "./usage-pricing-BqX1kU-N.js";
-import { i as readAgentInvocationStream, n as agentInvocationStreamRoute, t as agentInvocationStreamHeader } from "./invocation-stream-DFGK1iYd.js";
-import { a as createAgentEvalInclude, n as resolveAgentEvalOptions, r as writeAgentEvaliteConfig, s as discoverAgentEvalFiles } from "./evalite-config-D6Q9AQNQ.js";
-import { isExecutionAuthority } from "@vite-hub/runtime";
+import { _ as agentInvocationStreamHeader, b as readAgentInvocationStream, v as agentInvocationStreamRoute } from "./src-CktB4unw.js";
+import { h as discoverAgentEvalFiles, i as createViteAgentDiscoveryContext, m as discoverAgentDefinitions, n as resolveAgentEvalOptions, p as createAgentEvalInclude, r as writeAgentEvaliteConfig, s as loadViteAgent } from "./evalite-config-7KBtA3Zi.js";
+import { isExecutionAuthority, resolveRuntimeValue } from "@vite-hub/runtime";
 import { readFile } from "node:fs/promises";
-import { resolve } from "node:path";
+import { join, resolve } from "node:path";
 import { readWorkspaceDevToken, workspaceDevTokenHeader } from "@vite-hub/workspace/server";
 //#region src/internal/agent-info-cli.ts
 function writeInfoUsage(context) {
@@ -208,6 +208,302 @@ async function runAgentInfoCli(args, context, options = {}) {
 	return 0;
 }
 //#endregion
+//#region src/internal/webhook-cli.ts
+function writeWebhookUsage(context) {
+	context.stdout.write([
+		"Usage: vitehub channels sync --stage <name> --url <https-origin> [--agent <name>] [--channel <name>] [--apply | --delete] [--allow-delete] [--confirm-origin <https-origin>] [--json]",
+		"",
+		"Inspect configured Channel webhooks and print a synchronization plan. Remote changes require target-bound confirmation.",
+		"",
+		"Options:",
+		"  --agent <name>    Select one discovered Agent.",
+		"  --channel <name>  Select one configured Channel.",
+		"  --stage <name>    Name the deployment stage. VITEHUB_STAGE is the supported environment source.",
+		"  --url <url>       Exact deployed origin. VITEHUB_DEPLOYMENT_URL is the supported environment source.",
+		"  --apply           Register or update the selected webhook after confirmation and route preflight.",
+		"  --delete          Plan deletion of the selected remote webhook.",
+		"  --allow-delete    Permit --delete to mutate remote state.",
+		"  --confirm-origin  Confirm the exact normalized origin for apply or delete.",
+		"  --json            Print structured results without prompts or decoration.",
+		"  -h, --help        Show this help.",
+		""
+	].join("\n"));
+}
+function optionValue(args, index, flag) {
+	const value = args[index + 1];
+	if (!value || value.startsWith("-")) throw new Error(`Missing value for ${flag}.`);
+	return value;
+}
+function parseWebhookArgs(args, env) {
+	const parsed = {
+		action: "sync",
+		allowDelete: false,
+		help: false,
+		json: false,
+		stage: env.VITEHUB_STAGE?.trim() || void 0,
+		stageSource: env.VITEHUB_STAGE?.trim() ? "VITEHUB_STAGE" : void 0,
+		url: env.VITEHUB_DEPLOYMENT_URL?.trim() || void 0,
+		urlSource: env.VITEHUB_DEPLOYMENT_URL?.trim() ? "VITEHUB_DEPLOYMENT_URL" : void 0
+	};
+	for (let index = 0; index < args.length; index++) {
+		const arg = args[index];
+		if (arg === "-h" || arg === "--help") parsed.help = true;
+		else if (arg === "--json") parsed.json = true;
+		else if (arg === "--allow-delete") parsed.allowDelete = true;
+		else if (arg === "--apply" || arg === "--delete") {
+			const action = arg.slice(2);
+			if (parsed.action !== "sync" && parsed.action !== action) throw new Error("Choose either --apply or --delete.");
+			parsed.action = action;
+		} else if ([
+			"--agent",
+			"--channel",
+			"--confirm-origin",
+			"--stage",
+			"--url"
+		].includes(arg)) {
+			const value = optionValue(args, index, arg);
+			index++;
+			if (arg === "--agent") parsed.agent = value;
+			else if (arg === "--channel") parsed.channel = value;
+			else if (arg === "--confirm-origin") parsed.confirmOrigin = value;
+			else if (arg === "--url") {
+				parsed.url = value;
+				parsed.urlSource = "--url";
+			} else {
+				parsed.stage = value;
+				parsed.stageSource = "--stage";
+			}
+		} else if (arg.startsWith("--agent=")) parsed.agent = arg.slice(8);
+		else if (arg.startsWith("--channel=")) parsed.channel = arg.slice(10);
+		else if (arg.startsWith("--confirm-origin=")) parsed.confirmOrigin = arg.slice(17);
+		else if (arg.startsWith("--url=")) {
+			parsed.url = arg.slice(6);
+			parsed.urlSource = "--url";
+		} else if (arg.startsWith("--stage=")) {
+			parsed.stage = arg.slice(8);
+			parsed.stageSource = "--stage";
+		} else if (arg.startsWith("-")) throw new Error(`Unknown option: ${arg}.`);
+		else throw new Error(`Unexpected argument: ${arg}.`);
+	}
+	if (parsed.allowDelete && parsed.action !== "delete") throw new Error("--allow-delete requires --delete.");
+	if (parsed.confirmOrigin && parsed.action === "sync") throw new Error("--confirm-origin requires --apply or --delete.");
+	return parsed;
+}
+function registrations(channelId, channel) {
+	if (channel.webhooks === false) return [];
+	const inputs = channel.webhooks === void 0 || channel.webhooks === true ? [{}] : Array.isArray(channel.webhooks) ? channel.webhooks : [channel.webhooks];
+	return inputs.map((registration, index) => ({
+		...registration,
+		id: registration.id || (inputs.length > 1 ? `${channelId}-${index + 1}` : channelId),
+		provider: registration.provider || channel.kind
+	}));
+}
+function routePath(route, values) {
+	return route.replace(/\[([^\]]+)\]/g, (_match, name) => encodeURIComponent(values[name] || name));
+}
+async function targetsFromServer(server, serverDirs, webhookRoute) {
+	const definitions = discoverAgentDefinitions({
+		mode: "server-agents",
+		scanDirs: serverDirs
+	});
+	const targets = [];
+	for (const definition of definitions) {
+		const loaded = await loadViteAgent(server, definition);
+		if (!loaded) continue;
+		const context = createViteAgentDiscoveryContext(loaded.identity);
+		const channels = loaded.agent.channels || {};
+		for (const [channelId, channel] of Object.entries(channels)) {
+			if (!channel.webhookManager) continue;
+			const manager = await resolveRuntimeValue(channel.webhookManager, context);
+			if (!manager) continue;
+			for (const registration of registrations(channelId, channel)) {
+				const webhook = registration.id || channelId;
+				targets.push({
+					agent: definition.name,
+					channel: channelId,
+					...registration.url ? { configuredUrl: registration.url } : {},
+					manager,
+					provider: registration.provider || channel.kind,
+					...registration.path ? { route: registration.path } : webhookRoute ? { route: routePath(webhookRoute, {
+						agent: definition.name,
+						webhook
+					}) } : {},
+					webhook
+				});
+			}
+		}
+	}
+	return targets;
+}
+async function discoverWebhookTargets(context, options) {
+	if (options.discover) return await options.discover();
+	const { createServer } = await import("vite");
+	const server = await createServer({
+		appType: "custom",
+		...options.configFile !== void 0 ? { configFile: options.configFile } : {},
+		logLevel: "silent",
+		root: context.rootDir,
+		server: {
+			hmr: false,
+			middlewareMode: true
+		}
+	});
+	try {
+		return await targetsFromServer(server, options.serverDirs || [join(context.rootDir, "server")], options.webhookRoute);
+	} finally {
+		await server.close();
+	}
+}
+function targetUrl(target, deploymentUrl) {
+	if (target.configuredUrl) return target.configuredUrl;
+	if (!deploymentUrl || !target.route) return;
+	let base;
+	try {
+		base = new URL(deploymentUrl);
+	} catch {
+		throw new Error(`Invalid deployment URL: ${deploymentUrl}`);
+	}
+	if (base.protocol !== "https:") throw new Error("Webhook apply requires an HTTPS deployed app URL.");
+	if (base.username || base.password) throw new Error("Webhook app URLs cannot contain credentials.");
+	return new URL(target.route.startsWith("/") ? target.route : `/${target.route}`, base.origin).href;
+}
+function normalizedOrigin(value, label) {
+	let url;
+	try {
+		url = new URL(value);
+	} catch {
+		throw new Error(`Invalid ${label}: ${value}`);
+	}
+	if (url.protocol !== "https:") throw new Error(`${label} must use HTTPS.`);
+	if (url.username || url.password) throw new Error(`${label} cannot contain credentials.`);
+	return url.origin;
+}
+function assertConfirmedOrigin(parsed, url) {
+	const expected = normalizedOrigin(url, "deployment URL");
+	if (!parsed.confirmOrigin) throw new Error(`Remote mutation requires --confirm-origin ${expected}.`);
+	const confirmed = normalizedOrigin(parsed.confirmOrigin, "confirmed origin");
+	if (confirmed !== expected) throw new Error(`Confirmed origin ${confirmed} does not match deployment origin ${expected}.`);
+}
+async function preflightWebhookRoute(url, fetchImpl) {
+	const response = await fetchImpl(url, {
+		method: "GET",
+		redirect: "manual",
+		signal: AbortSignal.timeout(3e4)
+	});
+	const body = await response.text();
+	if (response.status !== 405 || !body.includes("Agent webhook route only accepts POST requests.")) throw new Error(`Webhook route preflight failed for ${url} with HTTP ${response.status}.`);
+}
+function writeHumanResult(context, result, parsed) {
+	const details = result.current.details || {};
+	const confirmationUrl = parsed.action === "delete" && result.current.url ? result.current.url : result.targetUrl;
+	context.stdout.write([
+		`Agent: ${result.agent}`,
+		`Channel: ${result.channel} (${result.provider})`,
+		`Webhook: ${result.webhook}`,
+		`Stage: ${result.stage || "unspecified"}${parsed.stageSource ? ` (${parsed.stageSource})` : ""}`,
+		`Route: ${result.route || "unavailable"}`,
+		`Target URL: ${result.targetUrl || "unresolved"}${parsed.urlSource ? ` (${parsed.urlSource})` : ""}`,
+		`Current URL: ${result.current.url || "not set"}`,
+		...typeof details.pendingUpdateCount === "number" ? [`Pending updates: ${details.pendingUpdateCount}`] : [],
+		...typeof details.lastErrorAt === "string" ? [`Last error: ${details.lastErrorAt}${typeof details.lastErrorMessage === "string" ? ` — ${details.lastErrorMessage}` : ""}`] : [],
+		`Plan: ${result.desiredAction}`,
+		`Preflight: ${result.preflight}`,
+		`Status: ${result.status}`,
+		...result.status === "planned" && parsed.action === "apply" && result.targetUrl ? [`Confirmation: add --confirm-origin ${new URL(result.targetUrl).origin} to apply.`] : [],
+		...result.status === "planned" && parsed.action === "delete" && confirmationUrl ? [`Confirmation: add --allow-delete --confirm-origin ${new URL(confirmationUrl).origin} to delete.`] : [],
+		""
+	].join("\n"));
+}
+function selectedTargets(targets, parsed) {
+	return targets.filter((target) => (!parsed.agent || target.agent === parsed.agent) && (!parsed.channel || target.channel === parsed.channel));
+}
+async function runChannelWebhookSyncCli(args, context, options = {}) {
+	let parsed;
+	try {
+		parsed = parseWebhookArgs(args, context.env);
+	} catch (error) {
+		context.stderr.write(`${error instanceof Error ? error.message : String(error)}\n`);
+		writeWebhookUsage({
+			...context,
+			stdout: context.stderr
+		});
+		return 1;
+	}
+	if (parsed.help) {
+		writeWebhookUsage(context);
+		return 0;
+	}
+	try {
+		const targets = selectedTargets(await discoverWebhookTargets(context, options), parsed);
+		if (!targets.length) {
+			context.stderr.write("No manageable Channel webhooks matched the selected Agent and Channel.\n");
+			return 1;
+		}
+		if (parsed.action !== "sync" && targets.length !== 1) {
+			context.stderr.write("Webhook mutations require exactly one target. Pass --agent and --channel.\n");
+			return 1;
+		}
+		if (!parsed.stage) {
+			context.stderr.write("Webhook synchronization requires --stage or VITEHUB_STAGE.\n");
+			return 1;
+		}
+		const results = [];
+		for (const target of targets) {
+			const url = targetUrl(target, parsed.url);
+			if (!url) {
+				context.stderr.write("Webhook synchronization requires --url, VITEHUB_DEPLOYMENT_URL, or a Channel-configured absolute URL.\n");
+				return 1;
+			}
+			normalizedOrigin(url, "deployment URL");
+			if (parsed.action === "delete" && !target.manager.delete) {
+				context.stderr.write(`${target.provider} does not support webhook deletion through ViteHub.\n`);
+				return 1;
+			}
+			let current = await target.manager.inspect({ signal: AbortSignal.timeout(3e4) });
+			const desiredAction = parsed.action === "delete" ? "delete" : current.url === url ? "none" : "set";
+			let preflight = "not-run";
+			let status = "planned";
+			if (parsed.action === "apply" && parsed.confirmOrigin) {
+				assertConfirmedOrigin(parsed, url);
+				await preflightWebhookRoute(url, options.fetch || globalThis.fetch);
+				preflight = "passed";
+				await target.manager.set({
+					signal: AbortSignal.timeout(3e4),
+					url
+				});
+				current = await target.manager.inspect({ signal: AbortSignal.timeout(3e4) });
+				if (current.url !== url) throw new Error(`${target.provider} webhook verification did not return the applied target URL.`);
+				status = "applied";
+			} else if (parsed.action === "delete" && parsed.allowDelete && parsed.confirmOrigin) {
+				assertConfirmedOrigin(parsed, current.url || url);
+				await target.manager.delete({ signal: AbortSignal.timeout(3e4) });
+				current = await target.manager.inspect({ signal: AbortSignal.timeout(3e4) });
+				if (current.url) throw new Error(`${target.provider} webhook verification still reports ${current.url}.`);
+				status = "deleted";
+			}
+			results.push({
+				agent: target.agent,
+				channel: target.channel,
+				current,
+				desiredAction,
+				preflight,
+				provider: target.provider,
+				route: target.route || null,
+				stage: parsed.stage || null,
+				status,
+				targetUrl: url || null,
+				webhook: target.webhook
+			});
+		}
+		if (parsed.json) context.stdout.write(`${JSON.stringify(results)}\n`);
+		else for (const result of results) writeHumanResult(context, result, parsed);
+		return 0;
+	} catch (error) {
+		context.stderr.write(`${error instanceof Error ? error.message : String(error)}\n`);
+		return 1;
+	}
+}
+//#endregion
 //#region src/cli.ts
 const workspaceCommandFeedbackIntervalMs = 15e3;
 const workspaceCommandStartedMessage = "[workspace] command started; first run may materialize sources.\n";
@@ -630,7 +926,7 @@ function parseEvalArgs(args) {
 	return parsed;
 }
 async function loadEvaliteRunner() {
-	const { runAgentEvalite } = await import("./evalite-runner-CMREQ3QJ.js");
+	const { runAgentEvalite } = await import("./evalite-runner-C5QSseWL.js");
 	return runAgentEvalite;
 }
 async function runAgentEvalCli(args, context, options, runner, writeConfig = writeAgentEvaliteConfig, include) {
@@ -1275,7 +1571,20 @@ function createAgentCliContributor(options) {
 		description: "Agent development workflows.",
 		features,
 		name: "agent"
+	}, {
+		description: "Channel configuration workflows.",
+		features: [{
+			description: "Inspect and synchronize configured Channel webhooks.",
+			name: "sync",
+			run: async (args, context) => await runChannelWebhookSyncCli(args, context, {
+				configFile: options?.configFile,
+				serverDirs: options?.serverDirs,
+				webhookRoute: options?.webhookRoute
+			}),
+			usage: "vitehub channels sync --stage <name> --url <https-origin> [--agent <name>] [--channel <name>] [--apply | --delete] [--allow-delete] [--confirm-origin <https-origin>]"
+		}],
+		name: "channels"
 	}] };
 }
 //#endregion
-export { createAgentCliContributor, runAgentDevCli, runAgentEvalCli, runAgentInfoCli };
+export { createAgentCliContributor, runAgentDevCli, runAgentEvalCli, runAgentInfoCli, runChannelWebhookSyncCli };
diff --git a/dist/delivery-effects-Bol6CuhN.d.ts b/dist/delivery-effects-Bol6CuhN.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..f2b4075a12227d4bab693cfb5bec4530e1f9015b
--- /dev/null
+++ b/dist/delivery-effects-Bol6CuhN.d.ts
@@ -0,0 +1,6 @@
+import { Z as AgentChannelDeliveryFinishEffectCallback, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
+
+//#region src/delivery-effects.d.ts
+declare function defineFinishEffect<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown>(effect: AgentChannelDeliveryFinishEffectCallback<TRuntimeConfig, CALL_OPTIONS>): AgentChannelDeliveryFinishEffectCallback<TRuntimeConfig, CALL_OPTIONS>;
+//#endregion
+export { defineFinishEffect as t };
\ No newline at end of file
diff --git a/dist/eval.d.ts b/dist/eval.d.ts
index 6e2f351d7b992c1fa4fbbcb05c8666984a74b2fe..01e32dfa112792929fb1d2f8c1057b11cb800333 100644
--- a/dist/eval.d.ts
+++ b/dist/eval.d.ts
@@ -1,4 +1,4 @@
-import { Qr as MaybePromise, Rn as AgentModelInput, Tr as AgentToolStep, Wt as AgentFinishExtensions, lr as AgentRuntimeConfig, rr as AgentRunInput, sn as AgentInput, ur as AgentRuntimeContext } from "./types-C4oyRXNr.js";
+import { $r as MaybePromise, Er as AgentToolStep, Gt as AgentFinishExtensions, cn as AgentInput, dr as AgentRuntimeContext, ir as AgentRunInput, ur as AgentRuntimeConfig, zn as AgentModelInput } from "./types-CW2XmfE7.js";
 import { TraceRunView } from "@vite-hub/runtime";
 import { WorkspaceName } from "@vite-hub/workspace";
 
diff --git a/dist/eval.js b/dist/eval.js
index 1b318f4bb7288aea565069b00a3e59b0479dc3c4..44177cb86b9efd5560ba7a55f825a39a5b38fb62 100644
--- a/dist/eval.js
+++ b/dist/eval.js
@@ -1,5 +1,5 @@
 import { createMessage } from "./messages.js";
-import { n as defineAgent } from "./src-B3GiXaIA.js";
+import { n as defineAgent } from "./src-CktB4unw.js";
 import { createAgentTestRunner } from "./test.js";
 import { basename, dirname, extname, join, resolve } from "node:path";
 import { statSync } from "node:fs";
diff --git a/dist/evalite-config-7KBtA3Zi.js b/dist/evalite-config-7KBtA3Zi.js
new file mode 100644
index 0000000000000000000000000000000000000000..25db183760da7a93230575e0dd25ba1064f9ac16
--- /dev/null
+++ b/dist/evalite-config-7KBtA3Zi.js
@@ -0,0 +1,632 @@
+import { r as resolveInstructionImports } from "./instruction-composition-CBkhC2F8.js";
+import { t as agentWithColocatedInstructions } from "./src-CktB4unw.js";
+import { i as withColocatedAgentSkills, r as decodeColocatedAgentSkills } from "./colocated-agent-skills-BAz3kNyV.js";
+import { u as workspaceAgentWithSourceRoot } from "./workspace-agent-BtTRGp6h.js";
+import { t as createAgentRuntimeContext } from "./context-KghRPYRx.js";
+import { n as withColocatedAgentHome, t as decodeColocatedAgentHome } from "./colocated-agent-home-EYZ8BwLn.js";
+import { mkdir, readFile, rm, writeFile } from "node:fs/promises";
+import { basename, dirname, join, relative, resolve } from "node:path";
+import { existsSync, lstatSync, readFileSync, readdirSync, statSync } from "node:fs";
+import { pathToFileURL } from "node:url";
+//#region ../../node_modules/.pnpm/pathe@2.0.3/node_modules/pathe/dist/shared/pathe.M-eThtNZ.mjs
+const _DRIVE_LETTER_START_RE = /^[A-Za-z]:\//;
+function normalizeWindowsPath(input = "") {
+	if (!input) return input;
+	return input.replace(/\\/g, "/").replace(_DRIVE_LETTER_START_RE, (r) => r.toUpperCase());
+}
+const _IS_ABSOLUTE_RE = /^[/\\](?![/\\])|^[/\\]{2}(?!\.)|^[A-Za-z]:[/\\]/;
+const _DRIVE_LETTER_RE = /^[A-Za-z]:$/;
+const _ROOT_FOLDER_RE = /^\/([A-Za-z]:)?$/;
+function cwd() {
+	if (typeof process !== "undefined" && typeof process.cwd === "function") return process.cwd().replace(/\\/g, "/");
+	return "/";
+}
+const resolve$1 = function(...arguments_) {
+	arguments_ = arguments_.map((argument) => normalizeWindowsPath(argument));
+	let resolvedPath = "";
+	let resolvedAbsolute = false;
+	for (let index = arguments_.length - 1; index >= -1 && !resolvedAbsolute; index--) {
+		const path = index >= 0 ? arguments_[index] : cwd();
+		if (!path || path.length === 0) continue;
+		resolvedPath = `${path}/${resolvedPath}`;
+		resolvedAbsolute = isAbsolute$1(path);
+	}
+	resolvedPath = normalizeString(resolvedPath, !resolvedAbsolute);
+	if (resolvedAbsolute && !isAbsolute$1(resolvedPath)) return `/${resolvedPath}`;
+	return resolvedPath.length > 0 ? resolvedPath : ".";
+};
+function normalizeString(path, allowAboveRoot) {
+	let res = "";
+	let lastSegmentLength = 0;
+	let lastSlash = -1;
+	let dots = 0;
+	let char = null;
+	for (let index = 0; index <= path.length; ++index) {
+		if (index < path.length) char = path[index];
+		else if (char === "/") break;
+		else char = "/";
+		if (char === "/") {
+			if (lastSlash === index - 1 || dots === 1);
+			else if (dots === 2) {
+				if (res.length < 2 || lastSegmentLength !== 2 || res[res.length - 1] !== "." || res[res.length - 2] !== ".") {
+					if (res.length > 2) {
+						const lastSlashIndex = res.lastIndexOf("/");
+						if (lastSlashIndex === -1) {
+							res = "";
+							lastSegmentLength = 0;
+						} else {
+							res = res.slice(0, lastSlashIndex);
+							lastSegmentLength = res.length - 1 - res.lastIndexOf("/");
+						}
+						lastSlash = index;
+						dots = 0;
+						continue;
+					} else if (res.length > 0) {
+						res = "";
+						lastSegmentLength = 0;
+						lastSlash = index;
+						dots = 0;
+						continue;
+					}
+				}
+				if (allowAboveRoot) {
+					res += res.length > 0 ? "/.." : "..";
+					lastSegmentLength = 2;
+				}
+			} else {
+				if (res.length > 0) res += `/${path.slice(lastSlash + 1, index)}`;
+				else res = path.slice(lastSlash + 1, index);
+				lastSegmentLength = index - lastSlash - 1;
+			}
+			lastSlash = index;
+			dots = 0;
+		} else if (char === "." && dots !== -1) ++dots;
+		else dots = -1;
+	}
+	return res;
+}
+const isAbsolute$1 = function(p) {
+	return _IS_ABSOLUTE_RE.test(p);
+};
+const relative$1 = function(from, to) {
+	const _from = resolve$1(from).replace(_ROOT_FOLDER_RE, "$1").split("/");
+	const _to = resolve$1(to).replace(_ROOT_FOLDER_RE, "$1").split("/");
+	if (_to[0][1] === ":" && _from[0][1] === ":" && _from[0] !== _to[0]) return _to.join("/");
+	const _fromCopy = [..._from];
+	for (const segment of _fromCopy) {
+		if (_to[0] !== segment) break;
+		_from.shift();
+		_to.shift();
+	}
+	return [..._from.map(() => ".."), ..._to].join("/");
+};
+const dirname$1 = function(p) {
+	const segments = normalizeWindowsPath(p).replace(/\/$/, "").split("/").slice(0, -1);
+	if (segments.length === 1 && _DRIVE_LETTER_RE.test(segments[0])) segments[0] += "/";
+	return segments.join("/") || (isAbsolute$1(p) ? "/" : ".");
+};
+//#endregion
+//#region ../internal/src/build/paths.ts
+function generatedDirSegments(productName) {
+	return [".vitehub", productName];
+}
+//#endregion
+//#region ../internal/src/definition-catalog.ts
+const sourceFilePattern = /\.(?:c|m)?[jt]sx?$/i;
+const declarationFilePattern = /\.d\.(?:c|m)?[jt]sx?$/i;
+const ignoredDirs = new Set([
+	"node_modules",
+	"dist",
+	".output",
+	".nuxt",
+	".vercel",
+	".git",
+	".vitehub"
+]);
+function createDirectoryDefinitionSource(source, scanDirs, subdir, options = {}) {
+	return {
+		...options,
+		kind: "directory",
+		scanDirs,
+		source,
+		subdir
+	};
+}
+function readDirEntries(root) {
+	try {
+		return readdirSync(root, { withFileTypes: true });
+	} catch (error) {
+		if (error.code === "ENOENT") return [];
+		throw error;
+	}
+}
+function listMatchingFiles(root, predicate, options = {}) {
+	const files = [];
+	for (const entry of readDirEntries(root)) {
+		const absolute = resolve$1(root, entry.name);
+		if (entry.isDirectory() && !entry.isSymbolicLink()) {
+			if (entry.name.startsWith(".")) continue;
+			if (ignoredDirs.has(entry.name)) continue;
+			files.push(...listMatchingFiles(absolute, predicate, options));
+			continue;
+		}
+		if (entry.name.startsWith(".") && !options.includeHidden) continue;
+		if (entry.isFile() && predicate(entry.name)) files.push(absolute);
+	}
+	return files.sort();
+}
+function listSourceFiles(root, options = {}) {
+	return listMatchingFiles(root, (name) => sourceFilePattern.test(name) && !declarationFilePattern.test(name), options);
+}
+function normalizePathDefinitionName(rootDir, file) {
+	return relative$1(rootDir, file).replace(sourceFilePattern, "").replace(/\/index$/i, "");
+}
+function normalizeSuffixDefinitionName(rootDir, file, pattern, options = {}) {
+	const normalized = relative$1(rootDir, file).replace(pattern, "");
+	if (options.stripPrefix && normalized.startsWith(options.stripPrefix)) return normalized.slice(options.stripPrefix.length);
+	return normalized;
+}
+function sortDefinitions(definitions) {
+	return [...definitions.values()].sort((left, right) => left.name.localeCompare(right.name));
+}
+function registerDefinition(definitions, definition, sourceLabel) {
+	const existing = definitions.get(definition.name);
+	if (existing) throw new Error(`Duplicate ${sourceLabel} name "${definition.name}":\n  - ${existing.handler}\n  - ${definition.handler}`);
+	definitions.set(definition.name, definition);
+}
+function mergeDefinitions(feature, ...sources) {
+	const definitions = /* @__PURE__ */ new Map();
+	for (const source of sources) {
+		if (!source) continue;
+		for (const definition of source) {
+			const existing = definitions.get(definition.name);
+			if (existing && existing.handler !== definition.handler) throw new Error(`Duplicate ${feature} name "${definition.name}" from multiple discovery sources:\n  - ${existing.handler} (${existing.source ?? "unknown"})\n  - ${definition.handler} (${definition.source ?? "unknown"})`);
+			if (!existing) definitions.set(definition.name, definition);
+		}
+	}
+	return sortDefinitions(definitions);
+}
+function createDefinition(source, file, name) {
+	if (source.createDefinition) return source.createDefinition({
+		file,
+		name
+	});
+	return {
+		handler: file,
+		name,
+		source: source.source
+	};
+}
+function scanSuffixDefinitions(feature, source) {
+	const definitions = /* @__PURE__ */ new Map();
+	for (const root of source.roots) for (const file of listMatchingFiles(root, (name) => source.pattern.test(name), { includeHidden: source.includeHidden })) {
+		const name = source.normalizeName(root, file);
+		if (!name) continue;
+		registerDefinition(definitions, createDefinition(source, file, name), feature);
+	}
+	return sortDefinitions(definitions);
+}
+function scanDirectoryDefinitions(feature, source) {
+	const definitions = /* @__PURE__ */ new Map();
+	for (const scanDir of source.scanDirs) {
+		const directory = resolve$1(scanDir, source.subdir);
+		for (const file of listSourceFiles(directory, { includeHidden: source.includeHidden })) {
+			const name = source.normalizeName ? source.normalizeName(directory, file) : normalizePathDefinitionName(directory, file);
+			if (!name) continue;
+			registerDefinition(definitions, createDefinition(source, file, name), feature);
+		}
+	}
+	return sortDefinitions(definitions);
+}
+function discoverDefinitions(feature, sources) {
+	return mergeDefinitions(feature, ...sources.map((source) => source.kind === "suffix" ? scanSuffixDefinitions(feature, source) : scanDirectoryDefinitions(feature, source)));
+}
+function createGeneratedDefinitionPath(rootDir, options) {
+	const segments = options.segments ?? generatedDirSegments(options.productName);
+	return resolve$1(...options.buildDir ? [
+		rootDir,
+		options.buildDir,
+		...segments,
+		options.fileName
+	] : [
+		rootDir,
+		...segments,
+		options.fileName
+	]);
+}
+async function writeFileIfChanged(file, contents) {
+	if (await readFile(file, "utf8").catch(() => void 0) === contents) return;
+	await mkdir(resolve$1(file, ".."), { recursive: true });
+	await writeFile(file, contents, "utf8");
+}
+//#endregion
+//#region src/discovery.ts
+const agentSuffixPattern = /\.agent\.(?:c|m)?[jt]s$/i;
+const folderAgentPattern = /^agent\.(?:c|m)?[jt]s$/i;
+const legacyFolderAgentPattern = /^config\.(?:c|m)?[jt]s$/i;
+const evalDefinitionPattern = /^(?:.+\.)?eval\.(?:c|m)?[jt]s$/i;
+const indexDefinitionPattern = /^index\.(?:c|m)?[jt]s$/i;
+const colocatedAgentResourceDirectories = new Set(["home", "skills"]);
+const agentEvalFileConvention = {
+	include: [
+		"**/*.eval.?(m)ts",
+		"**/eval.?(m)ts",
+		"**/*.eval.tsx",
+		"**/eval.tsx"
+	],
+	pattern: /^(?:.+\.)?eval\.(?:m?ts|tsx)$/
+};
+function createAgentEvalInclude(rootDirs) {
+	return [...new Set(rootDirs.flatMap((rootDir) => {
+		const root = resolve(rootDir).replace(/\\/g, "/").replace(/([*?[\]{}()!])/g, "\\$1");
+		return agentEvalFileConvention.include.map((pattern) => `${root}/${pattern}`);
+	}))].sort();
+}
+function isColocatedAgentResourcePath(path) {
+	return colocatedAgentResourceDirectories.has(path.split("/")[0] || "");
+}
+function isEvalDefinitionFile(file) {
+	return evalDefinitionPattern.test(basename(file));
+}
+function discoverAgentEvalFiles(rootDirs) {
+	return [...new Set(rootDirs.flatMap((rootDir) => listMatchingFiles(resolve(rootDir), (file) => agentEvalFileConvention.pattern.test(file))))].sort();
+}
+function normalizeSuffixAgentName(rootDir, file) {
+	const name = normalizeSuffixDefinitionName(rootDir, file, agentSuffixPattern, { stripPrefix: "src/" });
+	return name.startsWith("server/") ? void 0 : name;
+}
+function stripComments(source) {
+	return source.replace(/\/\*[\s\S]*?\*\//g, "").replace(/(^|[^:])\/\/.*$/gm, "$1");
+}
+function isWorkspaceAgentDefinition(source) {
+	return /\bdefineAgent\s*\(\s*\{[\s\S]*?\bworkspace\s*:/.test(stripComments(source));
+}
+function isAgentDefinitionSource(source) {
+	const stripped = stripComments(source);
+	return /\bdefineAgent\s*\(/.test(stripped) || /\bexport\s*\{\s*default\s*\}\s*from\b/.test(stripped) || /\bexport\s+default\s+\w*Agent\b/.test(stripped);
+}
+function isInsideFolderAgent(file, folderAgentDirs) {
+	const directory = dirname(file);
+	if (folderAgentDirs.has(directory) && indexDefinitionPattern.test(basename(file))) return false;
+	if (isAgentDefinitionSource(readFileSync(file, "utf8"))) return false;
+	for (const agentDir of folderAgentDirs) {
+		const path = relative(agentDir, directory);
+		if (path === "" || !path.startsWith("..") && path !== "..") return true;
+	}
+	return false;
+}
+function isWorkspaceSourceConfig(file, folderAgentDirs) {
+	const directory = dirname(file);
+	for (const agentDir of folderAgentDirs) {
+		const path = relative(agentDir, directory).replace(/\\/g, "/");
+		if (!path || path.startsWith("../") || path === "..") continue;
+		if (path.split("/")[0] === "workspace") return true;
+	}
+	return false;
+}
+function discoverFolderAgentDefinitions(scanDirs) {
+	const candidates = [];
+	const walk = (agentsRoot, current) => {
+		let entries;
+		try {
+			entries = readdirSync(current, { withFileTypes: true });
+		} catch (error) {
+			if (error.code === "ENOENT") return;
+			throw error;
+		}
+		for (const entry of entries) {
+			const file = resolve(current, entry.name);
+			if (entry.isDirectory() && !entry.isSymbolicLink() && !entry.name.startsWith(".")) {
+				walk(agentsRoot, file);
+				continue;
+			}
+			if (!entry.isFile() || !folderAgentPattern.test(basename(file)) && !indexDefinitionPattern.test(basename(file))) continue;
+			const source = readFileSync(file, "utf8");
+			const agent = relative(agentsRoot, dirname(file)).replace(/\\/g, "/");
+			if (!agent || agent === ".") continue;
+			const workspace = isWorkspaceAgentDefinition(source);
+			candidates.push({
+				handler: file,
+				name: agent,
+				source: workspace ? "server-agent-workspace" : "server-agents",
+				workspace: workspace ? agent : void 0
+			});
+		}
+	};
+	for (const scanDir of scanDirs) walk(resolve(scanDir, "agents"), resolve(scanDir, "agents"));
+	const ownedResourceEntries = new Set(candidates.flatMap((definition) => candidates.some((parent) => {
+		if (parent === definition) return false;
+		return isColocatedAgentResourcePath(relative(dirname(parent.handler), dirname(definition.handler)).replace(/\\/g, "/"));
+	}) ? [definition.handler] : []));
+	const nestedHelperIndexes = new Set(candidates.flatMap((definition) => indexDefinitionPattern.test(basename(definition.handler)) && !isAgentDefinitionSource(readFileSync(definition.handler, "utf8")) && candidates.some((parent) => {
+		if (parent === definition) return false;
+		const path = relative(dirname(parent.handler), dirname(definition.handler)).replace(/\\/g, "/");
+		return path !== "" && path !== ".." && !path.startsWith("../");
+	}) ? [definition.handler] : []));
+	const discoveredCandidates = candidates.filter((definition) => !ownedResourceEntries.has(definition.handler) && !nestedHelperIndexes.has(definition.handler));
+	const folderAgentDirs = new Set(discoveredCandidates.filter((definition) => definition.source === "server-agent-workspace").map((definition) => dirname(definition.handler)));
+	return discoveredCandidates.filter((definition) => !isWorkspaceSourceConfig(definition.handler, folderAgentDirs));
+}
+function discoverAgentDefinitions(options) {
+	if (options.mode === "server-agents") {
+		const folderDefinitions = discoverFolderAgentDefinitions(options.scanDirs);
+		const folderAgentDirs = new Set(folderDefinitions.map((definition) => dirname(definition.handler)));
+		return mergeDefinitions("agent", discoverDefinitions("agent", [createDirectoryDefinitionSource("server-agents", options.scanDirs, "agents", {
+			normalizeName(directory, file) {
+				const fileName = basename(file);
+				if (folderAgentPattern.test(fileName) && dirname(file) !== directory) return;
+				if (legacyFolderAgentPattern.test(fileName) || indexDefinitionPattern.test(fileName) || isEvalDefinitionFile(file)) return;
+				for (const agentDir of folderAgentDirs) if (isColocatedAgentResourcePath(relative(agentDir, file).replace(/\\/g, "/"))) return;
+				if (isInsideFolderAgent(file, folderAgentDirs)) return;
+				return relative(directory, file).replace(/\.(?:c|m)?[jt]s$/i, "").replace(/\/index$/i, "");
+			},
+			createDefinition({ file, name }) {
+				return {
+					handler: file,
+					name,
+					source: "server-agents"
+				};
+			}
+		})]), folderDefinitions);
+	}
+	return discoverDefinitions("agent", [{
+		kind: "suffix",
+		normalizeName: normalizeSuffixAgentName,
+		pattern: agentSuffixPattern,
+		roots: [...new Set([options.rootDir, ...options.scanDirs || []].filter(Boolean))].map((root) => resolve(root)),
+		source: "vite-suffix"
+	}]);
+}
+//#endregion
+//#region ../internal/src/build/colocated-agent-files.ts
+function formatSize(bytes) {
+	const mebibyte = 1024 * 1024;
+	return bytes % mebibyte === 0 ? `${bytes / mebibyte} MiB` : `${bytes} bytes`;
+}
+function isFolderAgentEntry(file) {
+	if (!/^(?:agent|index)\.(?:c|m)?[jt]s$/i.test(basename(file))) return false;
+	return !(basename(file).toLowerCase().startsWith("agent.") && basename(dirname(file)) === "agents" && basename(dirname(dirname(file))) === "server");
+}
+function readColocatedAgentFiles(handler, directoryName, options = {}) {
+	if (!isFolderAgentEntry(handler)) return;
+	const root = join(dirname(handler), directoryName);
+	if (!existsSync(root)) return;
+	if (!(options.rejectUnsupportedEntries ? lstatSync(root) : statSync(root)).isDirectory()) {
+		if (options.rejectUnsupportedEntries) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} supports regular files and directories only: ${directoryName}`);
+		return;
+	}
+	const files = {};
+	let fileCount = 0;
+	let totalSize = 0;
+	const visit = (directory) => {
+		for (const entry of readdirSync(directory, { withFileTypes: true }).sort((left, right) => left.name.localeCompare(right.name))) {
+			const file = join(directory, entry.name);
+			const target = relative(root, file).replace(/\\/g, "/");
+			if (entry.isDirectory()) {
+				visit(file);
+				continue;
+			}
+			if (!entry.isFile()) {
+				if (options.rejectUnsupportedEntries) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} supports regular files and directories only: ${target}`);
+				continue;
+			}
+			const size = statSync(file).size;
+			fileCount += 1;
+			if (options.fileCountLimit !== void 0 && fileCount > options.fileCountLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${options.fileCountLimit} files.`);
+			if (options.fileSizeLimit !== void 0 && size > options.fileSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent file"} file exceeds ${formatSize(options.fileSizeLimit)}: ${target}`);
+			totalSize += size;
+			if (options.totalSizeLimit !== void 0 && totalSize > options.totalSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${formatSize(options.totalSizeLimit)}.`);
+			const content = readFileSync(file);
+			if (content.byteLength !== size) {
+				if (options.fileSizeLimit !== void 0 && content.byteLength > options.fileSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent file"} file exceeds ${formatSize(options.fileSizeLimit)}: ${target}`);
+				totalSize += content.byteLength - size;
+				if (options.totalSizeLimit !== void 0 && totalSize > options.totalSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${formatSize(options.totalSizeLimit)}.`);
+			}
+			files[target] = {
+				content: content.toString("base64"),
+				encoding: "base64"
+			};
+		}
+	};
+	visit(root);
+	return Object.keys(files).length ? files : void 0;
+}
+//#endregion
+//#region src/vite/colocated-agent-home.ts
+const fileSizeLimit = 1024 * 1024;
+const totalSizeLimit = 4 * 1024 * 1024;
+function readColocatedAgentHome(handler) {
+	const files = readColocatedAgentFiles(handler, "home", {
+		fileCountLimit: 1024,
+		fileSizeLimit,
+		label: "Colocated Agent Home",
+		rejectUnsupportedEntries: true,
+		totalSizeLimit
+	});
+	if (!files) return;
+	return Object.fromEntries(Object.entries(files).map(([target, file]) => [target, {
+		contents: file.content,
+		encoding: file.encoding
+	}]));
+}
+//#endregion
+//#region src/vite/colocated-agent-instructions.ts
+async function readColocatedAgentInstructions(handler, options = {}) {
+	const file = join(dirname(handler), "instructions.md");
+	options.dependencies?.add(file);
+	if (!existsSync(file) || !statSync(file).isFile()) return;
+	return await resolveInstructionImports(readFileSync(file, "utf8"), {
+		file,
+		read(specifier, importer) {
+			const imported = resolve(dirname(importer), specifier);
+			options.dependencies?.add(imported);
+			return {
+				content: readFileSync(imported, "utf8"),
+				file: imported
+			};
+		}
+	});
+}
+//#endregion
+//#region src/vite/colocated-agent-skills.ts
+function readColocatedAgentSkills(handler) {
+	const files = readColocatedAgentFiles(handler, "skills");
+	if (!files) return;
+	return Object.fromEntries(Object.entries(files).map(([path, file]) => {
+		const workspacePath = `skills/${path}`;
+		return [`__vitehubAgentSkill:${workspacePath}`, {
+			...file,
+			materialize: "build",
+			mount: "",
+			workspacePath
+		}];
+	}));
+}
+//#endregion
+//#region src/vite/runtime-adapter.ts
+function isRecord(value) {
+	return !!value && typeof value === "object" && !Array.isArray(value);
+}
+function resolveAgentModule(module) {
+	if (isRecord(module) && "default" in module) return module.default;
+	return module;
+}
+function workspaceSourceRoot(file) {
+	const workspaceDirectory = join(dirname(file), "workspace");
+	return existsSync(workspaceDirectory) && statSync(workspaceDirectory).isDirectory() ? workspaceDirectory : dirname(file);
+}
+function colocatedSkills(file) {
+	return decodeColocatedAgentSkills(readColocatedAgentSkills(file));
+}
+function colocatedHome(file) {
+	return decodeColocatedAgentHome(readColocatedAgentHome(file));
+}
+async function loadViteAgent(server, definition) {
+	const agent = withColocatedAgentHome(withColocatedAgentSkills(agentWithColocatedInstructions(resolveAgentModule(await server.ssrLoadModule(pathToFileURL(definition.handler).href)), await readColocatedAgentInstructions(definition.handler)), colocatedSkills(definition.handler)), colocatedHome(definition.handler));
+	if (!agent) return;
+	return {
+		agent,
+		definition,
+		identity: {
+			name: definition.name,
+			...definition.workspace ? { workspace: definition.workspace } : {}
+		}
+	};
+}
+function createViteWorkspaceAgentLoader(server, definition) {
+	return async () => {
+		const module = await server.ssrLoadModule(pathToFileURL(definition.handler).href);
+		return {
+			...module,
+			default: withColocatedAgentHome(workspaceAgentWithSourceRoot(withColocatedAgentSkills(module.default, colocatedSkills(definition.handler)), workspaceSourceRoot(definition.handler)), colocatedHome(definition.handler))
+		};
+	};
+}
+function headersFromNode(headers) {
+	const result = new Headers();
+	for (const [name, value] of Object.entries(headers)) if (typeof value === "string") result.set(name, value);
+	else if (Array.isArray(value)) for (const item of value) result.append(name, item);
+	return result;
+}
+function createRequest(server, req, fallbackRoute) {
+	const base = server.resolvedUrls?.local?.[0] || `http://localhost:${server.config.server.port || 5173}/`;
+	return new Request(new URL(req.url || fallbackRoute, base), {
+		headers: headersFromNode(req.headers),
+		method: req.method || "GET"
+	});
+}
+function createViteAgentRuntimeContext(server, req, identity, options) {
+	return createAgentRuntimeContext({
+		agentIdentity: identity,
+		request: createRequest(server, req, options.fallbackRoute),
+		...options.run ? { run: options.run } : {},
+		runtime: "vite",
+		runtimeConfig: {},
+		waitUntil: (task) => void Promise.resolve(task).catch(() => {})
+	});
+}
+function createViteAgentDiscoveryContext(identity) {
+	return createAgentRuntimeContext({
+		agentIdentity: identity,
+		runtime: "vite",
+		runtimeConfig: {},
+		waitUntil: (task) => void Promise.resolve(task).catch(() => {})
+	});
+}
+async function writeViteResponse(res, response) {
+	res.statusCode = response.status;
+	for (const [name, value] of response.headers) res.setHeader(name, value);
+	if (!response.body) {
+		res.end();
+		return;
+	}
+	const reader = response.body.getReader();
+	let closed = false;
+	const cancel = () => {
+		if (closed) return;
+		closed = true;
+		Promise.resolve().then(() => reader.cancel()).catch(() => {});
+	};
+	res.once("close", cancel);
+	try {
+		for (;;) {
+			const { done, value } = await reader.read();
+			if (done) break;
+			res.write(value);
+		}
+		closed = true;
+		res.end();
+	} catch (error) {
+		const wasClosed = closed;
+		closed = true;
+		if (wasClosed && error instanceof Error && error.name === "AbortError") return;
+		res.destroy(error instanceof Error ? error : void 0);
+	} finally {
+		closed = true;
+		res.off("close", cancel);
+		reader.releaseLock();
+	}
+}
+//#endregion
+//#region src/internal/evalite-config.ts
+const defaultForceRerunTriggers = [
+	"server/agents/**",
+	"src/**/*.agent.*",
+	"src/**/*.eval.*"
+];
+function resolveAgentEvalOptions(options) {
+	return {
+		...options,
+		forceRerunTriggers: options?.forceRerunTriggers ?? defaultForceRerunTriggers
+	};
+}
+function createAgentEvaliteConfigPath(rootDir) {
+	return createGeneratedDefinitionPath(rootDir, {
+		fileName: "evalite.config.ts",
+		productName: "agent"
+	});
+}
+function withoutUndefined(value) {
+	if (Array.isArray(value)) return value.map((item) => withoutUndefined(item));
+	if (!value || typeof value !== "object") return value;
+	const entries = Object.entries(value).filter(([, item]) => item !== void 0).map(([key, item]) => [key, withoutUndefined(item)]).filter(([, item]) => !(item && typeof item === "object" && !Array.isArray(item) && !Object.keys(item).length));
+	return Object.fromEntries(entries);
+}
+function renderAgentEvaliteConfig(options) {
+	return [
+		`import { defineConfig } from "evalite/config"`,
+		"",
+		`export default defineConfig(${JSON.stringify(withoutUndefined(options), null, 2)})`,
+		""
+	].join("\n");
+}
+async function writeAgentEvaliteConfig(rootDir, options) {
+	const file = createAgentEvaliteConfigPath(rootDir);
+	await writeFileIfChanged(file, renderAgentEvaliteConfig(options));
+	return file;
+}
+async function removeAgentEvaliteConfig(rootDir) {
+	await rm(createAgentEvaliteConfigPath(rootDir), { force: true });
+}
+//#endregion
+export { relative$1 as _, createViteAgentRuntimeContext as a, writeViteResponse as c, readColocatedAgentHome as d, agentEvalFileConvention as f, dirname$1 as g, discoverAgentEvalFiles as h, createViteAgentDiscoveryContext as i, readColocatedAgentSkills as l, discoverAgentDefinitions as m, resolveAgentEvalOptions as n, createViteWorkspaceAgentLoader as o, createAgentEvalInclude as p, writeAgentEvaliteConfig as r, loadViteAgent as s, removeAgentEvaliteConfig as t, readColocatedAgentInstructions as u, resolve$1 as v };
diff --git a/dist/evalite-runner-C5QSseWL.js b/dist/evalite-runner-C5QSseWL.js
new file mode 100644
index 0000000000000000000000000000000000000000..b97d099b6d7727910c6133328c78ad13c7c3bd91
--- /dev/null
+++ b/dist/evalite-runner-C5QSseWL.js
@@ -0,0 +1,159 @@
+import { f as agentEvalFileConvention } from "./evalite-config-7KBtA3Zi.js";
+import { mkdir, writeFile } from "node:fs/promises";
+import path from "node:path";
+import { fileURLToPath } from "node:url";
+import process from "node:process";
+//#region src/evalite-runner.ts
+async function exportResultsToJSON(options) {
+	const latestFullRun = (await options.storage.runs.getMany({
+		limit: 1,
+		orderBy: "created_at",
+		orderDirection: "desc",
+		runType: "full"
+	}))[0];
+	if (!latestFullRun) throw new Error("No completed run found to export");
+	const suites = await options.storage.suites.getMany({
+		runIds: [latestFullRun.id],
+		statuses: ["fail", "success"]
+	});
+	const evals = await options.storage.evals.getMany({ suiteIds: suites.map((suite) => suite.id) });
+	const scores = await options.storage.scores.getMany({ evalIds: evals.map((item) => item.id) });
+	const traces = await options.storage.traces.getMany({ evalIds: evals.map((item) => item.id) });
+	const outputData = {
+		run: {
+			createdAt: latestFullRun.created_at,
+			id: latestFullRun.id,
+			runType: latestFullRun.runType
+		},
+		suites: suites.map((suite) => {
+			const suiteEvals = evals.filter((item) => item.suite_id === suite.id);
+			const suiteScores = scores.filter((score) => suiteEvals.some((item) => item.id === score.eval_id));
+			return {
+				averageScore: suiteScores.length ? suiteScores.reduce((total, score) => total + score.score, 0) / suiteScores.length : 0,
+				createdAt: suite.created_at,
+				duration: suite.duration,
+				evals: suiteEvals.map((item) => {
+					const itemScores = scores.filter((score) => score.eval_id === item.id);
+					const itemTraces = traces.filter((trace) => trace.eval_id === item.id);
+					return {
+						averageScore: itemScores.length ? itemScores.reduce((total, score) => total + score.score, 0) / itemScores.length : 0,
+						colOrder: item.col_order,
+						createdAt: item.created_at,
+						duration: item.duration,
+						expected: item.expected,
+						id: item.id,
+						input: item.input,
+						output: item.output,
+						renderedColumns: item.rendered_columns,
+						scores: itemScores.map((score) => ({
+							createdAt: score.created_at,
+							description: score.description,
+							id: score.id,
+							metadata: score.metadata,
+							name: score.name,
+							score: score.score
+						})),
+						status: item.status,
+						traces: itemTraces.map((trace) => ({
+							colOrder: trace.col_order,
+							endTime: trace.end_time,
+							id: trace.id,
+							input: trace.input,
+							inputTokens: trace.input_tokens,
+							output: trace.output,
+							outputTokens: trace.output_tokens,
+							startTime: trace.start_time,
+							totalTokens: trace.total_tokens
+						}))
+					};
+				}),
+				filepath: suite.filepath,
+				id: suite.id,
+				name: suite.name,
+				status: suite.status,
+				variantGroup: suite.variant_group,
+				variantName: suite.variant_name
+			};
+		})
+	};
+	const outputPath = path.isAbsolute(options.outputPath) ? options.outputPath : path.join(options.cwd, options.outputPath);
+	await mkdir(path.dirname(outputPath), { recursive: true });
+	await writeFile(outputPath, JSON.stringify(outputData, null, 2), "utf8");
+}
+async function runAgentEvalite(options) {
+	const [{ FILES_LOCATION }, { DEFAULT_SERVER_PORT }, { default: EvaliteReporter }, { createServer }, { createInMemoryStorage }, { configDefaults }, { createVitest, registerConsoleShortcuts }] = await Promise.all([
+		import("evalite/backend-only-constants"),
+		import("evalite/constants"),
+		import("evalite/reporter"),
+		import("evalite/server"),
+		import("evalite/in-memory-storage"),
+		import("vitest/config"),
+		import("vitest/node")
+	]);
+	const cwd = options.cwd;
+	await mkdir(path.join(cwd, FILES_LOCATION), { recursive: true });
+	const storage = createInMemoryStorage();
+	const serverPort = options.server?.port ?? DEFAULT_SERVER_PORT;
+	const server = createServer({ storage });
+	server.start(serverPort);
+	let exitCode;
+	const setupFiles = [fileURLToPath(import.meta.resolve("evalite/env-setup-file")), ...options.setupFiles || []];
+	const forceRerunTriggers = options.forceRerunTriggers ?? configDefaults.forceRerunTriggers;
+	process.env.EVALITE_REPORT_TRACES = "true";
+	const reporters = [new EvaliteReporter({
+		hideTable: options.hideTable,
+		isWatching: options.mode === "watch-for-file-changes" || options.mode === "run-once-and-serve",
+		logNewState: (newState) => server.updateState(newState),
+		mode: options.mode,
+		modifyExitCode: (code) => {
+			exitCode = code;
+		},
+		port: serverPort,
+		scoreThreshold: options.scoreThreshold,
+		storage
+	})];
+	const vitest = await createVitest("test", {
+		browser: void 0,
+		config: false,
+		forceRerunTriggers,
+		include: options.include ?? agentEvalFileConvention.include,
+		maxConcurrency: options.maxConcurrency,
+		mode: "test",
+		reporters,
+		root: cwd,
+		sequence: { concurrent: true },
+		setupFiles,
+		testTimeout: options.testTimeout ?? 3e4,
+		watch: options.mode === "watch-for-file-changes"
+	}, {}, {
+		stderr: options.testOutputWritable || process.stderr,
+		stdout: options.testOutputWritable || process.stdout
+	});
+	const provide = vitest.provide;
+	provide("cwd", cwd);
+	provide("trialCount", options.trialCount);
+	provide("serverPort", serverPort);
+	provide("cacheDebug", false);
+	provide("cacheEnabled", options.cacheEnabled ?? options.cache ?? true);
+	await vitest.start(options.path ? [options.path] : void 0);
+	const disposeConsoleShortcuts = registerConsoleShortcuts(vitest, process.stdin, process.stdout);
+	const rerun = async () => {
+		await vitest.cancelCurrentRun("keyboard-input");
+		const specs = vitest.state.getFilepaths().flatMap((filepath) => vitest.getModuleSpecifications(filepath));
+		await vitest.rerunTestSpecifications(specs, true);
+	};
+	server.setRerunFn(rerun);
+	if (!vitest.shouldKeepServer() && options.mode !== "run-once-and-serve") {
+		disposeConsoleShortcuts();
+		await vitest.close();
+		await server.stop();
+		if (options.outputPath) await exportResultsToJSON({
+			cwd,
+			outputPath: options.outputPath,
+			storage
+		});
+	}
+	return { exitCode };
+}
+//#endregion
+export { runAgentEvalite };
diff --git a/dist/harness-agent-CklIYSAq.js b/dist/harness-agent-CklIYSAq.js
new file mode 100644
index 0000000000000000000000000000000000000000..64df9789ca289989676692f95e91bb5a5f5d585f
--- /dev/null
+++ b/dist/harness-agent-CklIYSAq.js
@@ -0,0 +1,1054 @@
+import { isAttachmentData, isAttachmentPart } from "./messages.js";
+import { c as isAsyncIterable } from "./delivery-artifacts-BOKCLStf.js";
+import { M as agentInvocationSourceContext, V as hasTrustedWorkspaceAccessScope, b as abortSignalError, st as resolveMessageChannelInstructions, tt as markMessageChannelInstructionConsumer, x as nextWithAbort } from "./capability-runtime-Be85oBbC.js";
+import { t as composeInstructionDocument } from "./instruction-composition-CBkhC2F8.js";
+import { a as streamAgentOutputToEvents, m as defineAgentUsageMetadata, p as createHarnessUsageMetadata } from "./agent-output-CyEZfgrQ.js";
+import { _ as withAgentToolStepReporting, m as applyAgentToolPolicies, v as withJsonCompatibleToolOutputs, y as agentOutputInstructions } from "./trace-GIqXV-YM.js";
+import { toAiSdkModelMessages } from "./ai-sdk.js";
+import { a as setHarnessWorkspaceDiff, i as setActiveHarnessWorkspaceFiles } from "./harness-runtime-D4hlzXYA.js";
+import { t as colocatedAgentSkillsContextKey } from "./colocated-agent-skills-BAz3kNyV.js";
+import { f as workspaceDefinitionWithAutoCommitRules, o as resolveColocatedAgentInstructionDocument } from "./workspace-agent-BtTRGp6h.js";
+import { normalizeWorkspaceSourcesMetadata } from "@vite-hub/workspace/source-metadata";
+import { posix } from "node:path";
+//#region src/harness-agent.ts
+const harnessResumeStates = /* @__PURE__ */ new WeakMap();
+const harnessGeneratedFiles = ["harness-tool.mjs"];
+const harnessInstructionFiles = ["AGENTS.md", "CLAUDE.md"];
+const harnessAttachmentDirectory = ".vitehub/attachments";
+const harnessAttachmentMaxBytes = 25 * 1024 * 1024;
+const harnessAgentPackage = "@ai-sdk/harness/agent";
+const harnessRemoveDirectory = Symbol.for("vitehub.harnessRemoveDirectory");
+const harnessSandboxAdapter = Symbol.for("vitehub.harnessSandboxAdapter");
+const harnessGlobalSkillsDirectory = Symbol.for("vitehub.harnessGlobalSkillsDirectory");
+const harnessSessionPrepare = Symbol.for("vitehub.harnessSessionPrepare");
+function isHarnessRelativeDirectory(value) {
+	return typeof value === "string" && value !== "." && posix.normalize(value) === value && !posix.isAbsolute(value) && !value.split("/").includes("..");
+}
+function hasEntries(value) {
+	return typeof value === "object" && value !== null && Object.keys(value).length > 0;
+}
+function defaultHarnessPermissionMode(harness) {
+	if (hasEntries(harness) && harness.harnessId === "claude-code" && typeof process.getuid === "function" && process.getuid() === 0) return "allow-edits";
+	return "allow-all";
+}
+function unsupportedHarnessContributionKinds(context) {
+	const kinds = /* @__PURE__ */ new Set();
+	if (context.providerTools?.length) kinds.add("provider tools");
+	return kinds;
+}
+function formatContributionNames(names) {
+	const uniqueNames = Array.from(new Set(names || [])).filter(Boolean).sort();
+	return uniqueNames.length ? ` (${uniqueNames.join(", ")})` : "";
+}
+function formatUnsupportedHarnessContributions(context) {
+	const unsupportedKinds = unsupportedHarnessContributionKinds(context);
+	if (!unsupportedKinds.size) return [];
+	const contributed = /* @__PURE__ */ new Map();
+	for (const contribution of context.driverContributions || []) {
+		if (!unsupportedKinds.has(contribution.kind)) continue;
+		const key = `${contribution.capabilityId}\0${contribution.kind}`;
+		const current = contributed.get(key) || {
+			capabilityId: contribution.capabilityId,
+			kind: contribution.kind,
+			names: /* @__PURE__ */ new Set()
+		};
+		for (const name of contribution.names || []) current.names.add(name);
+		contributed.set(key, current);
+	}
+	if (contributed.size) return Array.from(contributed.values()).map((contribution) => `${contribution.capabilityId}: ${contribution.kind}${formatContributionNames(contribution.names)}`);
+	return [unsupportedKinds.has("provider tools") ? "provider tools" : void 0].filter((value) => Boolean(value));
+}
+function assertSupportedHarnessDriverContributions(context) {
+	const unsupported = formatUnsupportedHarnessContributions(context);
+	if (unsupported.length) throw new Error(`[vitehub] Harness Agent Drivers do not support these Capability Driver Contributions yet: ${unsupported.join("; ")}. Move model-facing tools to harness-native workspace files or remove those capabilities for harness.`);
+}
+function staticWorkspaceRulePath(pattern) {
+	const normalizedPattern = pattern.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/");
+	const wildcard = normalizedPattern.search(/[*{[(?]/);
+	if (wildcard === 0 && normalizedPattern !== "**" && !normalizedPattern.startsWith("**/")) return;
+	const normalized = (wildcard === -1 ? normalizedPattern : normalizedPattern.slice(0, wildcard)).replace(/\/+$/, "");
+	if (normalized.split("/").filter(Boolean).some((part) => part === "." || part === "..")) return;
+	return normalized;
+}
+function workspaceRuleHarnessPaths(definition) {
+	if (!definition) return [];
+	return [...Object.entries(definition.rules || {}), ...(definition.plugins || []).flatMap((plugin) => Object.entries(plugin.rules || {}))].flatMap(([pattern, rule]) => rule.write ? [staticWorkspaceRulePath(pattern)].filter((path) => path !== void 0) : []);
+}
+function hasWorkspaceCommitRules(definition) {
+	return definition?.commit === true || typeof definition?.commit === "string" || [...Object.values(definition?.rules || {}), ...(definition?.plugins || []).flatMap((plugin) => Object.values(plugin.rules || {}))].some((rule) => rule.commit !== void 0);
+}
+function workspaceSourceHarnessPaths(context) {
+	const sources = context.workspaceDefinition?.sources;
+	if (!sources) return [];
+	return normalizeWorkspaceSourcesMetadata(sources).flatMap((source) => {
+		if (source.materialize === "lazy" && !source.requestOnly) return [source.mountPath];
+		if (source.probeKeys?.length) return source.probeKeys.map((key) => [source.mountPath, key].filter(Boolean).join("/"));
+		return [];
+	});
+}
+function hasLazyWorkspaceSources(context) {
+	const sources = context.workspaceDefinition?.sources;
+	if (!sources) return false;
+	return normalizeWorkspaceSourcesMetadata(sources).some((source) => {
+		return source.materialize === "lazy";
+	});
+}
+function pathContains(container, path) {
+	return !container || path === container || path.startsWith(`${container}/`);
+}
+function normalizeActiveHarnessWorkspacePath(sessionWorkDir, path) {
+	const normalizedWorkDir = sessionWorkDir.replace(/\\/g, "/").replace(/\/+$/g, "").replace(/\/+/g, "/");
+	const normalizedPath = path.replace(/\\/g, "/").replace(/\/+/g, "/");
+	const parts = (normalizedPath.startsWith(`${normalizedWorkDir}/`) || normalizedPath === normalizedWorkDir ? normalizedPath.slice(normalizedWorkDir.length) : normalizedPath.replace(/^\/workspace(?:\/|$)/, "")).replace(/^\/+|\/+$/g, "").replace(/\/+/g, "/").split("/").filter(Boolean);
+	if (parts.some((part) => part === "." || part === "..")) throw new Error(`[vitehub] Workspace path must stay inside the workspace: "${path}".`);
+	return parts.join("/");
+}
+function joinHarnessSessionPath(sessionWorkDir, workspacePath) {
+	return [sessionWorkDir.replace(/\/+$/, ""), workspacePath].filter(Boolean).join("/");
+}
+function activeHarnessWorkspaceFiles(session, sessionWorkDir, abortSignal) {
+	if (!session.readBinaryFile) return;
+	return { async readFile(path) {
+		const workspacePath = normalizeActiveHarnessWorkspacePath(sessionWorkDir, path);
+		if (!workspacePath) return {
+			active: true,
+			body: void 0
+		};
+		return {
+			active: true,
+			body: await session.readBinaryFile?.({
+				abortSignal,
+				path: joinHarnessSessionPath(sessionWorkDir, workspacePath)
+			}) ?? void 0
+		};
+	} };
+}
+function compactWorkspacePaths(paths) {
+	const sorted = [...new Set(paths)].sort((left, right) => left.length - right.length || left.localeCompare(right));
+	return sorted.filter((path, index) => !sorted.some((candidate, candidateIndex) => candidateIndex < index && pathContains(candidate, path)));
+}
+function harnessSupportWorkspacePaths(context, definition = context.workspaceDefinition) {
+	const materializationContext = context;
+	return compactWorkspacePaths([
+		...definition?.commit === true || typeof definition?.commit === "string" ? [""] : [],
+		...materializationContext.workspaceMaterializationPaths || [],
+		...workspaceRuleHarnessPaths(definition)
+	]);
+}
+function withHarnessInstructionPaths(paths, includeEmpty = false) {
+	return paths.length || includeEmpty ? compactWorkspacePaths([...harnessInstructionFiles, ...paths]) : [];
+}
+function explicitHarnessWorkspacePaths(context, definition = context.workspaceDefinition) {
+	return withHarnessInstructionPaths([...harnessSupportWorkspacePaths(context, definition), ...workspaceSourceHarnessPaths(context)], hasLazyWorkspaceSources(context));
+}
+function selectedWorkspaceScopePaths(context, definition = context.workspaceDefinition) {
+	const harnessPaths = explicitHarnessWorkspacePaths(context, definition);
+	if (!hasTrustedWorkspaceAccessScope(context.context)) return harnessPaths.length ? harnessPaths : void 0;
+	const scope = context.context.get("access")?.workspaceScope;
+	if (!scope) return harnessPaths.length ? harnessPaths : void 0;
+	if (scope.all) return [""];
+	const paths = [...new Set([...scope.paths || [], ...harnessSupportWorkspacePaths(context, definition).filter((path) => path !== "")])];
+	return paths.length ? withHarnessInstructionPaths(paths) : [];
+}
+async function composeHarnessInstructions(content, context) {
+	return await composeInstructionDocument(content, {
+		context: context.context.toJSON(),
+		workspace: context.workspaceInstructionBindings
+	});
+}
+function harnessSerializableMessages(messages) {
+	return messages.map((message) => ({
+		...message,
+		parts: message.parts.flatMap((part) => {
+			if (!isAttachmentPart(part) || !part.fetchData && !(part.data instanceof Blob)) return [part];
+			const { fetchData: _fetchData, ...reference } = part;
+			if (reference.data instanceof Blob) delete reference.data;
+			return reference.data || reference.url ? [reference] : [];
+		})
+	}));
+}
+function projectHarnessChatMessages(messages) {
+	const modelMessages = toAiSdkModelMessages(messages);
+	const lastUserMessage = modelMessages.findLast((message) => message.role === "user");
+	if (!lastUserMessage) throw new Error("[vitehub] Harness chat history must contain a user message.");
+	if (messages.length === 1) return lastUserMessage;
+	const content = [];
+	for (const message of modelMessages) {
+		const projected = [];
+		if (typeof message.content === "string") projected.push({
+			text: message.content,
+			type: "text"
+		});
+		else for (const part of message.content) if (part.type === "text") projected.push({
+			text: part.text,
+			type: "text"
+		});
+		else if (message.role === "user" && part.type === "image") projected.push(part);
+		else projected.push({
+			text: JSON.stringify(part),
+			type: "text"
+		});
+		if (!projected.length) continue;
+		content.push({
+			text: `<message role="${message.role}">\n`,
+			type: "text"
+		});
+		content.push(...projected);
+		content.push({
+			text: "\n</message>\n",
+			type: "text"
+		});
+	}
+	return {
+		content,
+		role: "user"
+	};
+}
+function latestHarnessUserMessage(messages) {
+	for (let index = messages.length - 1; index >= 0; index--) {
+		const message = messages[index];
+		if (message?.role === "user") return [message];
+	}
+	return messages.slice(-1);
+}
+function harnessAttachmentExtension(mediaType) {
+	return {
+		"application/pdf": "pdf",
+		"image/apng": "apng",
+		"image/avif": "avif",
+		"image/gif": "gif",
+		"image/jpeg": "jpg",
+		"image/png": "png",
+		"image/svg+xml": "svg",
+		"image/webp": "webp",
+		"text/plain": "txt"
+	}[mediaType.toLowerCase()] || "bin";
+}
+function assertHarnessAttachmentSize(byteLength, remainingBytes, type) {
+	if (byteLength > remainingBytes) throw new Error(`[vitehub] ${type} attachment is ${byteLength} bytes, which exceeds the remaining Harness attachment limit (${remainingBytes} bytes).`);
+}
+function harnessBase64ByteLength(value) {
+	let encodedCharacters = 0;
+	for (let index = 0; index < value.length; index++) {
+		const code = value.charCodeAt(index);
+		if (code === 61) break;
+		if (code >= 48 && code <= 57 || code >= 65 && code <= 90 || code >= 97 && code <= 122 || code === 43 || code === 45 || code === 47 || code === 95) encodedCharacters++;
+	}
+	return Math.floor(encodedCharacters * 6 / 8);
+}
+function harnessAttachmentStringBytes(value, mediaType, remainingBytes, type) {
+	const dataUrl = /^data:([^,]*?),(.*)$/is.exec(value);
+	if (dataUrl) {
+		const encoded = dataUrl[2];
+		if (dataUrl[1].split(";").some((parameter) => parameter.toLowerCase() === "base64")) {
+			assertHarnessAttachmentSize(harnessBase64ByteLength(encoded), remainingBytes, type);
+			return new Uint8Array(Buffer.from(encoded, "base64"));
+		}
+		if (encoded.length > remainingBytes * 3) assertHarnessAttachmentSize(Math.ceil(encoded.length / 3), remainingBytes, type);
+		const bytes = new TextEncoder().encode(encoded);
+		let readIndex = 0;
+		let writeIndex = 0;
+		while (readIndex < bytes.length) if (bytes[readIndex] === 37) {
+			const encodedByte = String.fromCharCode(bytes[readIndex + 1], bytes[readIndex + 2]);
+			if (!/^[\da-f]{2}$/i.test(encodedByte)) throw new URIError("URI malformed");
+			bytes[writeIndex++] = Number.parseInt(encodedByte, 16);
+			readIndex += 3;
+		} else bytes[writeIndex++] = bytes[readIndex++];
+		assertHarnessAttachmentSize(writeIndex, remainingBytes, type);
+		return bytes.slice(0, writeIndex);
+	}
+	const normalizedMediaType = mediaType.toLowerCase();
+	if (normalizedMediaType.startsWith("text/") || normalizedMediaType === "application/json" || normalizedMediaType.endsWith("+json") || normalizedMediaType.endsWith("+xml")) {
+		assertHarnessAttachmentSize(Buffer.byteLength(value), remainingBytes, type);
+		return new TextEncoder().encode(value);
+	}
+	assertHarnessAttachmentSize(harnessBase64ByteLength(value), remainingBytes, type);
+	return new Uint8Array(Buffer.from(value, "base64"));
+}
+async function harnessAttachmentBytes(data, mediaType, remainingBytes, type) {
+	if (data instanceof Blob) {
+		assertHarnessAttachmentSize(data.size, remainingBytes, type);
+		return new Uint8Array(await data.arrayBuffer());
+	}
+	if (data instanceof ArrayBuffer) {
+		assertHarnessAttachmentSize(data.byteLength, remainingBytes, type);
+		return new Uint8Array(data);
+	}
+	if (data instanceof Uint8Array) {
+		assertHarnessAttachmentSize(data.byteLength, remainingBytes, type);
+		return data;
+	}
+	return harnessAttachmentStringBytes(data, mediaType, remainingBytes, type);
+}
+async function resolveHarnessAttachmentData(part, abortSignal) {
+	if (typeof part.fetchData !== "function") return isAttachmentData(part.data) ? part.data : void 0;
+	if (abortSignal?.aborted) throw abortSignalError(abortSignal, "[vitehub] Harness attachment resolution aborted.");
+	const fetchedPromise = Promise.resolve(part.fetchData());
+	const fetched = abortSignal ? await new Promise((resolve, reject) => {
+		const onAbort = () => {
+			abortSignal.removeEventListener("abort", onAbort);
+			reject(abortSignalError(abortSignal, "[vitehub] Harness attachment resolution aborted."));
+		};
+		if (abortSignal.aborted) return onAbort();
+		abortSignal.addEventListener("abort", onAbort, { once: true });
+		fetchedPromise.then(resolve, reject).finally(() => abortSignal.removeEventListener("abort", onAbort));
+	}) : await fetchedPromise;
+	return isAttachmentData(fetched) ? fetched : void 0;
+}
+async function removeHarnessAttachmentDirectory(session, directory) {
+	const removeDirectory = session[harnessRemoveDirectory];
+	if (!removeDirectory) throw new Error("[vitehub] Harness attachment materialization requires sandbox directory removal support.");
+	await removeDirectory.call(session, directory);
+}
+async function materializeHarnessChatMessages(messages, prepared) {
+	if (!messages.some((message) => message.role === "user" && message.parts.some(isAttachmentPart))) return { messages };
+	if (!prepared.session[harnessRemoveDirectory]) throw new Error("[vitehub] Harness attachment materialization requires sandbox directory removal support.");
+	let remainingBytes = harnessAttachmentMaxBytes;
+	const directory = joinHarnessSessionPath(prepared.sessionWorkDir, `${harnessAttachmentDirectory}/${globalThis.crypto.randomUUID()}`);
+	try {
+		const materialized = [];
+		for (const [messageIndex, message] of messages.entries()) {
+			if (message.role !== "user") {
+				materialized.push(message);
+				continue;
+			}
+			const parts = [];
+			for (const [partIndex, part] of message.parts.entries()) {
+				if (!isAttachmentPart(part)) {
+					parts.push(part);
+					continue;
+				}
+				const data = await resolveHarnessAttachmentData(part, prepared.abortSignal);
+				if (!data) {
+					if (part.url) throw new Error(`[vitehub] Harness ${part.type} attachment requires data or fetchData(); URL-only attachments must be resolved by the Channel adapter.`);
+					throw new TypeError(`[vitehub] Harness ${part.type} attachment fetchData() did not return supported attachment data.`);
+				}
+				const bytes = await harnessAttachmentBytes(data, part.mediaType, remainingBytes, part.type);
+				assertHarnessAttachmentSize(bytes.byteLength, remainingBytes, part.type);
+				remainingBytes -= bytes.byteLength;
+				const path = `${directory}/message-${messageIndex + 1}-attachment-${partIndex + 1}.${harnessAttachmentExtension(part.mediaType)}`;
+				await prepared.session.writeBinaryFile({
+					abortSignal: prepared.abortSignal,
+					content: bytes,
+					path
+				});
+				if (part.type === "image") {
+					const { fetchData: _fetchData, url: _url, ...image } = part;
+					parts.push({
+						...image,
+						data: path
+					});
+				} else parts.push({
+					id: part.id,
+					text: `Attachment ${JSON.stringify(part.name || part.type)} (${JSON.stringify(part.mediaType)}) is available at ${path}.`,
+					type: "text"
+				});
+			}
+			materialized.push({
+				...message,
+				parts
+			});
+		}
+		return {
+			directory,
+			messages: materialized
+		};
+	} catch (error) {
+		try {
+			await removeHarnessAttachmentDirectory(prepared.session, directory);
+		} catch (cleanupError) {
+			throw new AggregateError([error, cleanupError], "[vitehub] Harness attachment preparation and cleanup failed.");
+		}
+		throw error;
+	}
+}
+async function prepareHarnessChatPrompt(context, resumesSession, prepared) {
+	const selectedMessages = resumesSession ? latestHarnessUserMessage(context.messages) : context.messages;
+	if (selectedMessages.some((message) => message.role === "user" && message.parts.some(isAttachmentPart)) && !prepared) throw new Error("[vitehub] Harness attachment materialization requires a prepared sandbox session.");
+	const materialized = prepared ? await materializeHarnessChatMessages(selectedMessages, prepared) : { messages: selectedMessages };
+	try {
+		return {
+			...materialized.directory ? { directory: materialized.directory } : {},
+			prompt: projectHarnessChatMessages(materialized.messages)
+		};
+	} catch (error) {
+		if (materialized.directory && prepared) try {
+			await removeHarnessAttachmentDirectory(prepared.session, materialized.directory);
+		} catch (cleanupError) {
+			throw new AggregateError([error, cleanupError], "[vitehub] Harness chat projection and attachment cleanup failed.");
+		}
+		throw error;
+	}
+}
+async function toHarnessCallInput(context, resumesSession = false, chatPrompt) {
+	const base = {
+		abortSignal: context.input.abortSignal,
+		timeout: context.input.timeout,
+		..."options" in context.input ? { options: context.input.options } : {}
+	};
+	if (context.messages.length) {
+		if (context.context.has("chat")) return {
+			...base,
+			messages: [chatPrompt || projectHarnessChatMessages(resumesSession ? latestHarnessUserMessage(context.messages) : context.messages)]
+		};
+		return {
+			...base,
+			messages: toAiSdkModelMessages(harnessSerializableMessages(context.messages))
+		};
+	}
+	if (context.prompt) return {
+		...base,
+		prompt: context.prompt
+	};
+	return {
+		...base,
+		messages: []
+	};
+}
+function toHarnessTools(context) {
+	if (!hasEntries(context.tools)) return;
+	return withAgentToolStepReporting(withJsonCompatibleToolOutputs(applyAgentToolPolicies(context.tools) || {}), context.toolStepReporter);
+}
+function harnessWriteBackIgnorePaths(context, instructions) {
+	const colocatedSkills = context.context.get(colocatedAgentSkillsContextKey);
+	const colocatedSkillDirectories = new Set(Object.values(colocatedSkills || {}).flatMap((source) => {
+		if (!source || typeof source !== "object" || !("workspacePath" in source) || typeof source.workspacePath !== "string") return [];
+		const [root, skill] = source.workspacePath.split("/");
+		return root === "skills" && skill ? [`${root}/${skill}`] : [];
+	}));
+	return [
+		...hasEntries(context.tools) ? harnessGeneratedFiles : [],
+		...instructions ? harnessInstructionFiles : [],
+		...colocatedSkillDirectories
+	];
+}
+function toRunCallbackContext(context) {
+	const { runtimeConfig: _runtimeConfig, ...runtime } = context.runtime;
+	return {
+		...runtime,
+		actor: context.actor,
+		context: context.context,
+		input: context.input,
+		invoker: context.invoker,
+		run: context.runtime.run
+	};
+}
+async function resolveHarnessSessionKey(sessionKey, context) {
+	if (typeof sessionKey === "function") return await sessionKey(toRunCallbackContext(context)) || void 0;
+	return sessionKey || void 0;
+}
+async function resolveHarnessDriverInstructions(instructions, context) {
+	const resolved = typeof instructions === "function" ? await instructions(toRunCallbackContext(context)) : instructions;
+	if (resolved !== void 0 && typeof resolved !== "string") throw new TypeError("[vitehub] defineAgent({ driver.instructions }) must resolve to a string.");
+	return [
+		resolved ? await composeHarnessInstructions(resolved, context) : void 0,
+		resolveMessageChannelInstructions(context.context, context),
+		agentOutputInstructions(context.output)
+	].filter(Boolean).join("\n\n") || void 0;
+}
+async function resolveHarnessWorkDir(workDir, context) {
+	const resolved = typeof workDir === "function" ? await workDir(toRunCallbackContext(context)) : workDir;
+	if (resolved !== void 0 && (typeof resolved !== "string" || !resolved.trim() || resolved.startsWith("/") || resolved.includes("\\") || resolved.split("/").includes(".."))) throw new TypeError("[vitehub] defineAgent({ driver.workDir }) must resolve to a non-empty relative POSIX path inside the sandbox default working directory.");
+	return resolved;
+}
+function isProcesslessHarnessRuntime(context) {
+	return context.runtime.runtime === "cloudflare-agents" || context.runtime.runtime === "deno";
+}
+function assertHarnessSandboxRuntime(sandbox, context) {
+	if (isProcesslessHarnessRuntime(context) && sandbox.providerId === "local") throw new Error(`[vitehub] Harness Agent Drivers on ${context.runtime.runtime} require a process-capable driver.sandbox provider.`);
+}
+const defaultHarnessEnvKeys = [
+	"ComSpec",
+	"HOME",
+	"Path",
+	"PATH",
+	"PATHEXT",
+	"SHELL",
+	"SystemRoot",
+	"TEMP",
+	"TMP",
+	"TMPDIR",
+	"USER"
+];
+function defaultHarnessEnv() {
+	return Object.fromEntries(defaultHarnessEnvKeys.map((key) => [key, process.env[key]]).filter((entry) => entry[1] !== void 0));
+}
+async function createDefaultHarnessSandbox(context) {
+	if (isProcesslessHarnessRuntime(context)) throw new Error(`[vitehub] Harness Agent Drivers on ${context.runtime.runtime} require a process-capable driver.sandbox provider.`);
+	const { createLocalHarnessSandbox } = await import("./harness/local-sandbox.js");
+	return createLocalHarnessSandbox({ env: defaultHarnessEnv() });
+}
+function hasHarnessInstructionDocument(context) {
+	return normalizeWorkspaceSourcesMetadata(context.workspaceDefinition?.sources).some((source) => source.key === "__vitehubAgentInstructions" && source.mountPath === "" && source.probeKeys?.includes("AGENTS.md"));
+}
+async function resolveHarnessInstructions(context) {
+	if (!context.workspace) return;
+	if (!hasHarnessInstructionDocument(context)) return;
+	if (!await context.workspace.fs.exists("AGENTS.md")) return;
+	const document = await resolveColocatedAgentInstructionDocument(await context.workspace.fs.readFile("AGENTS.md"), context.workspaceDefinition?.sourceRootDir);
+	return document ? composeHarnessInstructions(document, context) : void 0;
+}
+async function writeHarnessInstructionFiles(session, sessionWorkDir, abortSignal, instructions) {
+	if (!instructions) return;
+	const bytes = new TextEncoder().encode(`${instructions}\n`);
+	await Promise.all(harnessInstructionFiles.map(async (file) => {
+		await session.writeBinaryFile({
+			abortSignal,
+			content: bytes,
+			path: `${sessionWorkDir.replace(/\/+$/, "")}/${file}`
+		});
+	}));
+}
+async function resolveHarness(harness, context) {
+	if (typeof harness === "function") return await harness(toRunCallbackContext(context));
+	return harness;
+}
+function resolveHarnessGlobalSkillsDirectory(harness, context) {
+	const directory = harness[harnessGlobalSkillsDirectory];
+	return typeof directory === "function" ? directory(context) : directory;
+}
+async function resolveHarnessSandboxProvider(sandbox, context) {
+	const provider = typeof sandbox === "function" ? await sandbox(toRunCallbackContext(context)) : sandbox;
+	if (provider !== void 0 && (!provider || typeof provider !== "object")) throw new TypeError("[vitehub] defineAgent({ driver.sandbox }) must return a harness sandbox provider object.");
+	return provider;
+}
+async function createHarnessAgent(options, context, prepareWorkspaceSession) {
+	assertSupportedHarnessDriverContributions(context);
+	const { HarnessAgent } = await import(
+		/* @vite-ignore */
+		harnessAgentPackage
+);
+	const harness = await resolveHarness(options.harness, context);
+	const globalSkillsDirectory = resolveHarnessGlobalSkillsDirectory(harness, context);
+	if (context.globalSkills?.length && !isHarnessRelativeDirectory(globalSkillsDirectory)) throw new Error("[vitehub] This Harness Agent Driver does not support skills({ scope: \"global\" }).");
+	const globalSkillsWorkspace = await resolveHarnessGlobalSkills(context);
+	const driverSandbox = context.harnessSandboxProvider === void 0 ? await resolveHarnessSandboxProvider(options.sandbox, context) : void 0;
+	const defaultSandbox = context.harnessSandboxProvider === void 0 && driverSandbox === void 0;
+	const baseSandbox = context.harnessSandboxProvider ?? driverSandbox ?? await createDefaultHarnessSandbox(context);
+	assertHarnessSandboxRuntime(baseSandbox, context);
+	const adaptSandbox = harness[harnessSandboxAdapter];
+	let sandbox = baseSandbox;
+	if (typeof adaptSandbox === "function") sandbox = adaptSandbox(baseSandbox, {
+		box: Boolean(context.box),
+		defaultSandbox
+	});
+	else if (defaultSandbox && harness.harnessId === "codex") {
+		const { adaptCodexHarnessSandbox } = await import("./codex-sandbox-GIB8_EHo.js");
+		sandbox = adaptCodexHarnessSandbox(baseSandbox, {
+			defaultSandbox: true,
+			isolateHome: false
+		});
+	} else if (defaultSandbox) {
+		const bootstrap = await harness.getBootstrap?.();
+		if (bootstrap?.bootstrapDir) {
+			const { adaptLocalHarnessSandbox } = await import("./local-sandbox-BBsKybfM.js");
+			sandbox = adaptLocalHarnessSandbox(baseSandbox, bootstrap.bootstrapDir);
+		}
+	}
+	const instructions = await resolveHarnessDriverInstructions(options.instructions, context);
+	const workDir = await resolveHarnessWorkDir(options.workDir, context) ?? context.harnessWorkDir;
+	const tools = toHarnessTools(context);
+	return {
+		agent: new HarnessAgent({
+			harness,
+			...instructions ? { instructions } : {},
+			sandboxConfig: {
+				...workDir !== void 0 ? { workDir } : {},
+				onSession: async ({ abortSignal, session, sessionWorkDir }) => {
+					setActiveHarnessWorkspaceFiles(context.context, activeHarnessWorkspaceFiles(session, sessionWorkDir, abortSignal));
+					await prepareWorkspaceSession(session, sessionWorkDir, abortSignal, globalSkillsDirectory, globalSkillsWorkspace, harness[harnessSessionPrepare]);
+				}
+			},
+			permissionMode: defaultHarnessPermissionMode(harness),
+			sandbox,
+			...tools ? { tools } : {}
+		}),
+		instructions,
+		workDir
+	};
+}
+async function resolveHarnessGlobalSkills(context) {
+	const skills = context.globalSkills || [];
+	if (!skills.length) return;
+	const { useWorkspace } = await import("@vite-hub/workspace");
+	const { resolveWorkspaceSources } = await import("@vite-hub/workspace/runtime");
+	const workspace = useWorkspace("__vitehub_global_skills", {
+		definition: await resolveWorkspaceSources({
+			name: "__vitehub_global_skills",
+			sources: Object.fromEntries(skills.map((skill) => [skill.sourceKey, skill.source])),
+			store: { provider: "memory" }
+		}, { invocation: {
+			context: agentInvocationSourceContext(context.context),
+			run: context.runtime.run
+		} }),
+		mode: "read"
+	});
+	for (const skill of skills) if (!await workspace.fs.exists(`${skill.path}/SKILL.md`)) throw new Error(`[vitehub] Global Skill source must contain ${skill.path}/SKILL.md.`);
+	return {
+		paths: skills.map((skill) => skill.path),
+		workspace
+	};
+}
+async function resolveHarnessColocatedSkills(context) {
+	const sources = context.context.get(colocatedAgentSkillsContextKey);
+	if (!sources || !Object.keys(sources).length) return;
+	const workspaceOptions = {
+		definition: {
+			name: "__vitehub_agent_skills",
+			sources,
+			store: { provider: "memory" }
+		},
+		mode: "read"
+	};
+	const { useWorkspace } = await import("@vite-hub/workspace");
+	return useWorkspace("__vitehub_agent_skills", workspaceOptions);
+}
+async function prepareHarnessColocatedSkills(workspace, session, destination, target, abortSignal, refresh = false, workingDirectory = destination) {
+	if (!workspace) return false;
+	const { prepareHarnessWorkspaceSession } = await import("@vite-hub/workspace");
+	const prepared = await prepareHarnessWorkspaceSession(workspace, {
+		abortSignal,
+		paths: ["skills"],
+		session,
+		sessionWorkDir: `${destination.replace(/\/+$/, "")}/.vitehub-agent-skills`
+	});
+	try {
+		const refreshCommand = refresh ? `manifest=${target}/.vitehub-colocated && if [ -f "$manifest" ]; then while IFS= read -r managed || [ -n "$managed" ]; do case "$managed" in ''|*/*|.. ) exit 1 ;; esac; rm -rf -- ${target}/"$managed"; done < "$manifest"; fi && : > "$manifest" && find .vitehub-agent-skills/skills -mindepth 1 -maxdepth 1 -type d -exec basename {} \\; >> "$manifest" && ` : "";
+		const result = await session.run({
+			abortSignal,
+			command: `find .vitehub-agent-skills/skills -type f -path "*/scripts/*" -exec chmod +x {} + && mkdir -p ${target} && ${refreshCommand}cp -Rn .vitehub-agent-skills/skills/. ${target} && rm -rf .vitehub-agent-skills`,
+			workingDirectory
+		});
+		if (result.exitCode !== 0) throw new Error(result.stderr || "sandbox command failed");
+	} catch (error) {
+		await prepared.close(error);
+		throw new Error(`[vitehub] Failed to install colocated Agent Skills: ${error instanceof Error ? error.message : String(error)}`, { cause: error });
+	}
+	await prepared.close();
+	return true;
+}
+async function prepareHarnessGlobalSkills(resolved, session, directory, abortSignal, workingDirectory) {
+	if (!isHarnessRelativeDirectory(directory)) {
+		if (!resolved) return;
+		throw new Error("[vitehub] This Harness Agent Driver does not support skills({ scope: \"global\" }).");
+	}
+	const quotedDirectory = `'${directory.replace(/'/g, "'\\''")}'`;
+	const quotedManagedManifest = `'${`${directory}.vitehub-managed`.replace(/'/g, "'\\''")}'`;
+	const ensure = await session.run({
+		abortSignal,
+		...workingDirectory ? { workingDirectory } : {},
+		command: `mkdir -p -- ${quotedDirectory} && if [ -f ${quotedManagedManifest} ]; then while IFS= read -r encoded || [ -n "$encoded" ]; do managed=$(printf '%s' "$encoded" | base64 -d) || exit 1; case "$managed" in ''|/*|..|../*|*/..|*/../*) printf '%s\\n' 'Invalid ViteHub-managed Skill path.' >&2; exit 1 ;; esac; rm -rf -- ${quotedDirectory}/"$managed"; done < ${quotedManagedManifest}; fi && rm -f -- ${quotedManagedManifest}`
+	});
+	if (ensure.exitCode !== 0) throw new Error(`[vitehub] Failed to prepare global Skill directory: ${ensure.stderr || "sandbox command failed"}`);
+	if (!resolved) return;
+	const skillDirectories = resolved.paths.map((path) => `'${`${directory}/${path}`.replace(/'/g, "'\\''")}'`).join(" ");
+	const { prepareHarnessWorkspaceSession } = await import("@vite-hub/workspace");
+	const stagingDirectory = `${directory}.vitehub-global-skills-${globalThis.crypto.randomUUID()}`;
+	const quotedStagingDirectory = `'${stagingDirectory.replace(/'/g, "'\\''")}'`;
+	const prepared = await prepareHarnessWorkspaceSession(resolved.workspace, {
+		abortSignal,
+		paths: resolved.paths,
+		session,
+		sessionWorkDir: workingDirectory ? posix.join(workingDirectory, stagingDirectory) : stagingDirectory
+	});
+	const install = await session.run({
+		abortSignal,
+		...workingDirectory ? { workingDirectory } : {},
+		command: `cp -R ${quotedStagingDirectory}/. ${quotedDirectory} && rm -rf -- ${quotedStagingDirectory} && find ${skillDirectories} -type f -path "*/scripts/*" -exec chmod +x {} + && printf '%s\\n' ${resolved.paths.map((path) => `'${Buffer.from(path).toString("base64")}'`).join(" ")} > ${quotedManagedManifest}`
+	});
+	if (install.exitCode !== 0) {
+		const error = /* @__PURE__ */ new Error(`[vitehub] Failed to refresh global Skills: ${install.stderr || "sandbox command failed"}`);
+		try {
+			await session.run({
+				abortSignal,
+				...workingDirectory ? { workingDirectory } : {},
+				command: `rm -rf -- ${quotedStagingDirectory}`
+			});
+		} catch {}
+		await prepared.close(error);
+		throw error;
+	}
+	return prepared;
+}
+function hasDetach(session) {
+	return typeof session.detach === "function";
+}
+async function destroySession(session) {
+	await session.destroy();
+}
+function withCleanup(iterable, cleanup, abortSignal) {
+	return (async function* () {
+		const iterator = iterable[Symbol.asyncIterator]();
+		let caught;
+		try {
+			for (;;) {
+				const result = await nextWithAbort(iterator.next(), abortSignal, "[vitehub] Harness Agent Driver stream aborted.");
+				if (result.done) break;
+				yield result.value;
+			}
+		} catch (error) {
+			caught = error;
+			iterator.return?.().catch(() => {});
+			throw error;
+		} finally {
+			await cleanup(caught);
+		}
+	})();
+}
+function withCleanupStream(stream, cleanup) {
+	const reader = stream.getReader();
+	let cleaned = false;
+	const cleanupOnce = async (error) => {
+		if (cleaned) return;
+		cleaned = true;
+		await cleanup(error);
+	};
+	return new ReadableStream({
+		async cancel(reason) {
+			try {
+				await reader.cancel(reason);
+			} finally {
+				await cleanupOnce(reason);
+			}
+		},
+		async pull(controller) {
+			try {
+				const result = await reader.read();
+				if (result.done) {
+					await cleanupOnce();
+					controller.close();
+					return;
+				}
+				controller.enqueue(result.value);
+			} catch (error) {
+				await cleanupOnce(error);
+				controller.error(error);
+			}
+		}
+	});
+}
+function createSharedCleanupIterableWrapper(cleanup, abortSignal) {
+	let activeIterators = 0;
+	return function wrap(iterable) {
+		return { [Symbol.asyncIterator]() {
+			const iterator = iterable[Symbol.asyncIterator]();
+			activeIterators++;
+			let done = false;
+			const finish = async (error) => {
+				if (done) return;
+				done = true;
+				activeIterators--;
+				if (error || activeIterators === 0) await cleanup(error);
+			};
+			return {
+				async next() {
+					try {
+						const result = await nextWithAbort(iterator.next(), abortSignal, "[vitehub] Harness Agent Driver stream aborted.");
+						if (result.done) await finish();
+						return result;
+					} catch (error) {
+						iterator.return?.().catch(() => {});
+						await finish(error);
+						throw error;
+					}
+				},
+				async return(value) {
+					try {
+						return await iterator.return?.(value) ?? {
+							done: true,
+							value
+						};
+					} finally {
+						await finish();
+					}
+				}
+			};
+		} };
+	};
+}
+async function withSessionCleanup(result, cleanup, abortSignal, waitUntil) {
+	let cleanupTask;
+	let markCleanupStarted;
+	const cleanupStarted = new Promise((resolve) => {
+		markCleanupStarted = resolve;
+	});
+	let onAbort;
+	const cleanupOnce = (error) => {
+		if (!cleanupTask) {
+			cleanupTask = cleanup(error).finally(() => {
+				if (onAbort) abortSignal?.removeEventListener("abort", onAbort);
+			});
+			markCleanupStarted();
+		}
+		return cleanupTask;
+	};
+	const observeAbort = () => {
+		if (!abortSignal) return;
+		onAbort = () => {
+			waitUntil(cleanupOnce(abortSignal.reason));
+		};
+		if (abortSignal.aborted) onAbort();
+		else abortSignal.addEventListener("abort", onAbort, { once: true });
+	};
+	if (isAsyncIterable(result)) {
+		observeAbort();
+		return withCleanup(result, cleanupOnce, abortSignal);
+	}
+	if (!result || typeof result !== "object") {
+		await cleanupOnce();
+		return result;
+	}
+	const entries = [
+		"stream",
+		"fullStream",
+		"textStream"
+	].map((key) => [key, result[key]]).filter((entry) => isAsyncIterable(entry[1]));
+	if (!entries.length) {
+		await cleanupOnce();
+		return result;
+	}
+	const clone = Object.create(Object.getPrototypeOf(result));
+	Object.defineProperties(clone, Object.getOwnPropertyDescriptors(result));
+	const wrappedIterables = /* @__PURE__ */ new Map();
+	const wrapSharedCleanupIterable = createSharedCleanupIterableWrapper(cleanupOnce, abortSignal);
+	for (const [key, iterable] of entries) {
+		let wrapped = wrappedIterables.get(iterable);
+		if (!wrapped) {
+			wrapped = wrapSharedCleanupIterable(iterable);
+			wrappedIterables.set(iterable, wrapped);
+		}
+		Object.defineProperty(clone, key, {
+			configurable: true,
+			enumerable: true,
+			value: wrapped
+		});
+	}
+	const toUIMessageStream = result.toUIMessageStream;
+	if (typeof toUIMessageStream === "function") Object.defineProperty(clone, "toUIMessageStream", {
+		configurable: true,
+		enumerable: false,
+		value: (...args) => {
+			try {
+				const stream = toUIMessageStream.apply(result, args);
+				return typeof stream === "object" && stream !== null && typeof stream.getReader === "function" ? withCleanupStream(stream, cleanupOnce) : stream;
+			} catch (error) {
+				cleanupOnce(error);
+				throw error;
+			}
+		}
+	});
+	Object.defineProperty(clone, Symbol.asyncIterator, {
+		configurable: true,
+		value: () => streamAgentOutputToEvents(clone)[Symbol.asyncIterator]()
+	});
+	observeAbort();
+	const responseMessages = result.responseMessages;
+	if (typeof responseMessages === "object" && responseMessages !== null || typeof responseMessages === "function") {
+		if (typeof responseMessages.then === "function") {
+			const producerCleanup = Promise.resolve(responseMessages).then(() => cleanupOnce(), (error) => cleanupOnce(error));
+			waitUntil(Promise.race([producerCleanup, cleanupStarted.then(() => cleanupTask)]));
+		}
+	}
+	return clone;
+}
+function getResumeStates(options) {
+	const existing = harnessResumeStates.get(options);
+	if (existing) return existing;
+	const states = /* @__PURE__ */ new Map();
+	harnessResumeStates.set(options, states);
+	return states;
+}
+async function resolveHarnessProviderSessionId(sessionId, resumeKey, identity) {
+	if (identity.box === void 0 && identity.instructions === void 0 && identity.workDir === void 0) return sessionId;
+	const digest = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(resumeKey));
+	return `vitehub-${Array.from(new Uint8Array(digest), (byte) => byte.toString(16).padStart(2, "0")).join("")}`;
+}
+function createHarnessAgentAdapter(options) {
+	const resumeStates = getResumeStates(options);
+	const usageMetadata = createHarnessUsageMetadata(options.credentials);
+	async function createSession(agent, context, getWorkspaceSession, agentIdentity) {
+		const identity = {
+			...agentIdentity,
+			...context.box ? { box: {
+				identity: context.box.plan.identity,
+				runtime: context.box.plan.runtime,
+				workspace: context.box.plan.workspace.path
+			} } : {}
+		};
+		const sessionId = await resolveHarnessSessionKey(options.sessionKey, context);
+		const resumeKey = sessionId ? JSON.stringify([
+			sessionId,
+			identity.instructions,
+			identity.workDir,
+			identity.box
+		]) : void 0;
+		const providerSessionId = sessionId && resumeKey ? await resolveHarnessProviderSessionId(sessionId, resumeKey, identity) : void 0;
+		const resumesSession = resumeKey ? resumeStates.has(resumeKey) : false;
+		const resumeFrom = resumeKey ? resumeStates.get(resumeKey) : void 0;
+		const sessionOptions = {
+			...context.input.abortSignal ? { abortSignal: context.input.abortSignal } : {},
+			...providerSessionId ? { sessionId: providerSessionId } : {},
+			...resumeFrom !== void 0 ? { resumeFrom } : {},
+			...typeof context.input.timeout === "number" ? { timeout: context.input.timeout } : {}
+		};
+		const session = await agent.createSession(Object.keys(sessionOptions).length ? sessionOptions : void 0);
+		const cleanup = async (error) => {
+			let closeError = error;
+			try {
+				await getWorkspaceSession()?.close(error);
+			} catch (nextError) {
+				closeError = nextError;
+			} finally {
+				setActiveHarnessWorkspaceFiles(context.context, void 0);
+			}
+			if (!resumeKey || closeError) {
+				if (resumeKey) resumeStates.delete(resumeKey);
+				await destroySession(session);
+				if (closeError !== error) throw closeError;
+				return;
+			}
+			if (!hasDetach(session)) {
+				resumeStates.delete(resumeKey);
+				await destroySession(session);
+				return;
+			}
+			resumeStates.set(resumeKey, await session.detach());
+		};
+		if (context.input.abortSignal?.aborted) {
+			const error = context.input.abortSignal.reason || /* @__PURE__ */ new Error("[vitehub] Harness Agent Driver session aborted.");
+			await cleanup(error);
+			throw error;
+		}
+		return {
+			cleanup,
+			session,
+			resumesSession
+		};
+	}
+	async function createAgentAndSession(context) {
+		let workspaceSession;
+		let globalSkillsSession;
+		let preparedSandbox;
+		let attachmentDirectory;
+		const colocatedSkillsWorkspace = await resolveHarnessColocatedSkills(context);
+		const resolved = await createHarnessAgent(options, context, async (session, sessionWorkDir, abortSignal, globalSkillsDirectory, globalSkillsWorkspace, sessionPrepare) => {
+			preparedSandbox = {
+				abortSignal,
+				session,
+				sessionWorkDir
+			};
+			let globalSkillsWorkingDirectory;
+			if (context.box && isHarnessRelativeDirectory(globalSkillsDirectory)) {
+				const home = await session.run({
+					abortSignal,
+					command: `printf '%s' "$HOME"`
+				});
+				globalSkillsWorkingDirectory = home.stdout;
+				if (home.exitCode !== 0 || !globalSkillsWorkingDirectory || !posix.isAbsolute(globalSkillsWorkingDirectory)) throw new Error(`[vitehub] Failed to resolve the Box Home directory: ${home.stderr || "sandbox command failed"}`);
+			}
+			const harnessInstructions = context.workspace ? await resolveHarnessInstructions(context) : void 0;
+			try {
+				if (context.workspace) {
+					const { prepareHarnessWorkspaceSession } = await import("@vite-hub/workspace");
+					const commitDefinition = context.workspaceDefinition && context.workspaceAutoCommit !== void 0 ? workspaceDefinitionWithAutoCommitRules(context.workspaceDefinition, context.workspaceAutoCommit) : context.workspaceDefinition;
+					workspaceSession = await prepareHarnessWorkspaceSession(context.workspaceMaterializationSource || context.workspace, {
+						abortSignal,
+						...hasWorkspaceCommitRules(commitDefinition) ? { definition: commitDefinition } : {},
+						ignoreWriteBackPaths: harnessWriteBackIgnorePaths(context, harnessInstructions),
+						onWriteBack: (diff) => setHarnessWorkspaceDiff(context.context, diff),
+						paths: selectedWorkspaceScopePaths(context, commitDefinition),
+						session,
+						sessionWorkDir
+					});
+					await writeHarnessInstructionFiles(session, sessionWorkDir, abortSignal, harnessInstructions);
+					if (harnessInstructions) await workspaceSession.refreshGitBaseline?.();
+				}
+				globalSkillsSession = await prepareHarnessGlobalSkills(globalSkillsWorkspace, session, globalSkillsDirectory, abortSignal, globalSkillsWorkingDirectory);
+				const installedWorkspaceSkills = await prepareHarnessColocatedSkills(colocatedSkillsWorkspace, session, sessionWorkDir, "skills", abortSignal);
+				if (isHarnessRelativeDirectory(globalSkillsDirectory)) await prepareHarnessColocatedSkills(colocatedSkillsWorkspace, session, globalSkillsDirectory, ".", abortSignal, true, globalSkillsWorkingDirectory ? posix.join(globalSkillsWorkingDirectory, globalSkillsDirectory) : globalSkillsDirectory);
+				if (installedWorkspaceSkills) await workspaceSession?.refreshGitBaseline?.();
+				if (typeof sessionPrepare === "function") await sessionPrepare(session);
+			} catch (error) {
+				try {
+					await globalSkillsSession?.close(error);
+				} finally {
+					await workspaceSession?.close(error);
+				}
+				workspaceSession = void 0;
+				globalSkillsSession = void 0;
+				throw error;
+			}
+		});
+		const preparationSession = { async close(error) {
+			let closeError = error;
+			if (attachmentDirectory && preparedSandbox) {
+				try {
+					await removeHarnessAttachmentDirectory(preparedSandbox.session, attachmentDirectory);
+				} catch (nextError) {
+					closeError = nextError;
+				}
+				attachmentDirectory = void 0;
+			}
+			try {
+				await globalSkillsSession?.close(closeError);
+			} catch (nextError) {
+				closeError = nextError;
+			} finally {
+				try {
+					await workspaceSession?.close(closeError);
+				} catch (nextError) {
+					closeError = nextError;
+				}
+			}
+			if (closeError !== error) throw closeError;
+		} };
+		const created = await createSession(resolved.agent, context, () => preparationSession, resolved);
+		let chatPrompt;
+		try {
+			if (context.context.has("chat") && context.messages.length) {
+				const prepared = await prepareHarnessChatPrompt(context, created.resumesSession, preparedSandbox);
+				attachmentDirectory = prepared.directory;
+				chatPrompt = prepared.prompt;
+			}
+		} catch (error) {
+			await created.cleanup(error);
+			throw error;
+		}
+		return {
+			agent: resolved.agent,
+			chatPrompt,
+			...created
+		};
+	}
+	return markMessageChannelInstructionConsumer({
+		async generate(context) {
+			const { agent, chatPrompt, cleanup, session, resumesSession } = await createAgentAndSession(context);
+			try {
+				const result = defineAgentUsageMetadata(await agent.generate({
+					...await toHarnessCallInput(context, resumesSession, chatPrompt),
+					session
+				}), usageMetadata);
+				await cleanup();
+				return result;
+			} catch (error) {
+				await cleanup(error);
+				throw error;
+			}
+		},
+		name: "ai-sdk-harness",
+		async stream(context) {
+			const { agent, chatPrompt, cleanup, session, resumesSession } = await createAgentAndSession(context);
+			try {
+				return await withSessionCleanup(defineAgentUsageMetadata(await agent.stream({
+					...await toHarnessCallInput(context, resumesSession, chatPrompt),
+					session
+				}), usageMetadata), cleanup, context.input.abortSignal, context.runtime.waitUntil);
+			} catch (error) {
+				await cleanup(error);
+				throw error;
+			}
+		}
+	});
+}
+//#endregion
+export { createHarnessAgentAdapter };
diff --git a/dist/index-B69oMVI0.d.ts b/dist/index-B69oMVI0.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..b6f0f9b2f8622150ca5a44da0b0710f4291f935d
--- /dev/null
+++ b/dist/index-B69oMVI0.d.ts
@@ -0,0 +1,264 @@
+import { $r as MaybePromise, Br as AgentWebhookRegistrationDefinition, Dn as AgentInvokerOptions, G as AgentChannelDeliveryEffectIntent, Hn as AgentModelResolver, Hr as AgentWorkflowRuntimeBinding, Lt as AgentDriver, N as AgentCapabilityDefinition, Nt as AgentDefinition, On as AgentInvokerProfile, Sn as AgentInvocationContextValues, V as AgentCapabilityTypeContract, Zn as AgentRegistry, Zr as CustomAgentDriver, cn as AgentInput, dr as AgentRuntimeContext, ei as MaybeResolvable, gr as AgentSettings, h as AgentCallbackContext, hn as AgentInspectionMetadata, i as AgentAdapter, ii as ResolvedAgentRuntimeContext, ir as AgentRunInput, l as AgentAdapterMetadataContext, oi as ResolvedAgentTriggerDefinition, or as AgentRunMetadata, sr as AgentRunResult, ur as AgentRuntimeConfig, wr as AgentToolSet, y as AgentCapabilitiesResolver, zi as StreamEvent, zr as AgentWebhookInvocationOwnership } from "./types-CW2XmfE7.js";
+import { n as WorkspaceAgentDefinition, r as WorkspaceAgentOptions } from "./workspace-agent-u4sFNw76.js";
+import { ReadonlyWorkspaceFacade, WorkspaceDefinition, WorkspaceName } from "@vite-hub/workspace";
+import { GatewayProviderSettings } from "@ai-sdk/gateway";
+
+//#region src/agent-invocation.d.ts
+type AgentInvocationStatus = "pending" | "running" | "completed" | "failed" | "cancelled";
+interface AgentInvocationSnapshot<TOutput = unknown> {
+  error?: unknown;
+  id: string;
+  output?: TOutput;
+  status: AgentInvocationStatus;
+}
+type AgentInvocationInspection<TOutput = unknown> = {
+  invocation: AgentInvocationSnapshot<TOutput>;
+  outcome: "available";
+} | {
+  id: string;
+  outcome: "unavailable";
+};
+type AgentInvocationControlOutcome = "accepted" | "unsupported" | "unavailable" | "invalid-state";
+interface AgentInvocationControlResult<TOutput = unknown> {
+  id: string;
+  invocation?: AgentInvocationSnapshot<TOutput>;
+  outcome: AgentInvocationControlOutcome;
+}
+type AgentInvocationInputMode = "follow-up" | "steer";
+interface AgentInvocationInputSupport {
+  followUp: boolean;
+  steer: boolean;
+}
+interface AgentInvocationController<TOutput = unknown, CALL_OPTIONS = unknown> {
+  cancel: (reason?: unknown) => Promise<AgentInvocationControlResult<TOutput>>;
+  id: string;
+  inspect: () => Promise<AgentInvocationInspection<TOutput>>;
+  sendInput: (input: AgentRunInput<CALL_OPTIONS>, options: {
+    mode: AgentInvocationInputMode;
+  }) => Promise<AgentInvocationControlResult<TOutput>>;
+  support: AgentInvocationInputSupport;
+}
+//#endregion
+//#region src/trigger-runtime.d.ts
+interface ResolvedAgentTriggerInvocation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> {
+  input: AgentRunInput<CALL_OPTIONS>;
+  metadata?: Record<string, unknown>;
+  run?: AgentRunMetadata;
+  trigger: ResolvedAgentTriggerDefinition<TRuntimeConfig, unknown, CALL_OPTIONS>;
+  webhook?: AgentWebhookInvocationOwnership;
+}
+interface ResolvedAgentTriggerHandledInvocation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> {
+  response: Response;
+  trigger: ResolvedAgentTriggerDefinition<TRuntimeConfig, unknown, CALL_OPTIONS>;
+}
+type ResolvedAgentTriggerInvocationResult<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = ResolvedAgentTriggerInvocation<TRuntimeConfig, CALL_OPTIONS> | ResolvedAgentTriggerHandledInvocation<TRuntimeConfig, CALL_OPTIONS>;
+declare function isResolvedAgentTriggerHandledInvocation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown>(invocation: ResolvedAgentTriggerInvocationResult<TRuntimeConfig, CALL_OPTIONS>): invocation is ResolvedAgentTriggerHandledInvocation<TRuntimeConfig, CALL_OPTIONS>;
+interface AgentWebhookVerificationResult {
+  registration?: AgentWebhookRegistrationDefinition;
+  verified: boolean;
+}
+interface AgentWebhookVerificationOptions {
+  requireSecretHeader?: boolean;
+}
+declare function verifyAgentWebhookRequest<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig>(registrations: AgentWebhookRegistrationDefinition<TRuntimeConfig>[], request: Request, context?: AgentCallbackContext<TRuntimeConfig>, options?: AgentWebhookVerificationOptions): Promise<AgentWebhookVerificationResult>;
+//#endregion
+//#region src/invoker.d.ts
+declare const agentInvokerContextKey = "invoker";
+declare function defineAgentInvoker<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, const TProfile extends AgentInvokerProfile = AgentInvokerProfile>(options: AgentInvokerOptions<TRuntimeConfig, CALL_OPTIONS, TProfile>): AgentInvokerOptions<TRuntimeConfig, CALL_OPTIONS, TProfile>;
+//#endregion
+//#region src/gateway.d.ts
+type GatewaySecret = string | {
+  unseal: () => string;
+};
+interface GatewayModelSettings extends Omit<GatewayProviderSettings, "apiKey"> {
+  apiKey?: GatewaySecret;
+}
+type GatewayModelSettingsResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<GatewayModelSettings, AgentAdapterMetadataContext<TRuntimeConfig>>;
+declare function gateway<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig>(model: string, settings?: GatewayModelSettingsResolver<TRuntimeConfig>): AgentModelResolver<TRuntimeConfig>;
+//#endregion
+//#region src/tool-runtime.d.ts
+declare function applyAgentToolPolicies<TTools extends Record<string, unknown>>(tools: TTools | undefined): TTools | undefined;
+type AgentToolStepReporter = AgentRuntimeContext["toolStepReporter"];
+declare function withAgentToolStepReporting<TTools extends AgentToolSet>(tools: TTools, reportToolStep?: AgentToolStepReporter): TTools;
+//#endregion
+//#region src/capability-runtime.d.ts
+type ExactOptions<TInput, TShape> = TInput & Record<Exclude<keyof TInput, keyof TShape>, never>;
+type AgentCapabilityDefinitionInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TTypeContract extends AgentCapabilityTypeContract = AgentCapabilityTypeContract> = AgentCapabilityDefinition<TRuntimeConfig, Name, TTypeContract>;
+declare function defineCapability<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TTypeContract extends AgentCapabilityTypeContract = AgentCapabilityTypeContract, const TCapability extends AgentCapabilityDefinition<TRuntimeConfig, Name, TTypeContract> = AgentCapabilityDefinition<TRuntimeConfig, Name, TTypeContract>>(capability: ExactOptions<TCapability, AgentCapabilityDefinitionInput<TRuntimeConfig, Name, TTypeContract>>): TCapability;
+//#endregion
+//#region src/agent-error.d.ts
+type AgentPublicErrorCode = "APPROVAL_REQUIRED" | "AUTHENTICATION_REQUIRED" | "CAPABILITY_DENIED" | "CAPABILITY_NOT_FOUND" | "INTERNAL" | "LLM_GATE_REJECTED" | "RATE_LIMIT_REJECTED" | "RATE_LIMIT_UNAVAILABLE";
+interface AgentPublicErrorDetails {
+  capability?: string;
+  category?: string;
+  retryAfter?: number;
+}
+//#endregion
+//#region src/invocation-stream.d.ts
+declare const agentInvocationStreamRoute = "/__vitehub/agent/invocation-stream";
+interface AgentDevLoopAgentSummary {
+  aliases?: string[];
+  name: string;
+  triggers: string[];
+}
+interface AgentDevLoopDiscoveryResponse {
+  agents: AgentDevLoopAgentSummary[];
+  inspection?: AgentInspectionMetadata;
+  root: string;
+  workspaceDevTokenServerId?: string;
+}
+interface AgentInvocationStreamErrorEvent {
+  code?: AgentPublicErrorCode | (string & {});
+  details?: AgentPublicErrorDetails;
+  error: string;
+  requestId?: string;
+  type: "error";
+}
+type AgentInvocationStreamEvent = StreamEvent | {
+  channelId?: string;
+  effect: AgentChannelDeliveryEffectIntent;
+  run?: AgentRunMetadata;
+  type: "delivery-preview";
+} | AgentInvocationStreamErrorEvent | {
+  data?: Record<string, unknown>;
+  durationMs?: number;
+  id: string;
+  label?: string;
+  phase: "workspace.prepare" | (string & {});
+  status: "completed" | "failed" | "started" | "updating";
+  type: "progress";
+} | {
+  agent: string;
+  metadata?: Record<string, unknown>;
+  run?: unknown;
+  trigger?: string;
+  type: "start";
+} | {
+  type: "done";
+};
+declare function readAgentInvocationStream(body: ReadableStream<Uint8Array>): AsyncGenerator<AgentInvocationStreamEvent>;
+declare function createAgentInvocationStreamResponse(run: (emit: (event: AgentInvocationStreamEvent) => void, signal: AbortSignal) => Promise<void>, options?: {
+  timeout?: number;
+}): Response;
+//#endregion
+//#region src/index.d.ts
+type WorkspaceSourceNames<TWorkspace> = TWorkspace extends {
+  sources: infer TSources;
+} ? Extract<keyof NonNullable<TSources>, string> : string;
+type WorkspaceContribution<TWorkspace> = TWorkspace extends ((...args: any[]) => infer TResult) ? NonNullable<Awaited<TResult>> : NonNullable<TWorkspace>;
+type WorkspaceContributionSourceNames<TWorkspace> = WorkspaceContribution<TWorkspace> extends {
+  sources?: infer TSources;
+} ? Extract<keyof NonNullable<TSources>, string> : never;
+type CapabilityWorkspaceSourceNames<TCapability> = (TCapability extends {
+  workspaceSources: infer TSources;
+} ? Extract<keyof NonNullable<TSources>, string> : never) | (TCapability extends {
+  workspace: infer TWorkspace;
+} ? WorkspaceContributionSourceNames<TWorkspace> : never) | (TCapability extends {
+  capabilities: infer TCapabilities;
+} ? AgentCapabilitiesWorkspaceSourceNames<TCapabilities> : never);
+type ResolvedAgentCapabilitiesInput<TCapabilities> = TCapabilities extends ((...args: any[]) => infer TResult) ? Awaited<TResult> : TCapabilities;
+type AgentCapabilitiesWorkspaceSourceNames<TCapabilities> = ResolvedAgentCapabilitiesInput<TCapabilities> extends readonly (infer TCapability)[] ? CapabilityWorkspaceSourceNames<TCapability> : never;
+type WorkspaceSourceHasRemovedScopes<TSource, TDepth extends readonly unknown[] = []> = TDepth["length"] extends 8 ? false : TSource extends object ? "scopes" extends keyof TSource ? true : TSource extends {
+  source: infer TWrappedSource;
+} ? WorkspaceSourceHasRemovedScopes<TWrappedSource, [...TDepth, unknown]> : false : false;
+type WorkspaceSourceNamesWithRemovedScopes<TSources> = { [TSourceName in keyof NonNullable<TSources>]: true extends WorkspaceSourceHasRemovedScopes<NonNullable<TSources>[TSourceName]> ? TSourceName : never }[keyof NonNullable<TSources>];
+type WorkspaceSourcesWithRemovedScopes<TWorkspace> = WorkspaceContribution<TWorkspace> extends {
+  sources?: infer TSources;
+} ? WorkspaceSourceNamesWithRemovedScopes<TSources> : never;
+type CapabilityWorkspaceSourcesWithRemovedScopes<TCapability> = (TCapability extends {
+  workspaceSources: infer TSources;
+} ? WorkspaceSourceNamesWithRemovedScopes<TSources> : never) | (TCapability extends {
+  workspace: infer TWorkspace;
+} ? WorkspaceSourcesWithRemovedScopes<TWorkspace> : never) | (TCapability extends {
+  capabilities: infer TCapabilities;
+} ? AgentCapabilitiesWorkspaceSourcesWithRemovedScopes<TCapabilities> : never);
+type AgentCapabilitiesWorkspaceSourcesWithRemovedScopes<TCapabilities> = ResolvedAgentCapabilitiesInput<TCapabilities> extends readonly (infer TCapability)[] ? CapabilityWorkspaceSourcesWithRemovedScopes<TCapability> : never;
+type InvalidWorkspaceSourceGrant<TSourceName> = {
+  readonly __vitehubInvalidWorkspaceSourceGrant: TSourceName;
+};
+type InvalidWorkspaceSourceOption<TSourceName> = {
+  readonly __vitehubInvalidWorkspaceSourceOption: TSourceName;
+};
+type ValidateCapabilityWorkspaceSources<TSourceName, TWorkspace, TCapabilities, TCapability> = [TSourceName] extends [never] ? TCapability : TSourceName extends string ? string extends TSourceName ? TCapability : Exclude<TSourceName, WorkspaceSourceNames<TWorkspace> | AgentCapabilitiesWorkspaceSourceNames<TCapabilities>> extends never ? TCapability : TCapability & InvalidWorkspaceSourceGrant<Exclude<TSourceName, WorkspaceSourceNames<TWorkspace> | AgentCapabilitiesWorkspaceSourceNames<TCapabilities>>> : TCapability;
+type ValidateAgentCapability<TCapability, TWorkspace, TCapabilities> = TCapability extends AgentCapabilityDefinition<any, any, infer TTypeContract> ? TTypeContract extends {
+  workspaceSources: infer TSourceName;
+} ? ValidateCapabilityWorkspaceSources<TSourceName, TWorkspace, TCapabilities, TCapability> : TCapability : TCapability;
+type ValidateAgentCapabilitiesList<TCapabilities, TWorkspace> = TCapabilities extends readonly [unknown, ...unknown[]] | readonly [] ? { [Index in keyof TCapabilities]: ValidateAgentCapability<TCapabilities[Index], TWorkspace, TCapabilities> } : TCapabilities extends readonly (infer TCapability)[] ? readonly ValidateAgentCapability<TCapability, TWorkspace, TCapabilities>[] : TCapabilities;
+type ValidateAgentCapabilities<TCapabilities, TWorkspace> = TCapabilities extends ((...args: infer TArgs) => infer TResult) ? (...args: TArgs) => TResult extends PromiseLike<infer TResolved> ? Promise<ValidateAgentCapabilitiesList<TResolved, TWorkspace>> : ValidateAgentCapabilitiesList<TResult, TWorkspace> : ValidateAgentCapabilitiesList<TCapabilities, TWorkspace>;
+type UnionToIntersection<T> = (T extends unknown ? (value: T) => void : never) extends ((value: infer TIntersection) => void) ? TIntersection : never;
+type CapabilityInvocationContextValues<TCapability> = TCapability extends AgentCapabilityDefinition<any, any, infer TTypeContract> ? TTypeContract extends AgentCapabilityTypeContract ? TTypeContract["invocationContext"] extends object ? TTypeContract["invocationContext"] : unknown : unknown : unknown;
+type AgentCapabilitiesInvocationContextValues<TCapabilities> = AgentInvocationContextValues & UnionToIntersection<ResolvedAgentCapabilitiesInput<TCapabilities> extends readonly [unknown, ...unknown[]] | readonly [] ? CapabilityInvocationContextValues<ResolvedAgentCapabilitiesInput<TCapabilities>[number]> : ResolvedAgentCapabilitiesInput<TCapabilities> extends readonly (infer TCapability)[] ? CapabilityInvocationContextValues<TCapability> : unknown>;
+type ValidateWorkspaceAgentOptions<TOptions> = TOptions extends {
+  capabilities?: infer TCapabilities;
+  workspace: infer TWorkspace;
+} ? {
+  capabilities?: ValidateAgentCapabilities<TCapabilities, TWorkspace>;
+} & ([WorkspaceSourcesWithRemovedScopes<TWorkspace> | AgentCapabilitiesWorkspaceSourcesWithRemovedScopes<TCapabilities>] extends [never] ? unknown : InvalidWorkspaceSourceOption<WorkspaceSourcesWithRemovedScopes<TWorkspace> | AgentCapabilitiesWorkspaceSourcesWithRemovedScopes<TCapabilities>>) : unknown;
+interface AgentWorkflowRun<TOutput = unknown> {
+  id: string;
+  metadata?: unknown;
+  payload?: unknown;
+  provider: string;
+  result?: TOutput;
+  status: "cancelled" | "completed" | "failed" | "queued" | "running" | "unknown";
+}
+interface ScheduleRunContextLike {
+  attemptId?: string;
+  id: string;
+  input?: unknown;
+  runId?: string;
+  scheduleId?: string;
+  scheduledAt: Date;
+  target?: string;
+  waitUntil?: (promise: PromiseLike<unknown>) => void;
+}
+declare function workflow(name?: string): AgentWorkflowRuntimeBinding;
+type AgentCapabilitiesOption<TRuntimeConfig extends AgentRuntimeConfig, Name extends WorkspaceName, CALL_OPTIONS, TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] | undefined> = TCapabilities | AgentCapabilitiesResolver<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] ? TCapabilities : readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[]>;
+interface DefineAgent {
+  <TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown, const TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, const TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] | undefined = readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] | undefined, TOutput = unknown, const TOptions extends WorkspaceAgentOptions<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>, TOutput, CustomAgentDriver<TRuntimeConfig, CALL_OPTIONS, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>> = WorkspaceAgentOptions<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>, TOutput, CustomAgentDriver<TRuntimeConfig, CALL_OPTIONS, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>>>(options: TOptions & {
+    capabilities?: AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>;
+    driver: CustomAgentDriver<TRuntimeConfig, CALL_OPTIONS, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>;
+  } & ValidateWorkspaceAgentOptions<TOptions>): WorkspaceAgentDefinition<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>, TOutput>;
+  <TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown, const TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, const TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] | undefined = readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[] | undefined, TOutput = unknown, const TOptions extends WorkspaceAgentOptions<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>> = WorkspaceAgentOptions<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>>>(options: TOptions & {
+    capabilities?: AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>;
+    driver: AgentDriver<TRuntimeConfig, CALL_OPTIONS, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>;
+  } & ValidateWorkspaceAgentOptions<TOptions>): WorkspaceAgentDefinition<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, Name, CALL_OPTIONS, TCapabilities>, TOutput>;
+  <TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, const TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, const TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig>[] | undefined = readonly AgentCapabilityDefinition<TRuntimeConfig>[] | undefined, TOutput = unknown>(options: AgentSettings<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, WorkspaceName, CALL_OPTIONS, TCapabilities>, TOutput, CustomAgentDriver<TRuntimeConfig, CALL_OPTIONS, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>> & {
+    capabilities?: AgentCapabilitiesOption<TRuntimeConfig, WorkspaceName, CALL_OPTIONS, TCapabilities>;
+    workspace?: never;
+  }): AgentDefinition<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>;
+  <TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, const TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, const TCapabilities extends readonly AgentCapabilityDefinition<TRuntimeConfig>[] | undefined = readonly AgentCapabilityDefinition<TRuntimeConfig>[] | undefined, TOutput = unknown>(options: AgentSettings<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, AgentCapabilitiesOption<TRuntimeConfig, WorkspaceName, CALL_OPTIONS, TCapabilities>, TOutput> & {
+    capabilities?: AgentCapabilitiesOption<TRuntimeConfig, WorkspaceName, CALL_OPTIONS, TCapabilities>;
+    workspace?: never;
+  }): AgentDefinition<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, AgentCapabilitiesInvocationContextValues<TCapabilities>, TOutput>;
+}
+declare const defineAgent: DefineAgent;
+declare function agentWithColocatedInstructions<Agent>(agent: Agent, instructions?: string): Agent;
+declare function resolveAgent<TContext extends AgentRuntimeContext>(agent: AgentInput<TContext>, context: TContext): Promise<AgentAdapter>;
+declare function getAgentFromRegistry<TContext extends AgentRuntimeContext>(name: string, registry?: AgentRegistry<TContext>): Promise<AgentInput<TContext>>;
+declare function resolveAgentTriggers<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>): Promise<Record<string, ResolvedAgentTriggerDefinition<TRuntimeConfig>>>;
+declare function resolveAgentTriggerInvocation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, TInput = unknown, CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>, triggerId: string, input: TInput): Promise<ResolvedAgentTriggerInvocationResult<TRuntimeConfig, CALL_OPTIONS>>;
+declare function runAgentTrigger<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, TInput = unknown, CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>, triggerId: string, input: TInput): Promise<Response | AgentRunResult | unknown>;
+declare function streamAgentTrigger<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, TInput = unknown, CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>, triggerId: string, input: TInput, options?: {
+  onInvocation?: (invocation: ResolvedAgentTriggerInvocation<TRuntimeConfig, CALL_OPTIONS>) => MaybePromise<void>;
+  output?: "events" | "ui-message-stream";
+}): Promise<Response | AsyncIterable<StreamEvent> | unknown>;
+interface RunAgentInlineOptions {
+  output?: "raw" | "rendered";
+}
+declare function runAgentInline<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TOutput = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>, TOutput>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>, options: {
+  output: "raw";
+}): Promise<unknown>;
+declare function runAgentInline<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TOutput = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>, TOutput>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>, options?: RunAgentInlineOptions): Promise<TOutput | Response>;
+declare function startAgentInvocation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TOutput = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>, TOutput>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>): Promise<AgentInvocationController<TOutput | Response, CALL_OPTIONS>>;
+declare function runAgent<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TOutput = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>, TOutput>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>): Promise<TOutput | Response | AgentWorkflowRun<TOutput>>;
+declare function runScheduledAgent<CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext>, context: ScheduleRunContextLike, runtimeContext?: Partial<ResolvedAgentRuntimeContext>, input?: AgentRunInput<CALL_OPTIONS>): Promise<unknown>;
+declare function streamAgentInline<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>, options?: {
+  output?: "events" | "ui-message-stream";
+}): Promise<Response | AsyncIterable<StreamEvent> | unknown>;
+declare function streamAgent<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown>(agent: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, context: AgentRuntimeContext<TRuntimeConfig>, input: AgentRunInput<CALL_OPTIONS>, options?: {
+  output?: "events" | "ui-message-stream";
+}): Promise<Response | AsyncIterable<StreamEvent> | unknown>;
+declare function getAgent<TContext extends AgentRuntimeContext>(agent: AgentInput<TContext>, context: TContext): Promise<AgentAdapter>;
+//#endregion
+export { gateway as A, AgentInvocationControlResult as B, createAgentInvocationStreamResponse as C, withAgentToolStepReporting as D, applyAgentToolPolicies as E, ResolvedAgentTriggerInvocation as F, AgentInvocationSnapshot as G, AgentInvocationInputMode as H, ResolvedAgentTriggerInvocationResult as I, AgentInvocationStatus as K, isResolvedAgentTriggerHandledInvocation as L, defineAgentInvoker as M, AgentWebhookVerificationResult as N, GatewayModelSettings as O, ResolvedAgentTriggerHandledInvocation as P, verifyAgentWebhookRequest as R, agentInvocationStreamRoute as S, defineCapability as T, AgentInvocationInputSupport as U, AgentInvocationController as V, AgentInvocationInspection as W, workflow as _, getAgentFromRegistry as a, AgentInvocationStreamErrorEvent as b, resolveAgentTriggers as c, runAgentTrigger as d, runScheduledAgent as f, streamAgentTrigger as g, streamAgentInline as h, getAgent as i, agentInvokerContextKey as j, GatewayModelSettingsResolver as k, runAgent as l, streamAgent as m, agentWithColocatedInstructions as n, resolveAgent as o, startAgentInvocation as p, defineAgent as r, resolveAgentTriggerInvocation as s, DefineAgent as t, runAgentInline as u, AgentDevLoopAgentSummary as v, readAgentInvocationStream as w, AgentInvocationStreamEvent as x, AgentDevLoopDiscoveryResponse as y, AgentInvocationControlOutcome as z };
\ No newline at end of file
diff --git a/dist/index.d.ts b/dist/index.d.ts
index e258ac8b647900d6c4e4ef29ca128e644cc4bf93..5c14a5b5d5f9a57790dd5b0484af3652045c4ce7 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,6 +1,6 @@
-import { $ as AgentChannelDeliveryFinishEffectResult, $i as serializeMessages, $n as AgentRunCallbackContext, $r as MaybeResolvable, $t as AgentHarnessSessionKey, A as AgentCapabilityCliStandardSchemaResultSuccess, Ai as ImagePart, An as AgentMessageConcurrency, Ar as AgentTriggerRunInvokeResult, B as AgentCapabilityRuntimeContext, Bi as ToolCallPart, Bn as AgentModelInstrumentationContext, C as AgentCapabilityCliExecutionInput, Ci as AttachmentPart, Cn as AgentInvocationHooks, Ct as AgentChatOptions, D as AgentCapabilityCliResolver, Di as DataPart, Dn as AgentInvokerProfile, Dr as AgentTriggerContext, Dt as AgentChatSendMessage, E as AgentCapabilityCliOutputFormat, Ei as CreateMessageOptions, En as AgentInvokerOptions, Er as AgentToolTransform, Et as AgentChatPlatformsResolver, F as AgentCapabilityHookName, Fi as RunEvent, Fn as AgentModelDriver, Fr as AgentUsageCredentialSource, Ft as AgentDeliveryArtifactPlacement, G as AgentChannelDeliveryEffectIntent, Gi as applyStreamEvent, Gn as AgentOutputExtensionProvider, Gr as ClaudeCodeDriverOptions, Gt as AgentFinishHook, H as AgentChannelDefinition, Hi as ToolInvocationState, Hn as AgentModuleOptions, Hr as BuiltInAgentDriver, Ht as AgentFinishEvent, I as AgentCapabilityHooks, Ii as SerializedMessages, In as AgentModelExecutionInstrumentation, Ir as AgentUsageRecord, It as AgentDriver, J as AgentChannelDeliveryEffectPayload, Ji as deserializeMessages, Jn as AgentOutputRenderer, Jr as CodexDriverOptions, Jt as AgentHarnessDriver, K as AgentChannelDeliveryEffectIntentOptions, Ki as collectStreamEvents, Kn as AgentOutputExtensionValues, Kr as ClaudeCodeThinkingConfig, Kt as AgentFinishHookEvent, L as AgentCapabilityInput, Li as SourcePart, Ln as AgentModelExecutionOptions, Lr as AgentWaitUntil, Lt as AgentDriverContribution, M as AgentCapabilityContext, Mi as MessageMetadata, Mn as AgentMessageFilter, Mr as AgentUIMessageStreamProjectionResolver, Mt as AgentDefinition, N as AgentCapabilityDefinition, Ni as MessagePart, Nn as AgentMessageFilterContext, Nr as AgentUsage, Nt as AgentDefinitionCliOptions, O as AgentCapabilityCliRunContext, Oi as ErrorPart, On as AgentInvokerResolveContext, Or as AgentTriggerDefinition, Ot as AgentChatSessionOptions, P as AgentCapabilityHandle, Pi as MessageRole, Pn as AgentMessageLockScope, Pr as AgentUsageCost, Pt as AgentDeliveryArtifact, Q as AgentChannelDeliveryFinishEffectContext, Qi as isAttachmentPart, Qn as AgentRegistryModule, Qr as MaybePromise, Qt as AgentHarnessSandboxProviderInput, R as AgentCapabilityMode, Ri as StreamEvent, Rn as AgentModelInput, Rt as AgentDriverContributionKind, S as AgentCapabilityCliContribution, Si as AttachmentData, Sn as AgentInvocationExtensions, Sr as AgentToolSchema, St as AgentChatMessageHookArgs, T as AgentCapabilityCliOutputDefinition, Ti as AudioPart, Tn as AgentInvokerMeta, Tr as AgentToolStep, Tt as AgentChatPlatformResolver, U as AgentChannelDeliveryEffectContext, Ui as ToolResultPart, Un as AgentOutputDefinition, Ur as BuiltInAgentDriverName, Ut as AgentFinishExtensionValues, Vi as ToolInvocation, Vn as AgentModelResolver, Vr as AgentWorkflowRuntimeBinding, Vt as AgentExecution, W as AgentChannelDeliveryEffectHandler, Wi as appendMessageText, Wn as AgentOutputExtensionEvent, Wr as ClaudeCodeAuthOptions, Wt as AgentFinishExtensions, X as AgentChannelDeliveryFinishEffect, Xi as getToolInvocations, Xn as AgentRegistry, Xr as CustomAgentDriver, Xt as AgentHarnessInstructions, Y as AgentChannelDeliveryEffects, Yi as getMessageText, Yn as AgentProvidersOptions, Yr as CodexDriverSandboxOptions, Yt as AgentHarnessDriverInput, Z as AgentChannelDeliveryFinishEffectCallback, Zi as isAttachmentData, Zn as AgentRegistryHandlerOptions, Zr as DiscoveredAgentDefinition, Zt as AgentHarnessSandboxProvider, _ as AgentCapabilitiesInput, _n as AgentInspectionToolDefinition, _r as AgentToolDefinition, _t as AgentChatErrorHookArgs, a as AgentAdapterFactory, ai as ResolvedAgentTriggerDefinition, an as AgentHookOwner, ar as AgentRunMetadata, at as AgentChannelDeliveryStatusInput, b as AgentCapabilitiesResolverContext, bi as ApprovalDecisionPart, bn as AgentInvocationContextStore, br as AgentToolResolver, bt as AgentChatFinishExtension, c as AgentAdapterInstructionsValue, ci as AgentRunEvent, cn as AgentInputHook, cr as AgentRuntimeBinding, ct as AgentChannelFactory, d as AgentAdapterRunContext, dn as AgentInspectionDriverMetadata, dr as AgentRuntimeHooks, dt as AgentChannelTriggerContext, ea as validateMessage, ei as PublishedAgentDeliveryArtifact, en as AgentHarnessWorkDir, er as AgentRunContext, et as AgentChannelDeliveryReactionInput, fi as AgentRunEventStore, fn as AgentInspectionFileTreeItem, fr as AgentRuntimeName, ft as AgentChannelWebhookRegistrationDefinition, g as AgentCapabilities, gi as AgentRunEvents, gn as AgentInspectionModelMetadata, gr as AgentStateProviderOptions, hn as AgentInspectionModelExecutionMetadata, hr as AgentSettings, ht as AgentChatAgentHookArgs, i as AgentAdapter, ii as ResolvedAgentStateProviderOptions, in as AgentHookOutcome, ir as AgentRunInputContextValues, it as AgentChannelDeliveryReplyStream, j as AgentCapabilityCliStandardSchemaV1, ji as Message, jn as AgentMessageDeliveryKind, jr as AgentUIMessageStreamProjection, jt as AgentChatTriggerHistory, k as AgentCapabilityCliStandardSchemaResultFailure, ki as FilePart, kn as AgentMessageChannelSettings, kr as AgentTriggerInvokeResult, l as AgentAdapterMetadataContext, li as AgentRunEventInput, ln as AgentInspectionConfigMetadata, lr as AgentRuntimeConfig, lt as AgentChannelInput, m as AgentCallSettingsInstrumentationContext, mi as AgentRunEventStoreResolver, mn as AgentInspectionMetadata, mr as AgentSchedulerProviderOptions, n as AgentAccessWorkspaceScopeContext, ni as ResolvedAgentModuleOptions, nn as AgentHookObserverEvent, nr as AgentRunHandler, nt as AgentChannelDeliveryReplyInput, o as AgentAdapterInstructions, oi as WorkspaceAgentWorkspaceConfig, on as AgentHostIdentity, or as AgentRunResult, ot as AgentChannelDeliveryStatusPayload, p as AgentCallSettingsInstrumentation, pi as AgentRunEventStoreResolveContext, pn as AgentInspectionHarnessMetadata, pr as AgentSandboxProviderOptions, pt as AgentChannels, q as AgentChannelDeliveryEffectKind, qi as createMessage, qn as AgentOutputExtensions, qr as CodexAuthOptions, qt as AgentHarnessCredentialSource, r as AgentActor, ri as ResolvedAgentRuntimeContext, rn as AgentHookObserverHooks, rr as AgentRunInput, rt as AgentChannelDeliveryReplyPayload, s as AgentAdapterInstructionsPart, si as WorkspaceAgentWorkspaceOptions, sn as AgentInput, sr as AgentRuntime, st as AgentChannelDeliveryStatusState, t as AgentAccessInvocationContextValue, ti as Resolvable, tn as AgentHookObserver, tr as AgentRunDriver, tt as AgentChannelDeliveryReactionPayload, u as AgentAdapterResult, ui as AgentRunEventPublisher, un as AgentInspectionConfigValue, ur as AgentRuntimeContext, ut as AgentChannelInputs, v as AgentCapabilitiesList, vn as AgentIntegrationOption, vr as AgentToolPolicyContext, vt as AgentChatEventHookArgs, w as AgentCapabilityCliExecutionResult, wi as AudioData, wn as AgentInvoker, wr as AgentToolStandardSchema, wt as AgentChatPlatformAdapter, x as AgentCapabilityCliCommand, xi as ApprovalRequestPart, xn as AgentInvocationContextValues, xt as AgentChatMessage, y as AgentCapabilitiesResolver, yi as AgentMessagePhase, yn as AgentIntegrationsOptions, yr as AgentToolPolicyDecision, yt as AgentChatEventHooks, z as AgentCapabilityPhase, zi as TextPart, zn as AgentModelInstrumentation, zr as AgentWebhookRegistrationDefinition, zt as AgentDriverKind } from "./types-C4oyRXNr.js";
-import { a as AgentChatRunContext, c as getAgentChatContext, o as agentChatContextKey, r as AgentChatContext, t as AgentChannelContext } from "./chat-trigger-BXf37awy.js";
-import { t as defineFinishEffect } from "./delivery-effects-B8D8m9OC.js";
-import { A as gateway, B as AgentInvocationControlResult, C as createAgentInvocationStreamResponse, D as withAgentToolStepReporting, E as applyAgentToolPolicies, F as ResolvedAgentTriggerInvocation, G as AgentInvocationSnapshot, H as AgentInvocationInputMode, I as ResolvedAgentTriggerInvocationResult, K as AgentInvocationStatus, L as isResolvedAgentTriggerHandledInvocation, M as defineAgentInvoker, N as AgentWebhookVerificationResult, O as GatewayModelSettings, P as ResolvedAgentTriggerHandledInvocation, R as verifyAgentWebhookRequest, S as agentInvocationStreamRoute, T as defineCapability, U as AgentInvocationInputSupport, V as AgentInvocationController, W as AgentInvocationInspection, _ as workflow, a as getAgentFromRegistry, b as AgentInvocationStreamErrorEvent, c as resolveAgentTriggers, d as runAgentTrigger, f as runScheduledAgent, g as streamAgentTrigger, h as streamAgentInline, i as getAgent, j as agentInvokerContextKey, k as GatewayModelSettingsResolver, l as runAgent, m as streamAgent, n as agentWithColocatedInstructions, o as resolveAgent, p as startAgentInvocation, r as defineAgent, s as resolveAgentTriggerInvocation, t as DefineAgent, u as runAgentInline, v as AgentDevLoopAgentSummary, w as readAgentInvocationStream, x as AgentInvocationStreamEvent, y as AgentDevLoopDiscoveryResponse, z as AgentInvocationControlOutcome } from "./index-DrjcTrO9.js";
-import { a as materializeAgentInspectionSourceMetadata, i as createAgentInspectionMetadata, l as workspaceDefinitionFromOptions, n as WorkspaceAgentDefinition, o as resolveAgentInspectionMetadata, r as WorkspaceAgentOptions, s as workspaceAgentOwnsWorkspaceDefinition, t as WorkspaceAgentDefaults } from "./workspace-agent-DQS6D-dz.js";
+import { $ as AgentChannelDeliveryFinishEffectResult, $i as isAttachmentPart, $n as AgentRegistryModule, $r as MaybePromise, $t as AgentHarnessSandboxProviderInput, A as AgentCapabilityCliStandardSchemaResultSuccess, Ai as FilePart, An as AgentMessageChannelSettings, Ar as AgentTriggerInvokeResult, B as AgentCapabilityRuntimeContext, Bi as TextPart, Bn as AgentModelInstrumentation, Br as AgentWebhookRegistrationDefinition, Bt as AgentDriverKind, C as AgentCapabilityCliExecutionInput, Ci as AttachmentData, Cn as AgentInvocationExtensions, Cr as AgentToolSchema, Ct as AgentChatMessageHookArgs, D as AgentCapabilityCliResolver, Di as CreateMessageOptions, Dn as AgentInvokerOptions, Dr as AgentToolTransform, Dt as AgentChatPlatformsResolver, E as AgentCapabilityCliOutputFormat, Ei as AudioPart, En as AgentInvokerMeta, Er as AgentToolStep, Et as AgentChatPlatformResolver, F as AgentCapabilityHookName, Fi as MessageRole, Fn as AgentMessageLockScope, Fr as AgentUsageCost, Ft as AgentDeliveryArtifact, G as AgentChannelDeliveryEffectIntent, Gi as appendMessageText, Gn as AgentOutputExtensionEvent, Gr as ClaudeCodeAuthOptions, Gt as AgentFinishExtensions, H as AgentChannelDefinition, Hi as ToolInvocation, Hn as AgentModelResolver, Hr as AgentWorkflowRuntimeBinding, Ht as AgentExecution, I as AgentCapabilityHooks, Ii as RunEvent, In as AgentModelDriver, Ir as AgentUsageCredentialSource, It as AgentDeliveryArtifactPlacement, J as AgentChannelDeliveryEffectPayload, Ji as createMessage, Jn as AgentOutputExtensions, Jr as CodexAuthOptions, Jt as AgentHarnessCredentialSource, K as AgentChannelDeliveryEffectIntentOptions, Ki as applyStreamEvent, Kn as AgentOutputExtensionProvider, Kr as ClaudeCodeDriverOptions, Kt as AgentFinishHook, L as AgentCapabilityInput, Li as SerializedMessages, Ln as AgentModelExecutionInstrumentation, Lr as AgentUsageRecord, Lt as AgentDriver, M as AgentCapabilityContext, Mi as Message, Mn as AgentMessageDeliveryKind, Mr as AgentUIMessageStreamProjection, Mt as AgentChatTriggerHistory, N as AgentCapabilityDefinition, Ni as MessageMetadata, Nn as AgentMessageFilter, Nr as AgentUIMessageStreamProjectionResolver, Nt as AgentDefinition, O as AgentCapabilityCliRunContext, Oi as DataPart, On as AgentInvokerProfile, Or as AgentTriggerContext, Ot as AgentChatSendMessage, P as AgentCapabilityHandle, Pi as MessagePart, Pn as AgentMessageFilterContext, Pr as AgentUsage, Pt as AgentDefinitionCliOptions, Q as AgentChannelDeliveryFinishEffectContext, Qi as isAttachmentData, Qn as AgentRegistryHandlerOptions, Qr as DiscoveredAgentDefinition, Qt as AgentHarnessSandboxProvider, R as AgentCapabilityMode, Ri as SourcePart, Rn as AgentModelExecutionOptions, Rr as AgentWaitUntil, Rt as AgentDriverContribution, S as AgentCapabilityCliContribution, Si as ApprovalRequestPart, Sn as AgentInvocationContextValues, St as AgentChatMessage, T as AgentCapabilityCliOutputDefinition, Ti as AudioData, Tn as AgentInvoker, Tr as AgentToolStandardSchema, Tt as AgentChatPlatformAdapter, U as AgentChannelDeliveryEffectContext, Ui as ToolInvocationState, Un as AgentModuleOptions, Ur as BuiltInAgentDriver, Ut as AgentFinishEvent, Vi as ToolCallPart, Vn as AgentModelInstrumentationContext, W as AgentChannelDeliveryEffectHandler, Wi as ToolResultPart, Wn as AgentOutputDefinition, Wr as BuiltInAgentDriverName, Wt as AgentFinishExtensionValues, X as AgentChannelDeliveryFinishEffect, Xi as getMessageText, Xn as AgentProvidersOptions, Xr as CodexDriverSandboxOptions, Xt as AgentHarnessDriverInput, Y as AgentChannelDeliveryEffects, Yi as deserializeMessages, Yn as AgentOutputRenderer, Yr as CodexDriverOptions, Yt as AgentHarnessDriver, Z as AgentChannelDeliveryFinishEffectCallback, Zi as getToolInvocations, Zn as AgentRegistry, Zr as CustomAgentDriver, Zt as AgentHarnessInstructions, _ as AgentCapabilitiesInput, _i as AgentRunEvents, _n as AgentInspectionModelMetadata, _r as AgentStateProviderOptions, a as AgentAdapterFactory, ai as ResolvedAgentStateProviderOptions, an as AgentHookOutcome, ar as AgentRunInputContextValues, at as AgentChannelDeliveryStatusInput, b as AgentCapabilitiesResolverContext, bi as AgentMessagePhase, bn as AgentIntegrationsOptions, br as AgentToolPolicyDecision, bt as AgentChatEventHooks, c as AgentAdapterInstructionsValue, ci as WorkspaceAgentWorkspaceOptions, cn as AgentInput, cr as AgentRuntime, ct as AgentChannelFactory, d as AgentAdapterRunContext, di as AgentRunEventPublisher, dn as AgentInspectionConfigValue, dr as AgentRuntimeContext, dt as AgentChannelTriggerContext, ea as serializeMessages, ei as MaybeResolvable, en as AgentHarnessSessionKey, er as AgentRunCallbackContext, et as AgentChannelDeliveryReactionInput, fn as AgentInspectionDriverMetadata, fr as AgentRuntimeHooks, g as AgentCapabilities, gn as AgentInspectionModelExecutionMetadata, gr as AgentSettings, gt as AgentChatAgentHookArgs, hi as AgentRunEventStoreResolver, hn as AgentInspectionMetadata, hr as AgentSchedulerProviderOptions, i as AgentAdapter, ii as ResolvedAgentRuntimeContext, in as AgentHookObserverHooks, ir as AgentRunInput, it as AgentChannelDeliveryReplyStream, j as AgentCapabilityCliStandardSchemaV1, ji as ImagePart, jn as AgentMessageConcurrency, jr as AgentTriggerRunInvokeResult, k as AgentCapabilityCliStandardSchemaResultFailure, ki as ErrorPart, kn as AgentInvokerResolveContext, kr as AgentTriggerDefinition, kt as AgentChatSessionOptions, l as AgentAdapterMetadataContext, li as AgentRunEvent, ln as AgentInputHook, lr as AgentRuntimeBinding, lt as AgentChannelInput, m as AgentCallSettingsInstrumentationContext, mi as AgentRunEventStoreResolveContext, mn as AgentInspectionHarnessMetadata, mr as AgentSandboxProviderOptions, mt as AgentChannels, n as AgentAccessWorkspaceScopeContext, ni as Resolvable, nn as AgentHookObserver, nr as AgentRunDriver, nt as AgentChannelDeliveryReplyInput, o as AgentAdapterInstructions, oi as ResolvedAgentTriggerDefinition, on as AgentHookOwner, or as AgentRunMetadata, ot as AgentChannelDeliveryStatusPayload, p as AgentCallSettingsInstrumentation, pi as AgentRunEventStore, pn as AgentInspectionFileTreeItem, pr as AgentRuntimeName, pt as AgentChannelWebhookRegistrationDefinition, q as AgentChannelDeliveryEffectKind, qi as collectStreamEvents, qn as AgentOutputExtensionValues, qr as ClaudeCodeThinkingConfig, qt as AgentFinishHookEvent, r as AgentActor, ri as ResolvedAgentModuleOptions, rn as AgentHookObserverEvent, rr as AgentRunHandler, rt as AgentChannelDeliveryReplyPayload, s as AgentAdapterInstructionsPart, si as WorkspaceAgentWorkspaceConfig, sn as AgentHostIdentity, sr as AgentRunResult, st as AgentChannelDeliveryStatusState, t as AgentAccessInvocationContextValue, ta as validateMessage, ti as PublishedAgentDeliveryArtifact, tn as AgentHarnessWorkDir, tr as AgentRunContext, tt as AgentChannelDeliveryReactionPayload, u as AgentAdapterResult, ui as AgentRunEventInput, un as AgentInspectionConfigMetadata, ur as AgentRuntimeConfig, ut as AgentChannelInputs, v as AgentCapabilitiesList, vn as AgentInspectionToolDefinition, vr as AgentToolDefinition, vt as AgentChatErrorHookArgs, w as AgentCapabilityCliExecutionResult, wi as AttachmentPart, wn as AgentInvocationHooks, wt as AgentChatOptions, x as AgentCapabilityCliCommand, xi as ApprovalDecisionPart, xn as AgentInvocationContextStore, xr as AgentToolResolver, xt as AgentChatFinishExtension, y as AgentCapabilitiesResolver, yn as AgentIntegrationOption, yr as AgentToolPolicyContext, yt as AgentChatEventHookArgs, z as AgentCapabilityPhase, zi as StreamEvent, zn as AgentModelInput, zt as AgentDriverContributionKind } from "./types-CW2XmfE7.js";
+import { a as AgentChatRunContext, c as getAgentChatContext, o as agentChatContextKey, r as AgentChatContext, t as AgentChannelContext } from "./chat-trigger-mwxwRC8l.js";
+import { t as defineFinishEffect } from "./delivery-effects-Bol6CuhN.js";
+import { A as gateway, B as AgentInvocationControlResult, C as createAgentInvocationStreamResponse, D as withAgentToolStepReporting, E as applyAgentToolPolicies, F as ResolvedAgentTriggerInvocation, G as AgentInvocationSnapshot, H as AgentInvocationInputMode, I as ResolvedAgentTriggerInvocationResult, K as AgentInvocationStatus, L as isResolvedAgentTriggerHandledInvocation, M as defineAgentInvoker, N as AgentWebhookVerificationResult, O as GatewayModelSettings, P as ResolvedAgentTriggerHandledInvocation, R as verifyAgentWebhookRequest, S as agentInvocationStreamRoute, T as defineCapability, U as AgentInvocationInputSupport, V as AgentInvocationController, W as AgentInvocationInspection, _ as workflow, a as getAgentFromRegistry, b as AgentInvocationStreamErrorEvent, c as resolveAgentTriggers, d as runAgentTrigger, f as runScheduledAgent, g as streamAgentTrigger, h as streamAgentInline, i as getAgent, j as agentInvokerContextKey, k as GatewayModelSettingsResolver, l as runAgent, m as streamAgent, n as agentWithColocatedInstructions, o as resolveAgent, p as startAgentInvocation, r as defineAgent, s as resolveAgentTriggerInvocation, t as DefineAgent, u as runAgentInline, v as AgentDevLoopAgentSummary, w as readAgentInvocationStream, x as AgentInvocationStreamEvent, y as AgentDevLoopDiscoveryResponse, z as AgentInvocationControlOutcome } from "./index-B69oMVI0.js";
+import { a as materializeAgentInspectionSourceMetadata, i as createAgentInspectionMetadata, l as workspaceDefinitionFromOptions, n as WorkspaceAgentDefinition, o as resolveAgentInspectionMetadata, r as WorkspaceAgentOptions, s as workspaceAgentOwnsWorkspaceDefinition, t as WorkspaceAgentDefaults } from "./workspace-agent-u4sFNw76.js";
 export { type AgentAccessInvocationContextValue, type AgentAccessWorkspaceScopeContext, type AgentActor, type AgentAdapter, type AgentAdapterFactory, type AgentAdapterInstructions, type AgentAdapterInstructionsPart, type AgentAdapterInstructionsValue, type AgentAdapterMetadataContext, type AgentAdapterResult, type AgentAdapterRunContext, type AgentCallSettingsInstrumentation, type AgentCallSettingsInstrumentationContext, type AgentCapabilities, type AgentCapabilitiesInput, type AgentCapabilitiesList, type AgentCapabilitiesResolver, type AgentCapabilitiesResolverContext, type AgentCapabilityCliCommand, type AgentCapabilityCliContribution, type AgentCapabilityCliExecutionInput, type AgentCapabilityCliExecutionResult, type AgentCapabilityCliOutputDefinition, type AgentCapabilityCliOutputFormat, type AgentCapabilityCliResolver, type AgentCapabilityCliRunContext, type AgentCapabilityCliStandardSchemaResultFailure, type AgentCapabilityCliStandardSchemaResultSuccess, type AgentCapabilityCliStandardSchemaV1, type AgentCapabilityContext, type AgentCapabilityDefinition, type AgentCapabilityHandle, type AgentCapabilityHookName, type AgentCapabilityHooks, type AgentCapabilityInput, type AgentCapabilityMode, type AgentCapabilityPhase, type AgentCapabilityRuntimeContext, type AgentChannelContext, type AgentChannelDefinition, type AgentChannelDeliveryEffectContext, type AgentChannelDeliveryEffectHandler, type AgentChannelDeliveryEffectIntent, type AgentChannelDeliveryEffectIntentOptions, type AgentChannelDeliveryEffectKind, type AgentChannelDeliveryEffectPayload, type AgentChannelDeliveryEffects, type AgentChannelDeliveryFinishEffect, type AgentChannelDeliveryFinishEffectCallback, type AgentChannelDeliveryFinishEffectContext, type AgentChannelDeliveryFinishEffectResult, type AgentChannelDeliveryReactionInput, type AgentChannelDeliveryReactionPayload, type AgentChannelDeliveryReplyInput, type AgentChannelDeliveryReplyPayload, type AgentChannelDeliveryReplyStream, type AgentChannelDeliveryStatusInput, type AgentChannelDeliveryStatusPayload, type AgentChannelDeliveryStatusState, type AgentChannelFactory, type AgentChannelInput, type AgentChannelInputs, type AgentChannelTriggerContext, type AgentChannelWebhookRegistrationDefinition, type AgentChannels, type AgentChatAgentHookArgs, type AgentChatContext, type AgentChatErrorHookArgs, type AgentChatEventHookArgs, type AgentChatEventHooks, type AgentChatFinishExtension, type AgentChatMessage, type AgentChatMessageHookArgs, type AgentChatOptions, type AgentChatPlatformAdapter, type AgentChatPlatformResolver, type AgentChatPlatformsResolver, type AgentChatRunContext, type AgentChatSendMessage, type AgentChatSessionOptions, type AgentChatTriggerHistory, type AgentDefinition, type AgentDefinitionCliOptions, type AgentDeliveryArtifact, type AgentDeliveryArtifactPlacement, type AgentDevLoopAgentSummary, type AgentDevLoopDiscoveryResponse, type AgentDriver, type AgentDriverContribution, type AgentDriverContributionKind, type AgentDriverKind, type AgentExecution, type AgentFinishEvent, type AgentFinishExtensionValues, type AgentFinishExtensions, type AgentFinishHook, type AgentFinishHookEvent, type AgentHarnessCredentialSource, type AgentHarnessDriver, type AgentHarnessDriverInput, type AgentHarnessInstructions, type AgentHarnessSandboxProvider, type AgentHarnessSandboxProviderInput, type AgentHarnessSessionKey, type AgentHarnessWorkDir, type AgentHookObserver, type AgentHookObserverEvent, type AgentHookObserverHooks, type AgentHookOutcome, type AgentHookOwner, type AgentHostIdentity, type AgentInput, type AgentInputHook, type AgentInspectionConfigMetadata, type AgentInspectionConfigValue, type AgentInspectionDriverMetadata, type AgentInspectionFileTreeItem, type AgentInspectionHarnessMetadata, type AgentInspectionMetadata, type AgentInspectionModelExecutionMetadata, type AgentInspectionModelMetadata, type AgentInspectionToolDefinition, type AgentIntegrationOption, type AgentIntegrationsOptions, type AgentInvocationContextStore, type AgentInvocationContextValues, type AgentInvocationControlOutcome, type AgentInvocationControlResult, type AgentInvocationController, type AgentInvocationExtensions, type AgentInvocationHooks, type AgentInvocationInputMode, type AgentInvocationInputSupport, type AgentInvocationInspection, type AgentInvocationSnapshot, type AgentInvocationStatus, type AgentInvocationStreamErrorEvent, type AgentInvocationStreamEvent, type AgentInvoker, type AgentInvokerMeta, type AgentInvokerOptions, type AgentInvokerProfile, type AgentInvokerResolveContext, type AgentMessageChannelSettings, type AgentMessageConcurrency, type AgentMessageDeliveryKind, type AgentMessageFilter, type AgentMessageFilterContext, type AgentMessageLockScope, AgentMessagePhase, type AgentModelDriver, type AgentModelExecutionInstrumentation, type AgentModelExecutionOptions, type AgentModelInput, type AgentModelInstrumentation, type AgentModelInstrumentationContext, type AgentModelResolver, type AgentModuleOptions, type AgentOutputDefinition, type AgentOutputExtensionEvent, type AgentOutputExtensionProvider, type AgentOutputExtensionValues, type AgentOutputExtensions, type AgentOutputRenderer, type AgentProvidersOptions, type AgentRegistry, type AgentRegistryHandlerOptions, type AgentRegistryModule, type AgentRunCallbackContext, type AgentRunContext, type AgentRunDriver, type AgentRunEvent, type AgentRunEventInput, type AgentRunEventPublisher, type AgentRunEventStore, type AgentRunEventStoreResolveContext, type AgentRunEventStoreResolver, type AgentRunEvents, type AgentRunHandler, type AgentRunInput, type AgentRunInputContextValues, type AgentRunMetadata, type AgentRunResult, type AgentRuntime, type AgentRuntimeBinding, type AgentRuntimeConfig, type AgentRuntimeContext, type AgentRuntimeHooks, type AgentRuntimeName, type AgentSandboxProviderOptions, type AgentSchedulerProviderOptions, type AgentSettings, type AgentStateProviderOptions, type AgentToolDefinition, type AgentToolPolicyContext, type AgentToolPolicyDecision, type AgentToolResolver, type AgentToolSchema, type AgentToolStandardSchema, type AgentToolStep, type AgentToolTransform, type AgentTriggerContext, type AgentTriggerDefinition, type AgentTriggerInvokeResult, type AgentTriggerRunInvokeResult, type AgentUIMessageStreamProjection, type AgentUIMessageStreamProjectionResolver, type AgentUsage, type AgentUsageCost, type AgentUsageCredentialSource, type AgentUsageRecord, type AgentWaitUntil, type AgentWebhookRegistrationDefinition, type AgentWebhookVerificationResult, type AgentWorkflowRuntimeBinding, ApprovalDecisionPart, ApprovalRequestPart, AttachmentData, AttachmentPart, AudioData, AudioPart, type BuiltInAgentDriver, type BuiltInAgentDriverName, type ClaudeCodeAuthOptions, type ClaudeCodeDriverOptions, type ClaudeCodeThinkingConfig, type CodexAuthOptions, type CodexDriverOptions, type CodexDriverSandboxOptions, CreateMessageOptions, type CustomAgentDriver, DataPart, DefineAgent, type DiscoveredAgentDefinition, ErrorPart, FilePart, type GatewayModelSettings, type GatewayModelSettingsResolver, ImagePart, type MaybePromise, type MaybeResolvable, Message, MessageMetadata, MessagePart, MessageRole, type PublishedAgentDeliveryArtifact, type Resolvable, type ResolvedAgentModuleOptions, type ResolvedAgentRuntimeContext, type ResolvedAgentStateProviderOptions, type ResolvedAgentTriggerDefinition, type ResolvedAgentTriggerHandledInvocation, type ResolvedAgentTriggerInvocation, type ResolvedAgentTriggerInvocationResult, RunEvent, SerializedMessages, SourcePart, StreamEvent, TextPart, ToolCallPart, ToolInvocation, ToolInvocationState, ToolResultPart, type WorkspaceAgentDefaults, type WorkspaceAgentDefinition, type WorkspaceAgentOptions, type WorkspaceAgentWorkspaceConfig, type WorkspaceAgentWorkspaceOptions, agentChatContextKey, agentInvocationStreamRoute, agentInvokerContextKey, agentWithColocatedInstructions, appendMessageText, applyAgentToolPolicies, applyStreamEvent, collectStreamEvents, createAgentInspectionMetadata, createAgentInvocationStreamResponse, createMessage, defineAgent, defineAgentInvoker, defineCapability, defineFinishEffect, deserializeMessages, gateway, getAgent, getAgentChatContext, getAgentFromRegistry, getMessageText, getToolInvocations, isAttachmentData, isAttachmentPart, isResolvedAgentTriggerHandledInvocation, materializeAgentInspectionSourceMetadata, readAgentInvocationStream, resolveAgent, resolveAgentInspectionMetadata, resolveAgentTriggerInvocation, resolveAgentTriggers, runAgent, runAgentInline, runAgentTrigger, runScheduledAgent, serializeMessages, startAgentInvocation, streamAgent, streamAgentInline, streamAgentTrigger, validateMessage, verifyAgentWebhookRequest, withAgentToolStepReporting, workflow, workspaceAgentOwnsWorkspaceDefinition, workspaceDefinitionFromOptions };
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index 8bc350a7c76ca3b2bdc447a130f765c579e6d9cc..43b8b1ac1f3876a96c4ac24101463239c8e0cc6b 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,8 +1,7 @@
 import { appendMessageText, applyStreamEvent, collectStreamEvents, createMessage, deserializeMessages, getMessageText, getToolInvocations, isAttachmentData, isAttachmentPart, serializeMessages, validateMessage } from "./messages.js";
 import { C as agentInvokerContextKey, T as defineAgentInvoker, c as defineCapability } from "./capability-runtime-Be85oBbC.js";
-import { f as withAgentToolStepReporting, l as applyAgentToolPolicies } from "./trace-DtP8Cj3K.js";
-import { A as getAgentChatContext, E as agentChatContextKey, g as defineFinishEffect } from "./channels-DNuHTrNn.js";
-import { _ as gateway, a as resolveAgent, b as verifyAgentWebhookRequest, c as runAgent, d as runScheduledAgent, f as startAgentInvocation, g as workflow, h as streamAgentTrigger, i as getAgentFromRegistry, l as runAgentInline, m as streamAgentInline, n as defineAgent, o as resolveAgentTriggerInvocation, p as streamAgent, r as getAgent, s as resolveAgentTriggers, t as agentWithColocatedInstructions, u as runAgentTrigger, y as isResolvedAgentTriggerHandledInvocation } from "./src-B3GiXaIA.js";
+import { _ as withAgentToolStepReporting, m as applyAgentToolPolicies } from "./trace-GIqXV-YM.js";
+import { A as getAgentChatContext, E as agentChatContextKey, g as defineFinishEffect } from "./channels-D6wOMJN1.js";
+import { C as isResolvedAgentTriggerHandledInvocation, a as resolveAgent, b as readAgentInvocationStream, c as runAgent, d as runScheduledAgent, f as startAgentInvocation, g as workflow, h as streamAgentTrigger, i as getAgentFromRegistry, l as runAgentInline, m as streamAgentInline, n as defineAgent, o as resolveAgentTriggerInvocation, p as streamAgent, r as getAgent, s as resolveAgentTriggers, t as agentWithColocatedInstructions, u as runAgentTrigger, v as agentInvocationStreamRoute, w as verifyAgentWebhookRequest, x as gateway, y as createAgentInvocationStreamResponse } from "./src-CktB4unw.js";
 import { a as resolveAgentInspectionMetadata, d as workspaceDefinitionFromOptions, i as materializeAgentInspectionSourceMetadata, l as workspaceAgentOwnsWorkspaceDefinition, n as createAgentInspectionMetadata } from "./workspace-agent-BtTRGp6h.js";
-import { i as readAgentInvocationStream, n as agentInvocationStreamRoute, r as createAgentInvocationStreamResponse } from "./invocation-stream-DFGK1iYd.js";
 export { agentChatContextKey, agentInvocationStreamRoute, agentInvokerContextKey, agentWithColocatedInstructions, appendMessageText, applyAgentToolPolicies, applyStreamEvent, collectStreamEvents, createAgentInspectionMetadata, createAgentInvocationStreamResponse, createMessage, defineAgent, defineAgentInvoker, defineCapability, defineFinishEffect, deserializeMessages, gateway, getAgent, getAgentChatContext, getAgentFromRegistry, getMessageText, getToolInvocations, isAttachmentData, isAttachmentPart, isResolvedAgentTriggerHandledInvocation, materializeAgentInspectionSourceMetadata, readAgentInvocationStream, resolveAgent, resolveAgentInspectionMetadata, resolveAgentTriggerInvocation, resolveAgentTriggers, runAgent, runAgentInline, runAgentTrigger, runScheduledAgent, serializeMessages, startAgentInvocation, streamAgent, streamAgentInline, streamAgentTrigger, validateMessage, verifyAgentWebhookRequest, withAgentToolStepReporting, workflow, workspaceAgentOwnsWorkspaceDefinition, workspaceDefinitionFromOptions };
diff --git a/dist/mcp/stdio.d.ts b/dist/mcp/stdio.d.ts
index aad43dc61ce16078ba65035099537fe36ca0d6fb..d1327813e752d2856165defa4b5ab05e5a073fa6 100644
--- a/dist/mcp/stdio.d.ts
+++ b/dist/mcp/stdio.d.ts
@@ -1,4 +1,4 @@
-import { r as McpClientConfig } from "../types-DPF3rS0I.js";
+import { r as McpClientConfig } from "../types-BZoKvU1a.js";
 import { StdioConfig, StdioConfig as StdioConfig$1 } from "@ai-sdk/mcp/mcp-stdio";
 
 //#region src/mcp/stdio.d.ts
diff --git a/dist/mcp.d.ts b/dist/mcp.d.ts
index 5ad4cbd0f974f4e8cb645d0d55087a51b42d6885..a6ca0078b47aa3eb65a2a82fd45644aeeb2bb4fc 100644
--- a/dist/mcp.d.ts
+++ b/dist/mcp.d.ts
@@ -1,4 +1,4 @@
-import { r as McpClientConfig } from "./types-DPF3rS0I.js";
+import { r as McpClientConfig } from "./types-BZoKvU1a.js";
 import { MCPClient, MCPClientConfig, MCPClientConfig as MCPClientConfig$1, MCPTransport } from "@ai-sdk/mcp";
 
 //#region src/mcp.d.ts
diff --git a/dist/messages.d.ts b/dist/messages.d.ts
index 53bec0efea80c5080f92fcc4959e23b767754afa..4d486f49481a7fd12610aff2370cbec4fa0defe8 100644
--- a/dist/messages.d.ts
+++ b/dist/messages.d.ts
@@ -1,2 +1,2 @@
-import { $i as serializeMessages, Ai as ImagePart, Bi as ToolCallPart, Ci as AttachmentPart, Di as DataPart, Ei as CreateMessageOptions, Fi as RunEvent, Gi as applyStreamEvent, Hi as ToolInvocationState, Ii as SerializedMessages, Ji as deserializeMessages, Ki as collectStreamEvents, Li as SourcePart, Mi as MessageMetadata, Ni as MessagePart, Oi as ErrorPart, Pi as MessageRole, Qi as isAttachmentPart, Ri as StreamEvent, Si as AttachmentData, Ti as AudioPart, Ui as ToolResultPart, Vi as ToolInvocation, Wi as appendMessageText, Xi as getToolInvocations, Yi as getMessageText, Zi as isAttachmentData, bi as ApprovalDecisionPart, ea as validateMessage, ji as Message, ki as FilePart, qi as createMessage, wi as AudioData, xi as ApprovalRequestPart, yi as AgentMessagePhase, zi as TextPart } from "./types-C4oyRXNr.js";
+import { $i as isAttachmentPart, Ai as FilePart, Bi as TextPart, Ci as AttachmentData, Di as CreateMessageOptions, Ei as AudioPart, Fi as MessageRole, Gi as appendMessageText, Hi as ToolInvocation, Ii as RunEvent, Ji as createMessage, Ki as applyStreamEvent, Li as SerializedMessages, Mi as Message, Ni as MessageMetadata, Oi as DataPart, Pi as MessagePart, Qi as isAttachmentData, Ri as SourcePart, Si as ApprovalRequestPart, Ti as AudioData, Ui as ToolInvocationState, Vi as ToolCallPart, Wi as ToolResultPart, Xi as getMessageText, Yi as deserializeMessages, Zi as getToolInvocations, bi as AgentMessagePhase, ea as serializeMessages, ji as ImagePart, ki as ErrorPart, qi as collectStreamEvents, ta as validateMessage, wi as AttachmentPart, xi as ApprovalDecisionPart, zi as StreamEvent } from "./types-CW2XmfE7.js";
 export { AgentMessagePhase, ApprovalDecisionPart, ApprovalRequestPart, AttachmentData, AttachmentPart, AudioData, AudioPart, CreateMessageOptions, DataPart, ErrorPart, FilePart, ImagePart, Message, MessageMetadata, MessagePart, MessageRole, RunEvent, SerializedMessages, SourcePart, StreamEvent, TextPart, ToolCallPart, ToolInvocation, ToolInvocationState, ToolResultPart, appendMessageText, applyStreamEvent, collectStreamEvents, createMessage, deserializeMessages, getMessageText, getToolInvocations, isAttachmentData, isAttachmentPart, serializeMessages, validateMessage };
\ No newline at end of file
diff --git a/dist/output.d.ts b/dist/output.d.ts
index fba706e131f97d55e0762150a66d81753873d6fb..4f7e7b5f7b5c9d61d5947153284c0812ee8410c4 100644
--- a/dist/output.d.ts
+++ b/dist/output.d.ts
@@ -1,4 +1,4 @@
-import { Ri as StreamEvent, or as AgentRunResult, yi as AgentMessagePhase } from "./types-C4oyRXNr.js";
+import { bi as AgentMessagePhase, sr as AgentRunResult, zi as StreamEvent } from "./types-CW2XmfE7.js";
 
 //#region src/internal/stream-result.d.ts
 declare function isAsyncIterable<T = unknown>(value: unknown): value is AsyncIterable<T>;
diff --git a/dist/repository-host-context-DB2_VeVi.d.ts b/dist/repository-host-context-DB2_VeVi.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..2244230d3f39f2d83c915a9994416b2feaa94c69
--- /dev/null
+++ b/dist/repository-host-context-DB2_VeVi.d.ts
@@ -0,0 +1,202 @@
+import { $r as MaybePromise, M as AgentCapabilityContext, N as AgentCapabilityDefinition, R as AgentCapabilityMode, V as AgentCapabilityTypeContract, br as AgentToolPolicyDecision, kr as AgentTriggerDefinition, ur as AgentRuntimeConfig, yr as AgentToolPolicyContext } from "./types-CW2XmfE7.js";
+import { WorkspaceName } from "@vite-hub/workspace";
+
+//#region src/capabilities/repository-host.d.ts
+type RepositoryHostProvider = "github" | "gitlab" | "bitbucket" | (string & {});
+type RepositoryHostTargetKind = "repository" | "changeRequest" | "issue" | "comment";
+type RepositoryHostReadOperation = "repository" | "changeRequests" | "changeRequest" | "changeRequestFiles" | "issues" | "issue" | "comments" | "checks" | "statuses";
+type RepositoryHostWriteOperation = "comment" | "reaction";
+type RepositoryHostToolPolicy = AgentToolPolicyDecision | ((context: AgentToolPolicyContext) => MaybePromise<AgentToolPolicyDecision>);
+interface RepositoryHostTarget {
+  host?: string;
+  id?: number | string;
+  kind?: RepositoryHostTargetKind;
+  owner?: string;
+  repository: string;
+}
+interface RepositoryHostReadRequest {
+  operation: RepositoryHostReadOperation;
+  params?: Record<string, unknown>;
+  target: RepositoryHostTarget;
+}
+interface RepositoryHostWriteRequest {
+  body?: string;
+  operation: RepositoryHostWriteOperation;
+  params?: Record<string, unknown>;
+  target: RepositoryHostTarget;
+}
+interface RepositoryHostClient {
+  provider?: RepositoryHostProvider;
+  read: (request: RepositoryHostReadRequest) => MaybePromise<unknown>;
+  write?: (request: RepositoryHostWriteRequest) => MaybePromise<unknown>;
+}
+interface RepositoryHostOptions {
+  client?: RepositoryHostClient | ((context: AgentCapabilityContext) => MaybePromise<RepositoryHostClient>);
+  mode?: AgentCapabilityMode;
+  policy?: RepositoryHostToolPolicy;
+  provider?: RepositoryHostProvider;
+}
+declare function repositoryHost(options?: RepositoryHostOptions): AgentCapabilityDefinition;
+//#endregion
+//#region src/capabilities/repository-host-context.d.ts
+type JsonPrimitive = string | number | boolean | null;
+type JsonValue = JsonPrimitive | JsonObject | JsonValue[];
+interface JsonObject {
+  [key: string]: JsonValue | undefined;
+}
+interface AsyncRecord<T extends { [K in keyof T]: JsonValue | undefined }> {
+  keys(): Promise<readonly (keyof T & string)[]>;
+  has<K extends keyof T & string>(key: K): Promise<boolean>;
+  has(key: string): Promise<boolean>;
+  get<K extends keyof T & string>(key: K): Promise<T[K] | undefined>;
+  get(key: string): Promise<T[keyof T] | undefined>;
+  pick<const K extends readonly (keyof T & string)[]>(keys: K): Promise<Partial<Pick<T, K[number]>>>;
+  pick(keys: readonly string[]): Promise<Partial<T>>;
+  entries(keys?: readonly (keyof T & string)[]): AsyncIterable<readonly [keyof T & string, T[keyof T & string]]>;
+  entries(keys?: readonly string[]): AsyncIterable<readonly [keyof T & string, T[keyof T & string]]>;
+  resolveAll(): Promise<Partial<T>>;
+  toJSON(): never;
+}
+interface PullRequestContextUser extends JsonObject {
+  id?: number | string;
+  login?: string;
+  type?: string;
+}
+interface PullRequestContextComment extends JsonObject {
+  authorAssociation?: string;
+  body?: string;
+  createdAt?: string;
+  htmlUrl?: string;
+  id?: number | string;
+  nodeId?: string;
+  updatedAt?: string;
+  user?: PullRequestContextUser;
+}
+interface PullRequestContextFile extends JsonObject {
+  additions?: number | string;
+  deletions?: number | string;
+  filename?: string;
+  status?: string;
+}
+interface PullRequestContextRef extends JsonObject {
+  ref?: string;
+  repo?: string;
+  sha?: string;
+}
+interface PullRequestContextMetadata extends JsonObject {
+  omittedComments?: number | string;
+  omittedFiles?: number | string;
+  unavailable?: string;
+}
+interface PullRequestContextValue extends JsonObject {
+  actor?: string;
+  apiUrl?: string;
+  base?: PullRequestContextRef;
+  baseRef?: string;
+  body?: string;
+  comments?: PullRequestContextComment[];
+  deliveryId?: string;
+  files?: PullRequestContextFile[];
+  head?: PullRequestContextRef;
+  headRef?: string;
+  htmlUrl?: string;
+  id?: number | string;
+  labels?: string[];
+  metadata?: PullRequestContextMetadata;
+  number: number | string;
+  provider?: string;
+  repository: string;
+  run?: JsonObject & {
+    messageId?: string;
+    origin?: string;
+    runId?: string;
+    threadId?: string;
+  };
+  source?: JsonObject & {
+    checkout?: boolean;
+    mount?: string;
+    ref?: string;
+    repo?: string;
+  };
+  title?: string;
+  trigger?: JsonObject & {
+    action?: string;
+    actor?: PullRequestContextUser;
+    args?: string;
+    command?: string;
+    comment?: PullRequestContextComment;
+    deliveryId?: string;
+    event?: string;
+    installationId?: number | string;
+    sender?: PullRequestContextUser;
+  };
+}
+interface RepositoryHostIssueContext extends JsonObject {
+  apiUrl?: string;
+  body?: string;
+  htmlUrl?: string;
+  id?: number | string;
+  labels?: string[];
+  nodeId?: string;
+  number: number | string;
+  pullRequest?: JsonObject & {
+    apiUrl?: string;
+    htmlUrl?: string;
+  };
+  repository?: string;
+  state?: string;
+  title?: string;
+  user?: PullRequestContextUser;
+}
+interface RepositoryHostContextValue {
+  body?: string;
+  comments?: PullRequestContextComment[];
+  files?: PullRequestContextFile[];
+  issue?: RepositoryHostIssueContext;
+  labels?: string[];
+  pullRequest?: PullRequestContextValue;
+}
+interface RepositoryHostIssueContextInput extends Omit<RepositoryHostIssueContext, "labels"> {
+  labels?: JsonValue[];
+}
+type RepositoryHostContextStaticInput = Partial<Omit<RepositoryHostContextValue, "issue" | "labels"> & {
+  issue?: RepositoryHostIssueContextInput;
+  labels?: JsonValue[];
+}>;
+type RepositoryHostContextInput = RepositoryHostContextStaticInput | PullRequestContextValue;
+type RepositoryHostContextTargetValue = number | string | RepositoryHostTarget | (JsonObject & {
+  number?: number | string;
+});
+interface RepositoryHostContextTarget {
+  host?: string;
+  issue?: RepositoryHostContextTargetValue;
+  number?: number | string;
+  owner?: string;
+  pullRequest?: RepositoryHostContextTargetValue;
+  repo?: string;
+  repository?: string;
+}
+type RepositoryHostContextResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = (context: AgentCapabilityContext<TRuntimeConfig, Name>) => MaybePromise<RepositoryHostContextInput | false | null | undefined>;
+type RepositoryHostContextTargetResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = (context: AgentCapabilityContext<TRuntimeConfig, Name>) => MaybePromise<RepositoryHostContextTarget | false | null | undefined>;
+interface RepositoryHostContextOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> {
+  client?: RepositoryHostClient | ((context: AgentCapabilityContext<TRuntimeConfig, Name>) => MaybePromise<RepositoryHostClient>);
+  context?: RepositoryHostContextInput | RepositoryHostContextResolver<TRuntimeConfig, Name>;
+  contextKey?: string;
+  id?: string;
+  materialize?: string;
+  provider?: RepositoryHostProvider;
+  target?: RepositoryHostContextTarget | RepositoryHostContextTargetResolver<TRuntimeConfig, Name>;
+  triggers?: Record<string, AgentTriggerDefinition<TRuntimeConfig, Name, any, any>>;
+}
+type RepositoryHostContextCapabilityTypeContract<TContextKey extends string = "repositoryHost"> = AgentCapabilityTypeContract & {
+  invocationContext: Record<TContextKey, AsyncRecord<RepositoryHostContextValue>>;
+};
+interface RepositoryHostContextCapabilityFactory {
+  <TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, const TContextKey extends string = "repositoryHost">(options?: RepositoryHostContextOptions<TRuntimeConfig, Name> & {
+    contextKey?: TContextKey;
+  }): AgentCapabilityDefinition<TRuntimeConfig, Name, RepositoryHostContextCapabilityTypeContract<TContextKey>>;
+  read(input: unknown, contextKey?: string): AsyncRecord<RepositoryHostContextValue>;
+}
+declare const repositoryHostContext: RepositoryHostContextCapabilityFactory;
+//#endregion
+export { RepositoryHostWriteRequest as A, RepositoryHostProvider as C, RepositoryHostTargetKind as D, RepositoryHostTarget as E, RepositoryHostToolPolicy as O, RepositoryHostOptions as S, RepositoryHostReadRequest as T, RepositoryHostContextTargetValue as _, PullRequestContextComment as a, repositoryHostContext as b, PullRequestContextRef as c, RepositoryHostContextCapabilityFactory as d, RepositoryHostContextInput as f, RepositoryHostContextTargetResolver as g, RepositoryHostContextTarget as h, JsonValue as i, repositoryHost as j, RepositoryHostWriteOperation as k, PullRequestContextUser as l, RepositoryHostContextResolver as m, JsonObject as n, PullRequestContextFile as o, RepositoryHostContextOptions as p, JsonPrimitive as r, PullRequestContextMetadata as s, AsyncRecord as t, PullRequestContextValue as u, RepositoryHostContextValue as v, RepositoryHostReadOperation as w, RepositoryHostClient as x, RepositoryHostIssueContext as y };
\ No newline at end of file
diff --git a/dist/routes-1Y1zW1HK.d.ts b/dist/routes-1Y1zW1HK.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..0d3cb47942f93f59b6fcd0afe34d929960f263de
--- /dev/null
+++ b/dist/routes-1Y1zW1HK.d.ts
@@ -0,0 +1,100 @@
+import { $r as MaybePromise, Rr as AgentWaitUntil, Vr as AgentWebhookStateResolver, cn as AgentInput, dr as AgentRuntimeContext, jt as AgentChatStateResolver, or as AgentRunMetadata, pr as AgentRuntimeName, sn as AgentHostIdentity, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
+import { l as AgentChatMessageTriggerInput } from "./chat-trigger-mwxwRC8l.js";
+
+//#region src/server/routes.d.ts
+interface ViteAgentRouteRuntimeConfig extends AgentRuntimeConfig {
+  agent?: unknown;
+}
+interface ViteAgentRouteRuntimeContext extends AgentRuntimeContext<ViteAgentRouteRuntimeConfig> {
+  request: Request;
+  runtime: AgentRuntimeName;
+  runtimeConfig: ViteAgentRouteRuntimeConfig;
+}
+interface AgentRouteRuntimeOptions {
+  capabilities?: ViteAgentRouteRuntimeContext["capabilities"];
+  agentIdentity?: AgentHostIdentity;
+  cloudflare?: ViteAgentRouteRuntimeContext["cloudflare"];
+  runtime?: AgentRuntimeName;
+  waitUntil?: AgentWaitUntil;
+}
+interface AgentChannelWebhookRouteOptions extends AgentRouteRuntimeOptions {
+  agentName?: string;
+  state?: AgentChatStateResolver<ViteAgentRouteRuntimeConfig>;
+  webhookState?: AgentWebhookStateResolver<ViteAgentRouteRuntimeConfig>;
+}
+interface AgentDiscordGatewayRouteOptions extends AgentRouteRuntimeOptions {
+  agentName?: string;
+  durationMs?: number;
+  state?: AgentChatStateResolver<ViteAgentRouteRuntimeConfig>;
+  webhookUrl: string | ((adapterName: string) => string);
+}
+interface AgentTelegramPollingRouteOptions extends AgentRouteRuntimeOptions {
+  agentName?: string;
+  state?: AgentChatStateResolver<ViteAgentRouteRuntimeConfig>;
+}
+interface AgentChannelChatRouteRequestOptions extends AgentRouteRuntimeOptions {
+  agentName?: string;
+}
+interface AgentChannelChatRouteStandardSchemaResultSuccess<T = unknown> {
+  issues?: undefined;
+  value: T;
+}
+interface AgentChannelChatRouteStandardSchemaResultFailure {
+  issues: readonly unknown[];
+}
+interface AgentChannelChatRouteStandardSchemaV1<T = unknown> {
+  "~standard": {
+    validate: (input: unknown) => AgentChannelChatRouteStandardSchemaResultSuccess<T> | AgentChannelChatRouteStandardSchemaResultFailure | Promise<AgentChannelChatRouteStandardSchemaResultSuccess<T> | AgentChannelChatRouteStandardSchemaResultFailure>;
+  };
+}
+interface AgentChannelChatRouteAdmissionContext<TBody extends AgentChannelChatRouteBody = AgentChannelChatRouteBody> {
+  agentName: string;
+  body: TBody;
+  rawBody: string;
+  request: Request;
+}
+type AgentChannelChatRouteInputPatch = Omit<Partial<AgentChatMessageTriggerInput>, "run"> & {
+  run?: Partial<AgentRunMetadata>;
+};
+type AgentChannelChatRouteTrustedInputField = "meta" | "session" | "timeout" | "user";
+interface AgentChannelChatRouteContext<TBody extends AgentChannelChatRouteBody = AgentChannelChatRouteBody, TAuth = unknown> extends AgentChannelChatRouteAdmissionContext<TBody> {
+  auth: Exclude<TAuth, false>;
+  input: AgentChatMessageTriggerInput;
+}
+interface AgentChannelChatRouteMapInputContext<TBody extends AgentChannelChatRouteBody = AgentChannelChatRouteBody, TAuth = unknown> extends AgentChannelChatRouteContext<TBody, TAuth> {}
+interface AgentChannelChatRouteAdmissionOptions<TBody extends AgentChannelChatRouteBody = AgentChannelChatRouteBody, TAuth = unknown> {
+  authenticate?: (context: AgentChannelChatRouteAdmissionContext) => MaybePromise<TAuth | false>;
+  body?: AgentChannelChatRouteStandardSchemaV1<TBody>;
+  context?: (context: AgentChannelChatRouteContext<TBody, TAuth>) => MaybePromise<AgentChannelChatRouteInputPatch | undefined | void>;
+}
+interface AgentChannelChatRouteInputOptions {
+  trust?: readonly AgentChannelChatRouteTrustedInputField[];
+}
+interface AgentChannelChatRouteHandlerOptions<TBody extends AgentChannelChatRouteBody = AgentChannelChatRouteBody, TAuth = unknown> {
+  admission?: AgentChannelChatRouteAdmissionOptions<TBody, TAuth>;
+  channelId?: string;
+  input?: AgentChannelChatRouteInputOptions;
+  mapInput?: (context: AgentChannelChatRouteMapInputContext<TBody, TAuth>) => MaybePromise<AgentChannelChatRouteInputPatch | undefined | void>;
+  origin?: string;
+}
+type AgentChannelChatRouteBody = {
+  id?: string;
+  invoker?: unknown;
+  invokerProfileId?: unknown;
+  messageId?: string;
+  meta?: unknown;
+  messages?: unknown;
+  run?: unknown;
+  session?: unknown;
+  timeout?: unknown;
+  trigger?: string;
+  triggerHistory?: AgentChatMessageTriggerInput["triggerHistory"];
+  user?: unknown;
+};
+declare function hasChannelChatRoute(agent: AgentInput<ViteAgentRouteRuntimeContext>): boolean;
+declare function createChannelChatRouteHandler(agent: AgentInput<ViteAgentRouteRuntimeContext>, options?: AgentChannelChatRouteHandlerOptions): (request: Request, options?: AgentChannelChatRouteRequestOptions) => Promise<Response>;
+declare function createChannelWebhookRouteHandler(agent: AgentInput<ViteAgentRouteRuntimeContext>): (request: Request, webhook?: string, options?: AgentChannelWebhookRouteOptions) => Promise<Response>;
+declare function createTelegramPollingRouteHandler(agent: AgentInput<ViteAgentRouteRuntimeContext>): (request: Request, options?: AgentTelegramPollingRouteOptions) => Promise<Response>;
+declare function createDiscordGatewayRouteHandler(agent: AgentInput<ViteAgentRouteRuntimeContext>): (request: Request, options: AgentDiscordGatewayRouteOptions) => Promise<Response>;
+//#endregion
+export { createChannelWebhookRouteHandler as _, AgentChannelChatRouteHandlerOptions as a, hasChannelChatRoute as b, AgentChannelChatRouteRequestOptions as c, AgentChannelChatRouteStandardSchemaV1 as d, AgentChannelChatRouteTrustedInputField as f, createChannelChatRouteHandler as g, AgentTelegramPollingRouteOptions as h, AgentChannelChatRouteContext as i, AgentChannelChatRouteStandardSchemaResultFailure as l, AgentDiscordGatewayRouteOptions as m, AgentChannelChatRouteAdmissionOptions as n, AgentChannelChatRouteInputOptions as o, AgentChannelWebhookRouteOptions as p, AgentChannelChatRouteBody as r, AgentChannelChatRouteMapInputContext as s, AgentChannelChatRouteAdmissionContext as t, AgentChannelChatRouteStandardSchemaResultSuccess as u, createDiscordGatewayRouteHandler as v, createTelegramPollingRouteHandler as y };
\ No newline at end of file
diff --git a/dist/routes-Bk8z0sOK.js b/dist/routes-Bk8z0sOK.js
new file mode 100644
index 0000000000000000000000000000000000000000..e4e1c5bd44a30c920e1d2261fc72bf27562e076b
--- /dev/null
+++ b/dist/routes-Bk8z0sOK.js
@@ -0,0 +1,2053 @@
+import { isAttachmentData } from "./messages.js";
+import { c as isAsyncIterable, t as deliveryArtifactAttachments } from "./delivery-artifacts-BOKCLStf.js";
+import { A as withResolvedAgentInvokerInput, B as parseStandardSchema, N as createAgentInvocationContextStore, d as normalizeCapabilities, k as resolveAgentInvoker, nt as messageChannelStateContextKey } from "./capability-runtime-Be85oBbC.js";
+import { a as streamAgentOutputToEvents, r as hasTraceableStreamResult, u as finalChannelOutputContextKey } from "./agent-output-CyEZfgrQ.js";
+import { p as toAgentPublicError } from "./trace-GIqXV-YM.js";
+import { D as assertChatDeliveryOptions, F as uiMessagesToAgentMessages, M as chatTriggerHistoryLimit, N as createChatMessageTriggerInput, P as resolveChatTriggerHistory, T as CHAT_FINISH_EXTENSION_CONTEXT_KEY, j as getChatCapabilityOptions } from "./channels-D6wOMJN1.js";
+import { t as isWorkflowRun } from "./http-response-BHcgRXcy.js";
+import { f as createAgentUIMessageStreamResponse, x as agentOutputEventObserverContextKey } from "./agent-invocation-D9tJRj_l.js";
+import { C as isResolvedAgentTriggerHandledInvocation, E as toHttpErrorResponse, T as AgentHttpError, c as runAgent, h as streamAgentTrigger, l as runAgentInline, o as resolveAgentTriggerInvocation, p as streamAgent, s as resolveAgentTriggers, w as verifyAgentWebhookRequest } from "./src-CktB4unw.js";
+import { n as loadAgentWorkflowRuntimeStateModule } from "./workflow-runtime-loaders-T5jPFIAJ.js";
+import { t as createAgentRuntimeContext } from "./context-KghRPYRx.js";
+import { r as runWithActiveCloudflareEnv } from "./cloudflare-env-fCklgYbd.js";
+import { createRuntimeWaitUntilController } from "@vite-hub/runtime";
+import { Chat, StreamingPlan, convertEmojiPlaceholders } from "chat";
+//#region src/capabilities/access-metadata.ts
+function isAccessMetadata(value) {
+	return typeof value === "object" && value !== null && value.kind === "access" && typeof value.access === "object" && value.access !== null;
+}
+function getAccessCapabilityOptions(capabilities) {
+	return capabilities.map((capability) => capability.id === "access" && isAccessMetadata(capability.metadata) ? capability.metadata.access : void 0).filter((options) => !!options);
+}
+//#endregion
+//#region src/server/routes.ts
+const defaultChatErrorFallbackText = "Sorry, I couldn't process that message.";
+const chatFinishMessagesKey = Symbol("vitehub.chat.finish.messages");
+const chatNativeStreamUpdateIntervalMs = 1;
+const chatTypingRefreshIntervalMs = 4e3;
+const chatTypingRefreshTimeoutMs = 2e3;
+const vercelFunctionsPackage = "@vercel/functions";
+function createRouteError(statusCode, message) {
+	return new AgentHttpError(statusCode, message);
+}
+function createRouteBodyError(message) {
+	return createRouteError(400, message);
+}
+function firstString(...values) {
+	return values.find((value) => typeof value === "string" && value.length > 0);
+}
+function readableStreamFromResult(value) {
+	if (value instanceof ReadableStream) return value;
+	if (value instanceof Response && value.body) return value.body;
+	throw new Error("[vitehub] Agent chat trigger expected a UI message stream.");
+}
+function isUiMessageStreamResponse(response) {
+	return response.headers.get("x-vercel-ai-ui-message-stream") === "v1";
+}
+function withCleanUiMessageStreamResponse(response) {
+	if (!response.body || !isUiMessageStreamResponse(response)) return response;
+	const doneFrame = new TextEncoder().encode("data: [DONE]\n\n");
+	const decoder = new TextDecoder();
+	const reader = response.body.getReader();
+	let tail = "";
+	const headers = new Headers(response.headers);
+	headers.delete("content-encoding");
+	headers.delete("content-length");
+	function enqueueDone(controller) {
+		if (!/(^|\r?\n)data: \[DONE]\r?\n\r?\n$/.test(tail)) controller.enqueue(doneFrame);
+	}
+	return new Response(new ReadableStream({
+		async pull(controller) {
+			try {
+				const result = await reader.read();
+				if (result.done) {
+					enqueueDone(controller);
+					controller.close();
+					return;
+				}
+				tail = `${tail}${decoder.decode(result.value, { stream: true })}`.slice(-128);
+				controller.enqueue(result.value);
+			} catch (error) {
+				controller.error(error);
+			}
+		},
+		async cancel(reason) {
+			await reader.cancel(reason);
+		}
+	}), {
+		headers,
+		status: response.status,
+		statusText: response.statusText
+	});
+}
+function createJsonErrorResponse(status, message) {
+	return Response.json({
+		error: true,
+		status,
+		statusText: message,
+		message
+	}, { status });
+}
+function createBadRequest(message) {
+	return createJsonErrorResponse(400, message);
+}
+const errorLogMaxDepth = 5;
+const errorLogMaxNodes = 100;
+const errorLogMaxProperties = 20;
+const errorLogMaxStringLength = 2048;
+const errorLogStringBudget = 12e3;
+function serializeLogString(value, state) {
+	if (value.startsWith("data:")) return `[Data URL ${value.length} characters]`;
+	if (state.remainingStringCharacters <= 0) return "[Truncated]";
+	const available = Math.min(errorLogMaxStringLength, state.remainingStringCharacters);
+	if (value.length <= available) {
+		state.remainingStringCharacters -= value.length;
+		return value;
+	}
+	const suffix = `… [${value.length - available} chars omitted]`;
+	const result = available > suffix.length ? `${value.slice(0, available - suffix.length)}${suffix}` : "[Truncated]";
+	state.remainingStringCharacters -= result.length;
+	return result;
+}
+function serializeErrorForLog(value, state = {
+	nodes: 0,
+	remainingStringCharacters: errorLogStringBudget,
+	seen: /* @__PURE__ */ new WeakSet()
+}, depth = 0) {
+	if (typeof value === "string") return serializeLogString(value, state);
+	if (value === null || typeof value === "number" || typeof value === "boolean" || typeof value === "undefined") return value;
+	if (typeof value === "bigint" || typeof value === "symbol" || typeof value === "function") return serializeLogString(String(value), state);
+	if (typeof value !== "object") return serializeLogString(String(value), state);
+	if (value instanceof ArrayBuffer) return `[ArrayBuffer ${value.byteLength} bytes]`;
+	if (ArrayBuffer.isView(value)) return `[${value.constructor.name} ${value.byteLength} bytes]`;
+	if (value instanceof Blob) return `[Blob ${value.size} bytes, ${value.type || "unknown type"}]`;
+	if (value instanceof Date) return value.toISOString();
+	if (state.seen.has(value)) return "[Circular]";
+	if (depth >= errorLogMaxDepth || state.nodes >= errorLogMaxNodes) return "[Truncated]";
+	state.seen.add(value);
+	state.nodes++;
+	if (Array.isArray(value)) {
+		const output = value.slice(0, errorLogMaxProperties).map((item) => serializeErrorForLog(item, state, depth + 1));
+		if (value.length > output.length) output.push(`[${value.length - output.length} items omitted]`);
+		return output;
+	}
+	const output = {};
+	const source = value;
+	if (value instanceof Error) {
+		output.message = serializeErrorForLog(value.message, state, depth + 1);
+		output.name = serializeErrorForLog(value.name, state, depth + 1);
+		if (value.stack) output.stack = serializeErrorForLog(value.stack, state, depth + 1);
+	}
+	const keys = Object.keys(value).filter((key) => !(value instanceof Error && (key === "message" || key === "name" || key === "stack" || key === "cause"))).slice(0, errorLogMaxProperties);
+	for (const key of keys) {
+		const outputKey = key.length > 128 ? `${key.slice(0, 112)}… [key truncated]` : key;
+		try {
+			output[outputKey] = serializeErrorForLog(source[key], state, depth + 1);
+		} catch {
+			output[outputKey] = "[Unserializable property]";
+		}
+	}
+	if (Object.keys(value).length > keys.length) output["[truncated]"] = `${Object.keys(value).length - keys.length} properties omitted`;
+	if (value instanceof Error && "cause" in value && typeof value.cause !== "undefined") output.cause = serializeErrorForLog(value.cause, state, depth + 1);
+	return output;
+}
+function detectRuntime() {
+	if ("Deno" in globalThis) return "deno";
+	if ((typeof process === "object" && process ? process.env : void 0)?.VERCEL) return "vercel";
+	return "unknown";
+}
+function routeAgentIdentity(options) {
+	return options.agentIdentity || (options.agentName ? { name: options.agentName } : void 0);
+}
+function createRuntimeContext(request, run, waitUntil, cloudflare, runtimeOverride, capabilities, agentIdentity) {
+	const waitUntilController = createRuntimeWaitUntilController({ forward: waitUntil });
+	const runtime = cloudflare ? "cloudflare-agents" : runtimeOverride || detectRuntime();
+	return createAgentRuntimeContext({
+		...capabilities ? { capabilities } : {},
+		...agentIdentity ? { agentIdentity } : {},
+		...cloudflare ? { cloudflare } : {},
+		flushWaitUntil: waitUntilController.flushWaitUntil,
+		request,
+		...run ? { run } : {},
+		runtime,
+		runtimeConfig: {},
+		...runtime === "vercel" && waitUntil ? { vercel: { waitUntil } } : {},
+		waitUntil: waitUntilController.waitUntil
+	});
+}
+function createRuntimeRequest(request, body) {
+	return new Request(request.url, {
+		...body ? { body } : {},
+		headers: request.headers,
+		method: request.method
+	});
+}
+async function runWithRuntimeCloudflareEnv(context, callback) {
+	if (!context.cloudflare?.env) return callback();
+	return await runWithActiveCloudflareEnv(context.cloudflare.env, callback);
+}
+async function resolveRuntimeWaitUntil(waitUntil) {
+	if (detectRuntime() !== "vercel") return waitUntil;
+	return (await import(
+		/* @vite-ignore */
+		vercelFunctionsPackage
+).catch(() => void 0))?.waitUntil || waitUntil;
+}
+function isRecord(value) {
+	return typeof value === "object" && value !== null;
+}
+function isResolvableObject(value) {
+	return isRecord(value) && typeof value.resolve === "function";
+}
+async function resolveMaybe(value, context) {
+	if (value === void 0) return void 0;
+	if (typeof value === "function") return await value(context);
+	if (isResolvableObject(value)) return await value.resolve(context);
+	return value;
+}
+function getAgentCapabilities(agent) {
+	if (!isRecord(agent)) return [];
+	const definition = agent;
+	const capabilities = definition.capabilities || definition.__vitehubWorkspaceAgentOptions?.capabilities;
+	return normalizeCapabilities(Array.isArray(capabilities) ? capabilities : void 0);
+}
+function getAgentChatOptions(agent) {
+	if (!isRecord(agent)) return void 0;
+	const definition = agent;
+	return getChatCapabilityOptions(getAgentCapabilities(agent)) || definition.chat;
+}
+function getChannelChatOptions(agent, channelId, options) {
+	if (!channelId || !isRecord(agent) || !isRecord(agent.channels)) return options;
+	const channel = agent.channels[channelId];
+	if (!isRecord(channel) || !isRecord(channel.messages)) return options;
+	return {
+		...options,
+		...channel.messages
+	};
+}
+function hasExplicitNonStreamingMessages(agent, channelId) {
+	if (!isRecord(agent)) return false;
+	if (isRecord(agent.messages) && agent.messages.stream === false) return true;
+	if (!isRecord(agent.channels)) return true;
+	if (channelId) {
+		const channel = agent.channels[channelId];
+		return isRecord(channel) && isRecord(channel.messages) && channel.messages.stream === false;
+	}
+	const channels = Object.values(agent.channels);
+	if (!channels.some((channel) => isRecord(channel) && channel.adapter && channel.messages !== false)) return true;
+	return channels.some((channel) => isRecord(channel) && isRecord(channel.messages) && channel.messages.stream === false);
+}
+function normalizeWebhookPath(path) {
+	const normalized = path.startsWith("/") ? path : `/${path}`;
+	return normalized.length > 1 ? normalized.replace(/\/+$/, "") : normalized;
+}
+function webhookRegistrationPathMatches(request, registration) {
+	return typeof registration.path === "string" && normalizeWebhookPath(new URL(request.url).pathname) === normalizeWebhookPath(registration.path);
+}
+async function findAgentWebhookRegistration(agent, context, request, webhook) {
+	const triggers = await resolveAgentTriggers(agent, context);
+	const registrations = [];
+	for (const trigger of Object.values(triggers)) for (const registration of trigger.webhooks || []) {
+		const match = {
+			registration,
+			trigger
+		};
+		registrations.push(match);
+	}
+	const pathMatches = registrations.filter((match) => webhookRegistrationPathMatches(request, match.registration));
+	if (pathMatches.length === 1) return pathMatches[0];
+	if (pathMatches.length > 1) return void 0;
+	if (webhook === "" && registrations.length === 1) return registrations[0];
+	if (!webhook) return void 0;
+	const directMatches = registrations.filter(({ registration }) => registration.id === webhook || registration.channelId === webhook && registration.id === registration.channelId);
+	if (directMatches.length === 1) return directMatches[0];
+	if (directMatches.length > 1) return void 0;
+	const providerMatches = registrations.filter(({ registration }) => registration.provider === webhook);
+	return providerMatches.length === 1 ? providerMatches[0] : void 0;
+}
+async function matchedWebhookRegistrationRequiresVerification(registration, context, requireConfiguredSecret) {
+	if (registration.secretToken !== void 0) return await resolveMaybe(registration.secretToken, context) !== false;
+	return requireConfiguredSecret && registration.secretHeader !== void 0;
+}
+function parseWebhookPayload(body) {
+	if (!body) return void 0;
+	try {
+		return JSON.parse(body);
+	} catch {
+		return;
+	}
+}
+function requestHeaders(request) {
+	return Object.fromEntries(request.headers.entries());
+}
+function githubInstallationId(payload) {
+	if (!payload || typeof payload !== "object") return;
+	const installation = payload.installation;
+	if (!installation || typeof installation !== "object") return;
+	const id = installation.id;
+	return typeof id === "number" ? id : void 0;
+}
+async function createAgentWebhookTriggerInput(request, registration) {
+	const body = await request.clone().text();
+	const payload = parseWebhookPayload(body);
+	const headers = requestHeaders(request);
+	const webhook = {
+		...registration.channelId ? { channelId: registration.channelId } : {},
+		...registration.id ? { id: registration.id } : {},
+		...registration.path ? { path: registration.path } : {},
+		provider: registration.provider
+	};
+	return {
+		body,
+		payload,
+		provider: registration.provider,
+		request: {
+			headers,
+			method: request.method,
+			url: request.url
+		},
+		webhook,
+		...registration.provider === "github" ? { github: {
+			...headers["x-github-delivery"] ? { deliveryId: headers["x-github-delivery"] } : {},
+			...headers["x-github-event"] ? { event: headers["x-github-event"] } : {},
+			...headers["x-github-hook-id"] ? { hookId: headers["x-github-hook-id"] } : {},
+			...headers["x-github-hook-installation-target-id"] ? { hookInstallationTargetId: headers["x-github-hook-installation-target-id"] } : {},
+			...headers["x-github-hook-installation-target-type"] ? { hookInstallationTargetType: headers["x-github-hook-installation-target-type"] } : {},
+			...githubInstallationId(payload) ? { installationId: githubInstallationId(payload) } : {}
+		} } : {}
+	};
+}
+const defaultWebhookConcurrencyTtlMs = 3e4;
+async function resolveAgentWebhookState(context, registration, handlerOptions) {
+	const stateOption = handlerOptions.webhookState;
+	if (!stateOption) return;
+	const agentName = routeAgentIdentity(handlerOptions)?.name || "agent";
+	const origin = chatRegistrationOrigin(registration);
+	const keyPrefix = `webhook:${agentName}:${origin}:${registration.id || registration.path || origin}:`;
+	const state = await resolveMaybe(stateOption, {
+		...context,
+		webhook: {
+			agentName,
+			...registration.channelId ? { channelId: registration.channelId } : {},
+			provider: registration.provider,
+			stateKeyPrefix: keyPrefix
+		}
+	});
+	if (!state) return;
+	await state.connect();
+	return {
+		keyPrefix,
+		state
+	};
+}
+function webhookOwnershipKey(prefix, kind, value) {
+	return `${prefix}${kind}:${value}`;
+}
+function positiveWebhookDuration(value, fallback, name) {
+	const duration = value ?? fallback;
+	if (!Number.isFinite(duration) || duration <= 0) throw new TypeError(`[vitehub] Webhook delivery ownership ${name} must be a positive finite number.`);
+	return duration;
+}
+async function hasActiveWorkflowRuntime(agent, context) {
+	if (!isRecord(agent) || !isRecord(agent.runtime) || agent.runtime.kind !== "workflow") return false;
+	if (agent.runtime.discoveryDefault !== true) return true;
+	if (!context.agentIdentity || Object.values(context.capabilities || {}).some((capability) => capability !== false)) return false;
+	try {
+		return Boolean((await loadAgentWorkflowRuntimeStateModule()).getWorkflowRuntimeConfig());
+	} catch (error) {
+		if (error.code === "ERR_MODULE_NOT_FOUND") return false;
+		throw error;
+	}
+}
+function startWebhookLockHeartbeat(state, lock, ttlMs, onLost) {
+	const intervalMs = Math.max(1, Math.floor(ttlMs / 2));
+	let stopped = false;
+	let timer;
+	const extend = async () => {
+		if (stopped) return;
+		try {
+			if (!await state.extendLock(lock, ttlMs)) {
+				stopped = true;
+				onLost();
+			}
+		} catch {
+			stopped = true;
+			onLost();
+		}
+		if (!stopped) timer = setTimeout(extend, intervalMs);
+	};
+	timer = setTimeout(extend, intervalMs);
+	return () => {
+		stopped = true;
+		if (timer) clearTimeout(timer);
+	};
+}
+async function resolveChatAdapters(options, context) {
+	const adapters = await resolveMaybe(options?.platforms, context);
+	const resolved = {};
+	for (const [name, adapter] of Object.entries(adapters || {})) {
+		const value = await resolveMaybe(adapter, context);
+		if (value) resolved[name] = value;
+	}
+	return resolved;
+}
+function resolveChatAdapterName(adapters, registration) {
+	if (registration.adapter && adapters[registration.adapter]) return registration.adapter;
+	if (registration.channelId && adapters[registration.channelId]) return registration.channelId;
+	if (adapters[registration.provider]) return registration.provider;
+	if (registration.id && adapters[registration.id]) return registration.id;
+}
+function resolveDiscordAdapters(adapters) {
+	return Object.entries(adapters).filter(([name, adapter]) => name === "discord" || adapter.name === "discord");
+}
+async function resolveDiscordWebhookRegistration(agent, context, adapters, adapterName) {
+	const triggers = await resolveAgentTriggers(agent, context);
+	const matches = Object.values(triggers).flatMap((trigger) => (trigger.webhooks || []).filter((registration) => registration.provider === "discord" && resolveChatAdapterName(adapters, registration) === adapterName));
+	return matches.length === 1 ? matches[0] : void 0;
+}
+function chatRegistrationOrigin(registration) {
+	return registration.channelId || registration.provider;
+}
+function objectWithoutUndefined(value) {
+	return Object.fromEntries(Object.entries(value).filter(([, item]) => item !== void 0));
+}
+function fallbackWebhookFromRequest(request) {
+	return new URL(request.url).pathname.split("/").filter(Boolean).at(-1) || void 0;
+}
+async function collectAgentOutput(result, onProgress) {
+	if (result instanceof Response) {
+		if (result.headers.get("content-type")?.includes("application/json")) {
+			const body = await result.clone().json().catch(() => void 0);
+			if (isRecord(body) && typeof body.text === "string") return body.text;
+		}
+		return await result.text();
+	}
+	if (result && typeof result === "object" && !isAsyncIterable(result) && !hasTraceableStreamResult(result)) {
+		const descriptor = Object.getOwnPropertyDescriptor(result, "text");
+		const text = descriptor && "value" in descriptor ? descriptor.value : void 0;
+		if (typeof text === "string") return text.trim();
+	}
+	let explicitPhaseSeen = false;
+	let finalText = "";
+	let unphasedText = "";
+	for await (const event of streamAgentOutputToEvents(result)) {
+		if (event.type === "text-delta") if (event.phase === void 0) {
+			if (!explicitPhaseSeen) unphasedText += event.text;
+		} else {
+			explicitPhaseSeen = true;
+			unphasedText = "";
+			if (event.phase === "final") finalText += event.text;
+		}
+		const summary = progressSummaryFromEvent(event);
+		if (summary) onProgress?.(summary);
+		if (event.type === "error") throw new Error(event.error);
+	}
+	return (explicitPhaseSeen ? finalText : unphasedText).trim();
+}
+const manualDeliveryProgressDrainTimeoutMs = 100;
+function progressSummaryFromEvent(event) {
+	if (!isRecord(event) || event.type !== "data-progress-summary") return;
+	return (isRecord(event.data) && typeof event.data.summary === "string" ? event.data.summary.trim() : "") || void 0;
+}
+function createManualDeliveryProgressUpdater(manualDelivery, waitUntil) {
+	let latest;
+	let active = true;
+	let draining;
+	const drain = async () => {
+		while (active && latest && manualDelivery.placeholder) {
+			const summary = latest;
+			latest = void 0;
+			await replaceManualDeliveryPlaceholder(manualDelivery.placeholder, { markdown: summary }).catch(() => false);
+		}
+	};
+	const startDrain = () => {
+		if (draining) return;
+		draining = drain().finally(() => {
+			draining = void 0;
+			if (active && latest && manualDelivery.placeholder) startDrain();
+		});
+	};
+	return {
+		async finish() {
+			active = false;
+			if (!draining) return;
+			let timeout;
+			const drained = await Promise.race([draining.then(() => true), new Promise((resolve) => {
+				timeout = setTimeout(() => resolve(false), manualDeliveryProgressDrainTimeoutMs);
+			})]);
+			if (timeout) clearTimeout(timeout);
+			if (drained || !manualDelivery.placeholder) return;
+			const stalePlaceholder = manualDelivery.placeholder;
+			manualDelivery.placeholder = void 0;
+			waitUntil(draining.then(async () => {
+				await deleteManualDeliveryPlaceholder(stalePlaceholder).catch(() => void 0);
+			}));
+		},
+		update(summary) {
+			if (!manualDelivery.placeholder) return;
+			latest = summary;
+			startDrain();
+		}
+	};
+}
+const discordMaxContentLength = 2e3;
+const discordLongContentModeSymbol = Symbol.for("vitehub.discord.longContent.mode");
+function startChatTypingRefresh(thread, context) {
+	let stopped = false;
+	let timeout;
+	let wake;
+	const sleep = (ms) => new Promise((resolve) => {
+		wake = resolve;
+		timeout = setTimeout(() => {
+			timeout = void 0;
+			wake = void 0;
+			resolve();
+		}, ms);
+	});
+	const boundedStartTyping = async () => {
+		let limit;
+		try {
+			await Promise.race([thread.startTyping().catch(() => void 0), new Promise((resolve) => {
+				limit = setTimeout(resolve, chatTypingRefreshTimeoutMs);
+			})]);
+		} finally {
+			if (limit) clearTimeout(limit);
+		}
+	};
+	const task = (async () => {
+		while (!stopped) {
+			await boundedStartTyping();
+			if (!stopped) await sleep(chatTypingRefreshIntervalMs);
+		}
+	})();
+	context.waitUntil(task);
+	return { stop() {
+		if (stopped) return;
+		stopped = true;
+		if (timeout) {
+			clearTimeout(timeout);
+			timeout = void 0;
+		}
+		wake?.();
+		wake = void 0;
+	} };
+}
+function createChatTextStream() {
+	const chunks = [];
+	let collected = "";
+	let discarded = false;
+	let failure;
+	let finished = false;
+	let wake;
+	const notify = () => {
+		wake?.();
+		wake = void 0;
+	};
+	return {
+		close() {
+			if (finished) return;
+			finished = true;
+			notify();
+		},
+		discard() {
+			discarded = true;
+			chunks.length = 0;
+			finished = true;
+			notify();
+		},
+		fail(error) {
+			if (finished) return;
+			failure = error;
+			notify();
+		},
+		stream: {
+			async *[Symbol.asyncIterator]() {
+				try {
+					for (;;) {
+						if (chunks.length) {
+							yield chunks.shift();
+							continue;
+						}
+						if (failure !== void 0) throw failure;
+						if (finished) return;
+						await new Promise((resolve) => {
+							wake = resolve;
+						});
+					}
+				} finally {
+					if (!finished) {
+						discarded = true;
+						chunks.length = 0;
+					}
+				}
+			},
+			getText: () => collected
+		},
+		write(text) {
+			if (!text || discarded || finished) return;
+			collected += text;
+			chunks.push(text);
+			notify();
+		}
+	};
+}
+function streamAgentOutputToChatText(result) {
+	let collected = "";
+	return {
+		async *[Symbol.asyncIterator]() {
+			const output = await result;
+			if (output instanceof Response) {
+				if (output.headers.get("content-type")?.includes("application/json")) {
+					const body = await output.clone().json().catch(() => void 0);
+					if (isRecord(body)) {
+						if (typeof body.text === "string") {
+							collected += body.text;
+							yield body.text;
+						}
+						return;
+					}
+				}
+				const bodyText = await output.text();
+				collected += bodyText;
+				yield bodyText;
+				return;
+			}
+			for await (const event of streamAgentOutputToEvents(output)) {
+				if (event.type === "text-delta") {
+					collected += event.text;
+					yield event.text;
+				}
+				if (event.type === "error") throw new Error(event.error);
+			}
+		},
+		getText: () => collected
+	};
+}
+function streamAgentOutputToChatReplies(result, options) {
+	const commentary = createChatTextStream();
+	const final = createChatTextStream();
+	let commentaryStarted = false;
+	let finalStarted = false;
+	let explicitPhaseSeen = false;
+	const unphasedText = [];
+	const startFinal = () => {
+		if (finalStarted) return;
+		finalStarted = true;
+		commentary.close();
+		options.onFinal(final.stream);
+	};
+	return { completion: (async () => {
+		try {
+			const output = await result;
+			const events = output instanceof Response ? (async function* () {
+				for await (const text of streamAgentOutputToChatText(Promise.resolve(output))) yield {
+					phase: void 0,
+					text,
+					type: "text-delta"
+				};
+			})() : streamAgentOutputToEvents(output);
+			for await (const event of events) {
+				if (event.type === "error") throw new Error(event.error);
+				if (event.type !== "text-delta" || !event.text) continue;
+				if (event.phase === void 0) {
+					if (!explicitPhaseSeen) unphasedText.push(event.text);
+					continue;
+				}
+				explicitPhaseSeen = true;
+				unphasedText.length = 0;
+				if (event.phase === "commentary" && !finalStarted) {
+					if (options.commentary === "message" && !commentaryStarted) {
+						commentaryStarted = true;
+						options.onCommentary(commentary.stream, commentary.discard);
+					}
+					if (commentaryStarted) commentary.write(event.text);
+					continue;
+				}
+				if (event.phase !== "final") continue;
+				startFinal();
+				final.write(event.text);
+			}
+			if (!explicitPhaseSeen && unphasedText.length) {
+				startFinal();
+				for (const text of unphasedText) final.write(text);
+			}
+		} catch (error) {
+			commentary.fail(error);
+			final.fail(error);
+			throw error;
+		} finally {
+			commentary.close();
+			final.close();
+		}
+	})() };
+}
+function chatStreamPostable(thread, response) {
+	return thread.adapter.stream ? new StreamingPlan(response, { updateIntervalMs: chatNativeStreamUpdateIntervalMs }) : response;
+}
+function discordLongContentMode(adapter) {
+	return adapter[discordLongContentModeSymbol];
+}
+function splitContentAtLimit(content, limit) {
+	const parts = [];
+	let rest = content.trimEnd();
+	while (rest.length > limit) {
+		let index = -1;
+		for (const marker of ["\n\n", "\n"]) {
+			index = rest.lastIndexOf(marker, limit);
+			if (index > 0) {
+				index += marker.length;
+				break;
+			}
+		}
+		if (index <= 0) {
+			for (let i = limit; i > 0; i--) if (/\s/.test(rest[i] || "")) {
+				index = i + 1;
+				break;
+			}
+		}
+		if (index <= 0) index = limit;
+		parts.push(rest.slice(0, index).trimEnd());
+		rest = rest.slice(index).trimStart();
+	}
+	if (rest) parts.push(rest);
+	return parts;
+}
+function discordContentParts(content) {
+	if (content.length <= discordMaxContentLength) return [content];
+	let total = Math.ceil(content.length / (discordMaxContentLength - 6));
+	while (true) {
+		const markerLength = ` (${total}/${total})`.length;
+		const parts = splitContentAtLimit(content, discordMaxContentLength - markerLength);
+		if (parts.length === total) return parts.map((part, index) => `${part} (${index + 1}/${total})`);
+		total = parts.length;
+	}
+}
+function hasChatFiles(postable) {
+	if (typeof postable !== "object" || postable === null) return false;
+	const value = postable;
+	return Array.isArray(value.attachments) && value.attachments.length > 0 || Array.isArray(value.files) && value.files.length > 0;
+}
+function renderDiscordPostable(adapter, postable) {
+	if (hasChatFiles(postable)) return void 0;
+	if (typeof postable === "object" && postable !== null && ("card" in postable || "type" in postable)) return void 0;
+	const converter = adapter.formatConverter;
+	if (converter?.renderPostable) return convertEmojiPlaceholders(converter.renderPostable(postable), "discord");
+	if (typeof postable === "string") return postable;
+	if (typeof postable === "object" && postable !== null && "raw" in postable && typeof postable.raw === "string") return postable.raw;
+	if (typeof postable === "object" && postable !== null && "markdown" in postable && typeof postable.markdown === "string") return postable.markdown;
+}
+async function postDiscordSplitContent(thread, postable) {
+	if (discordLongContentMode(thread.adapter) !== "split") return false;
+	const rendered = renderDiscordPostable(thread.adapter, postable);
+	if (!rendered || rendered.length <= discordMaxContentLength) return false;
+	for (const part of discordContentParts(rendered)) await thread.post({ raw: part });
+	return true;
+}
+async function finishDiscordSplitStream(thread, sent, markdown) {
+	if (!markdown || discordLongContentMode(thread.adapter) !== "split") return;
+	const rendered = renderDiscordPostable(thread.adapter, { markdown });
+	if (!rendered || rendered.length <= discordMaxContentLength) return;
+	const [first, ...rest] = discordContentParts(rendered);
+	if (first && sent && typeof sent === "object" && "edit" in sent && typeof sent.edit === "function") await sent.edit({
+		attachments: [],
+		raw: first
+	});
+	else if (first) await thread.post({ raw: first });
+	for (const part of rest) await thread.post({ raw: part });
+}
+function sentMessageText(sent) {
+	return sent && typeof sent === "object" && "text" in sent && typeof sent.text === "string" ? sent.text : "";
+}
+async function postChatStream(thread, response, fallback, waitUntil) {
+	let sent;
+	if (fallback === void 0) {
+		sent = await thread.post(chatStreamPostable(thread, response));
+		await finishDiscordSplitStream(thread, sent, response.getText() || sentMessageText(sent));
+		return;
+	}
+	if (thread.adapter.stream) {
+		const adapter = thread.adapter;
+		const nativeStream = adapter.stream.bind(adapter);
+		const placeholder = fallback === null ? Promise.resolve(void 0) : adapter.postMessage(thread.id, fallback).catch(() => void 0);
+		let cleared = false;
+		let clearing;
+		let clearRequested = false;
+		const clearPlaceholder = async () => {
+			if (cleared) return;
+			if (clearing) return clearing;
+			clearing = placeholder.then(async (message) => {
+				if (!message?.id) return;
+				await adapter.deleteMessage(message.threadId || thread.id, message.id);
+				cleared = true;
+			}).catch(() => void 0).finally(() => {
+				clearing = void 0;
+			});
+			return clearing;
+		};
+		const finishPlaceholder = () => {
+			waitUntil(clearPlaceholder().then(() => cleared ? void 0 : clearPlaceholder()));
+		};
+		const nativeResponse = {
+			getText: response.getText,
+			async *[Symbol.asyncIterator]() {
+				for await (const chunk of response) {
+					if (!clearRequested) {
+						clearRequested = true;
+						clearPlaceholder();
+					}
+					yield chunk;
+				}
+			}
+		};
+		const chatThread = thread;
+		const previousAdapter = chatThread._adapter;
+		const previousFallback = chatThread._fallbackStreamingPlaceholderText;
+		chatThread._adapter = new Proxy(adapter, { get(target, property) {
+			if (property === "stream") return async (...args) => {
+				return await nativeStream(...args);
+			};
+			if (property === "postMessage" && fallback !== null) return async (...args) => {
+				if (args[0] === thread.id && args[1] === fallback) {
+					const message = await placeholder;
+					if (message) {
+						cleared = true;
+						return message;
+					}
+				}
+				return adapter.postMessage(...args);
+			};
+			const value = Reflect.get(target, property, target);
+			return typeof value === "function" ? value.bind(target) : value;
+		} });
+		chatThread._fallbackStreamingPlaceholderText = fallback;
+		try {
+			sent = await thread.post(chatStreamPostable(thread, nativeResponse));
+			await finishDiscordSplitStream(thread, sent, response.getText() || sentMessageText(sent));
+		} finally {
+			chatThread._adapter = previousAdapter;
+			chatThread._fallbackStreamingPlaceholderText = previousFallback;
+			finishPlaceholder();
+		}
+		return;
+	}
+	const chatThread = thread;
+	const previous = chatThread._fallbackStreamingPlaceholderText;
+	chatThread._fallbackStreamingPlaceholderText = fallback;
+	try {
+		sent = await thread.post(chatStreamPostable(thread, response));
+		await finishDiscordSplitStream(thread, sent, response.getText() || sentMessageText(sent));
+	} finally {
+		chatThread._fallbackStreamingPlaceholderText = previous;
+	}
+}
+function randomToken() {
+	return globalThis.crypto?.randomUUID?.() || `${Date.now()}-${Math.random().toString(36).slice(2)}`;
+}
+function isExpired(expiresAt) {
+	return typeof expiresAt === "number" && expiresAt <= Date.now();
+}
+var ViteHubInMemoryChatStateAdapter = class {
+	cache = /* @__PURE__ */ new Map();
+	connected = false;
+	lists = /* @__PURE__ */ new Map();
+	locks = /* @__PURE__ */ new Map();
+	queues = /* @__PURE__ */ new Map();
+	subscriptions = /* @__PURE__ */ new Set();
+	async acquireLock(threadId, ttlMs) {
+		this.ensureConnected();
+		const existing = this.locks.get(threadId);
+		if (existing && !isExpired(existing.expiresAt)) return null;
+		const lock = {
+			expiresAt: Date.now() + ttlMs,
+			threadId,
+			token: randomToken()
+		};
+		this.locks.set(threadId, lock);
+		return lock;
+	}
+	async appendToList(key, value, options) {
+		this.ensureConnected();
+		const expiresAt = options?.ttlMs ? Date.now() + options.ttlMs : void 0;
+		const list = (this.lists.get(key) || []).filter((item) => !isExpired(item.expiresAt));
+		list.push({
+			expiresAt,
+			value
+		});
+		if (options?.maxLength && options.maxLength > 0) {
+			this.lists.set(key, list.slice(-options.maxLength));
+			return;
+		}
+		this.lists.set(key, list);
+	}
+	async connect() {
+		this.connected = true;
+	}
+	async delete(key) {
+		this.ensureConnected();
+		this.cache.delete(key);
+		this.lists.delete(key);
+	}
+	async dequeue(threadId) {
+		this.ensureConnected();
+		const queue = (this.queues.get(threadId) || []).filter((entry) => !isExpired(entry.expiresAt));
+		const entry = queue.shift() || null;
+		this.queues.set(threadId, queue);
+		return entry;
+	}
+	async disconnect() {
+		this.connected = false;
+	}
+	async enqueue(threadId, entry, maxSize) {
+		this.ensureConnected();
+		const queue = (this.queues.get(threadId) || []).filter((item) => !isExpired(item.expiresAt));
+		queue.push(entry);
+		const trimmed = maxSize > 0 ? queue.slice(-maxSize) : queue;
+		this.queues.set(threadId, trimmed);
+		return trimmed.length;
+	}
+	async extendLock(lock, ttlMs) {
+		this.ensureConnected();
+		const existing = this.locks.get(lock.threadId);
+		if (!existing || existing.token !== lock.token || isExpired(existing.expiresAt)) return false;
+		existing.expiresAt = Date.now() + ttlMs;
+		return true;
+	}
+	async forceReleaseLock(threadId) {
+		this.ensureConnected();
+		this.locks.delete(threadId);
+	}
+	async get(key) {
+		this.ensureConnected();
+		const item = this.cache.get(key);
+		if (!item || isExpired(item.expiresAt)) {
+			this.cache.delete(key);
+			return null;
+		}
+		return item.value;
+	}
+	async getList(key) {
+		this.ensureConnected();
+		const list = (this.lists.get(key) || []).filter((item) => !isExpired(item.expiresAt));
+		this.lists.set(key, list);
+		return list.map((item) => item.value);
+	}
+	async isSubscribed(threadId) {
+		this.ensureConnected();
+		return this.subscriptions.has(threadId);
+	}
+	async queueDepth(threadId) {
+		this.ensureConnected();
+		const queue = (this.queues.get(threadId) || []).filter((entry) => !isExpired(entry.expiresAt));
+		this.queues.set(threadId, queue);
+		return queue.length;
+	}
+	async releaseLock(lock) {
+		this.ensureConnected();
+		if (this.locks.get(lock.threadId)?.token === lock.token) this.locks.delete(lock.threadId);
+	}
+	async set(key, value, ttlMs) {
+		this.ensureConnected();
+		this.cache.set(key, {
+			expiresAt: ttlMs ? Date.now() + ttlMs : void 0,
+			value
+		});
+	}
+	async setIfNotExists(key, value, ttlMs) {
+		this.ensureConnected();
+		const existing = this.cache.get(key);
+		if (existing && !isExpired(existing.expiresAt)) return false;
+		this.cache.set(key, {
+			expiresAt: ttlMs ? Date.now() + ttlMs : void 0,
+			value
+		});
+		return true;
+	}
+	async subscribe(threadId) {
+		this.ensureConnected();
+		this.subscriptions.add(threadId);
+	}
+	async unsubscribe(threadId) {
+		this.ensureConnected();
+		this.subscriptions.delete(threadId);
+	}
+	ensureConnected() {
+		if (!this.connected) throw new Error("[vitehub] In-memory Chat State is not connected. Call connect() before using state.");
+	}
+};
+const inMemoryChatStates = /* @__PURE__ */ new Map();
+function getInMemoryChatState(key) {
+	let state = inMemoryChatStates.get(key);
+	if (!state) {
+		state = new ViteHubInMemoryChatStateAdapter();
+		inMemoryChatStates.set(key, state);
+	}
+	return state;
+}
+function withChatStateScope(state, channelPrefix, agentPrefix) {
+	const key = (value) => `${value.startsWith("transcripts:user:") ? agentPrefix : channelPrefix}${value}`;
+	const lock = (value) => ({
+		...value,
+		threadId: key(value.threadId)
+	});
+	return {
+		async acquireLock(threadId, ttlMs) {
+			const acquired = await state.acquireLock(key(threadId), ttlMs);
+			return acquired ? {
+				...acquired,
+				threadId
+			} : null;
+		},
+		appendToList: (listKey, value, options) => state.appendToList(key(listKey), value, options),
+		connect: () => state.connect(),
+		delete: (cacheKey) => state.delete(key(cacheKey)),
+		dequeue: (threadId) => state.dequeue(key(threadId)),
+		disconnect: () => state.disconnect(),
+		enqueue: (threadId, entry, maxSize) => state.enqueue(key(threadId), entry, maxSize),
+		extendLock: (value, ttlMs) => state.extendLock(lock(value), ttlMs),
+		forceReleaseLock: (threadId) => state.forceReleaseLock(key(threadId)),
+		get: (cacheKey) => state.get(key(cacheKey)),
+		getList: (listKey) => state.getList(key(listKey)),
+		isSubscribed: (threadId) => state.isSubscribed(key(threadId)),
+		queueDepth: (threadId) => state.queueDepth(key(threadId)),
+		releaseLock: (value) => state.releaseLock(lock(value)),
+		set: (cacheKey, value, ttlMs) => state.set(key(cacheKey), value, ttlMs),
+		setIfNotExists: (cacheKey, value, ttlMs) => state.setIfNotExists(key(cacheKey), value, ttlMs),
+		subscribe: (threadId) => state.subscribe(key(threadId)),
+		unsubscribe: (threadId) => state.unsubscribe(key(threadId))
+	};
+}
+function chatStateOwnsScope(state) {
+	if (typeof state === "function") return state.ownsScope !== false;
+	return isRecord(state) && typeof state.resolve === "function";
+}
+async function resolveChatState(options, context, registration, handlerOptions) {
+	const agentName = routeAgentIdentity(handlerOptions)?.name || "agent";
+	const origin = chatRegistrationOrigin(registration);
+	const agentKeyPrefix = `chat:${agentName}:`;
+	const stateKeyPrefix = `${agentKeyPrefix}${origin}:`;
+	const state = await resolveMaybe(options?.state ?? handlerOptions.state, {
+		...context,
+		chat: {
+			agentName,
+			stateKeyPrefix
+		}
+	});
+	if (!state) return {
+		state: withChatStateScope(getInMemoryChatState(agentName), stateKeyPrefix, agentKeyPrefix),
+		titleKeyPrefix: ""
+	};
+	if (options?.state === void 0 && !chatStateOwnsScope(handlerOptions.state)) return {
+		state: withChatStateScope(state, stateKeyPrefix, agentKeyPrefix),
+		titleKeyPrefix: ""
+	};
+	return {
+		state,
+		titleKeyPrefix: stateKeyPrefix
+	};
+}
+function chatSdkOption(options, key) {
+	return isRecord(options) ? options[key] : void 0;
+}
+function createChatSdkConfig(adapterName, adapter, state, options) {
+	const fallbackStreamingPlaceholderText = typeof options?.fallbackStreamingPlaceholderText === "string" ? options.fallbackStreamingPlaceholderText : options?.fallbackStreamingPlaceholderText === null ? null : void 0;
+	const identity = options?.identity ?? (options?.transcripts ? ({ author }) => author.isBot === true ? null : `${adapterName}:${author.userId}` : void 0);
+	const concurrency = chatSdkOption(options, "concurrency");
+	return objectWithoutUndefined({
+		adapters: { [adapterName]: adapter },
+		concurrency: concurrency === "parallel" ? "concurrent" : concurrency,
+		dedupeTtlMs: chatSdkOption(options, "dedupeTtlMs"),
+		fallbackStreamingPlaceholderText,
+		identity,
+		lockScope: chatSdkOption(options, "lockScope"),
+		logger: chatSdkOption(options, "logger"),
+		messageHistory: chatSdkOption(options, "messageHistory"),
+		state,
+		streamingUpdateIntervalMs: chatSdkOption(options, "streamingUpdateIntervalMs"),
+		threadHistory: chatSdkOption(options, "threadHistory"),
+		transcripts: options?.transcripts,
+		userName: options?.userName || "vitehub"
+	});
+}
+function isoDate(value) {
+	return value instanceof Date ? value.toISOString() : void 0;
+}
+function chatMessageAuthorEmail(message) {
+	return firstString(message.author.email, message.author.mail, message.author.userPrincipalName);
+}
+const chatTextAttachmentMaxBytes = 8 * 1024 * 1024;
+const textAttachmentExtensions = new Set([
+	"csv",
+	"json",
+	"log",
+	"md",
+	"txt",
+	"yaml",
+	"yml"
+]);
+const textAttachmentMimeTypes = new Set([
+	"application/json",
+	"application/x-yaml",
+	"application/yaml",
+	"text/csv"
+]);
+const chatTextAttachmentOversizeMessage = `[vitehub] Chat text attachment exceeds ${chatTextAttachmentMaxBytes} bytes.`;
+const imageAttachmentMimeTypes = {
+	avif: "image/avif",
+	gif: "image/gif",
+	jpeg: "image/jpeg",
+	jpg: "image/jpeg",
+	png: "image/png",
+	svg: "image/svg+xml",
+	webp: "image/webp"
+};
+function imageAttachmentMediaType(attachment) {
+	if (attachment.data instanceof Blob && attachment.data.type.startsWith("image/")) return attachment.data.type;
+	for (const path of [attachment.name, attachment.url]) {
+		const extension = path?.split(/[?#]/)[0]?.split(".").pop()?.toLowerCase();
+		if (extension && imageAttachmentMimeTypes[extension]) return imageAttachmentMimeTypes[extension];
+	}
+}
+function isTextAttachment(attachment) {
+	if (attachment.type !== "file") return false;
+	const mimeType = typeof attachment.mimeType === "string" ? attachment.mimeType.split(";")[0]?.trim().toLowerCase() : void 0;
+	if (mimeType?.startsWith("text/") || mimeType && textAttachmentMimeTypes.has(mimeType) || mimeType?.endsWith("+json") || mimeType?.endsWith("+yaml")) return true;
+	const extension = attachment.name?.split(".").pop()?.toLowerCase();
+	return !!extension && textAttachmentExtensions.has(extension);
+}
+function checkedTextAttachmentBytes(value, options = {}) {
+	const bytes = value instanceof ArrayBuffer ? new Uint8Array(value) : value instanceof Uint8Array ? value : void 0;
+	if (!bytes) return;
+	if (bytes.byteLength > chatTextAttachmentMaxBytes) {
+		if (!options.rejectOversizedTextAttachments) return;
+		throw new Error(chatTextAttachmentOversizeMessage);
+	}
+	return bytes;
+}
+function isTextAttachmentOversizeError(error) {
+	return error instanceof Error && error.message === chatTextAttachmentOversizeMessage;
+}
+async function fetchTextAttachmentBytes(url, options = {}) {
+	const response = await fetch(url);
+	if (!response.ok) return;
+	const contentLengthHeader = response.headers.get("content-length");
+	const contentLength = contentLengthHeader === null ? void 0 : Number(contentLengthHeader);
+	if (typeof contentLength === "number" && Number.isFinite(contentLength) && contentLength > chatTextAttachmentMaxBytes) {
+		if (!options.rejectOversizedTextAttachments) return;
+		throw new Error(chatTextAttachmentOversizeMessage);
+	}
+	if (!response.body) return checkedTextAttachmentBytes(await response.arrayBuffer(), options);
+	const reader = response.body.getReader();
+	const chunks = [];
+	let byteLength = 0;
+	while (true) {
+		const { done, value } = await reader.read();
+		if (done) break;
+		const chunk = value instanceof Uint8Array ? value : new Uint8Array(value);
+		byteLength += chunk.byteLength;
+		if (byteLength > chatTextAttachmentMaxBytes) {
+			await reader.cancel().catch(() => void 0);
+			if (!options.rejectOversizedTextAttachments) return;
+			throw new Error(chatTextAttachmentOversizeMessage);
+		}
+		chunks.push(chunk);
+	}
+	const bytes = new Uint8Array(byteLength);
+	let offset = 0;
+	for (const chunk of chunks) {
+		bytes.set(chunk, offset);
+		offset += chunk.byteLength;
+	}
+	return bytes;
+}
+async function textAttachmentBytes(attachment, options = {}) {
+	if (typeof attachment.size === "number" && attachment.size > chatTextAttachmentMaxBytes) {
+		if (!options.rejectOversizedTextAttachments) return;
+		throw new Error(chatTextAttachmentOversizeMessage);
+	}
+	if (typeof attachment.fetchData === "function") try {
+		return checkedTextAttachmentBytes(await attachment.fetchData(), options);
+	} catch (error) {
+		if (isTextAttachmentOversizeError(error)) throw error;
+		return;
+	}
+	if (attachment.data instanceof Blob) return checkedTextAttachmentBytes(await attachment.data.arrayBuffer(), options);
+	const bytes = checkedTextAttachmentBytes(attachment.data, options);
+	if (bytes) return bytes;
+	if (typeof attachment.url !== "string" || !attachment.url) return void 0;
+	try {
+		return await fetchTextAttachmentBytes(attachment.url, options);
+	} catch (error) {
+		if (isTextAttachmentOversizeError(error)) throw error;
+		return;
+	}
+}
+async function textPartFromAttachment(attachment, index, options = {}) {
+	if (!isTextAttachment(attachment)) return void 0;
+	const bytes = await textAttachmentBytes(attachment, options);
+	if (!bytes?.byteLength) return void 0;
+	const name = attachment.name || `attachment-${index + 1}`;
+	return {
+		id: `attachment-${index + 1}`,
+		text: `\n\nText attachment (${name}):\n\n${new TextDecoder().decode(bytes).trimEnd()}`,
+		type: "text"
+	};
+}
+function attachmentPartFromAttachment(attachment, index) {
+	const declaredMediaType = typeof attachment.mimeType === "string" && attachment.mimeType ? attachment.mimeType : void 0;
+	const type = declaredMediaType?.startsWith("audio/") || attachment.type === "audio" && !declaredMediaType ? "audio" : declaredMediaType?.startsWith("image/") || attachment.type === "image" && !declaredMediaType ? "image" : "file";
+	const mediaType = declaredMediaType ?? (type === "image" ? imageAttachmentMediaType(attachment) : void 0);
+	const data = isAttachmentData(attachment.data) ? attachment.data : void 0;
+	const fetchData = typeof attachment.fetchData === "function" ? async () => {
+		const value = await attachment.fetchData?.();
+		const resolved = isAttachmentData(value) ? value : void 0;
+		if (!resolved) throw new Error("[vitehub] Chat attachment fetchData() did not return supported attachment data.");
+		return resolved;
+	} : void 0;
+	const url = typeof attachment.url === "string" && attachment.url ? attachment.url : void 0;
+	if (!data && !fetchData && !url) return void 0;
+	return objectWithoutUndefined({
+		data,
+		fetchData,
+		fetchMetadata: attachment.fetchMetadata,
+		id: `attachment-${index + 1}`,
+		mediaType: mediaType ?? (type === "audio" ? "audio/ogg" : type === "image" ? "image/jpeg" : "application/octet-stream"),
+		name: attachment.name,
+		size: attachment.size,
+		type,
+		url
+	});
+}
+function attachmentFallbackLabel(attachment) {
+	if (typeof attachment.type === "string" && attachment.type) return attachment.type;
+	if (typeof attachment.mimeType === "string" && attachment.mimeType) return attachment.mimeType;
+	return "file";
+}
+function attachmentFallbackText(attachments) {
+	if (!attachments.length) return "";
+	const labels = attachments.map(attachmentFallbackLabel);
+	if (labels.length === 1) return `Sent ${/^[aeiou]/i.test(labels[0] ?? "") ? "an" : "a"} ${labels[0]} attachment.`;
+	const counts = /* @__PURE__ */ new Map();
+	for (const label of labels) counts.set(label, (counts.get(label) ?? 0) + 1);
+	return `Sent attachments: ${[...counts.entries()].map(([label, count]) => `${count} ${label}${count === 1 ? "" : "s"}`).join(", ")}.`;
+}
+async function chatMessageParts(message, options = {}) {
+	const parts = [];
+	if (message.text) parts.push({
+		id: "text-0",
+		text: message.text,
+		type: "text"
+	});
+	for (const [index, attachment] of message.attachments.entries()) {
+		const textPart = await textPartFromAttachment(attachment, index, options);
+		if (textPart) {
+			parts.push(textPart);
+			continue;
+		}
+		const attachmentPart = attachmentPartFromAttachment(attachment, index);
+		if (attachmentPart) parts.push(attachmentPart);
+	}
+	if (!parts.length) {
+		const text = attachmentFallbackText(message.attachments);
+		if (text) parts.push({
+			id: "text-0",
+			text,
+			type: "text"
+		});
+	}
+	return parts;
+}
+function chatMessageMetadata(thread, message, messageContext) {
+	const platformChannelId = thread.adapter.channelIdFromThreadId(message.threadId);
+	return objectWithoutUndefined({ chat: objectWithoutUndefined({
+		edited: message.metadata.edited,
+		editedAt: isoDate(message.metadata.editedAt),
+		isMention: message.isMention,
+		messageId: message.id,
+		platform: objectWithoutUndefined({
+			channelId: platformChannelId,
+			threadId: message.threadId
+		}),
+		skippedCount: messageContext?.skipped.length,
+		threadId: message.threadId,
+		totalSinceLastHandler: messageContext?.totalSinceLastHandler
+	}) });
+}
+async function chatSdkMessageToUiMessage(message, metadata, options) {
+	return {
+		createdAt: isoDate(message.metadata.dateSent),
+		id: message.id,
+		...metadata ? { metadata } : {},
+		parts: await chatMessageParts(message, options),
+		role: message.author.isMe ? "assistant" : "user"
+	};
+}
+function chatAuthorizationUiMessage(thread, message, messageContext) {
+	const metadata = chatMessageMetadata(thread, message, messageContext);
+	return {
+		createdAt: isoDate(message.metadata.dateSent),
+		id: message.id,
+		...metadata ? { metadata } : {},
+		parts: message.text ? [{
+			id: "text-0",
+			text: message.text,
+			type: "text"
+		}] : [],
+		role: message.author.isMe ? "assistant" : "user"
+	};
+}
+function chatThreadHistoryCache(thread) {
+	const history = thread._threadHistory;
+	if (!history || typeof history !== "object") return;
+	if (typeof history.getMessages !== "function") return;
+	return history;
+}
+async function durableChatThreadMessages(thread, limit) {
+	try {
+		return await chatThreadHistoryCache(thread)?.getMessages(thread.id, limit) ?? [];
+	} catch {
+		return [];
+	}
+}
+async function chatTriggerMessages(thread, message, options, messageContext) {
+	const current = await chatSdkMessageToUiMessage(message, chatMessageMetadata(thread, message, messageContext), { rejectOversizedTextAttachments: true });
+	const limit = chatTriggerHistoryLimit(resolveChatTriggerHistory(options));
+	if (!limit) return [current];
+	const fetchedNewestFirst = [];
+	try {
+		for await (const item of thread.messages) {
+			fetchedNewestFirst.push(item.id && message.id && item.id === message.id ? current : await chatSdkMessageToUiMessage(item));
+			if (fetchedNewestFirst.length >= limit) break;
+		}
+	} catch {}
+	const durable = await durableChatThreadMessages(thread, limit);
+	const messages = [...await Promise.all(durable.map((item) => item.id && message.id && item.id === message.id ? current : chatSdkMessageToUiMessage(item))), ...fetchedNewestFirst.slice().reverse()].reduce((deduped, item) => {
+		if (!item.id) {
+			deduped.push(item);
+			return deduped;
+		}
+		const existing = deduped.findIndex((message) => message.id === item.id);
+		if (existing === -1) deduped.push(item);
+		else deduped[existing] = item;
+		return deduped;
+	}, []);
+	if (!current.id || !messages.some((item) => item.id === current.id)) messages.push(current);
+	return messages.slice(-limit);
+}
+function createChatTriggerInput(provider, thread, message, messages, messageContext, channelId) {
+	const platformChannelId = thread.adapter.channelIdFromThreadId(message.threadId);
+	const user = objectWithoutUndefined({
+		id: message.author.userId,
+		isBot: message.author.isBot,
+		isMe: message.author.isMe,
+		name: message.author.fullName,
+		username: message.author.userName
+	});
+	const email = chatMessageAuthorEmail(message);
+	return {
+		...email ? { meta: { email } } : {},
+		messages,
+		run: {
+			channelId: channelId || platformChannelId,
+			messageId: message.id,
+			origin: provider,
+			runId: `${provider}:${message.id}`,
+			threadId: message.threadId
+		},
+		user
+	};
+}
+function accessChatInput(thread, message, messageContext) {
+	return {
+		message: objectWithoutUndefined({
+			attachmentCount: message.attachments.length,
+			id: message.id,
+			isMention: message.isMention,
+			text: message.text,
+			threadId: message.threadId
+		}),
+		queue: objectWithoutUndefined({
+			skippedCount: messageContext?.skipped.length,
+			totalSinceLastHandler: messageContext?.totalSinceLastHandler
+		}),
+		thread: {
+			id: thread.id,
+			isDM: thread.isDM
+		}
+	};
+}
+function chatErrorHookArgs(thread, message, input, run, error) {
+	const inputMessage = input?.messages.at(-1);
+	const metadata = inputMessage?.metadata && typeof inputMessage.metadata === "object" ? inputMessage.metadata : void 0;
+	return {
+		error,
+		history: input ? uiMessagesToAgentMessages(input.messages) : [],
+		message: {
+			id: inputMessage?.id || message.id,
+			...metadata ? { metadata } : {},
+			text: message.text
+		},
+		run,
+		thread: { post: async (postedMessage) => postChatMessage(thread, postedMessage) }
+	};
+}
+function isTextChatMessage(message) {
+	return typeof message === "object" && message !== null && "text" in message && typeof message.text === "string";
+}
+function chatMessageDeliveryArtifacts(message) {
+	const artifacts = message.artifacts;
+	return Array.isArray(artifacts) ? artifacts : [];
+}
+async function postChatMessage(thread, message) {
+	if (isAsyncIterable(message)) {
+		let markdown = "";
+		const stream = (async function* () {
+			for await (const chunk of message) {
+				markdown += chunk;
+				yield chunk;
+			}
+		})();
+		const sent = await thread.post(thread.adapter.stream ? new StreamingPlan(stream) : stream);
+		await finishDiscordSplitStream(thread, sent, markdown || sentMessageText(sent));
+		return;
+	}
+	if (typeof message !== "object" || message === null) {
+		if (await postDiscordSplitContent(thread, message)) return;
+		await thread.post(message);
+		return;
+	}
+	const attachments = deliveryArtifactAttachments(chatMessageDeliveryArtifacts(message));
+	if (!attachments.length) {
+		const postable = isTextChatMessage(message) ? message.text : message;
+		if (await postDiscordSplitContent(thread, postable)) return;
+		await thread.post(postable);
+		return;
+	}
+	if (isTextChatMessage(message)) {
+		await thread.post({
+			attachments,
+			raw: message.text
+		});
+		return;
+	}
+	const { artifacts: _artifacts, ...postable } = message;
+	await thread.post({
+		...postable,
+		attachments: [...Array.isArray(postable.attachments) ? postable.attachments : [], ...attachments]
+	});
+}
+async function replaceManualDeliveryPlaceholder(placeholder, message) {
+	if (!placeholder || typeof placeholder !== "object" || !("edit" in placeholder) || typeof placeholder.edit !== "function") return false;
+	const target = placeholder;
+	if (isAsyncIterable(message)) {
+		let markdown = "";
+		for await (const chunk of message) markdown += chunk;
+		await target.edit({ markdown });
+		return true;
+	}
+	if (typeof message !== "object" || message === null) {
+		await target.edit(message);
+		return true;
+	}
+	if (deliveryArtifactAttachments(chatMessageDeliveryArtifacts(message)).length) return false;
+	await target.edit(isTextChatMessage(message) ? message.text : message);
+	return true;
+}
+async function deleteManualDeliveryPlaceholder(placeholder) {
+	const message = "Manual chat delivery could not remove its placeholder.";
+	if (!placeholder || typeof placeholder !== "object" || !("delete" in placeholder) || typeof placeholder.delete !== "function") throw new Error(message);
+	try {
+		await placeholder.delete();
+	} catch (cause) {
+		throw new Error(message, { cause });
+	}
+}
+async function deliverToManualDeliveryPlaceholder(placeholder, message) {
+	if (!placeholder) return false;
+	if (await replaceManualDeliveryPlaceholder(placeholder, message).catch(() => false)) return true;
+	await deleteManualDeliveryPlaceholder(placeholder);
+	return false;
+}
+async function resolveChatErrorFallbackText(options, args) {
+	const fallback = options?.errorFallbackText;
+	if (fallback === null) return void 0;
+	if (typeof fallback === "function") return await fallback(args) || void 0;
+	if (typeof fallback === "string") return fallback;
+	const publicError = toAgentPublicError(args.error, "http");
+	if (publicError.code === "RATE_LIMIT_REJECTED") return publicError.error;
+	return defaultChatErrorFallbackText;
+}
+async function postChatErrorFallback(error, thread, message, options, input, run, manualDeliveryPlaceholder) {
+	console.error({
+		component: "@vite-hub/agent",
+		error: serializeErrorForLog(error),
+		event: "chat.message.error",
+		message_id: message.id,
+		thread_id: message.threadId
+	});
+	const fallback = await resolveChatErrorFallbackText(options, chatErrorHookArgs(thread, message, input, run, error));
+	if (!fallback) {
+		if (manualDeliveryPlaceholder) await deleteManualDeliveryPlaceholder(manualDeliveryPlaceholder);
+		return;
+	}
+	if (await deliverToManualDeliveryPlaceholder(manualDeliveryPlaceholder, fallback)) return;
+	await thread.post(fallback).catch(() => void 0);
+}
+function createChatFinishExtension(input, registration) {
+	const messages = [];
+	return {
+		[chatFinishMessagesKey]: messages,
+		provider: chatRegistrationOrigin(registration),
+		...input.run ? { run: input.run } : {},
+		sendMessage: async (message) => {
+			messages.push(message);
+		}
+	};
+}
+async function flushChatFinishExtensionMessages(thread, chat, manualDelivery) {
+	const messages = chat[chatFinishMessagesKey].splice(0);
+	for (let message of messages) {
+		if (manualDelivery.placeholder) {
+			if (isAsyncIterable(message)) {
+				let markdown = "";
+				for await (const chunk of message) markdown += chunk;
+				message = { markdown };
+			}
+			if (await deliverToManualDeliveryPlaceholder(manualDelivery.placeholder, message)) {
+				manualDelivery.placeholder = void 0;
+				continue;
+			}
+			manualDelivery.placeholder = void 0;
+		}
+		await postChatMessage(thread, message);
+	}
+}
+function withChatFinishExtension(input, chat) {
+	return {
+		...input,
+		context: {
+			...input.context || {},
+			[CHAT_FINISH_EXTENSION_CONTEXT_KEY]: chat
+		}
+	};
+}
+async function isChatMessageAuthorized(agent, context, registration, thread, message, input, run, messageContext) {
+	const invocationContext = createAgentInvocationContextStore(input.context);
+	const invoker = await resolveAgentInvoker(agent?.invoker, context, invocationContext, input, run);
+	for (const accessOptions of getAccessCapabilityOptions(getAgentCapabilities(agent))) {
+		if (!accessOptions.chat) continue;
+		if (await accessOptions.chat.resolve({
+			...context,
+			actor: invoker,
+			invoker,
+			input: accessChatInput(thread, message, messageContext),
+			provider: chatRegistrationOrigin(registration),
+			request: context.request,
+			webhook: registration
+		}) === false) return;
+	}
+	return invoker;
+}
+const cloudflareChatBackgroundTimeoutMs = 25e3;
+function chatInvocationTimeout(timeout, maximum) {
+	return timeout === void 0 || maximum === void 0 ? timeout : Math.min(timeout, maximum);
+}
+async function handleChatSdkMessage(agent, context, registration, thread, message, deliveryKind, options, state, messageContext, maximumInvocationTimeout) {
+	let input;
+	let run;
+	let typing;
+	let progress;
+	const manualDeliveryState = {};
+	try {
+		input = createChatTriggerInput(chatRegistrationOrigin(registration), thread, message, [chatAuthorizationUiMessage(thread, message, messageContext)], messageContext, registration.channelId);
+		if (typeof options?.timeout === "number" && Number.isFinite(options.timeout) && options.timeout > 0) input.timeout = options.timeout;
+		const authorizationInput = createChatMessageTriggerInput(options || {}, input).input;
+		const invoker = await isChatMessageAuthorized(agent, context, registration, thread, message, authorizationInput, input.run, messageContext);
+		if (!invoker) return;
+		const messages = await chatTriggerMessages(thread, message, options, messageContext);
+		const currentMessage = message.id ? messages.find((item) => item.id === message.id) : messages.at(-1);
+		if (!currentMessage || !Array.isArray(currentMessage.parts) || currentMessage.parts.length === 0) return;
+		const filter = options?.filter;
+		if (filter) {
+			const [current] = uiMessagesToAgentMessages([currentMessage]);
+			if (!current || !await filter({
+				...context,
+				deliveryKind,
+				message: current,
+				run: input.run,
+				thread: { post: async (postedMessage) => await postChatMessage(thread, postedMessage) }
+			})) return;
+		}
+		input = {
+			...input,
+			messages
+		};
+		const invocation = await resolveAgentTriggerInvocation(agent, context, "chat.message", input);
+		if (isResolvedAgentTriggerHandledInvocation(invocation)) return;
+		assertChatDeliveryOptions(options || {});
+		const manualDelivery = options?.delivery === "manual";
+		const streamsPhasedReplies = !manualDelivery && (options?.stream !== false || options?.commentary !== void 0);
+		const thinkingFallback = invocation.metadata?.thinkingFallback;
+		if (manualDelivery && typeof thinkingFallback === "string") manualDeliveryState.placeholder = await thread.post(thinkingFallback);
+		typing = streamsPhasedReplies ? startChatTypingRefresh(thread, context) : void 0;
+		run = invocation.run;
+		const runContext = {
+			...context,
+			...invocation.run ? { run: invocation.run } : {}
+		};
+		const chatFinish = createChatFinishExtension(input, registration);
+		progress = manualDelivery ? createManualDeliveryProgressUpdater(manualDeliveryState, context.waitUntil) : void 0;
+		const resolvedInvocationInput = invocation.input;
+		const invocationInput = withChatFinishExtension(withResolvedAgentInvokerInput({
+			...resolvedInvocationInput,
+			timeout: chatInvocationTimeout(resolvedInvocationInput.timeout, maximumInvocationTimeout),
+			context: {
+				...resolvedInvocationInput.context,
+				[messageChannelStateContextKey]: state,
+				...progress ? { [agentOutputEventObserverContextKey]: (event) => {
+					const summary = progressSummaryFromEvent(event);
+					if (summary) progress?.update(summary);
+				} } : {},
+				...options?.stream === false || manualDelivery ? { [finalChannelOutputContextKey]: true } : {}
+			}
+		}, invoker), chatFinish);
+		if (!streamsPhasedReplies) {
+			const text = await collectAgentOutput(manualDelivery ? await streamAgent(agent, runContext, invocationInput, { output: "events" }) : await runAgentInline(agent, runContext, invocationInput), progress?.update);
+			if (!manualDelivery && text) {
+				if (!await postDiscordSplitContent(thread, { markdown: text })) await thread.post({ markdown: text });
+			}
+			await progress?.finish();
+			await flushChatFinishExtensionMessages(thread, chatFinish, manualDeliveryState);
+			if (manualDeliveryState.placeholder) await deleteManualDeliveryPlaceholder(manualDeliveryState.placeholder);
+			manualDeliveryState.placeholder = void 0;
+		} else {
+			const result = streamAgent(agent, runContext, invocationInput, { output: "events" });
+			try {
+				if (options?.stream === true || options?.commentary === void 0) await postChatStream(thread, streamAgentOutputToChatText(result), typeof thinkingFallback === "string" || thinkingFallback === null ? thinkingFallback : void 0, context.waitUntil);
+				else {
+					let finalDelivery;
+					let finalDeliveryError;
+					const replies = streamAgentOutputToChatReplies(result, {
+						commentary: options.commentary,
+						onCommentary(response, discard) {
+							const delivery = postChatStream(thread, response, void 0, context.waitUntil).catch(() => discard());
+							context.waitUntil(delivery);
+						},
+						onFinal(response) {
+							finalDelivery = postChatStream(thread, response, typeof thinkingFallback === "string" || thinkingFallback === null ? thinkingFallback : void 0, context.waitUntil).catch((error) => {
+								finalDeliveryError = error;
+							});
+						}
+					});
+					let streamError;
+					await replies.completion.catch((error) => {
+						streamError = error;
+					});
+					await finalDelivery;
+					if (streamError !== void 0) throw streamError;
+					if (finalDeliveryError !== void 0) throw finalDeliveryError;
+				}
+			} finally {
+				typing?.stop();
+			}
+			await flushChatFinishExtensionMessages(thread, chatFinish, manualDeliveryState);
+		}
+	} catch (error) {
+		typing?.stop();
+		await progress?.finish();
+		await postChatErrorFallback(error, thread, message, options, input, run, manualDeliveryState.placeholder);
+		throw error;
+	} finally {
+		typing?.stop();
+	}
+}
+async function createChannelChat(agent, context, registration, adapterName, adapter, options, handlerOptions) {
+	const resolvedState = await resolveChatState(options, context, registration, handlerOptions);
+	const chat = new Chat(createChatSdkConfig(adapterName, adapter, resolvedState.state, options));
+	const state = {
+		keyPrefix: resolvedState.titleKeyPrefix,
+		state: resolvedState.state
+	};
+	const maximumInvocationTimeout = context.runtime === "cloudflare-agents" && handlerOptions.waitUntil ? cloudflareChatBackgroundTimeoutMs : void 0;
+	chat.onDirectMessage((thread, message, _channel, messageContext) => handleChatSdkMessage(agent, context, registration, thread, message, "direct", options, state, messageContext, maximumInvocationTimeout));
+	chat.onNewMention(async (thread, message, messageContext) => {
+		await thread.subscribe().catch(() => void 0);
+		await handleChatSdkMessage(agent, context, registration, thread, message, "mention", options, state, messageContext, maximumInvocationTimeout);
+	});
+	chat.onSubscribedMessage((thread, message, messageContext) => handleChatSdkMessage(agent, context, registration, thread, message, message.isMention ? "mention" : "subscribed", options, state, messageContext, maximumInvocationTimeout));
+	return chat;
+}
+async function createChatWebhookHandler(agent, context, registration, adapterName, adapter, options, handlerOptions) {
+	const handler = (await createChannelChat(agent, context, registration, adapterName, adapter, options, handlerOptions)).webhooks[adapterName];
+	if (!handler) throw new Error(`[vitehub] Chat adapter "${adapterName}" did not expose a webhook handler.`);
+	return handler;
+}
+function createHttpChatRunMetadata(agentName, body, messages, options = {}) {
+	const chatId = optionalBodyString(body.id, "id") || "default";
+	const messageId = optionalBodyString(body.messageId, "messageId") || messages.at(-1)?.id || randomToken();
+	const channelId = options.channelId || `http:${agentName}`;
+	const origin = options.origin || "http";
+	return {
+		channelId,
+		messageId,
+		origin,
+		runId: globalThis.crypto?.randomUUID?.() || `${origin}-run-${randomToken()}`,
+		threadId: `${channelId}:${chatId}`
+	};
+}
+async function parseAgentChannelChatRouteBody(request) {
+	const raw = await request.text();
+	if (!raw.trim()) throw createRouteBodyError("Missing agent chat payload.");
+	try {
+		const body = JSON.parse(raw);
+		if (!isRecord(body)) throw createRouteBodyError("Agent chat payload must be a JSON object.");
+		return {
+			body,
+			rawBody: raw
+		};
+	} catch (error) {
+		if (error instanceof Error && "statusCode" in error) throw error;
+		throw createRouteBodyError("Malformed agent chat payload.");
+	}
+}
+async function parseAgentChannelChatRouteAdmissionBody(body, schema) {
+	if (!schema) return body;
+	try {
+		return await parseStandardSchema(schema, body, "agent chat route body");
+	} catch (error) {
+		throw createRouteBodyError(error instanceof Error ? error.message : "Invalid agent chat route body.");
+	}
+}
+function optionalBodyRecord(value, label) {
+	if (value === void 0) return void 0;
+	if (!isRecord(value)) throw createRouteBodyError(`${label} must be an object when provided.`);
+	return { ...value };
+}
+function optionalBodyString(value, label) {
+	if (value === void 0) return void 0;
+	if (typeof value !== "string") throw createRouteBodyError(`${label} must be a string when provided.`);
+	return value.trim() || void 0;
+}
+function agentChannelChatRouteInput(body, agentName, allowTrustedInput = false, options = {}) {
+	if (!Array.isArray(body.messages)) throw createRouteBodyError("Agent chat payload requires a messages array.");
+	if (!allowTrustedInput && ("invoker" in body || "invokerProfileId" in body || "meta" in body || "run" in body || "session" in body || "timeout" in body || "user" in body)) throw createRouteBodyError("Agent chat route identity must be derived server-side with defineAgent({ invoker }).");
+	const messages = body.messages;
+	return {
+		messages,
+		run: createHttpChatRunMetadata(agentName, body, messages, options),
+		...body.triggerHistory !== void 0 ? { triggerHistory: body.triggerHistory } : {}
+	};
+}
+function agentChannelRouteOptions(channelId, channel) {
+	if (channel.route === void 0 || channel.route === false) return void 0;
+	if (channel.route === true) return {
+		channelId,
+		origin: channel.kind
+	};
+	if (isRecord(channel.route) && !Array.isArray(channel.route)) return {
+		channelId,
+		origin: channel.kind,
+		...channel.route
+	};
+	throw new TypeError(`[vitehub] Channel "${channelId}" route must be true or an agent chat route options object.`);
+}
+function hasChannelChatRoute(agent) {
+	if (!isRecord(agent) || !isRecord(agent.channels) || Array.isArray(agent.channels)) return false;
+	return Object.values(agent.channels).some((channel) => isRecord(channel) && channel.route !== void 0 && channel.route !== false);
+}
+function resolveAgentChannelChatRouteHandlerOptions(agent, options = {}) {
+	const channels = isRecord(agent) && isRecord(agent.channels) && !Array.isArray(agent.channels) ? agent.channels : {};
+	const routeEntries = Object.entries(channels).map(([channelId, channel]) => [channelId, agentChannelRouteOptions(channelId, channel)]).filter((entry) => entry[1] !== void 0);
+	if (routeEntries.length > 1) throw new TypeError("[vitehub] createChannelChatRouteHandler() found multiple route-enabled Channels. Keep one route-enabled Channel per generated chat route.");
+	const channelOptions = routeEntries[0]?.[1];
+	return {
+		...channelOptions,
+		...options,
+		admission: options.admission ?? channelOptions?.admission,
+		input: options.input ?? channelOptions?.input,
+		mapInput: options.mapInput ?? channelOptions?.mapInput
+	};
+}
+function trustAgentChannelChatRouteInput(body, options) {
+	if (!options?.trust?.length) return void 0;
+	const patch = {};
+	for (const field of options.trust) if (field === "meta" && body.meta !== void 0) patch.meta = optionalBodyRecord(body.meta, "meta");
+	else if (field === "session" && body.session !== void 0) patch.session = optionalBodyRecord(body.session, "session");
+	else if (field === "timeout" && typeof body.timeout === "number" && Number.isFinite(body.timeout) && body.timeout > 0) patch.timeout = body.timeout;
+	else if (field === "user" && body.user !== void 0) patch.user = optionalBodyRecord(body.user, "user");
+	return Object.keys(patch).length ? patch : void 0;
+}
+function mergeAgentChannelChatRouteInput(input, patch) {
+	if (patch === void 0) return input;
+	if (!isRecord(patch)) throw createRouteBodyError("Agent chat route input mapper must return an object.");
+	const { run, session, ...rest } = patch;
+	return {
+		...input,
+		...rest,
+		...run ? { run: {
+			...input.run,
+			...run
+		} } : {},
+		...session ? { session: {
+			...input.session,
+			...session
+		} } : {}
+	};
+}
+async function toAgentChatFetchResponse(result) {
+	if (result instanceof Response) return withCleanUiMessageStreamResponse(result);
+	return createAgentUIMessageStreamResponse({ stream: readableStreamFromResult(result) });
+}
+function agentChatFetchErrorResponse(error) {
+	const response = toHttpErrorResponse(error);
+	if (response) return response;
+	return toHttpErrorResponse(error, error instanceof TypeError ? 400 : 500);
+}
+function createChannelChatRouteHandler(agent, options = {}) {
+	const routeOptions = resolveAgentChannelChatRouteHandlerOptions(agent, options);
+	return async (request, handlerOptions = {}) => {
+		if (request.method !== "POST") return createJsonErrorResponse(405, "Agent chat route only accepts POST requests.");
+		try {
+			const parsed = await parseAgentChannelChatRouteBody(request);
+			const agentIdentity = routeAgentIdentity(handlerOptions);
+			const agentName = agentIdentity?.name || "agent";
+			const auth = await routeOptions.admission?.authenticate?.({
+				agentName,
+				body: parsed.body,
+				rawBody: parsed.rawBody,
+				request
+			});
+			if (auth === false) throw createRouteError(401, "Agent chat route request was not admitted.");
+			const body = await parseAgentChannelChatRouteAdmissionBody(parsed.body, routeOptions.admission?.body);
+			const context = createRuntimeContext(createRuntimeRequest(request, parsed.rawBody), void 0, await resolveRuntimeWaitUntil(handlerOptions.waitUntil), handlerOptions.cloudflare, handlerOptions.runtime, handlerOptions.capabilities, agentIdentity);
+			const trustInput = Boolean(routeOptions.admission?.authenticate && routeOptions.input?.trust?.length);
+			const trustedInput = mergeAgentChannelChatRouteInput(agentChannelChatRouteInput(body, agentName, Boolean(trustInput || routeOptions.admission?.context || routeOptions.mapInput), routeOptions), trustInput ? trustAgentChannelChatRouteInput(body, routeOptions.input) : void 0);
+			const inputContext = {
+				agentName,
+				auth,
+				body,
+				input: trustedInput,
+				rawBody: parsed.rawBody,
+				request
+			};
+			const admittedInput = mergeAgentChannelChatRouteInput(trustedInput, await routeOptions.admission?.context?.(inputContext));
+			const triggerInput = mergeAgentChannelChatRouteInput(admittedInput, await routeOptions.mapInput?.({
+				...inputContext,
+				input: admittedInput
+			}));
+			return await toAgentChatFetchResponse(await runWithRuntimeCloudflareEnv(context, async () => await streamAgentTrigger(agent, context, "chat.message", triggerInput, { output: "ui-message-stream" })));
+		} catch (error) {
+			return agentChatFetchErrorResponse(error);
+		}
+	};
+}
+function createChannelWebhookRouteHandler(agent) {
+	return async (request, webhook, handlerOptions = {}) => {
+		if (request.method !== "POST") return createJsonErrorResponse(405, "Agent webhook route only accepts POST requests.");
+		const webhookId = webhook === void 0 ? fallbackWebhookFromRequest(request) : webhook;
+		if (webhookId === void 0) return createBadRequest("Agent webhook route requires a webhook id.");
+		const waitUntil = await resolveRuntimeWaitUntil(handlerOptions.waitUntil);
+		const context = createRuntimeContext(request, void 0, waitUntil, handlerOptions.cloudflare, handlerOptions.runtime, handlerOptions.capabilities, routeAgentIdentity(handlerOptions));
+		return await runWithRuntimeCloudflareEnv(context, async () => {
+			const match = await findAgentWebhookRegistration(agent, context, request, webhookId);
+			if (!match) return createJsonErrorResponse(404, "Unknown ViteHub agent webhook.");
+			const { registration, trigger } = match;
+			if (await matchedWebhookRegistrationRequiresVerification(registration, context, trigger.id !== "chat.message")) try {
+				await verifyAgentWebhookRequest([registration], request, context, { requireSecretHeader: true });
+			} catch (error) {
+				const response = toHttpErrorResponse(error);
+				if (response) return response;
+				throw error;
+			}
+			if (trigger.id !== "chat.message") try {
+				const input = await createAgentWebhookTriggerInput(request, registration);
+				const invocation = await resolveAgentTriggerInvocation(agent, context, trigger.id, input);
+				if (isResolvedAgentTriggerHandledInvocation(invocation)) {
+					await context.flushWaitUntil?.();
+					return invocation.response;
+				}
+				if (invocation.webhook?.concurrencyKey !== void 0 && await hasActiveWorkflowRuntime(agent, context)) return createJsonErrorResponse(503, "Webhook concurrency ownership requires inline Agent execution.");
+				const webhookState = invocation.webhook ? await resolveAgentWebhookState(context, registration, handlerOptions) : void 0;
+				if (invocation.webhook && !webhookState) return createJsonErrorResponse(503, "Durable Agent state is required for webhook delivery ownership.");
+				let webhookLock = null;
+				let deliveryClaimKey;
+				let concurrencyTtlMs = defaultWebhookConcurrencyTtlMs;
+				if (invocation.webhook && webhookState) {
+					const { concurrencyKey, deliveryId } = invocation.webhook;
+					if (!deliveryId.trim()) return createJsonErrorResponse(500, "Webhook delivery ownership requires a non-empty deliveryId.");
+					if (concurrencyKey !== void 0 && !concurrencyKey.trim()) return createJsonErrorResponse(500, "Webhook delivery ownership requires a non-empty concurrencyKey when configured.");
+					concurrencyTtlMs = positiveWebhookDuration(invocation.webhook.concurrencyTtlMs, defaultWebhookConcurrencyTtlMs, "concurrencyTtlMs");
+					deliveryClaimKey = webhookOwnershipKey(webhookState.keyPrefix, "delivery", deliveryId);
+					if (await webhookState.state.get(deliveryClaimKey) === true) return Response.json({
+						accepted: false,
+						duplicate: true,
+						ok: true
+					});
+					if (concurrencyKey !== void 0) {
+						webhookLock = await webhookState.state.acquireLock(webhookOwnershipKey(webhookState.keyPrefix, "lease", concurrencyKey), concurrencyTtlMs);
+						if (!webhookLock) return Response.json({
+							accepted: false,
+							busy: true,
+							ok: true
+						}, { status: 503 });
+					}
+					let claimed;
+					try {
+						claimed = await webhookState.state.setIfNotExists(deliveryClaimKey, true);
+					} catch (error) {
+						if (webhookLock) await webhookState.state.releaseLock(webhookLock);
+						throw error;
+					}
+					if (!claimed) {
+						if (webhookLock) await webhookState.state.releaseLock(webhookLock);
+						return Response.json({
+							accepted: false,
+							duplicate: true,
+							ok: true
+						});
+					}
+				}
+				const runContext = createRuntimeContext(request, invocation.run, waitUntil, handlerOptions.cloudflare, handlerOptions.runtime, handlerOptions.capabilities, routeAgentIdentity(handlerOptions));
+				const ownershipAbort = webhookLock ? new AbortController() : void 0;
+				let stopHeartbeat;
+				if (webhookLock && webhookState && ownershipAbort) stopHeartbeat = startWebhookLockHeartbeat(webhookState.state, webhookLock, concurrencyTtlMs, () => {
+					ownershipAbort.abort(/* @__PURE__ */ new Error("[vitehub] Webhook concurrency ownership was lost during Agent execution."));
+				});
+				let dispatch;
+				const task = new Promise((resolve) => {
+					dispatch = resolve;
+				}).then(async (accepted) => {
+					if (!accepted) return;
+					await runWithRuntimeCloudflareEnv(runContext, async () => {
+						try {
+							const result = await runAgent(agent, runContext, {
+								...invocation.input,
+								...ownershipAbort ? { abortSignal: invocation.input.abortSignal ? AbortSignal.any([invocation.input.abortSignal, ownershipAbort.signal]) : ownershipAbort.signal } : {}
+							});
+							if (!isWorkflowRun(result) || result.status !== "queued") await runContext.flushWaitUntil?.();
+						} finally {
+							stopHeartbeat?.();
+							if (webhookLock && webhookState) await webhookState.state.releaseLock(webhookLock);
+						}
+					});
+				});
+				try {
+					context.waitUntil(task);
+					dispatch(true);
+				} catch (error) {
+					dispatch(false);
+					stopHeartbeat?.();
+					if (webhookLock && webhookState) await webhookState.state.releaseLock(webhookLock);
+					if (deliveryClaimKey && webhookState) await webhookState.state.delete(deliveryClaimKey);
+					throw error;
+				}
+				return Response.json({
+					accepted: true,
+					ok: true
+				});
+			} catch (error) {
+				const response = toHttpErrorResponse(error);
+				if (response) return response;
+				throw error;
+			}
+			const baseChatOptions = getAgentChatOptions(agent);
+			const adapters = await resolveChatAdapters(baseChatOptions, context);
+			const adapterName = resolveChatAdapterName(adapters, registration);
+			const adapter = adapterName ? adapters[adapterName] : void 0;
+			if (!adapter) return createJsonErrorResponse(500, `Agent chat webhook "${webhookId}" does not have a matching chat adapter.`);
+			try {
+				const chatOptions = getChannelChatOptions(agent, registration.channelId, baseChatOptions);
+				const response = await (await createChatWebhookHandler(agent, context, registration, adapterName, adapter, chatOptions, handlerOptions))(request, { waitUntil: context.waitUntil });
+				if (chatOptions?.stream === false && hasExplicitNonStreamingMessages(agent, registration.channelId)) await context.flushWaitUntil?.();
+				return response;
+			} catch (error) {
+				const response = toHttpErrorResponse(error);
+				if (response) return response;
+				throw error;
+			}
+		});
+	};
+}
+const telegramPollingChats = /* @__PURE__ */ new WeakMap();
+async function startChannelChat(chat) {
+	const initialize = Reflect.get(chat, "initialize");
+	if (typeof initialize !== "function") throw new TypeError("[vitehub] The installed Chat SDK does not support starting listener-based Channels.");
+	await Reflect.apply(initialize, chat, []);
+}
+function createTelegramPollingRouteHandler(agent) {
+	return async (request, handlerOptions = {}) => {
+		if (request.method !== "GET") return createJsonErrorResponse(405, "Telegram polling route only accepts GET requests.");
+		const context = createRuntimeContext(request, void 0, await resolveRuntimeWaitUntil(handlerOptions.waitUntil), handlerOptions.cloudflare, handlerOptions.runtime, handlerOptions.capabilities, routeAgentIdentity(handlerOptions));
+		return await runWithRuntimeCloudflareEnv(context, async () => {
+			const channels = isRecord(agent.channels) ? agent.channels : {};
+			const entries = Object.entries(channels).filter((entry) => isRecord(entry[1]) && entry[1].kind === "telegram" && entry[1].listener?.kind === "telegram-polling" && entry[1].messages !== false && entry[1].adapter !== void 0);
+			if (entries.length === 0) return createJsonErrorResponse(500, "Telegram polling route requires a polling Telegram Channel.");
+			let chats = telegramPollingChats.get(agent);
+			if (!chats) {
+				chats = /* @__PURE__ */ new Map();
+				telegramPollingChats.set(agent, chats);
+			}
+			const chatOptions = getAgentChatOptions(agent);
+			await Promise.all(entries.map(async ([channelId, channel]) => {
+				let chat = chats.get(channelId);
+				if (!chat) {
+					chat = (async () => {
+						const adapter = await resolveMaybe(channel.adapter, context);
+						if (!adapter) throw new Error(`[vitehub] Telegram polling Channel "${channelId}" did not resolve a chat adapter.`);
+						const instance = await createChannelChat(agent, context, {
+							adapter: channelId,
+							channelId,
+							id: channelId,
+							provider: "telegram"
+						}, channelId, adapter, getChannelChatOptions(agent, channelId, chatOptions), handlerOptions);
+						await startChannelChat(instance);
+						return instance;
+					})();
+					chats.set(channelId, chat);
+					chat.catch(() => chats.delete(channelId));
+				}
+				await chat;
+			}));
+			return Response.json({
+				ok: true,
+				polling: entries.length
+			});
+		});
+	};
+}
+function createDiscordGatewayRouteHandler(agent) {
+	return async (request, handlerOptions) => {
+		if (request.method !== "GET") return createJsonErrorResponse(405, "Discord Gateway route only accepts GET requests.");
+		const context = createRuntimeContext(request, void 0, await resolveRuntimeWaitUntil(handlerOptions.waitUntil), handlerOptions.cloudflare, handlerOptions.runtime, handlerOptions.capabilities, routeAgentIdentity(handlerOptions));
+		return await runWithRuntimeCloudflareEnv(context, async () => {
+			const chatOptions = getAgentChatOptions(agent);
+			const adapters = await resolveChatAdapters(chatOptions, context);
+			const entries = resolveDiscordAdapters(adapters);
+			if (entries.length === 0) return createJsonErrorResponse(500, "Discord Gateway route requires a Discord chat adapter.");
+			const responsePromises = [];
+			for (const [adapterName, adapter] of entries) {
+				const startGatewayListener = adapter.startGatewayListener;
+				if (typeof startGatewayListener !== "function") return createJsonErrorResponse(500, `Discord chat adapter "${adapterName}" does not expose startGatewayListener().`);
+				const { state } = await resolveChatState(chatOptions, context, {
+					adapter: adapterName,
+					channelId: adapterName,
+					id: adapterName,
+					provider: "discord"
+				}, handlerOptions);
+				await new Chat(createChatSdkConfig(adapterName, adapter, state, chatOptions)).initialize?.();
+				const webhookId = (await resolveDiscordWebhookRegistration(agent, context, adapters, adapterName))?.id || adapterName;
+				const webhookUrl = typeof handlerOptions.webhookUrl === "function" ? handlerOptions.webhookUrl(webhookId) : handlerOptions.webhookUrl;
+				responsePromises.push(startGatewayListener.call(adapter, { waitUntil: context.waitUntil }, handlerOptions.durationMs, void 0, webhookUrl));
+			}
+			const responses = await Promise.all(responsePromises);
+			if (responses.length === 1) return responses[0];
+			const failed = responses.find((response) => !response.ok);
+			if (failed) return failed;
+			return Response.json({
+				gateways: responses.length,
+				ok: true
+			});
+		});
+	};
+}
+//#endregion
+export { hasChannelChatRoute as a, createTelegramPollingRouteHandler as i, createChannelWebhookRouteHandler as n, createDiscordGatewayRouteHandler as r, createChannelChatRouteHandler as t };
diff --git a/dist/runtime/workflow.d.ts b/dist/runtime/workflow.d.ts
index a2d4961001841319dac277292c8bfccc77d76ffc..d945f848084791a8a570dc8d8f43fecbf39ab8b0 100644
--- a/dist/runtime/workflow.d.ts
+++ b/dist/runtime/workflow.d.ts
@@ -1,5 +1,5 @@
-import { ar as AgentRunMetadata, fr as AgentRuntimeName, lr as AgentRuntimeConfig, on as AgentHostIdentity, or as AgentRunResult, rr as AgentRunInput, sn as AgentInput, ur as AgentRuntimeContext } from "../types-C4oyRXNr.js";
-import { c as workspaceAgentWithSourceRoot } from "../workspace-agent-DQS6D-dz.js";
+import { cn as AgentInput, dr as AgentRuntimeContext, ir as AgentRunInput, or as AgentRunMetadata, pr as AgentRuntimeName, sn as AgentHostIdentity, sr as AgentRunResult, ur as AgentRuntimeConfig } from "../types-CW2XmfE7.js";
+import { c as workspaceAgentWithSourceRoot } from "../workspace-agent-u4sFNw76.js";
 import { WorkspaceSourceInput } from "@vite-hub/workspace";
 import { WorkflowExecutionContext } from "@vite-hub/workflow";
 
diff --git a/dist/runtime/workflow.js b/dist/runtime/workflow.js
index 613cd545c37d1599541f931994a4595f0e3c3300..9d5f36aff93fb8b1eba66d20b44595072c1e57b7 100644
--- a/dist/runtime/workflow.js
+++ b/dist/runtime/workflow.js
@@ -2,8 +2,8 @@ import { n as loadAgentWorkflowRuntimeStateModule } from "../workflow-runtime-lo
 import { i as withColocatedAgentSkills, r as decodeColocatedAgentSkills } from "../colocated-agent-skills-BAz3kNyV.js";
 import { u as workspaceAgentWithSourceRoot } from "../workspace-agent-BtTRGp6h.js";
 import { t as createAgentRuntimeContext } from "../context-KghRPYRx.js";
-import { n as getCloudflareEnv, t as getActiveCloudflareEnv } from "../cloudflare-env-fCklgYbd.js";
 import { n as withColocatedAgentHome, t as decodeColocatedAgentHome } from "../colocated-agent-home-EYZ8BwLn.js";
+import { n as getCloudflareEnv, t as getActiveCloudflareEnv } from "../cloudflare-env-fCklgYbd.js";
 //#region src/runtime/workflow.ts
 function agentWithColocatedSkills(agent, sources) {
 	return withColocatedAgentSkills(agent, decodeColocatedAgentSkills(sources));
diff --git a/dist/server/internal.d.ts b/dist/server/internal.d.ts
index 7ffc40e37fcb852754c78a0e2c9a2bb064133af6..08d96ffbe18529963e3bfb42a8f317c80f0a5705 100644
--- a/dist/server/internal.d.ts
+++ b/dist/server/internal.d.ts
@@ -1,5 +1,5 @@
-import { ri as ResolvedAgentRuntimeContext, sn as AgentInput, ur as AgentRuntimeContext } from "../types-C4oyRXNr.js";
-import { _ as createChannelWebhookRouteHandler, a as AgentChannelChatRouteHandlerOptions, b as hasChannelChatRoute, c as AgentChannelChatRouteRequestOptions, d as AgentChannelChatRouteStandardSchemaV1, f as AgentChannelChatRouteTrustedInputField, g as createChannelChatRouteHandler, h as AgentTelegramPollingRouteOptions, i as AgentChannelChatRouteContext, l as AgentChannelChatRouteStandardSchemaResultFailure, m as AgentDiscordGatewayRouteOptions, n as AgentChannelChatRouteAdmissionOptions, o as AgentChannelChatRouteInputOptions, p as AgentChannelWebhookRouteOptions, r as AgentChannelChatRouteBody, s as AgentChannelChatRouteMapInputContext, t as AgentChannelChatRouteAdmissionContext, u as AgentChannelChatRouteStandardSchemaResultSuccess, v as createDiscordGatewayRouteHandler, y as createTelegramPollingRouteHandler } from "../routes-DUlHQxPe.js";
+import { cn as AgentInput, dr as AgentRuntimeContext, ii as ResolvedAgentRuntimeContext } from "../types-CW2XmfE7.js";
+import { _ as createChannelWebhookRouteHandler, a as AgentChannelChatRouteHandlerOptions, b as hasChannelChatRoute, c as AgentChannelChatRouteRequestOptions, d as AgentChannelChatRouteStandardSchemaV1, f as AgentChannelChatRouteTrustedInputField, g as createChannelChatRouteHandler, h as AgentTelegramPollingRouteOptions, i as AgentChannelChatRouteContext, l as AgentChannelChatRouteStandardSchemaResultFailure, m as AgentDiscordGatewayRouteOptions, n as AgentChannelChatRouteAdmissionOptions, o as AgentChannelChatRouteInputOptions, p as AgentChannelWebhookRouteOptions, r as AgentChannelChatRouteBody, s as AgentChannelChatRouteMapInputContext, t as AgentChannelChatRouteAdmissionContext, u as AgentChannelChatRouteStandardSchemaResultSuccess, v as createDiscordGatewayRouteHandler, y as createTelegramPollingRouteHandler } from "../routes-1Y1zW1HK.js";
 
 //#region src/server/scheduled-turn.d.ts
 interface ScheduledAgentTargetRunContext {
diff --git a/dist/server/internal.js b/dist/server/internal.js
index fab8505e47a3b21df593db35e1adca5e28f3a5d0..98cde93b615304e9ca9e9d24b533f746b562b8f4 100644
--- a/dist/server/internal.js
+++ b/dist/server/internal.js
@@ -1,6 +1,6 @@
-import { d as runScheduledAgent } from "../src-B3GiXaIA.js";
+import { d as runScheduledAgent } from "../src-CktB4unw.js";
 import { r as setAgentWorkflowRuntimeLoaders } from "../workflow-runtime-loaders-T5jPFIAJ.js";
-import { a as hasChannelChatRoute, i as createTelegramPollingRouteHandler, n as createChannelWebhookRouteHandler, r as createDiscordGatewayRouteHandler, t as createChannelChatRouteHandler } from "../routes-xIDDO363.js";
+import { a as hasChannelChatRoute, i as createTelegramPollingRouteHandler, n as createChannelWebhookRouteHandler, r as createDiscordGatewayRouteHandler, t as createChannelChatRouteHandler } from "../routes-Bk8z0sOK.js";
 //#region src/server/scheduled-turn.ts
 function defineScheduledAgentTarget(agent, runtimeContext = {}) {
 	return {
diff --git a/dist/server/workspace.d.ts b/dist/server/workspace.d.ts
index 7cb0c8cd00919342691284375a241b43557d38c1..16f0692dcd73cf45b4b852a4069203bad19dff0a 100644
--- a/dist/server/workspace.d.ts
+++ b/dist/server/workspace.d.ts
@@ -1,5 +1,5 @@
-import { lr as AgentRuntimeConfig } from "../types-C4oyRXNr.js";
-import { n as WorkspaceAgentDefinition } from "../workspace-agent-DQS6D-dz.js";
+import { ur as AgentRuntimeConfig } from "../types-CW2XmfE7.js";
+import { n as WorkspaceAgentDefinition } from "../workspace-agent-u4sFNw76.js";
 import { setWorkspaceRuntimeRegistry } from "@vite-hub/workspace/runtime";
 import { WorkspaceName } from "@vite-hub/workspace";
 
diff --git a/dist/server.d.ts b/dist/server.d.ts
index e76c441e4e2a26e714548412252e8a4669c9a22e..3fc53b70ea59551da5ba09533fb3c714356a8e7f 100644
--- a/dist/server.d.ts
+++ b/dist/server.d.ts
@@ -1,3 +1,3 @@
-import { _i as AgentRunEventsOptions, ci as AgentRunEvent, di as AgentRunEventReadOptions, fi as AgentRunEventStore, gi as AgentRunEvents, hi as AgentRunEventSubscribeOptions, li as AgentRunEventInput, mi as AgentRunEventStoreResolver, pi as AgentRunEventStoreResolveContext, ui as AgentRunEventPublisher, vi as defineAgentRunEvents } from "./types-C4oyRXNr.js";
-import { a as AgentChannelChatRouteHandlerOptions, c as AgentChannelChatRouteRequestOptions, d as AgentChannelChatRouteStandardSchemaV1, f as AgentChannelChatRouteTrustedInputField, h as AgentTelegramPollingRouteOptions, i as AgentChannelChatRouteContext, l as AgentChannelChatRouteStandardSchemaResultFailure, m as AgentDiscordGatewayRouteOptions, n as AgentChannelChatRouteAdmissionOptions, o as AgentChannelChatRouteInputOptions, p as AgentChannelWebhookRouteOptions, r as AgentChannelChatRouteBody, s as AgentChannelChatRouteMapInputContext, t as AgentChannelChatRouteAdmissionContext, u as AgentChannelChatRouteStandardSchemaResultSuccess, v as createDiscordGatewayRouteHandler, y as createTelegramPollingRouteHandler } from "./routes-DUlHQxPe.js";
+import { _i as AgentRunEvents, di as AgentRunEventPublisher, fi as AgentRunEventReadOptions, gi as AgentRunEventSubscribeOptions, hi as AgentRunEventStoreResolver, li as AgentRunEvent, mi as AgentRunEventStoreResolveContext, pi as AgentRunEventStore, ui as AgentRunEventInput, vi as AgentRunEventsOptions, yi as defineAgentRunEvents } from "./types-CW2XmfE7.js";
+import { a as AgentChannelChatRouteHandlerOptions, c as AgentChannelChatRouteRequestOptions, d as AgentChannelChatRouteStandardSchemaV1, f as AgentChannelChatRouteTrustedInputField, h as AgentTelegramPollingRouteOptions, i as AgentChannelChatRouteContext, l as AgentChannelChatRouteStandardSchemaResultFailure, m as AgentDiscordGatewayRouteOptions, n as AgentChannelChatRouteAdmissionOptions, o as AgentChannelChatRouteInputOptions, p as AgentChannelWebhookRouteOptions, r as AgentChannelChatRouteBody, s as AgentChannelChatRouteMapInputContext, t as AgentChannelChatRouteAdmissionContext, u as AgentChannelChatRouteStandardSchemaResultSuccess, v as createDiscordGatewayRouteHandler, y as createTelegramPollingRouteHandler } from "./routes-1Y1zW1HK.js";
 export { type AgentChannelChatRouteAdmissionContext, type AgentChannelChatRouteAdmissionOptions, type AgentChannelChatRouteBody, type AgentChannelChatRouteContext, type AgentChannelChatRouteHandlerOptions, type AgentChannelChatRouteInputOptions, type AgentChannelChatRouteMapInputContext, type AgentChannelChatRouteRequestOptions, type AgentChannelChatRouteStandardSchemaResultFailure, type AgentChannelChatRouteStandardSchemaResultSuccess, type AgentChannelChatRouteStandardSchemaV1, type AgentChannelChatRouteTrustedInputField, type AgentChannelWebhookRouteOptions, type AgentDiscordGatewayRouteOptions, type AgentRunEvent, type AgentRunEventInput, type AgentRunEventPublisher, type AgentRunEventReadOptions, type AgentRunEventStore, type AgentRunEventStoreResolveContext, type AgentRunEventStoreResolver, type AgentRunEventSubscribeOptions, type AgentRunEvents, type AgentRunEventsOptions, type AgentTelegramPollingRouteOptions, createDiscordGatewayRouteHandler, createTelegramPollingRouteHandler, defineAgentRunEvents };
\ No newline at end of file
diff --git a/dist/server.js b/dist/server.js
index 20297dab07c9984f0aacc347b2a2acdf78da409b..1e768f00eb6783bb6dfd13ab30d3632efa322c75 100644
--- a/dist/server.js
+++ b/dist/server.js
@@ -1,3 +1,3 @@
-import { C as defineAgentRunEvents } from "./src-B3GiXaIA.js";
-import { i as createTelegramPollingRouteHandler, r as createDiscordGatewayRouteHandler } from "./routes-xIDDO363.js";
+import { D as defineAgentRunEvents } from "./src-CktB4unw.js";
+import { i as createTelegramPollingRouteHandler, r as createDiscordGatewayRouteHandler } from "./routes-Bk8z0sOK.js";
 export { createDiscordGatewayRouteHandler, createTelegramPollingRouteHandler, defineAgentRunEvents };
diff --git a/dist/src-CktB4unw.js b/dist/src-CktB4unw.js
new file mode 100644
index 0000000000000000000000000000000000000000..68212e5162b73a60bbca76a75c3c247c1e3b71b4
--- /dev/null
+++ b/dist/src-CktB4unw.js
@@ -0,0 +1,2365 @@
+import { c as isAsyncIterable, s as cloneWithPropertyDescriptors, u as toReadableAsyncIterableStream } from "./delivery-artifacts-BOKCLStf.js";
+import { $ as isMessageChannelTitleEffectIntent, D as normalizeAgentInvokerOptions, G as bindMessageChannelInstructions, N as createAgentInvocationContextStore, S as runObservedAgentHook, X as finishMessageChannelTitleDelivery, _ as withCapabilityCleanup, a as channelDeliveryEffectsContextKey, d as normalizeCapabilities, g as validateAgentCapabilityComposition, h as resolveStaticCapabilityTools, it as prepareMessageChannelTitleDelivery, j as agentInvocationCallbackContextValues, k as resolveAgentInvoker, m as resolveAgentCapabilityDefinitions, n as applyOutputRenderers, o as channelDeliveryFinishEffectsContextKey, ot as resolveAgentChannelChatOptions, p as resolveAgentCapabilities, rt as messageChannelTitleDeliveredContextKey, s as createAgentInvocationExtensions, t as applyCapabilityToolTransforms, v as withResponseCleanup, w as createFallbackAgentInvoker } from "./capability-runtime-Be85oBbC.js";
+import { a as tryAgentPromise, i as runAgentEffect, n as agentEffectCauseValues, o as EffectBoundaryFailure } from "./effect-runtime-CMnYHpMr.js";
+import { a as streamAgentOutputToEvents, c as usageRecordFromStreamChunk, d as finalChannelOutputSelectedSymbol, i as resolveAgentUsageRecord, l as synthesizedAgentOutputSymbol, n as finalTextFromAgentOutput, o as toAgentRunResult, r as hasTraceableStreamResult, s as toAgentStreamEvent, t as agentResultKind } from "./agent-output-CyEZfgrQ.js";
+import { _ as withAgentToolStepReporting, a as traceAgentInvocationFinish, b as validateAgentOutput, c as traceAgentStreamEvents, f as readAgentErrorProperty, i as traceAgentInvocationError, l as agentErrorDetails, m as applyAgentToolPolicies, o as traceAgentInvocationStart, p as toAgentPublicError, r as traceAgentChannelDeliveryEffect, s as traceAgentStreamEvent, u as agentErrorMessage, v as withJsonCompatibleToolOutputs } from "./trace-GIqXV-YM.js";
+import { h as createStatusDeliveryEffectIntent, j as getChatCapabilityOptions, k as defineChatCapability, m as createReplyDeliveryEffectIntent, o as messageChannelSupportsTitleEffect, p as createReactionDeliveryEffectIntent, s as messageChannelTitleSupportContextKey, t as channelHasCustomTitleEffect } from "./channels-D6wOMJN1.js";
+import { t as normalizeAgentDriver } from "./agent-driver-ktEa_Dj0.js";
+import { a as parseScheduledAgentTurnInput, b as withReadableStreamCleanup, f as createAgentUIMessageStreamResponse, g as normalizeUiMessageStream, h as isUIMessageStreamResult, l as scheduledAgentTurnContextKey, m as isUIMessageStreamResponse, n as createBackedAgentInvocationController, o as scheduledAgentChannelIdsContextKey, p as finalizeUiMessageStreamOutput, r as startLiveAgentInvocation, s as scheduledAgentNameContextKey, v as uiMessageStreamFromResponse, x as agentOutputEventObserverContextKey, y as uiMessageTextDelta } from "./agent-invocation-D9tJRj_l.js";
+import { n as loadAgentWorkflowRuntimeStateModule, t as loadAgentWorkflowModule } from "./workflow-runtime-loaders-T5jPFIAJ.js";
+import { n as colocatedAgentSkillsSymbol, t as colocatedAgentSkillsContextKey } from "./colocated-agent-skills-BAz3kNyV.js";
+import { c as resolveWorkspaceInstructionBindings, d as workspaceDefinitionFromOptions, f as workspaceDefinitionWithAutoCommitRules, l as workspaceAgentOwnsWorkspaceDefinition, m as workspaceNameFromOptions, p as workspaceModeFromOptions, r as isWorkspaceAgentOptions, s as resolveWorkspaceAgentDefaultInstructions } from "./workspace-agent-BtTRGp6h.js";
+import { createTraceEventLog, getViteHubErrorShape, resolveRuntimeContext, resolveRuntimeValue } from "@vite-hub/runtime";
+import { Deferred, Effect, Exit } from "effect";
+import agentRegistry from "#vitehub/agent/registry";
+//#region src/internal/invocation-lifecycle.ts
+const failInvocation = Effect.fn("Agent.InvocationLifecycle.fail")(function* (deferred, complete, outcome, error, message) {
+	const started = yield* Deferred.succeed(deferred, outcome);
+	const exit = yield* Effect.exit(complete);
+	if (Exit.isFailure(exit) && started) {
+		const causes = agentEffectCauseValues(exit.cause);
+		const finishError = causes.length === 1 ? causes[0] : new AggregateError(causes, "[vitehub] Agent finish lifecycle failed for multiple reasons.");
+		return yield* Effect.fail(new EffectBoundaryFailure({ cause: new AggregateError([error, finishError], message) }));
+	}
+	return yield* Effect.fail(new EffectBoundaryFailure({ cause: error }));
+});
+const makeInvocationLifecycle = Effect.fn("Agent.InvocationLifecycle.open")(function* (finish) {
+	const deferred = yield* Deferred.make();
+	const complete = yield* Effect.cached(Deferred.await(deferred).pipe(Effect.flatMap((value) => tryAgentPromise(() => finish(value)))));
+	return {
+		fail: (outcome, error, message) => runAgentEffect(failInvocation(deferred, complete, outcome, error, message)),
+		finish: (outcome) => runAgentEffect(Deferred.succeed(deferred, outcome).pipe(Effect.andThen(complete)))
+	};
+});
+function openAgentInvocationLifecycle(finish) {
+	return runAgentEffect(makeInvocationLifecycle(finish));
+}
+//#endregion
+//#region src/run-events.ts
+const bindAgentRunEventsSymbol = Symbol("vitehub.bindAgentRunEvents");
+const agentRunEventsBrand = Symbol("vitehub.agentRunEvents");
+function assertRunId(runId) {
+	if (typeof runId !== "string" || !runId.trim()) throw new TypeError("[vitehub] Agent Run Events require a non-empty run id.");
+}
+function assertEvent(event) {
+	if (!event || typeof event !== "object" || typeof event.type !== "string" || !event.type.trim()) throw new TypeError("[vitehub] Agent Run Events require an event with a non-empty type.");
+}
+function isStore(value) {
+	return typeof value === "object" && value !== null;
+}
+async function resolveStore(input, context) {
+	const store = isStore(input) ? input : await input(context);
+	if (!store || typeof store.append !== "function" || typeof store.read !== "function" || typeof store.subscribe !== "function") throw new TypeError("[vitehub] Agent Run Events require a store with append(), read(), and subscribe().");
+	return store;
+}
+function defineAgentRunEvents(options) {
+	if (!options || typeof options !== "object" || !("store" in options)) throw new TypeError("[vitehub] defineAgentRunEvents() requires a store.");
+	return {
+		[agentRunEventsBrand]: true,
+		[bindAgentRunEventsSymbol](runtime) {
+			const runId = runtime.run?.runId;
+			if (!runId) return;
+			return { publish: async (event) => {
+				assertEvent(event);
+				return await (await resolveStore(options.store, {
+					operation: "publish",
+					runId,
+					runtime
+				})).append(runId, event);
+			} };
+		},
+		async publish(runId, event) {
+			assertRunId(runId);
+			assertEvent(event);
+			return await (await resolveStore(options.store, {
+				operation: "publish",
+				runId
+			})).append(runId, event);
+		},
+		async read(runId, cursor) {
+			assertRunId(runId);
+			return await (await resolveStore(options.store, {
+				operation: "read",
+				runId
+			})).read(runId, cursor === void 0 ? void 0 : { after: cursor });
+		},
+		async *subscribe(runId, cursor, subscribeOptions = {}) {
+			assertRunId(runId);
+			yield* (await resolveStore(options.store, {
+				operation: "subscribe",
+				runId
+			})).subscribe(runId, {
+				...cursor === void 0 ? {} : { after: cursor },
+				...subscribeOptions.signal ? { signal: subscribeOptions.signal } : {}
+			});
+		}
+	};
+}
+function bindAgentRunEvents(events, runtime) {
+	if (!events) return;
+	const bind = events[bindAgentRunEventsSymbol];
+	if (typeof bind !== "function") throw new TypeError("[vitehub] defineAgent({ runEvents }) requires a definition created by defineAgentRunEvents().");
+	return bind.call(events, runtime);
+}
+//#endregion
+//#region src/registry-error.ts
+function editDistance(left, right) {
+	const previous = Array.from({ length: right.length + 1 }, (_, index) => index);
+	for (let leftIndex = 0; leftIndex < left.length; leftIndex++) {
+		let diagonal = previous[0];
+		previous[0] = leftIndex + 1;
+		for (let rightIndex = 0; rightIndex < right.length; rightIndex++) {
+			const nextDiagonal = previous[rightIndex + 1];
+			previous[rightIndex + 1] = left[leftIndex] === right[rightIndex] ? diagonal : Math.min(diagonal, previous[rightIndex], previous[rightIndex + 1]) + 1;
+			diagonal = nextDiagonal;
+		}
+	}
+	return previous[right.length];
+}
+function formatUnknownAgentMessage(name, available, options = {}) {
+	const details = [];
+	const suggestion = available.map((candidate) => ({
+		candidate,
+		distance: editDistance(name, candidate)
+	})).sort((left, right) => left.distance - right.distance)[0];
+	if (suggestion && suggestion.distance <= Math.max(2, Math.floor(name.length / 3))) details.push(`Did you mean "${suggestion.candidate}"?`);
+	if (available.length) details.push(`Discovered agents: ${available.slice(0, 10).join(", ")}${available.length > 10 ? ", ..." : ""}.`);
+	if (options.prefix) details.push("Make sure @vite-hub/agent is configured and the agent is discovered.");
+	return `${options.prefix ? "[vitehub] " : ""}Unknown agent: ${name}.${details.length ? ` ${details.join(" ")}` : ""}`;
+}
+//#endregion
+//#region src/http-error.ts
+var AgentHttpError = class extends Error {
+	status;
+	statusCode;
+	constructor(statusCode, message) {
+		super(message);
+		this.name = "AgentHttpError";
+		this.status = statusCode;
+		this.statusCode = statusCode;
+	}
+};
+function getHttpErrorStatusCode(error) {
+	const statusCode = readAgentErrorProperty(error, "statusCode");
+	if (typeof statusCode === "number" && statusCode >= 400 && statusCode <= 599) return statusCode;
+	const code = getViteHubErrorShape(error)?.code;
+	if (code === "AUTHENTICATION_REQUIRED") return 401;
+	if (code === "RATE_LIMIT_REJECTED") return 429;
+	if (code === "RATE_LIMIT_UNAVAILABLE") return 503;
+	if (code === "LLM_GATE_REJECTED" || code === "CAPABILITY_DENIED") return 403;
+	if (code === "CAPABILITY_NOT_FOUND") return 404;
+}
+function isAgentHttpError(error) {
+	try {
+		return error instanceof AgentHttpError;
+	} catch {
+		return false;
+	}
+}
+function getHttpErrorMessage(error) {
+	if (isAgentHttpError(error)) {
+		const message = readAgentErrorProperty(error, "message");
+		if (typeof message === "string") return message;
+	}
+	return toAgentPublicError(error, "http").error;
+}
+function toHttpErrorResponse(error, fallbackStatus) {
+	const statusCode = getHttpErrorStatusCode(error) ?? fallbackStatus;
+	if (!statusCode) return;
+	if (isAgentHttpError(error)) return Response.json({ error: getHttpErrorMessage(error) }, { status: statusCode });
+	const body = toAgentPublicError(error, "http");
+	const retryAfter = body.details?.retryAfter;
+	return Response.json(body, {
+		headers: retryAfter === void 0 ? void 0 : {
+			"retry-after": String(retryAfter),
+			"x-retry-after": String(retryAfter)
+		},
+		status: statusCode
+	});
+}
+//#endregion
+//#region src/trigger-runtime.ts
+const agentTriggerContextKey = "agent.trigger";
+function hasAgentDefinition$1(value) {
+	return typeof value === "object" && value !== null && "resolve" in value && typeof value.resolve === "function";
+}
+function createAgentCallbackContext$1(context) {
+	const { runtimeConfig: _runtimeConfig, ...callbackContext } = context;
+	return callbackContext;
+}
+function agentCapabilityOptions(agent) {
+	if (!hasAgentDefinition$1(agent)) return [];
+	const workspaceOptions = agent.__vitehubWorkspaceAgentOptions;
+	const capabilities = agent.capabilities || workspaceOptions?.capabilities;
+	return Array.isArray(capabilities) ? capabilities : [];
+}
+function agentChannelOptions(agent) {
+	if (!hasAgentDefinition$1(agent)) return {};
+	const workspaceOptions = agent.__vitehubWorkspaceAgentOptions;
+	return agent.channels || workspaceOptions?.channels || {};
+}
+function assertTriggerName(name, owner) {
+	if (typeof name !== "string" || !name.trim()) throw new TypeError(`[vitehub] ${owner} trigger names must be non-empty strings.`);
+	if (!/^[a-z][a-z0-9-_]*$/i.test(name)) throw new TypeError(`[vitehub] ${owner} trigger "${name}" must be a stable local identifier.`);
+}
+function normalizeChannelWebhookRegistrations(channelId, kind, input) {
+	if (input === void 0) return void 0;
+	if (input === false) return [];
+	const registrations = input === true ? [{}] : Array.isArray(input) ? input : [input];
+	return registrations.map((registration, index) => ({
+		...registration,
+		adapter: registration.adapter || channelId,
+		channelId: registration.channelId || channelId,
+		id: registration.id || (registrations.length > 1 ? `${channelId}-${index + 1}` : channelId),
+		method: registration.method || "POST",
+		provider: registration.provider || kind
+	}));
+}
+function capabilityWebhookTriggerForChannel(triggers, channelId, kind) {
+	return triggers[`${channelId}.webhook`] || (kind !== channelId ? triggers[`${kind}.webhook`] : void 0);
+}
+async function resolveAgentTriggers$1(agent, context) {
+	const runtimeContext = createAgentCallbackContext$1(context);
+	const capabilities = normalizeCapabilities(agentCapabilityOptions(agent));
+	const triggers = {};
+	for (const capability of capabilities) for (const [name, trigger] of Object.entries(capability.triggers || {})) {
+		const id = `${capability.id}.${name}`;
+		triggers[id] = {
+			capabilityId: capability.id,
+			definition: trigger,
+			id,
+			input: trigger.input,
+			invoke: (input) => trigger.invoke({
+				...runtimeContext,
+				capability,
+				trigger: {
+					capabilityId: capability.id,
+					id,
+					name,
+					source: "capability"
+				}
+			}, input),
+			name,
+			output: trigger.output,
+			source: "capability",
+			webhooks: trigger.webhooks
+		};
+	}
+	for (const [channelId, channel] of Object.entries(agentChannelOptions(agent))) {
+		const channelCapabilities = normalizeCapabilities([...capabilities, ...channel.capabilities || []]);
+		const channelWebhooks = normalizeChannelWebhookRegistrations(channelId, channel.kind, channel.webhooks);
+		const capabilityWebhookTrigger = capabilityWebhookTriggerForChannel(triggers, channelId, channel.kind);
+		if (capabilityWebhookTrigger && channelWebhooks?.length) capabilityWebhookTrigger.webhooks = [...capabilityWebhookTrigger.webhooks || [], ...channelWebhooks];
+		for (const [name, trigger] of Object.entries(channel.triggers || {})) {
+			assertTriggerName(name, `Channel "${channelId}"`);
+			const id = `${channelId}.${name}`;
+			if (triggers[id]) throw new Error(`[vitehub] Duplicate Agent trigger "${id}" from Channel "${channelId}".`);
+			triggers[id] = {
+				channelId,
+				definition: trigger,
+				id,
+				input: trigger.input,
+				invoke: (input) => trigger.invoke({
+					...runtimeContext,
+					agentCapabilities: channelCapabilities,
+					channel,
+					trigger: {
+						channelId,
+						id,
+						name,
+						source: "channel"
+					}
+				}, input),
+				name,
+				output: trigger.output,
+				source: "channel",
+				webhooks: trigger.webhooks || channelWebhooks
+			};
+		}
+	}
+	return triggers;
+}
+function isResolvedAgentTriggerHandledInvocation(invocation) {
+	return "response" in invocation && invocation.response instanceof Response;
+}
+function isRecord(value) {
+	return typeof value === "object" && value !== null;
+}
+function isResolvableObject(value) {
+	return isRecord(value) && typeof value.resolve === "function";
+}
+async function resolveMaybe(value, context) {
+	if (value === void 0) return void 0;
+	if (typeof value === "function") return await value(context);
+	if (isResolvableObject(value)) return await value.resolve(context);
+	return value;
+}
+async function sha256(value) {
+	const bytes = new TextEncoder().encode(value);
+	return new Uint8Array(await globalThis.crypto.subtle.digest("SHA-256", bytes));
+}
+async function hmacSha256(secret, value) {
+	const key = await globalThis.crypto.subtle.importKey("raw", new TextEncoder().encode(secret), {
+		hash: "SHA-256",
+		name: "HMAC"
+	}, false, ["sign"]);
+	const signature = await globalThis.crypto.subtle.sign("HMAC", key, new TextEncoder().encode(value));
+	return [...new Uint8Array(signature)].map((byte) => byte.toString(16).padStart(2, "0")).join("");
+}
+async function constantTimeEqual(left, right) {
+	const [leftDigest, rightDigest] = await Promise.all([sha256(left), sha256(right)]);
+	let diff = leftDigest.length ^ rightDigest.length;
+	for (let index = 0; index < Math.max(leftDigest.length, rightDigest.length); index += 1) diff |= (leftDigest[index] ?? 0) ^ (rightDigest[index] ?? 0);
+	return diff === 0;
+}
+function webhookVerificationError(message) {
+	return new AgentHttpError(401, message);
+}
+async function verifyRequiredWebhookHeaders(registrations, context) {
+	for (const registration of registrations) {
+		const secretToken = await resolveMaybe(registration.secretToken, context);
+		if (!registration.secretHeader) {
+			if (secretToken === false) return {
+				registration,
+				verified: true
+			};
+			if (secretToken) throw webhookVerificationError(`[vitehub] Webhook registration "${registration.id || registration.provider}" declares secretToken but no secretHeader is configured. Verification requires secretHeader; secretToken: false explicitly disables verification.`);
+			continue;
+		}
+		if (secretToken === false) return {
+			registration,
+			verified: true
+		};
+		if (!secretToken) throw webhookVerificationError(`[vitehub] Webhook registration "${registration.id || registration.provider}" declares secretHeader "${registration.secretHeader}" but no secretToken is configured. Verification requires secretToken from Server Env; secretToken: false explicitly disables verification.`);
+		throw webhookVerificationError(`[vitehub] Webhook secret header "${registration.secretHeader}" is required.`);
+	}
+	return { verified: true };
+}
+async function verifyAgentWebhookRequest(registrations, request, context, options = {}) {
+	const verificationContext = context ?? { runtime: "unknown" };
+	const targeted = registrations.map((registration) => ({
+		headerValue: registration.secretHeader ? request.headers.get(registration.secretHeader) : null,
+		registration
+	})).filter((entry) => entry.headerValue !== null);
+	if (!targeted.length) return options.requireSecretHeader ? await verifyRequiredWebhookHeaders(registrations, verificationContext) : { verified: true };
+	for (const { headerValue, registration } of targeted) {
+		const secretToken = await resolveMaybe(registration.secretToken, verificationContext);
+		if (secretToken === false) return {
+			registration,
+			verified: true
+		};
+		if (!secretToken) throw webhookVerificationError(`[vitehub] Webhook registration "${registration.id || registration.provider}" declares secretHeader "${registration.secretHeader}" but no secretToken is configured. Verification requires secretToken from Server Env; secretToken: false explicitly disables verification.`);
+		if (registration.signature === "github-sha256") {
+			if (await constantTimeEqual(`sha256=${await hmacSha256(secretToken, await request.clone().text())}`, headerValue)) return {
+				registration,
+				verified: true
+			};
+			continue;
+		}
+		if (await constantTimeEqual(secretToken, headerValue)) return {
+			registration,
+			verified: true
+		};
+	}
+	throw webhookVerificationError("[vitehub] Webhook secret verification failed.");
+}
+function requiresWebhookSecretHeader(registrations) {
+	return registrations.some((registration) => registration.secretToken !== void 0 && registration.secretToken !== false);
+}
+function withAgentTriggerContext(input, trigger, delivery) {
+	const context = { ...input.context };
+	const effects = delivery?.effects ? Array.isArray(delivery.effects) ? delivery.effects : [delivery.effects] : void 0;
+	const finishEffects = delivery?.finishEffects ? Array.isArray(delivery.finishEffects) ? delivery.finishEffects : [delivery.finishEffects] : void 0;
+	if (effects?.length) context[channelDeliveryEffectsContextKey] = effects;
+	if (finishEffects?.length) context[channelDeliveryFinishEffectsContextKey] = finishEffects;
+	return {
+		...input,
+		context: {
+			...context,
+			[agentTriggerContextKey]: {
+				...trigger.capabilityId ? { capabilityId: trigger.capabilityId } : {},
+				...trigger.channelId ? { channelId: trigger.channelId } : {},
+				id: trigger.id,
+				name: trigger.name,
+				source: trigger.source
+			}
+		}
+	};
+}
+async function resolveAgentTriggerInvocation$1(agent, context, triggerId, input) {
+	const trigger = (await resolveAgentTriggers$1(agent, context))[triggerId];
+	if (!trigger) throw new Error(`[vitehub] Agent trigger "${triggerId}" is not defined by this agent.`);
+	if (trigger.webhooks?.length && context.request) await verifyAgentWebhookRequest(trigger.webhooks, context.request, createAgentCallbackContext$1(context), { requireSecretHeader: requiresWebhookSecretHeader(trigger.webhooks) });
+	const invocation = await trigger.invoke(input);
+	if (invocation instanceof Response) return {
+		response: invocation,
+		trigger
+	};
+	const runInvocation = invocation;
+	return {
+		input: withAgentTriggerContext(runInvocation.input, trigger, runInvocation.delivery),
+		metadata: runInvocation.metadata,
+		run: runInvocation.run,
+		trigger,
+		webhook: runInvocation.webhook
+	};
+}
+async function runAgentTriggerWith(executor, agent, context, triggerId, input) {
+	const invocation = await resolveAgentTriggerInvocation$1(agent, context, triggerId, input);
+	if (isResolvedAgentTriggerHandledInvocation(invocation)) return invocation.response;
+	return await executor(agent, {
+		...context,
+		...invocation.run ? { run: invocation.run } : {}
+	}, invocation.input);
+}
+async function streamAgentTriggerWith(executor, agent, context, triggerId, input, options = {}) {
+	const invocation = await resolveAgentTriggerInvocation$1(agent, context, triggerId, input);
+	if (isResolvedAgentTriggerHandledInvocation(invocation)) return invocation.response;
+	await options.onInvocation?.(invocation);
+	const output = options.output || (invocation.trigger.output === "ui-message-stream" ? "ui-message-stream" : "events");
+	return await executor(agent, {
+		...context,
+		...invocation.run ? { run: invocation.run } : {}
+	}, invocation.input, { output });
+}
+//#endregion
+//#region src/harness/shared-box.ts
+const harnessBoxOpen = Symbol("vitehub.agent.harness-box-open");
+const harnessBoxActive = Symbol("vitehub.agent.harness-box-active");
+function isHarnessBoxActive(box) {
+	return box[harnessBoxActive]?.() ?? false;
+}
+async function openHarnessBox(box, options) {
+	const shared = box;
+	return shared[harnessBoxOpen] ? await shared[harnessBoxOpen](options) : await box.open(options);
+}
+function shareBoxSessions(box) {
+	let active;
+	let harnessActive = false;
+	let initializing;
+	let leases = 0;
+	const lease = (session) => {
+		leases++;
+		let closed = false;
+		return {
+			...session,
+			async close() {
+				if (closed) return;
+				closed = true;
+				leases--;
+				if (leases === 0) {
+					const current = active;
+					active = void 0;
+					harnessActive = false;
+					if (current) await (await current).close();
+				}
+			}
+		};
+	};
+	return {
+		plan: box.plan,
+		[harnessBoxActive]() {
+			return harnessActive;
+		},
+		async open(options) {
+			if (initializing) {
+				options?.signal?.throwIfAborted();
+				return lease(initializing);
+			}
+			await Promise.resolve();
+			options?.signal?.throwIfAborted();
+			active ||= box.open(options);
+			try {
+				return lease(await active);
+			} catch (error) {
+				active = void 0;
+				throw error;
+			}
+		},
+		async [harnessBoxOpen](options) {
+			if (!active) {
+				harnessActive = true;
+				let harnessLease;
+				active = box.open({
+					...options,
+					...options?.initialize ? { async initialize(session, context) {
+						initializing = session;
+						harnessLease = lease(session);
+						try {
+							await options.initialize(harnessLease, context);
+						} finally {
+							initializing = void 0;
+						}
+					} } : {}
+				});
+				try {
+					const session = await active;
+					return harnessLease || lease(session);
+				} catch (error) {
+					leases = 0;
+					harnessActive = false;
+					initializing = void 0;
+					active = void 0;
+					throw error;
+				}
+			}
+			return lease(await active);
+		}
+	};
+}
+//#endregion
+//#region src/gateway.ts
+function unseal(value) {
+	return typeof value === "object" ? value.unseal() : value;
+}
+function gateway(model, settings = {}) {
+	if (typeof model !== "string" || !model.trim()) throw new TypeError("[vitehub] gateway() requires a non-empty model identifier.");
+	return async (context) => {
+		const { apiKey, ...providerSettings } = await resolveRuntimeValue(settings, context);
+		const { createGateway } = await import("@ai-sdk/gateway");
+		return createGateway({
+			...providerSettings,
+			...apiKey ? { apiKey: unseal(apiKey) } : {}
+		})(model);
+	};
+}
+//#endregion
+//#region src/invocation-stream.ts
+const agentInvocationStreamRoute = "/__vitehub/agent/invocation-stream";
+const agentInvocationStreamHeader = "x-vitehub-agent-dev-loop";
+async function* readAgentInvocationStream(body) {
+	const reader = body.pipeThrough(new TextDecoderStream()).getReader();
+	let pending = "";
+	try {
+		for (;;) {
+			const { done, value } = await reader.read();
+			if (done) break;
+			pending += value;
+			const lines = pending.split("\n");
+			pending = lines.pop() || "";
+			for (const line of lines) if (line.trim()) yield JSON.parse(line);
+		}
+		if (pending.trim()) yield JSON.parse(pending);
+	} finally {
+		reader.releaseLock();
+	}
+}
+function createAgentInvocationStreamResponse(run, options = {}) {
+	const encoder = new TextEncoder();
+	const abortController = new AbortController();
+	let closed = false;
+	let timeout;
+	function clearTimeoutSignal() {
+		if (!timeout) return;
+		clearTimeout(timeout);
+		timeout = void 0;
+	}
+	function close(controller) {
+		clearTimeoutSignal();
+		closed = true;
+		controller.close();
+	}
+	function write(controller, event) {
+		controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
+	}
+	function closeFailed(controller) {
+		if (closed) return;
+		try {
+			write(controller, { type: "done" });
+			close(controller);
+		} catch (cause) {
+			closed = true;
+			controller.error(cause);
+		}
+	}
+	function fail(controller, cause, context = "serialization") {
+		if (closed) return;
+		clearTimeoutSignal();
+		abort(cause);
+		try {
+			write(controller, {
+				...toAgentPublicError(cause, context),
+				type: "error"
+			});
+			closeFailed(controller);
+		} catch {
+			closed = true;
+			controller.error(cause);
+		}
+	}
+	function abort(cause) {
+		try {
+			abortController.abort(cause);
+		} catch (error) {
+			if (!isAbortError(error)) throw error;
+		}
+	}
+	function emit(controller, event) {
+		if (closed || abortController.signal.aborted) return false;
+		try {
+			controller.enqueue(encoder.encode(`${JSON.stringify(event)}\n`));
+			return true;
+		} catch (cause) {
+			fail(controller, cause);
+			return false;
+		}
+	}
+	return new Response(new ReadableStream({
+		start(controller) {
+			if (options.timeout && options.timeout > 0) timeout = setTimeout(() => {
+				fail(controller, /* @__PURE__ */ new Error(`Agent Invocation Stream timed out after ${options.timeout}ms.`), "invocation");
+			}, options.timeout);
+			run((event) => emit(controller, event), abortController.signal).then(() => {
+				if (closed || abortController.signal.aborted) return;
+				if (emit(controller, { type: "done" })) close(controller);
+			}).catch((cause) => {
+				if (closed || abortController.signal.aborted) return;
+				clearTimeoutSignal();
+				emit(controller, {
+					...toAgentPublicError(cause, "invocation"),
+					type: "error"
+				});
+				if (emit(controller, { type: "done" })) close(controller);
+			});
+		},
+		cancel() {
+			clearTimeoutSignal();
+			closed = true;
+			abort();
+		}
+	}), { headers: { "content-type": "application/x-ndjson" } });
+}
+function isAbortError(error) {
+	return error instanceof Error && error.name === "AbortError";
+}
+//#endregion
+//#region src/index.ts
+const syntheticWorkspaceRun = Symbol.for("vitehub.syntheticWorkspaceRun");
+const baseAgentResolve = Symbol.for("vitehub.baseAgentResolve");
+const baseAgentModel = Symbol.for("vitehub.baseAgentModel");
+const baseAgentDriverKind = Symbol.for("vitehub.baseAgentDriverKind");
+const baseAgentDefinitionResolve = Symbol.for("vitehub.baseAgentDefinitionResolve");
+const baseAgentOutput = Symbol.for("vitehub.baseAgentOutput");
+const baseAgentBoxRequirements = Symbol.for("vitehub.baseAgentBoxRequirements");
+const baseAgentCapabilitiesResolver = Symbol.for("vitehub.baseAgentCapabilitiesResolver");
+const agentWorkflowHandles = /* @__PURE__ */ new WeakMap();
+const agentWorkflowNames = /* @__PURE__ */ new Set();
+const agentIdentityOwner = Symbol("vitehub.agentIdentityOwner");
+function withAgentIdentityOwner(agent, context) {
+	if (!context.agentIdentity || context[agentIdentityOwner]) return context;
+	return {
+		...context,
+		[agentIdentityOwner]: agent
+	};
+}
+function hasAgentDefinition(value) {
+	return typeof value === "object" && value !== null && "resolve" in value && typeof value.resolve === "function";
+}
+function resolveAgentWorkflowRuntimeBinding(agent) {
+	if (!hasAgentDefinition(agent)) return void 0;
+	return agent.runtime && agent.runtime.kind === "workflow" ? agent.runtime : void 0;
+}
+function resolveAgentWorkflowName(agent, binding, context) {
+	const definition = hasAgentDefinition(agent) ? agent : void 0;
+	const name = binding.name || ("discoveryDefault" in binding ? context.agentIdentity?.name : definition?.name || context.agentIdentity?.name);
+	if (name) return name;
+	throw new Error("[vitehub] Agent runtime workflow() requires a name when invoked directly. A stable Workflow Definition target requires workflow(\"name\").");
+}
+async function getAgentWorkflowHandle(agent, name, reuseRegistry) {
+	const handles = agentWorkflowHandles.get(agent) || /* @__PURE__ */ new Map();
+	const cacheKey = `${reuseRegistry ? "registry" : "inline"}:${name}`;
+	const existing = handles.get(cacheKey);
+	if (existing) return existing;
+	const { createWorkflow } = await loadAgentWorkflowModule();
+	const { getInlineWorkflowDefinitions, getWorkflowRuntimeRegistry } = await loadAgentWorkflowRuntimeStateModule();
+	const handle = reuseRegistry && getWorkflowRuntimeRegistry()?.[name] || agentWorkflowNames.has(name) && getInlineWorkflowDefinitions().has(name) ? createWorkflow(name) : createWorkflow(name, async (workflowContext) => {
+		const { runAgentWorkflowDefinition } = await import("./runtime/workflow.js");
+		return await runAgentWorkflowDefinition(agent, workflowContext, runAgentInline);
+	});
+	agentWorkflowNames.add(name);
+	handles.set(cacheKey, handle);
+	agentWorkflowHandles.set(agent, handles);
+	return handle;
+}
+async function runAgentAsWorkflow(agent, context, input, options = {}) {
+	const binding = resolveAgentWorkflowRuntimeBinding(agent);
+	if (!binding || "discoveryDefault" in binding && !context.agentIdentity) return void 0;
+	if ("discoveryDefault" in binding) {
+		const { getWorkflowRuntimeConfig } = await loadAgentWorkflowRuntimeStateModule();
+		if (!getWorkflowRuntimeConfig()) return void 0;
+	}
+	if ("discoveryDefault" in binding && context.agentIdentity) {
+		const owner = context[agentIdentityOwner];
+		if (owner && owner !== agent) return void 0;
+	}
+	const capabilityNames = Object.entries(context.capabilities || {}).filter(([, capability]) => capability !== false).map(([name]) => name);
+	const disabledCapabilities = Object.fromEntries(Object.entries(context.capabilities || {}).filter(([, capability]) => capability === false));
+	if ("discoveryDefault" in binding && capabilityNames.length) return void 0;
+	const handle = await getAgentWorkflowHandle(agent, resolveAgentWorkflowName(agent, binding, context), Boolean(context.agentIdentity));
+	const resolvedContext = createResolvedRuntimeContext(context);
+	const workflowInput = { ...input };
+	delete workflowInput.abortSignal;
+	const inheritedRun = options.fresh && context.run ? Object.fromEntries(Object.entries(context.run).filter(([key]) => key !== "runId")) : context.run;
+	const payload = {
+		...context.agentIdentity ? { agentIdentity: context.agentIdentity } : {},
+		...Object.keys(disabledCapabilities).length ? { capabilities: disabledCapabilities } : {},
+		input: workflowInput,
+		runtime: context.runtime,
+		runtimeConfig: resolvedContext.runtimeConfig,
+		...inheritedRun ? { run: inheritedRun } : {}
+	};
+	const workflowEvent = {
+		...context.cloudflare?.env ? { env: context.cloudflare.env } : {},
+		waitUntil: context.waitUntil,
+		context: {
+			...context.cloudflare ? { cloudflare: context.cloudflare } : {},
+			waitUntil: context.waitUntil
+		}
+	};
+	const { runWithWorkflowRuntimeEvent } = await loadAgentWorkflowRuntimeStateModule();
+	return {
+		handle,
+		run: await runWithWorkflowRuntimeEvent(workflowEvent, () => handle.run(payload, !options.fresh && context.run?.runId ? { id: context.run.runId } : {}))
+	};
+}
+function resolveRegistryModule(module) {
+	return typeof module === "object" && module !== null && "default" in module ? module.default : module;
+}
+function createResolvedRuntimeContext(context) {
+	return resolveRuntimeContext(context);
+}
+function createTraceId(run) {
+	return run?.runId || globalThis.crypto?.randomUUID?.() || `agent-${Date.now()}-${Math.random().toString(36).slice(2)}`;
+}
+function createAgentCallbackContext(context) {
+	const { runtimeConfig: _runtimeConfig, ...callbackContext } = createResolvedRuntimeContext(context);
+	return callbackContext;
+}
+function channelDeliveryEffectHandlers(channel, intent) {
+	const handlers = channel.effects?.[intent.kind];
+	if (!handlers) return [];
+	return typeof handlers === "function" ? [handlers] : [...handlers];
+}
+function activeAgentChannel(channels, context, run) {
+	const trigger = context.get("agent.trigger");
+	const channelId = run?.channelId || trigger?.channelId;
+	const channel = channelId ? channels?.[channelId] : void 0;
+	return channel && channelId ? {
+		channel,
+		channelId,
+		trigger
+	} : void 0;
+}
+async function setChannelDeliverySupportContext(channels, invocationContext, runtimeContext, input, run) {
+	const active = activeAgentChannel(channels, invocationContext, run);
+	if (!active) return;
+	if (!channelDeliveryEffectHandlers(active.channel, { kind: "title" }).length) {
+		invocationContext.set(messageChannelTitleSupportContextKey, false, { overwrite: true });
+		return;
+	}
+	if (channelHasCustomTitleEffect(active.channel)) {
+		invocationContext.set(messageChannelTitleSupportContextKey, true, { overwrite: true });
+		return;
+	}
+	const { runtimeConfig: _runtimeConfig, ...callbackContext } = runtimeContext;
+	const supported = await messageChannelSupportsTitleEffect({
+		...callbackContext,
+		channel: active.channel,
+		context: invocationContext,
+		effect: { kind: "title" },
+		input,
+		request: runtimeContext.request,
+		run,
+		trigger: {
+			channelId: active.channelId,
+			...active.trigger?.id ? { id: active.trigger.id } : {},
+			...active.trigger?.name ? { name: active.trigger.name } : {}
+		}
+	});
+	invocationContext.set(messageChannelTitleSupportContextKey, supported, { overwrite: true });
+}
+async function applyChannelDeliveryEffectIntents(context, intents, finish) {
+	if (!intents.length) return;
+	const active = activeAgentChannel(context.channels, context.context, context.run);
+	for (const intent of intents) {
+		const handlers = active ? channelDeliveryEffectHandlers(active.channel, intent) : [];
+		const metadata = {
+			"channel.effect.intent": intent.intent,
+			"channel.effect.kind": intent.kind,
+			"channel.effect.supported": handlers.length > 0
+		};
+		if (!active || !handlers.length) {
+			await runObservedAgentHook(context.hooks, {
+				ids: {
+					channelId: active?.channelId,
+					runId: context.run?.runId
+				},
+				metadata,
+				name: "channel:delivery-effect",
+				owner: "channel",
+				phase: "effect"
+			}, async () => {
+				await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, metadata);
+			});
+			continue;
+		}
+		const titleDelivery = isMessageChannelTitleEffectIntent(intent) ? await prepareMessageChannelTitleDelivery(context.context, context.run, intent).catch(async (error) => {
+			await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, {
+				...metadata,
+				"error.message": agentErrorMessage(error)
+			});
+			return { deliver: true };
+		}) : void 0;
+		if (titleDelivery?.error) await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, {
+			...metadata,
+			"error.message": agentErrorMessage(titleDelivery.error)
+		});
+		if (titleDelivery && !titleDelivery.deliver) {
+			await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, {
+				...metadata,
+				"channel.effect.skipped": titleDelivery.reason
+			});
+			continue;
+		}
+		let delivered = true;
+		for (const handler of handlers) try {
+			await runObservedAgentHook(context.hooks, {
+				ids: {
+					channelId: active.channelId,
+					runId: context.run?.runId
+				},
+				metadata,
+				name: "channel:delivery-effect",
+				owner: "channel",
+				phase: "effect"
+			}, async () => {
+				await handler({
+					...context.runtimeContext,
+					channel: active.channel,
+					context: context.context,
+					effect: intent,
+					...finish ? { finish } : {},
+					input: context.input,
+					request: context.runtimeContext.request,
+					run: context.run,
+					trigger: {
+						channelId: active.channelId,
+						...active.trigger?.id ? { id: active.trigger.id } : {},
+						...active.trigger?.name ? { name: active.trigger.name } : {}
+					},
+					workspace: context.workspace
+				});
+				await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, metadata);
+			});
+		} catch (error) {
+			delivered = false;
+			await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, {
+				...metadata,
+				"error.message": agentErrorMessage(error)
+			});
+		}
+		if (titleDelivery) try {
+			await finishMessageChannelTitleDelivery(titleDelivery, delivered, Boolean(finish));
+		} catch (error) {
+			await traceAgentChannelDeliveryEffect(toTraceContext(context), intent, {
+				...metadata,
+				"error.message": agentErrorMessage(error)
+			});
+		}
+		if (titleDelivery && delivered) context.context.set(messageChannelTitleDeliveredContextKey, true, { overwrite: true });
+	}
+}
+function resolveCapabilityCliRunSurface(definition) {
+	if (definition?.cli?.capabilities !== void 0) return definition.cli.capabilities !== false;
+	return true;
+}
+function normalizeAgentChannels(inputs) {
+	if (!inputs) return inputs;
+	let channels;
+	for (const [id, input] of Object.entries(inputs)) {
+		if (typeof input !== "function") continue;
+		const channel = input();
+		if (!channel || typeof channel !== "object" || typeof channel.kind !== "string") throw new TypeError(`[vitehub] Channel factory "${id}" must return an Agent Channel definition.`);
+		channels ||= { ...inputs };
+		channels[id] = channel;
+	}
+	return channels || inputs;
+}
+function capabilitiesContributeWorkspace(capabilities) {
+	return Array.isArray(capabilities) && normalizeCapabilities(capabilities).some((capability) => Boolean(capability.workspace));
+}
+function capabilitiesRequireWritableWorkspace(capabilities) {
+	return Array.isArray(capabilities) && normalizeCapabilities(capabilities).some((capability) => capability.requires?.some((requirement) => requirement.workspace?.mode === "write"));
+}
+function agentContributesWorkspace(options) {
+	if (capabilitiesContributeWorkspace(options.capabilities)) return true;
+	return Object.values(options.channels || {}).some((channel) => capabilitiesContributeWorkspace(channel.capabilities));
+}
+function agentRequiresWritableWorkspace(options) {
+	if (capabilitiesRequireWritableWorkspace(options.capabilities)) return true;
+	return Object.values(options.channels || {}).some((channel) => capabilitiesRequireWritableWorkspace(channel.capabilities));
+}
+function defineBaseAgent(options) {
+	const driver = normalizeAgentDriver(options);
+	const { box, capabilities, cli, description, hooks, messages, name, runtime = defaultAgentWorkflowRuntime(), runEvents, uiMessageStream, version, workspace } = options;
+	const channels = normalizeAgentChannels(options.channels);
+	if (box && driver.kind !== "harness") throw new Error("[vitehub] defineAgent({ box }) currently requires a harness Agent Driver.");
+	if (box && driver.kind === "harness" && (driver.hasSandbox || driver.sandbox !== void 0 || driver.workDir !== void 0)) throw new Error("[vitehub] defineAgent({ box }) owns harness execution. Move driver.sandbox and driver.workDir to the Box.");
+	const run = driver.kind === "run" ? driver.run : void 0;
+	const capabilitiesResolver = typeof capabilities === "function" ? capabilities : void 0;
+	const baseCapabilities = normalizeCapabilities(Array.isArray(capabilities) ? capabilities : void 0);
+	const invoker = normalizeAgentInvokerOptions(options.invoker);
+	const channelChat = resolveAgentChannelChatOptions(channels, messages);
+	const chatCapability = getChatCapabilityOptions(baseCapabilities);
+	if (chatCapability && channelChat) throw new TypeError("[vitehub] defineAgent({ channels }) cannot be combined with the chat() capability. Move chat options to defineAgent({ messages, channels }).");
+	const chat = chatCapability || channelChat;
+	const normalizedCapabilities = channelChat ? [...baseCapabilities, defineChatCapability(channelChat)] : baseCapabilities;
+	if (!workspace) validateAgentCapabilityComposition(normalizedCapabilities, { hasWorkspace: false });
+	const resolveBaseAgent = async (context) => {
+		const resolvedAdapter = driver.kind === "model" ? (await import("./ai-sdk.js")).createAiSdkAdapter({
+			execution: driver.execution,
+			instructions: driver.instructions,
+			model: driver.model
+		}) : driver.kind === "harness" ? (await import("./harness-agent-CklIYSAq.js")).createHarnessAgentAdapter(driver.resolve ? await driver.resolve() : driver) : void 0;
+		if (!resolvedAdapter) throw new Error("[vitehub] Agent Driver is required unless the agent uses driver.run.");
+		const resolvedContext = createResolvedRuntimeContext(context);
+		return typeof resolvedAdapter === "function" ? await resolvedAdapter(resolvedContext) : resolvedAdapter;
+	};
+	const definition = {
+		...driver.kind === "harness" && driver.requires?.length ? { [baseAgentBoxRequirements]: driver.requires } : {},
+		...driver.kind === "model" ? { [baseAgentModel]: driver.model } : {},
+		[baseAgentDriverKind]: driver.kind,
+		...driver.output ? { [baseAgentOutput]: driver.output } : {},
+		...capabilitiesResolver ? { [baseAgentCapabilitiesResolver]: capabilitiesResolver } : {},
+		[baseAgentResolve]: resolveBaseAgent,
+		box,
+		channels,
+		chat,
+		cli,
+		description,
+		hooks,
+		invoker,
+		messages,
+		name,
+		runtime,
+		runEvents,
+		run,
+		uiMessageStream,
+		version,
+		workspace,
+		...normalizedCapabilities.length ? { capabilities: normalizedCapabilities } : {},
+		async resolve(context) {
+			context = withAgentIdentityOwner(definition, context);
+			const adapterInstance = await resolveBaseAgent(context);
+			const resolvedContext = createResolvedRuntimeContext(context);
+			const resolvedTools = driver.kind === "model" && normalizedCapabilities.length && !workspace ? await resolveStaticCapabilityTools({ capabilities: normalizedCapabilities }, resolvedContext) : void 0;
+			const capabilityTools = Object.keys(resolvedTools || {}).length ? withAgentToolStepReporting(withJsonCompatibleToolOutputs(applyAgentToolPolicies(resolvedTools) || {}), context.toolStepReporter) : void 0;
+			return capabilityTools ? {
+				...adapterInstance,
+				tools: capabilityTools
+			} : adapterInstance;
+		}
+	};
+	Object.defineProperty(definition, "__vitehubAgentSettings", { value: channels === options.channels ? options : {
+		...options,
+		channels
+	} });
+	Object.defineProperty(definition, baseAgentDefinitionResolve, { value: definition.resolve });
+	return definition;
+}
+function workflow(name) {
+	return {
+		kind: "workflow",
+		...name ? { name } : {}
+	};
+}
+function defaultAgentWorkflowRuntime() {
+	return {
+		discoveryDefault: true,
+		kind: "workflow"
+	};
+}
+function createSyntheticWorkspaceRun(definition) {
+	const run = async (context) => {
+		const adapter = await resolveAgentForRun(definition, context);
+		const invocationContext = await createAgentInvocationContext(definition, context, context.input);
+		const result = await adapter.generate(toAgentAdapterRunContext(invocationContext));
+		return typeof result === "object" && result && "text" in result && typeof result.text === "string" ? result.text : result;
+	};
+	return Object.assign(run, { [syntheticWorkspaceRun]: true });
+}
+function createWorkspaceAgentDefinition(options) {
+	const workspaceDefinition = workspaceDefinitionFromOptions(options);
+	if (Array.isArray(options.capabilities)) validateAgentCapabilityComposition(options.capabilities, {
+		hasBox: Boolean(options.box),
+		hasWorkspace: true,
+		workspaceMode: workspaceModeFromOptions(options)
+	});
+	const definition = defineBaseAgent({
+		...options,
+		description: options.description,
+		hooks: options.hooks,
+		runtime: options.runtime,
+		version: options.version,
+		workspace: workspaceDefinition
+	});
+	if (!definition.run) definition.run = createSyntheticWorkspaceRun(definition);
+	Object.assign(definition, workspaceDefinition, {
+		__vitehubWorkspaceAgent: true,
+		__vitehubWorkspaceAgentOptions: options
+	});
+	return definition;
+}
+const defineAgent = ((options) => {
+	const agentOptions = options;
+	const channels = normalizeAgentChannels(agentOptions.channels);
+	const normalizedOptions = channels === agentOptions.channels ? agentOptions : {
+		...agentOptions,
+		channels
+	};
+	if (isWorkspaceAgentOptions(normalizedOptions)) return createWorkspaceAgentDefinition(normalizedOptions);
+	return agentContributesWorkspace({
+		capabilities: normalizedOptions.capabilities,
+		channels
+	}) ? createWorkspaceAgentDefinition({
+		...normalizedOptions,
+		workspace: { mode: agentRequiresWritableWorkspace({
+			capabilities: normalizedOptions.capabilities,
+			channels
+		}) ? "write" : "read" }
+	}) : defineBaseAgent(normalizedOptions);
+});
+function agentWithColocatedInstructions(agent, instructions) {
+	if (!instructions || !hasAgentDefinition(agent)) return agent;
+	const settings = agent.__vitehubAgentSettings;
+	if (!settings || settings.workspace) return agent;
+	const driver = normalizeAgentDriver(settings);
+	if (driver.kind !== "model" || driver.instructions !== void 0) return agent;
+	const definition = defineAgent({
+		...settings,
+		driver: {
+			...settings.driver,
+			instructions
+		}
+	});
+	const decorations = Object.getOwnPropertyDescriptors(agent);
+	delete decorations.__vitehubAgentSettings;
+	Reflect.deleteProperty(decorations, baseAgentResolve);
+	Reflect.deleteProperty(decorations, baseAgentModel);
+	Reflect.deleteProperty(decorations, baseAgentDriverKind);
+	Reflect.deleteProperty(decorations, baseAgentDefinitionResolve);
+	if (agent.resolve === agent[baseAgentDefinitionResolve]) delete decorations.resolve;
+	Object.setPrototypeOf(definition, Object.getPrototypeOf(agent));
+	Object.defineProperties(definition, decorations);
+	return definition;
+}
+async function resolveAgent(agent, context) {
+	if (hasAgentDefinition(agent)) return await agent.resolve(context);
+	throw new TypeError("[vitehub] Invalid agent definition.");
+}
+async function resolveAgentForRun(agent, context) {
+	if (hasAgentDefinition(agent)) {
+		const resolver = agent[baseAgentResolve];
+		if (resolver) return await resolver(context);
+	}
+	return await resolveAgent(agent, context);
+}
+async function getAgentFromRegistry(name, registry = agentRegistry) {
+	const loader = registry[name];
+	if (!loader) throw new Error(formatUnknownAgentMessage(name, Object.keys(registry).sort(), { prefix: true }));
+	const agent = resolveRegistryModule(await loader());
+	if (!agent) throw new Error(`[vitehub] Agent "${name}" did not export a valid default agent.`);
+	return agent;
+}
+async function resolveAgentTriggers(agent, context) {
+	return await resolveAgentTriggers$1(agent, createResolvedRuntimeContext(context));
+}
+async function resolveAgentTriggerInvocation(agent, context, triggerId, input) {
+	return await resolveAgentTriggerInvocation$1(agent, createResolvedRuntimeContext(context), triggerId, input);
+}
+async function runAgentTrigger(agent, context, triggerId, input) {
+	return await runAgentTriggerWith(runAgent, agent, createResolvedRuntimeContext(context), triggerId, input);
+}
+async function streamAgentTrigger(agent, context, triggerId, input, options = {}) {
+	return await streamAgentTriggerWith(streamAgent, agent, createResolvedRuntimeContext(context), triggerId, input, options);
+}
+function hasCustomRun(agent) {
+	return hasAgentDefinition(agent) && typeof agent.run === "function" && !(syntheticWorkspaceRun in agent.run);
+}
+function toAgentAdapterRunContext(context) {
+	return {
+		...context,
+		box: context.box,
+		instructions: context.instructions,
+		modelExecutionInstrumentation: context.modelExecutionInstrumentation,
+		runtime: context.runtimeContext,
+		workspace: context.workspace
+	};
+}
+async function resolveRegisteredAgentWorkspaceDefinition(name) {
+	try {
+		return await (await import("@vite-hub/workspace")).resolveRegisteredWorkspaceDefinition(name);
+	} catch (error) {
+		if (getViteHubErrorShape(error)?.code === "WORKSPACE_NOT_FOUND") return void 0;
+		throw error;
+	}
+}
+function withBoxWorkspaceSessions(workspace, box) {
+	async function startBoxSession(target, options) {
+		if (options?.host) return await target.startSession(options);
+		const host = await box.open();
+		try {
+			const session = await target.startSession({
+				...options,
+				attach: isHarnessBoxActive(box),
+				host,
+				target: options?.target || host.cwd
+			});
+			let closePromise;
+			return {
+				...session,
+				async close() {
+					closePromise ??= (async () => {
+						let sessionError;
+						try {
+							await session.close();
+						} catch (error) {
+							sessionError = error;
+						}
+						try {
+							await host.close();
+						} catch (error) {
+							if (sessionError) throw new AggregateError([sessionError, error], "[vitehub] Workspace and Box session cleanup failed.");
+							throw error;
+						}
+						if (sessionError) throw sessionError;
+					})();
+					await closePromise;
+				}
+			};
+		} catch (error) {
+			try {
+				await host.close();
+			} catch (closeError) {
+				throw new AggregateError([error, closeError], "[vitehub] Workspace session setup and Box cleanup failed.");
+			}
+			throw error;
+		}
+	}
+	return {
+		...workspace,
+		[Symbol.for("vitehub.workspace.start-box-session")]: startBoxSession,
+		async startSession(options) {
+			return await startBoxSession(workspace, options);
+		}
+	};
+}
+function mergeWorkspaceSources(registered, configured) {
+	if (!registered && !configured) return void 0;
+	const sources = { ...registered };
+	for (const [key, source] of Object.entries(configured || {})) {
+		if (key in sources) throw new Error(`[vitehub] Workspace source "${key}" is already defined.`);
+		sources[key] = source;
+	}
+	return sources;
+}
+function mergeAgentWorkspaceDefinition(name, registered, configured) {
+	if (!registered && !configured) return void 0;
+	if (!registered) return configured ? {
+		...configured,
+		name
+	} : void 0;
+	if (!configured) return {
+		...registered,
+		name: registered.name || name
+	};
+	const { name: _configuredName, sources: configuredSources, ...configuredFields } = configured;
+	const { mode: _mode, ...configuredDefinitionFields } = configuredFields;
+	if (!Object.keys(configuredDefinitionFields).length && !Object.keys(configuredSources || {}).length) return registered;
+	return {
+		...registered,
+		...configuredDefinitionFields,
+		name: registered.name || name,
+		sources: mergeWorkspaceSources(registered.sources, configuredSources)
+	};
+}
+function hasWorkspaceDefinitionOverlay(definition) {
+	if (!definition) return false;
+	const { name: _name, sources, mode: _mode, ...fields } = definition;
+	return Object.keys(fields).length > 0 || Object.keys(sources || {}).length > 0;
+}
+async function registerResolvedAgentWorkspaceDefinition(name, definition) {
+	if (!definition) return;
+	const { name: _name, ...workspace } = definition;
+	const { registerWorkspace } = await import("@vite-hub/workspace/runtime");
+	registerWorkspace(name, workspace);
+}
+async function createAgentInvocationContext(definition, context, input) {
+	const startedAt = Date.now();
+	const resolvedContext = createResolvedRuntimeContext(context);
+	const tracedRuntimeContext = resolvedContext.trace && resolvedContext.traceLog ? resolvedContext : {
+		...resolvedContext,
+		trace: resolvedContext.trace || { id: createTraceId(context.run) },
+		traceLog: resolvedContext.traceLog || createTraceEventLog()
+	};
+	const boundRunEvents = bindAgentRunEvents(definition?.runEvents, tracedRuntimeContext);
+	const runtimeContext = boundRunEvents ? {
+		...tracedRuntimeContext,
+		runEvents: boundRunEvents
+	} : tracedRuntimeContext;
+	const callbackContext = createAgentCallbackContext(runtimeContext);
+	const invocationContext = createAgentInvocationContextStore(input.context);
+	bindMessageChannelInstructions(invocationContext, activeAgentChannel(definition?.channels, invocationContext, context.run)?.channel);
+	invocationContext.set(scheduledAgentChannelIdsContextKey, Object.keys(definition?.channels || {}), { overwrite: true });
+	invocationContext.set(scheduledAgentNameContextKey, context.agentIdentity?.name, { overwrite: true });
+	const colocatedSkills = definition?.[colocatedAgentSkillsSymbol];
+	invocationContext.set(colocatedAgentSkillsContextKey, colocatedSkills, { overwrite: true });
+	let invoker = createFallbackAgentInvoker(context.run);
+	try {
+		invoker = await resolveAgentInvoker(definition?.invoker, callbackContext, invocationContext, input, context.run, invocationContext.get(scheduledAgentTurnContextKey) === true);
+		const internalDefinition = definition;
+		const workspaceDefinition = definition;
+		const workspaceOptions = workspaceDefinition?.__vitehubWorkspaceAgentOptions;
+		const driverKind = internalDefinition?.[baseAgentDriverKind] || "model";
+		const capabilitiesResolver = internalDefinition?.[baseAgentCapabilitiesResolver];
+		const invocationResolvedCapabilities = capabilitiesResolver ? await resolveAgentCapabilityDefinitions(capabilitiesResolver, {
+			...agentInvocationCallbackContextValues(invocationContext),
+			...callbackContext,
+			actor: invoker,
+			context: invocationContext,
+			driver: { kind: driverKind },
+			input,
+			invoker,
+			run: context.run
+		}) : [];
+		const channelCapabilities = (activeAgentChannel(definition?.channels, invocationContext, context.run)?.channel)?.capabilities || [];
+		const resolvedCapabilityDefinitions = normalizeCapabilities([
+			...invocationResolvedCapabilities,
+			...definition?.capabilities || [],
+			...channelCapabilities
+		]);
+		const workspaceMode = workspaceOptions ? workspaceModeFromOptions(workspaceOptions) : "read";
+		validateAgentCapabilityComposition(resolvedCapabilityDefinitions, {
+			hasBox: Boolean(definition?.box),
+			hasWorkspace: Boolean(workspaceOptions),
+			...workspaceOptions ? { workspaceMode } : {}
+		});
+		const boxDefinition = definition?.box;
+		if (boxDefinition && workspaceOptions && (boxDefinition.cwd !== void 0 || boxDefinition.checkout !== void 0)) throw new Error("[vitehub] defineAgent({ box, workspace }) cannot combine a Workspace with Box cwd or checkout because both own the same working tree. Remove cwd/checkout and let Workspace materialize into the Box.");
+		const box = boxDefinition ? shareBoxSessions(await (await import("@vite-hub/box")).resolveBox(boxDefinition, {
+			...callbackContext,
+			actor: invoker,
+			context: invocationContext,
+			input,
+			invoker,
+			run: context.run
+		}, { requires: definition?.[baseAgentBoxRequirements] })) : void 0;
+		const harnessSandboxProvider = box ? (await import("./box-sandbox-BUw2Q0iH.js")).createBoxHarnessSandbox(box) : void 0;
+		const workspaceName = workspaceOptions ? workspaceNameFromOptions(workspaceOptions, {}, context.agentIdentity) : void 0;
+		const configuredWorkspaceDefinition = workspaceOptions && workspaceName ? {
+			...workspaceDefinitionFromOptions(workspaceOptions),
+			name: workspaceName
+		} : void 0;
+		const registeredWorkspaceDefinition = workspaceName ? await resolveRegisteredAgentWorkspaceDefinition(workspaceName) : void 0;
+		const ownsWorkspaceDefinition = workspaceDefinition ? workspaceAgentOwnsWorkspaceDefinition(workspaceDefinition) : false;
+		const configuredDefinitionForMerge = ownsWorkspaceDefinition && registeredWorkspaceDefinition ? void 0 : configuredWorkspaceDefinition;
+		const resolvedWorkspaceDefinition = workspaceName ? mergeAgentWorkspaceDefinition(workspaceName, registeredWorkspaceDefinition, configuredDefinitionForMerge) : void 0;
+		if (workspaceName && ownsWorkspaceDefinition && configuredWorkspaceDefinition && !registeredWorkspaceDefinition) await registerResolvedAgentWorkspaceDefinition(workspaceName, resolvedWorkspaceDefinition);
+		const workspaceUseOptions = !ownsWorkspaceDefinition && hasWorkspaceDefinitionOverlay(configuredDefinitionForMerge) && resolvedWorkspaceDefinition ? { definition: resolvedWorkspaceDefinition } : void 0;
+		const workspaceModule = workspaceName ? await import("@vite-hub/workspace") : void 0;
+		const baseWorkspace = workspaceName && workspaceModule ? workspaceMode === "write" ? workspaceModule.useWorkspace(workspaceName, workspaceUseOptions ? {
+			...workspaceUseOptions,
+			mode: "write"
+		} : { mode: "write" }) : workspaceUseOptions ? workspaceModule.useWorkspace(workspaceName, {
+			...workspaceUseOptions,
+			mode: "read"
+		}) : workspaceModule.useWorkspace(workspaceName) : void 0;
+		const workspace = box && baseWorkspace && workspaceMode === "write" ? withBoxWorkspaceSessions(baseWorkspace, box) : baseWorkspace;
+		const capabilityOptions = resolvedCapabilityDefinitions.length ? {
+			capabilities: resolvedCapabilityDefinitions,
+			hooks: definition?.hooks
+		} : void 0;
+		const agentModel = internalDefinition?.[baseAgentModel];
+		const resolveCapabilityCli = resolveCapabilityCliRunSurface(definition);
+		const capabilities = await resolveAgentCapabilities(capabilityOptions, runtimeContext, input, workspace, workspaceMode, {
+			context: invocationContext,
+			driverKind,
+			invoker,
+			model: agentModel,
+			resolveCapabilityCli,
+			workspaceDefinition: resolvedWorkspaceDefinition
+		});
+		const inputHook = definition?.hooks?.["agent:input"];
+		if (inputHook && !capabilities.response) try {
+			await runObservedAgentHook(definition?.hooks, {
+				name: "agent:input",
+				owner: "agent",
+				phase: "input"
+			}, () => inputHook({
+				...callbackContext,
+				actor: invoker,
+				context: invocationContext,
+				input: capabilities.input,
+				invoker,
+				run: context.run
+			}));
+		} catch (error) {
+			try {
+				await capabilities.close();
+			} catch (closeError) {
+				throw new AggregateError([error, closeError], "[vitehub] Agent input hook failed and cleanup also failed.");
+			}
+			throw error;
+		}
+		const transformedTools = resolveCapabilityCli ? capabilities.tools : await applyCapabilityToolTransforms(capabilities.tools, capabilities.toolTransforms);
+		const tools = Object.keys(transformedTools || {}).length ? withAgentToolStepReporting(withJsonCompatibleToolOutputs(applyAgentToolPolicies(transformedTools) || {}), context.toolStepReporter) : void 0;
+		const activeWorkspace = capabilities.workspace || workspace;
+		const sourceResolvedWorkspaceDefinition = invocationContext.get("workspace.sourceResolution.definition");
+		const activeWorkspaceDefinition = capabilities.workspaceDefinition || sourceResolvedWorkspaceDefinition || resolvedWorkspaceDefinition;
+		const configuredWorkspace = workspaceOptions?.workspace;
+		const workspaceAutoCommit = configuredWorkspace && typeof configuredWorkspace === "object" && !("name" in configuredWorkspace) ? configuredWorkspace.commit : void 0;
+		const instructions = workspaceOptions && activeWorkspace ? await resolveWorkspaceAgentDefaultInstructions(workspaceOptions, activeWorkspace) : void 0;
+		const workspaceInstructionBindings = activeWorkspaceDefinition ? await resolveWorkspaceInstructionBindings(activeWorkspaceDefinition, activeWorkspace) : void 0;
+		const invocation = {
+			...callbackContext,
+			actor: invoker,
+			box,
+			channels: definition?.channels,
+			close: capabilities.close,
+			context: invocationContext,
+			deliveryEffectIntents: capabilities.registries.deliveryEffectIntents,
+			toolStepReporter: context.toolStepReporter,
+			driverContributions: capabilities.driverContributions,
+			finalOutputRenderers: capabilities.registries.finalOutputRenderers,
+			finishDeliveryEffectProviders: capabilities.registries.finishDeliveryEffectProviders,
+			finishExtensionProviders: capabilities.registries.finishExtensionProviders,
+			finishHook: definition?.hooks?.["agent:finish"],
+			globalSkills: capabilities.globalSkills,
+			hasCapabilityCleanup: capabilities.hasCloseCallbacks,
+			handledResponse: capabilities.response,
+			harnessSandboxProvider,
+			hooks: definition?.hooks,
+			input: capabilities.input,
+			instructions,
+			invoker,
+			messages: capabilities.messages,
+			modelExecutionInstrumentation: capabilities.registries.modelExecutionInstrumentation,
+			outputExtensionProviders: capabilities.registries.outputExtensionProviders,
+			output: internalDefinition?.[baseAgentOutput],
+			outputRenderers: capabilities.registries.outputRenderers,
+			prompt: typeof capabilities.input.prompt === "string" ? capabilities.input.prompt : void 0,
+			providerTools: capabilities.registries.providerTools,
+			run: context.run,
+			runtimeContext,
+			startTask: void 0,
+			startedAt,
+			tools,
+			workspace: activeWorkspace,
+			workspaceAutoCommit,
+			workspaceDefinition: activeWorkspaceDefinition,
+			workspaceInstructionBindings,
+			workspaceMaterializationSource: box ? activeWorkspace : void 0,
+			workspaceMaterializationPaths: capabilities.workspaceMaterializationPaths,
+			workspaceMode
+		};
+		invocationContext.set("agent.finishHook", Boolean(invocation.finishHook), { overwrite: true });
+		await traceAgentInvocationStart(toTraceContext(invocation));
+		await applyChannelDeliveryEffectIntents(invocation, invocation.deliveryEffectIntents);
+		const startCapabilities = capabilities.start;
+		if (!invocation.handledResponse && startCapabilities) try {
+			if (invocation.messages.some((message) => message.role === "user") && hasTitleDeliveryEffectProvider(invocation.finishDeliveryEffectProviders)) await setChannelDeliverySupportContext(invocation.channels, invocation.context, invocation.runtimeContext, invocation.input, invocation.run);
+			invocation.startTask = (async () => {
+				await applyChannelDeliveryEffectIntents(invocation, await startCapabilities());
+			})().catch((error) => traceAgentInvocationError(toTraceContext(invocation), error));
+			runtimeContext.waitUntil?.(invocation.startTask);
+		} catch (error) {
+			await traceAgentInvocationError(toTraceContext(invocation), error);
+		}
+		return invocation;
+	} catch (error) {
+		await traceAgentInvocationError({
+			context: invocationContext,
+			input,
+			invoker,
+			run: context.run,
+			runtime: runtimeContext
+		}, error);
+		throw error;
+	}
+}
+function toTraceContext(context) {
+	return {
+		context: context.context,
+		input: context.input,
+		invoker: context.invoker,
+		run: context.run,
+		runtime: context.runtimeContext
+	};
+}
+function maybeTraceAgentStream(stream, context) {
+	return context.runtimeContext.traceLog ? traceAgentStreamEvents(stream, toTraceContext(context)) : stream;
+}
+function withEagerStreamUsageExtensions(stream, context, result) {
+	const providers = context.finishExtensionProviders.filter((provider) => provider.eager);
+	if (!providers.length) return stream;
+	return (async function* () {
+		const toolNames = /* @__PURE__ */ new Map();
+		const textPhases = /* @__PURE__ */ new Map();
+		for await (const chunk of stream) {
+			toAgentStreamEvent(chunk, toolNames, textPhases);
+			const usageRecord = usageRecordFromStreamChunk(chunk, result);
+			if (!usageRecord) {
+				yield chunk;
+				continue;
+			}
+			const usage = { ...usageRecord };
+			await createAgentInvocationExtensions({
+				actor: context.actor,
+				input: context.input,
+				invoker: context.invoker,
+				invocation: {
+					durationMs: Date.now() - context.startedAt,
+					...context.run ? { run: context.run } : {},
+					usage
+				},
+				result,
+				runtime: context.runtimeContext
+			}, providers);
+			if (chunk && typeof chunk === "object") {
+				const prototype = Object.getPrototypeOf(chunk);
+				if (prototype !== Object.prototype && prototype !== null) {
+					if (Object.isExtensible(chunk)) try {
+						Object.defineProperty(chunk, "usageRecord", {
+							configurable: true,
+							enumerable: true,
+							value: usage,
+							writable: true
+						});
+						yield chunk;
+						continue;
+					} catch {}
+					yield chunk;
+					yield {
+						type: "usage",
+						usageRecord: usage
+					};
+					continue;
+				}
+				yield {
+					...chunk,
+					usageRecord: usage
+				};
+				continue;
+			}
+			yield {
+				type: "usage",
+				usageRecord: usage
+			};
+		}
+	})();
+}
+function withEagerUiMessageStreamUsageExtensions(rendered, context) {
+	if (!isUIMessageStreamResult(rendered)) return rendered;
+	const toUIMessageStream = rendered.toUIMessageStream;
+	return cloneWithPropertyDescriptors(rendered, { toUIMessageStream: {
+		configurable: true,
+		enumerable: false,
+		value: (...args) => toReadableAsyncIterableStream(withEagerStreamUsageExtensions(toReadableAsyncIterableStream(toUIMessageStream.apply(rendered, args)), context, rendered))
+	} });
+}
+function withStreamResultProperties(stream, result) {
+	if (typeof stream !== "object" || stream === null || typeof result !== "object" || result === null) return stream;
+	Object.defineProperties(stream, Object.fromEntries(["usage", "usageRecord"].map((key) => [key, {
+		configurable: true,
+		enumerable: true,
+		get: () => result[key]
+	}])));
+	return stream;
+}
+function resultWithStreamedText(result, text) {
+	if (!text || typeof result === "string") return result;
+	if (result && typeof result === "object" && !(result instanceof Response)) {
+		const descriptor = Object.getOwnPropertyDescriptor(result, "text");
+		const current = descriptor && "value" in descriptor ? descriptor.value : void 0;
+		if (typeof current === "string" && current) return result;
+		const prototype = Object.getPrototypeOf(result);
+		if (prototype !== Object.prototype && prototype !== null && !Object.isExtensible(result)) return result;
+		return resultWithPreservedProperties(result, { text: {
+			configurable: true,
+			enumerable: true,
+			value: text
+		} });
+	}
+	return {
+		raw: result,
+		text
+	};
+}
+function resultWithUsageRecord(result, usageRecord) {
+	if (!usageRecord || result instanceof Response) return result;
+	if (!result || typeof result !== "object") return {
+		raw: result,
+		...typeof result === "string" && result ? { text: result } : {},
+		usage: usageRecord.usage,
+		usageRecord
+	};
+	const record = result;
+	record.usageRecord ??= usageRecord;
+	record.usage ??= usageRecord.usage;
+	return result;
+}
+function resultWithResolvedUsageRecord(result, usageRecord) {
+	if (!usageRecord || result instanceof Response) return result;
+	if (!result || typeof result !== "object") return resultWithUsageRecord(result, usageRecord);
+	const prototype = Object.getPrototypeOf(result);
+	if (prototype !== Object.prototype && prototype !== null) {
+		if (Object.isExtensible(result)) try {
+			Object.defineProperties(result, {
+				...usageRecord.usage && !("usage" in result) ? { usage: {
+					configurable: true,
+					enumerable: true,
+					value: usageRecord.usage,
+					writable: true
+				} } : {},
+				usageRecord: {
+					configurable: true,
+					enumerable: true,
+					value: usageRecord,
+					writable: true
+				}
+			});
+			return result;
+		} catch {}
+		return {
+			...toAgentRunResult(result),
+			raw: result,
+			usage: usageRecord.usage,
+			usageRecord
+		};
+	}
+	return cloneWithPropertyDescriptors(result, {
+		...usageRecord.usage && !("usage" in result) ? { usage: {
+			configurable: true,
+			enumerable: true,
+			value: usageRecord.usage,
+			writable: true
+		} } : {},
+		usageRecord: {
+			configurable: true,
+			enumerable: true,
+			value: usageRecord,
+			writable: true
+		}
+	});
+}
+function resultWithPreservedProperties(result, descriptors) {
+	const prototype = Object.getPrototypeOf(result);
+	if (prototype !== Object.prototype && prototype !== null && Object.isExtensible(result)) try {
+		Object.defineProperties(result, descriptors);
+		return result;
+	} catch {}
+	return cloneWithPropertyDescriptors(result, descriptors);
+}
+function resultWithStreamedTextAndUsage(result, text, usageRecord, fallbackUsageRecord) {
+	return resultWithUsageRecord(resultWithStreamedText(result, text), usageRecord ?? fallbackUsageRecord);
+}
+function withStreamedResult(stream, result, fallbackUsageRecord) {
+	const toolNames = /* @__PURE__ */ new Map();
+	const textPhases = /* @__PURE__ */ new Map();
+	let explicitTextPhaseSeen = false;
+	let finalText = "";
+	let unphasedText = "";
+	let usageRecord;
+	return {
+		finishResult(resultOverride = result) {
+			return resultWithStreamedTextAndUsage(resultOverride, explicitTextPhaseSeen ? finalText : unphasedText, usageRecord, fallbackUsageRecord);
+		},
+		finishUsage() {
+			return usageRecord ?? fallbackUsageRecord;
+		},
+		stream: (async function* () {
+			for await (const chunk of stream) {
+				const event = toAgentStreamEvent(chunk, toolNames, textPhases);
+				if (chunk && typeof chunk === "object" && "phase" in chunk && chunk.phase !== void 0 && "type" in chunk && [
+					"text",
+					"text-delta",
+					"text-end",
+					"text-start"
+				].includes(String(chunk.type)) || event?.type === "text-delta" && event.phase !== void 0) {
+					explicitTextPhaseSeen = true;
+					unphasedText = "";
+				}
+				if (event?.type === "text-delta" && event.text) {
+					if (event.phase === "final") finalText += event.text;
+					else if (!explicitTextPhaseSeen && event.phase === void 0) unphasedText += event.text;
+				}
+				const attachedUsageRecord = chunk && typeof chunk === "object" && "usageRecord" in chunk ? chunk.usageRecord : void 0;
+				usageRecord = event?.type === "usage" ? event.usageRecord : attachedUsageRecord ?? usageRecordFromStreamChunk(chunk, result) ?? usageRecord;
+				yield chunk;
+			}
+		})()
+	};
+}
+async function finishStreamAgentInvocation(context, lifecycle, result, outcome, failureMessage, outputExtensions = /* @__PURE__ */ new Map()) {
+	if (outcome.status === "error") {
+		await lifecycle.finish(outcome);
+		return;
+	}
+	let finishResult;
+	let finishUsage;
+	try {
+		const usageRecord = await resolveFinishUsageRecord(context, result);
+		finishUsage = usageRecord;
+		const resolvedResult = resultWithResolvedUsageRecord(result, usageRecord);
+		if (usageRecord && resolvedResult !== result && result && typeof result === "object" && Object.isExtensible(result)) try {
+			Object.defineProperty(result, "usageRecord", {
+				configurable: true,
+				enumerable: true,
+				value: usageRecord,
+				writable: true
+			});
+		} catch {}
+		finishResult = await applyFinalOutputRenderers(resolvedResult, context, outputExtensions);
+		finishResult = context.output ? await validateAgentOutput(context.output, await materializeAgentStructuredOutput(finishResult, context.input.abortSignal), { allowMaterializedObject: finishResult !== result }) : resultWithUsageRecord(finishResult, usageRecord);
+	} catch (finishError) {
+		await lifecycle.fail({
+			error: finishError,
+			status: "error"
+		}, finishError, failureMessage);
+	}
+	await lifecycle.finish({
+		result: finishResult,
+		status: "success",
+		usage: finishUsage
+	});
+}
+function traceUiMessageStream(stream, context) {
+	const reader = stream.getReader();
+	const toolNames = /* @__PURE__ */ new Map();
+	const textPhases = /* @__PURE__ */ new Map();
+	let finished = false;
+	let released = false;
+	const release = () => {
+		if (released) return;
+		released = true;
+		reader.releaseLock();
+	};
+	return new ReadableStream({
+		async pull(controller) {
+			try {
+				const result = await reader.read();
+				if (result.done) {
+					if (!finished) await traceAgentStreamEvent(toTraceContext(context), { type: "finish" });
+					release();
+					controller.close();
+					return;
+				}
+				const event = toAgentStreamEvent(result.value, toolNames, textPhases);
+				if (event) {
+					if (event.type === "finish") finished = true;
+					await traceAgentStreamEvent(toTraceContext(context), event);
+				}
+				controller.enqueue(result.value);
+			} catch (error) {
+				release();
+				controller.error(error);
+			}
+		},
+		async cancel(reason) {
+			try {
+				await reader.cancel(reason);
+			} finally {
+				release();
+			}
+		}
+	});
+}
+function maybeTraceUiMessageStreamResult(rendered, context) {
+	const toUIMessageStream = rendered.toUIMessageStream;
+	return cloneWithPropertyDescriptors(rendered, { toUIMessageStream: {
+		configurable: true,
+		enumerable: false,
+		value: (...args) => traceUiMessageStream(normalizeUiMessageStream(toUIMessageStream.apply(rendered, args)), context)
+	} });
+}
+function maybeTraceUiMessageStreamOutput(rendered, context) {
+	if (context.runtimeContext.traceLog) {
+		if (isUIMessageStreamResult(rendered)) return maybeTraceUiMessageStreamResult(rendered, context);
+		if (isAsyncIterable(rendered)) return maybeTraceAgentStream(rendered, context);
+		if (!hasTraceableStreamResult(rendered)) return rendered;
+		return maybeTraceAgentStream(streamAgentOutputToEvents(rendered), context);
+	}
+	return isAsyncIterable(rendered) ? maybeTraceAgentStream(rendered, context) : rendered;
+}
+function hasFinishConsumer(context) {
+	return Boolean(context.finishHook || context.finishDeliveryEffectProviders.length);
+}
+function hasFinishWork(context) {
+	return hasFinishConsumer(context) || context.finishExtensionProviders.some((provider) => provider.eager);
+}
+async function resolveFinishUsageRecord(context, result) {
+	if (hasFinishConsumer(context)) return await resolveAgentUsageRecord(result, context.run);
+	if (!context.runtimeContext.traceLog && !context.finishExtensionProviders.some((provider) => provider.eager)) return void 0;
+	try {
+		return await resolveAgentUsageRecord(result, context.run);
+	} catch {
+		return;
+	}
+}
+function finishOutcomeFromCleanup(outcome, result) {
+	return outcome.failed ? {
+		error: outcome.error,
+		status: "error"
+	} : {
+		result,
+		status: "success"
+	};
+}
+function isWritableWorkspaceFacade(workspace) {
+	return Boolean(workspace && typeof workspace === "object" && "diff" in workspace && "snapshot" in workspace);
+}
+function hasWorkspaceAutoCommit(context) {
+	return context.workspaceMode === "write" && Boolean(context.workspaceDefinition && isWritableWorkspaceFacade(context.workspace));
+}
+function shouldDeferFinish(context) {
+	return context.hasCapabilityCleanup || hasFinishWork(context) || Boolean(context.finalOutputRenderers.length) || Boolean(context.output) || hasWorkspaceAutoCommit(context);
+}
+function shouldWrapInvocationOutput(context) {
+	return shouldDeferFinish(context) || Boolean(context.runtimeContext.traceLog);
+}
+async function commitWorkspaceChanges(context) {
+	if (!context.workspaceDefinition || !isWritableWorkspaceFacade(context.workspace)) return;
+	const diff = await context.workspace.diff();
+	const { resolveWorkspaceAutoCommit } = await import("@vite-hub/workspace");
+	const commit = resolveWorkspaceAutoCommit(workspaceDefinitionWithAutoCommitRules(context.workspaceDefinition, context.workspaceAutoCommit), diff);
+	if (!commit) return;
+	await context.workspace.snapshot({ name: commit.message });
+}
+async function applyFinalOutputRenderers(result, context, outputExtensions = /* @__PURE__ */ new Map()) {
+	return await applyOutputRenderers(result, context.finalOutputRenderers, context.outputExtensionProviders, outputExtensions);
+}
+function assertDeliveryEffectIntent(value) {
+	if (!value || typeof value !== "object" || typeof value.kind !== "string" || !value.kind.trim()) throw new TypeError("[vitehub] Channel finish delivery effect resolvers must return an effect intent with a non-empty kind.");
+}
+function appendDeliveryEffectIntent(intents, value) {
+	if (Array.isArray(value)) {
+		for (const item of value) appendDeliveryEffectIntent(intents, item);
+		return;
+	}
+	assertDeliveryEffectIntent(value);
+	intents.push(value);
+}
+async function resolveFinishDeliveryEffectIntents(providers, event, context) {
+	const intents = [];
+	const finishContext = createFinishDeliveryEffectContext(event, context);
+	for (const provider of providers) {
+		const intent = typeof provider === "function" ? await provider(finishContext, event) : provider;
+		if (!intent) continue;
+		appendDeliveryEffectIntent(intents, intent);
+	}
+	return intents;
+}
+function createFinishDeliveryEffectContext(event, context) {
+	const result = event.result === void 0 ? void 0 : toAgentRunResult(event.result);
+	const active = activeAgentChannel(context.channels, context.context, context.run);
+	const reply = (input, options = {}) => {
+		const inputArtifacts = typeof input === "object" && input !== null && "artifacts" in input ? input.artifacts : void 0;
+		return createReplyDeliveryEffectIntent(input, {
+			...options,
+			artifacts: options.artifacts ?? inputArtifacts ?? result?.artifacts
+		});
+	};
+	return {
+		...context.runtimeContext,
+		actor: context.actor,
+		...active ? { channel: active.channel } : {},
+		context: context.context,
+		...event.error !== void 0 ? { error: event.error } : {},
+		...event.errorMessage !== void 0 ? { errorMessage: event.errorMessage } : {},
+		event,
+		extensions: event.extensions,
+		input: context.input,
+		invocation: event.invocation,
+		invoker: context.invoker,
+		...event.result !== void 0 ? { output: event.result } : {},
+		reaction: createReactionDeliveryEffectIntent,
+		reply,
+		...result !== void 0 ? { result } : {},
+		request: context.runtimeContext.request,
+		run: context.run,
+		status: createStatusDeliveryEffectIntent,
+		...event.text !== void 0 ? { text: event.text } : {},
+		workspace: context.workspace
+	};
+}
+function createAgentFinishHookEvent(event, context) {
+	const delivery = createFinishDeliveryEffectContext(event, context);
+	return {
+		...event,
+		reaction: delivery.reaction,
+		reply: delivery.reply,
+		status: delivery.status
+	};
+}
+function activeFinishDeliveryEffectProviders(context, event) {
+	if (!context.finishDeliveryEffectProviders.length) return [];
+	const active = createFinishDeliveryEffectContext(event, context);
+	return context.finishDeliveryEffectProviders.filter((provider) => {
+		if (typeof provider !== "function" || !provider.active) return true;
+		return provider.active(active);
+	});
+}
+function provisionalFinishEvent(context, eventBase) {
+	return {
+		...eventBase,
+		extensions: { get: () => void 0 }
+	};
+}
+function hasTitleDeliveryEffectProvider(providers) {
+	return providers.some((provider) => {
+		if (typeof provider === "function") return provider.kind === "title";
+		return (Array.isArray(provider) ? provider : [provider]).some((effect) => effect.kind === "title");
+	});
+}
+function hasDeferredFinishDeliveryEffectProvider(providers) {
+	return providers.some((provider) => typeof provider === "function" && (provider.kind === void 0 || Boolean(provider.active)));
+}
+async function prepareProvisionalTitleDeliverySupport(context, eventBase) {
+	const activeDeliveryProviders = activeFinishDeliveryEffectProviders(context, provisionalFinishEvent(context, eventBase));
+	if (!hasTitleDeliveryEffectProvider(activeDeliveryProviders)) return activeDeliveryProviders;
+	await setChannelDeliverySupportContext(context.channels, context.context, context.runtimeContext, context.input, context.run);
+	return activeFinishDeliveryEffectProviders(context, provisionalFinishEvent(context, eventBase));
+}
+async function finishAgentInvocation(context, outcome) {
+	const durationMs = Date.now() - context.startedAt;
+	const failed = outcome.status === "error";
+	const error = failed ? outcome.error : void 0;
+	const result = outcome.status === "success" ? outcome.result : void 0;
+	const text = (failed || result === void 0 ? void 0 : toAgentRunResult(result))?.text;
+	try {
+		await context.startTask;
+		await context.close();
+		let resultKind;
+		let usage = failed ? void 0 : outcome.usage;
+		if (!failed) try {
+			resultKind = agentResultKind(result);
+			if (!outcome.usageResolved) usage ??= await resolveAgentUsageRecord(result, context.run);
+		} catch {}
+		if (hasFinishWork(context)) {
+			const details = failed ? agentErrorDetails(error) : void 0;
+			const eventBase = {
+				...failed ? { error } : {},
+				...details ? { errorMessage: details.message } : {},
+				actor: context.actor,
+				input: context.input,
+				invoker: context.invoker,
+				invocation: {
+					durationMs,
+					...resultKind !== void 0 ? { resultKind } : {},
+					...context.run ? { run: context.run } : {},
+					...usage ? { usage } : {}
+				},
+				...result !== void 0 ? { result } : {},
+				runtime: context.runtimeContext,
+				...text !== void 0 ? { text } : {}
+			};
+			const provisionalActiveDeliveryProviders = await prepareProvisionalTitleDeliverySupport(context, eventBase);
+			const hasFinishConsumer = Boolean(context.finishHook || provisionalActiveDeliveryProviders.length || hasDeferredFinishDeliveryEffectProvider(context.finishDeliveryEffectProviders));
+			const finishExtensionProviders = hasFinishConsumer ? context.finishExtensionProviders : context.finishExtensionProviders.filter((provider) => provider.eager);
+			if (hasFinishConsumer || finishExtensionProviders.length) {
+				const extensions = await createAgentInvocationExtensions(eventBase, finishExtensionProviders);
+				const finishEvent = {
+					...eventBase,
+					extensions
+				};
+				await applyChannelDeliveryEffectIntents(context, await resolveFinishDeliveryEffectIntents(activeFinishDeliveryEffectProviders(context, finishEvent), finishEvent, context), finishEvent);
+				let finishHookResult;
+				await runObservedAgentHook(context.hooks, {
+					ids: { runId: context.run?.runId },
+					name: "agent:finish",
+					owner: "agent",
+					phase: "finish"
+				}, async () => {
+					finishHookResult = await context.finishHook?.(createAgentFinishHookEvent(finishEvent, context));
+				});
+				if (finishHookResult) {
+					const finishHookIntents = [];
+					appendDeliveryEffectIntent(finishHookIntents, finishHookResult);
+					await applyChannelDeliveryEffectIntents(context, finishHookIntents, finishEvent);
+				}
+			}
+		}
+		if (!failed) await commitWorkspaceChanges(context);
+		if (!failed) await traceAgentInvocationFinish(toTraceContext(context), {
+			"invocation.durationMs": durationMs,
+			"result.hasValue": result !== void 0,
+			...resultKind !== void 0 ? { "result.kind": resultKind } : {},
+			...usage ? { "usage.record": usage } : {}
+		});
+		else await traceAgentInvocationError(toTraceContext(context), error);
+	} catch (finishError) {
+		await traceAgentInvocationError(toTraceContext(context), failed ? error : finishError);
+		throw finishError;
+	}
+}
+async function finalizeAgentInvocationResult(context, lifecycle, result, finalizeObject, failureMessage, options = {}) {
+	const shouldWrapOutput = shouldWrapInvocationOutput(context);
+	try {
+		if (result instanceof Response) {
+			const finalized = await options.finalizeResponse?.(result);
+			if (finalized) {
+				if (!finalized.deferFinish) await lifecycle.finish({
+					result: finalized.finishResult,
+					status: "success",
+					usage: finalized.finishUsage
+				});
+				return finalized.value;
+			}
+			const responseMediaType = result.headers.get("content-type")?.split(";", 1)[0]?.trim().toLowerCase();
+			const responseIsText = responseMediaType !== "text/event-stream" && (responseMediaType?.startsWith("text/") || responseMediaType === "application/json" || responseMediaType?.endsWith("+json") || responseMediaType === "application/xml" || responseMediaType?.endsWith("+xml"));
+			const responseDecoder = context.context.get("vitehub.title.response-fallback") === true && responseIsText ? new TextDecoder() : void 0;
+			let responseText = "";
+			return shouldWrapOutput ? await withResponseCleanup(result, async (outcome) => {
+				responseText += responseDecoder?.decode() ?? "";
+				const finishResult = responseText && !outcome.failed ? {
+					raw: result,
+					text: responseText
+				} : result;
+				if (!outcome.failed && !outcome.completed) await lifecycle.finish({
+					result: finishResult,
+					status: "success",
+					usageResolved: true
+				});
+				else await lifecycle.finish(finishOutcomeFromCleanup(outcome, finishResult));
+			}, { onChunk: (chunk) => responseText += responseDecoder?.decode(chunk, { stream: true }) ?? "" }) : result;
+		}
+		if (isAsyncIterable(result) && !hasTraceableStreamResult(result) && !options.finalizeRawStreams) {
+			const enrichedStream = withEagerStreamUsageExtensions(result, context, result);
+			const stream = options.wrapStream?.(enrichedStream) || enrichedStream;
+			if (shouldWrapOutput) {
+				const streamed = withStreamedResult(stream, result);
+				if (!context.finalOutputRenderers.length && (!context.output || !options.finalizeRawStreams)) return withCapabilityCleanup(streamed.stream, async (outcome) => {
+					const finishOutcome = finishOutcomeFromCleanup(outcome, result);
+					const usage = streamed.finishUsage();
+					if (!outcome.failed && !outcome.completed) return lifecycle.finish({
+						result,
+						status: "success",
+						...usage ? { usage: await resolveAgentUsageRecord({ usageRecord: usage }, context.run) } : {},
+						usageResolved: true
+					});
+					return lifecycle.finish(finishOutcome.status === "success" ? {
+						...finishOutcome,
+						usage: usage ? await resolveAgentUsageRecord({ usageRecord: usage }, context.run) : void 0
+					} : finishOutcome);
+				}, { abortSignal: context.input.abortSignal });
+				return withCapabilityCleanup(streamed.stream, (outcome) => finishStreamAgentInvocation(context, lifecycle, streamed.finishResult(), finishOutcomeFromCleanup(outcome), failureMessage, options.outputExtensions), { abortSignal: context.input.abortSignal });
+			}
+			return stream;
+		}
+		const finalized = await finalizeObject(result);
+		if (!finalized.deferFinish) await lifecycle.finish({
+			result: finalized.finishResult,
+			status: "success",
+			usage: finalized.finishUsage
+		});
+		return finalized.value;
+	} catch (error) {
+		return await lifecycle.fail({
+			error,
+			status: "error"
+		}, error, failureMessage);
+	}
+}
+async function materializeAgentStructuredOutput(result, abortSignal, onEvent) {
+	if (!isAsyncIterable(result) && !hasTraceableStreamResult(result)) return result;
+	if (toAgentRunResult(result).text !== void 0) return result;
+	let text = "";
+	let usageRecord;
+	const events = withCapabilityCleanup(streamAgentOutputToEvents(result), async () => {}, { abortSignal });
+	for await (const event of events) {
+		onEvent?.(event);
+		if (event.type === "error") throw new Error(event.error);
+		if (event.type === "text-delta") text += event.text;
+		if (event.type === "usage") usageRecord = event.usageRecord;
+	}
+	return resultWithUsageRecord(text, usageRecord);
+}
+async function executeAgentInvocation(agent, context, input, options) {
+	const customRun = hasCustomRun(agent);
+	const adapter = customRun ? void 0 : await resolveAgentForRun(agent, context);
+	const definition = hasAgentDefinition(agent) ? agent : void 0;
+	const invocation = await createAgentInvocationContext(definition, context, input);
+	const lifecycle = await openAgentInvocationLifecycle(async (outcome) => {
+		try {
+			await finishAgentInvocation(invocation, outcome);
+			options.onFinish?.(outcome);
+		} catch (error) {
+			options.onFinish?.({
+				error,
+				status: "error"
+			});
+			throw error;
+		}
+	});
+	const runFailureMessage = "[vitehub] Agent run failed and finish lifecycle also failed.";
+	const streamFailureMessage = "[vitehub] Agent stream failed and finish lifecycle also failed.";
+	const handledFailureMessage = options.kind === "run" ? runFailureMessage : streamFailureMessage;
+	if (invocation.handledResponse) return await finalizeAgentInvocationResult(invocation, lifecycle, invocation.handledResponse, async (result) => ({
+		finishResult: result,
+		value: result
+	}), handledFailureMessage);
+	const executionFailureMessage = options.kind === "run" || customRun ? runFailureMessage : streamFailureMessage;
+	let result;
+	try {
+		if (customRun) result = await agent.run(invocation);
+		else if (options.kind === "stream" && adapter?.stream && (invocation.context.get("vitehub.channel.final-output") !== true || invocation.context.get("agent.output.progressSummary") === true)) result = await adapter.stream(toAgentAdapterRunContext(invocation));
+		else result = await adapter.generate(toAgentAdapterRunContext(invocation));
+	} catch (error) {
+		return await lifecycle.fail({
+			error,
+			status: "error"
+		}, error, executionFailureMessage);
+	}
+	if (options.kind === "run" && invocation.context.get("vitehub.channel.final-output") === true && !isAsyncIterable(result) && !hasTraceableStreamResult(result)) {
+		const text = finalTextFromAgentOutput(result);
+		if (text !== void 0 && !(result instanceof Response)) {
+			result = {
+				raw: (typeof result === "object" && result !== null && Object.getOwnPropertyDescriptor(result, synthesizedAgentOutputSymbol)?.value === true ? Object.getOwnPropertyDescriptor(result, "raw")?.value : void 0) ?? result,
+				text
+			};
+			Object.defineProperty(result, finalChannelOutputSelectedSymbol, {
+				enumerable: true,
+				value: true
+			});
+		}
+	}
+	const outputExtensions = /* @__PURE__ */ new Map();
+	let renderedResult = false;
+	try {
+		if (options.kind === "run" ? customRun && options.renderOutput && isAsyncIterable(result) : isAsyncIterable(result) && options.output !== "ui-message-stream" && !invocation.finalOutputRenderers.length) {
+			result = await applyOutputRenderers(result, invocation.outputRenderers, invocation.outputExtensionProviders, outputExtensions);
+			renderedResult = true;
+		}
+	} catch (error) {
+		return await lifecycle.fail({
+			error,
+			status: "error"
+		}, error, executionFailureMessage);
+	}
+	if (options.kind === "run") return await finalizeAgentInvocationResult(invocation, lifecycle, result, async (result) => {
+		const driverUsageRecord = hasTraceableStreamResult(result) || isUIMessageStreamResult(result) ? void 0 : await resolveFinishUsageRecord(invocation, result);
+		const rendered = options.renderOutput ? renderedResult ? result : await applyOutputRenderers(result, invocation.outputRenderers, invocation.outputExtensionProviders, outputExtensions) : result;
+		if ((hasTraceableStreamResult(rendered) || isUIMessageStreamResult(rendered)) && invocation.finishExtensionProviders.some((provider) => provider.eager) && shouldWrapInvocationOutput(invocation) || options.renderOutput && !invocation.output && invocation.context.get("vitehub.title.response-fallback") === true && rendered !== result && (isAsyncIterable(rendered.stream) || isAsyncIterable(rendered.fullStream) || isUIMessageStreamResult(rendered)) && shouldWrapInvocationOutput(invocation)) {
+			if (isUIMessageStreamResult(rendered) && !isAsyncIterable(rendered.stream) && !isAsyncIterable(rendered.fullStream)) {
+				const toUIMessageStream = rendered.toUIMessageStream;
+				let finishTask;
+				let streamedText = "";
+				let streamedUsageRecord;
+				const preserved = resultWithPreservedProperties(rendered, { toUIMessageStream: {
+					configurable: true,
+					enumerable: false,
+					value: (...args) => withReadableStreamCleanup(toReadableAsyncIterableStream(withEagerStreamUsageExtensions(toReadableAsyncIterableStream(normalizeUiMessageStream(toUIMessageStream.apply(rendered, args))), invocation, rendered)), async (outcome) => {
+						if (finishTask) return await finishTask;
+						const finishResult = resultWithStreamedTextAndUsage(preserved, streamedText, streamedUsageRecord, driverUsageRecord);
+						finishTask = (async () => {
+							if (!outcome.failed && !outcome.completed) await lifecycle.finish({
+								result: finishResult,
+								status: "success",
+								...streamedUsageRecord ? { usage: await resolveAgentUsageRecord({ usageRecord: streamedUsageRecord }, invocation.run) } : {},
+								usageResolved: true
+							});
+							else await finishStreamAgentInvocation(invocation, lifecycle, finishResult, finishOutcomeFromCleanup(outcome), runFailureMessage, outputExtensions);
+							const usageRecord = finishResult && typeof finishResult === "object" ? finishResult.usageRecord : void 0;
+							if (usageRecord) resultWithUsageRecord(preserved, usageRecord);
+						})();
+						return await finishTask;
+					}, { onChunk(chunk) {
+						streamedText += uiMessageTextDelta(chunk) || "";
+						streamedUsageRecord = usageRecordFromStreamChunk(chunk, rendered) ?? streamedUsageRecord;
+					} })
+				} });
+				return {
+					deferFinish: true,
+					finishResult: preserved,
+					value: preserved
+				};
+			}
+			const streamProperties = ["stream", "fullStream"].filter((property) => isAsyncIterable(rendered[property]));
+			let finishTask;
+			let preserved;
+			const preserveStream = (renderedStream) => {
+				const streamed = withStreamedResult(withEagerStreamUsageExtensions(renderedStream, invocation, rendered), rendered, driverUsageRecord);
+				const value = withCapabilityCleanup(streamed.stream, async (outcome) => {
+					if (finishTask) return await finishTask;
+					const finishResult = streamed.finishResult(preserved);
+					finishTask = !outcome.failed && !outcome.completed ? lifecycle.finish({
+						result: finishResult,
+						status: "success",
+						...streamed.finishUsage() ? { usage: await resolveAgentUsageRecord({ usageRecord: streamed.finishUsage() }, invocation.run) } : {},
+						usageResolved: true
+					}) : (async () => {
+						await finishStreamAgentInvocation(invocation, lifecycle, finishResult, finishOutcomeFromCleanup(outcome), runFailureMessage, outputExtensions);
+						const usageRecord = finishResult && typeof finishResult === "object" ? finishResult.usageRecord : void 0;
+						if (usageRecord) resultWithUsageRecord(preserved, usageRecord);
+					})();
+					return await finishTask;
+				}, { abortSignal: invocation.input.abortSignal });
+				return typeof renderedStream.pipeThrough === "function" ? toReadableAsyncIterableStream(value) : value;
+			};
+			const descriptors = Object.fromEntries(streamProperties.map((property) => {
+				const renderedStream = rendered[property];
+				return [property, {
+					configurable: true,
+					enumerable: true,
+					value: preserveStream(renderedStream),
+					writable: true
+				}];
+			}));
+			let textStreamDescriptor;
+			for (let owner = rendered; owner && !textStreamDescriptor; owner = Object.getPrototypeOf(owner)) textStreamDescriptor = Object.getOwnPropertyDescriptor(owner, "textStream");
+			if (textStreamDescriptor) {
+				const resolveTextStream = "get" in textStreamDescriptor ? () => textStreamDescriptor.get?.call(rendered) : () => textStreamDescriptor.value;
+				let preservedTextStream;
+				let initialized = false;
+				descriptors.textStream = {
+					configurable: true,
+					enumerable: textStreamDescriptor.enumerable ?? false,
+					get() {
+						if (!initialized) {
+							const textStream = resolveTextStream();
+							preservedTextStream = isAsyncIterable(textStream) ? preserveStream(textStream) : textStream;
+							initialized = true;
+						}
+						return preservedTextStream;
+					}
+				};
+			}
+			preserved = resultWithPreservedProperties(rendered, descriptors);
+			return {
+				deferFinish: true,
+				finishResult: preserved,
+				value: preserved
+			};
+		}
+		const final = options.renderOutput ? await applyFinalOutputRenderers(rendered, invocation, outputExtensions) : rendered;
+		const structuredFinal = options.renderOutput && invocation.output ? await materializeAgentStructuredOutput(final, invocation.input.abortSignal, invocation.context.get(agentOutputEventObserverContextKey)) : final;
+		const resolvedUsageRecord = options.renderOutput && invocation.output ? await resolveFinishUsageRecord(invocation, structuredFinal) ?? driverUsageRecord : driverUsageRecord;
+		const hasEagerFinishExtension = invocation.finishExtensionProviders.some((provider) => provider.eager);
+		const structuredUsageRecord = hasEagerFinishExtension && resolvedUsageRecord ? { ...resolvedUsageRecord } : resolvedUsageRecord;
+		const finishResult = invocation.output ? void 0 : hasEagerFinishExtension ? resultWithResolvedUsageRecord(final, structuredUsageRecord) : hasFinishWork(invocation) ? resultWithUsageRecord(final, structuredUsageRecord) : final;
+		const value = options.renderOutput && invocation.output ? await validateAgentOutput(invocation.output, structuredFinal, { allowMaterializedObject: customRun ? structuredFinal === final : structuredFinal === final && final !== result }) : customRun ? hasEagerFinishExtension ? finishResult : final : options.renderOutput ? toAgentRunResult(finishResult) : final;
+		return {
+			finishResult: invocation.output ? value : finishResult,
+			finishUsage: structuredUsageRecord,
+			value
+		};
+	}, runFailureMessage, {
+		finalizeRawStreams: options.renderOutput && Boolean(invocation.output),
+		outputExtensions,
+		...customRun ? { wrapStream: (stream) => maybeTraceAgentStream(stream, invocation) } : {}
+	});
+	return await finalizeAgentInvocationResult(invocation, lifecycle, result, async (result) => {
+		const driverUsageRecord = invocation.finishExtensionProviders.some((provider) => provider.eager) && (hasTraceableStreamResult(result) || isUIMessageStreamResult(result)) ? void 0 : await resolveFinishUsageRecord(invocation, result);
+		const rendered = renderedResult ? result : await applyOutputRenderers(result, invocation.outputRenderers, invocation.outputExtensionProviders, outputExtensions);
+		if (options.output === "ui-message-stream") {
+			const projection = typeof definition?.uiMessageStream === "function" ? await definition.uiMessageStream(invocation) : definition?.uiMessageStream;
+			return finalizeUiMessageStreamOutput(maybeTraceUiMessageStreamOutput(isAsyncIterable(rendered) ? withEagerStreamUsageExtensions(rendered, invocation, rendered) : withEagerUiMessageStreamUsageExtensions(rendered, invocation), invocation), shouldWrapInvocationOutput(invocation), async (outcome, streamedText, streamedUsageRecord) => {
+				const finishResult = resultWithStreamedTextAndUsage(rendered, streamedText || "", streamedUsageRecord, driverUsageRecord);
+				if (!outcome.failed && !outcome.completed) await lifecycle.finish({
+					result: finishResult,
+					status: "success",
+					...streamedUsageRecord ? { usage: await resolveAgentUsageRecord({ usageRecord: streamedUsageRecord }, invocation.run) } : {},
+					usageResolved: true
+				});
+				else await finishStreamAgentInvocation(invocation, lifecycle, finishResult, finishOutcomeFromCleanup(outcome), streamFailureMessage, outputExtensions);
+			}, projection);
+		}
+		const isStreamResult = hasTraceableStreamResult(rendered);
+		if (customRun && !isAsyncIterable(rendered) && !isStreamResult) {
+			const final = await applyFinalOutputRenderers(rendered, invocation, outputExtensions);
+			const value = invocation.output ? await validateAgentOutput(invocation.output, final, { allowMaterializedObject: true }) : final;
+			return {
+				finishResult: value,
+				finishUsage: driverUsageRecord,
+				value
+			};
+		}
+		const streamed = withStreamedResult(withEagerStreamUsageExtensions(isStreamResult ? streamAgentOutputToEvents(rendered) : customRun ? rendered : streamAgentOutputToEvents(rendered), invocation, rendered), rendered, driverUsageRecord);
+		const tracedStream = maybeTraceAgentStream(streamed.stream, invocation);
+		const shouldWrapOutput = shouldWrapInvocationOutput(invocation);
+		const value = shouldWrapOutput ? withCapabilityCleanup(tracedStream, async (outcome) => {
+			const finishResult = streamed.finishResult();
+			if (!outcome.failed && !outcome.completed) await lifecycle.finish({
+				result: finishResult,
+				status: "success",
+				...streamed.finishUsage() ? { usage: await resolveAgentUsageRecord({ usageRecord: streamed.finishUsage() }, invocation.run) } : {},
+				usageResolved: true
+			});
+			else await finishStreamAgentInvocation(invocation, lifecycle, finishResult, finishOutcomeFromCleanup(outcome), streamFailureMessage, outputExtensions);
+		}, { abortSignal: invocation.input.abortSignal }) : tracedStream;
+		return {
+			deferFinish: shouldWrapOutput,
+			finishResult: rendered,
+			value: customRun ? withStreamResultProperties(value, rendered) : value
+		};
+	}, executionFailureMessage, {
+		finalizeResponse: options.output === "ui-message-stream" ? async (response) => {
+			if (!isUIMessageStreamResponse(response)) return;
+			const projection = typeof definition?.uiMessageStream === "function" ? await definition.uiMessageStream(invocation) : definition?.uiMessageStream;
+			const finalized = await finalizeUiMessageStreamOutput(withEagerUiMessageStreamUsageExtensions({ toUIMessageStream: () => uiMessageStreamFromResponse(response) }, invocation), shouldWrapInvocationOutput(invocation), async (outcome, streamedText, streamedUsageRecord) => {
+				if (!outcome.failed && !outcome.completed) await lifecycle.finish({
+					result: resultWithStreamedTextAndUsage(response, streamedText || "", streamedUsageRecord),
+					status: "success",
+					...streamedUsageRecord ? { usage: await resolveAgentUsageRecord({ usageRecord: streamedUsageRecord }, invocation.run) } : {},
+					usageResolved: true
+				});
+				else {
+					const driverUsageRecord = await resolveFinishUsageRecord(invocation, response);
+					await finishStreamAgentInvocation(invocation, lifecycle, resultWithStreamedTextAndUsage(response, streamedText || "", streamedUsageRecord, driverUsageRecord), finishOutcomeFromCleanup(outcome), streamFailureMessage, outputExtensions);
+				}
+			}, projection);
+			const headers = new Headers(response.headers);
+			headers.delete("content-encoding");
+			headers.delete("content-length");
+			return {
+				...finalized,
+				finishResult: response,
+				value: createAgentUIMessageStreamResponse({
+					headers,
+					status: response.status,
+					statusText: response.statusText,
+					stream: finalized.value
+				})
+			};
+		} : void 0,
+		finalizeRawStreams: options.output === "ui-message-stream" || Boolean(invocation.finalOutputRenderers.length) || Boolean(invocation.output),
+		outputExtensions,
+		...customRun ? { wrapStream: (stream) => maybeTraceAgentStream(stream, invocation) } : {}
+	});
+}
+async function runAgentInline(agent, context, input, options = {}) {
+	context = withAgentIdentityOwner(agent, context);
+	return await executeAgentInvocation(agent, context, input, {
+		kind: "run",
+		renderOutput: options.output !== "raw"
+	});
+}
+function agentInvocationSnapshotFromWorkflow(run) {
+	const status = run.status === "queued" ? "pending" : run.status === "unknown" ? void 0 : run.status;
+	if (!status) return void 0;
+	return {
+		...run.status === "failed" && run.metadata !== void 0 ? { error: run.metadata } : {},
+		id: run.id,
+		...run.result !== void 0 ? { output: run.result } : {},
+		status
+	};
+}
+function workflowOperationOutcome(error) {
+	return typeof error === "object" && error !== null && "code" in error && error.code === "WORKFLOW_OPERATION_UNSUPPORTED" ? "unsupported" : "unavailable";
+}
+function createWorkflowAgentInvocationController(started, parentAbortSignal) {
+	const { handle, run } = started;
+	return createBackedAgentInvocationController({
+		cancel: async () => agentInvocationSnapshotFromWorkflow(await handle.cancel(run.id)),
+		errorOutcome: workflowOperationOutcome,
+		id: run.id,
+		inspect: async () => agentInvocationSnapshotFromWorkflow(await handle.getRun(run.id)),
+		parentAbortSignal,
+		result: Promise.resolve(run)
+	});
+}
+function createInlineAgentInvocationController(agent, context, input) {
+	return startLiveAgentInvocation({
+		parentAbortSignal: input.abortSignal,
+		start: ({ abortSignal, id, onFinish }) => executeAgentInvocation(agent, {
+			...context,
+			run: {
+				...context.run,
+				runId: id
+			}
+		}, {
+			...input,
+			abortSignal
+		}, {
+			kind: "run",
+			onFinish(outcome) {
+				onFinish(outcome.status === "success" ? {
+					...outcome.result !== void 0 ? { output: outcome.result } : {},
+					status: "completed"
+				} : {
+					error: outcome.error,
+					status: "failed"
+				});
+			},
+			renderOutput: true
+		})
+	});
+}
+async function startAgentInvocation(agent, context, input) {
+	const invocationContext = withAgentIdentityOwner(agent, context);
+	const workflow = await runAgentAsWorkflow(agent, invocationContext, input, { fresh: true });
+	return workflow ? createWorkflowAgentInvocationController(workflow, input.abortSignal) : createInlineAgentInvocationController(agent, invocationContext, input);
+}
+async function runAgent(agent, context, input) {
+	const invocationContext = withAgentIdentityOwner(agent, context);
+	const workflow = await runAgentAsWorkflow(agent, invocationContext, input);
+	if (workflow) return workflow.run;
+	return await runAgentInline(agent, invocationContext, input);
+}
+async function runScheduledAgent(agent, context, runtimeContext = {}, input = {}) {
+	const memoValues = /* @__PURE__ */ new Map();
+	const runId = context.runId || context.id;
+	const turn = context.input && typeof context.input === "object" && context.input.kind === "agent-turn" ? parseScheduledAgentTurnInput(context.input) : void 0;
+	const forwardedInput = { ...input };
+	if (turn) {
+		delete forwardedInput.message;
+		delete forwardedInput.messages;
+		delete forwardedInput.prompt;
+	}
+	return await runAgent(agent, {
+		...runtimeContext,
+		memo(key, create) {
+			if (!memoValues.has(key)) memoValues.set(key, create());
+			return memoValues.get(key);
+		},
+		run: {
+			...runtimeContext.run,
+			...turn?.delivery,
+			runId
+		},
+		runtime: runtimeContext.runtime ?? "unknown",
+		waitUntil: runtimeContext.waitUntil ?? context.waitUntil ?? (() => {})
+	}, {
+		...forwardedInput,
+		context: {
+			...input.context,
+			...turn ? {
+				invoker: turn.invoker,
+				[scheduledAgentTurnContextKey]: true
+			} : {},
+			schedule: {
+				id: context.id,
+				kind: "schedule",
+				runId,
+				scheduleId: context.scheduleId,
+				scheduledAt: context.scheduledAt,
+				target: context.target
+			}
+		},
+		...turn ? { prompt: turn.prompt } : {}
+	});
+}
+async function streamAgentInline(agent, context, input, options = {}) {
+	return await executeAgentInvocation(agent, context, input, {
+		kind: "stream",
+		output: options.output || "events"
+	});
+}
+async function streamAgent(agent, context, input, options = {}) {
+	return await streamAgentInline(agent, context, input, options);
+}
+async function getAgent(agent, context) {
+	return await resolveAgent(agent, context);
+}
+//#endregion
+export { isResolvedAgentTriggerHandledInvocation as C, defineAgentRunEvents as D, toHttpErrorResponse as E, openHarnessBox as S, AgentHttpError as T, agentInvocationStreamHeader as _, resolveAgent as a, readAgentInvocationStream as b, runAgent as c, runScheduledAgent as d, startAgentInvocation as f, workflow as g, streamAgentTrigger as h, getAgentFromRegistry as i, runAgentInline as l, streamAgentInline as m, defineAgent as n, resolveAgentTriggerInvocation as o, streamAgent as p, getAgent as r, resolveAgentTriggers as s, agentWithColocatedInstructions as t, runAgentTrigger as u, agentInvocationStreamRoute as v, verifyAgentWebhookRequest as w, gateway as x, createAgentInvocationStreamResponse as y };
diff --git a/dist/test.d.ts b/dist/test.d.ts
index 0dd20c28a7e7da8c483949324903efc2cc413318..77eb93223c2237dc05a8ffb400fc5974b3800e13 100644
--- a/dist/test.d.ts
+++ b/dist/test.d.ts
@@ -1,4 +1,4 @@
-import { Lr as AgentWaitUntil, Qr as MaybePromise, Tr as AgentToolStep, Wt as AgentFinishExtensions, ar as AgentRunMetadata, fr as AgentRuntimeName, lr as AgentRuntimeConfig, rr as AgentRunInput, sn as AgentInput, ur as AgentRuntimeContext, zn as AgentModelInstrumentation } from "./types-C4oyRXNr.js";
+import { $r as MaybePromise, Bn as AgentModelInstrumentation, Er as AgentToolStep, Gt as AgentFinishExtensions, Rr as AgentWaitUntil, cn as AgentInput, dr as AgentRuntimeContext, ir as AgentRunInput, or as AgentRunMetadata, pr as AgentRuntimeName, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
 import { TraceRunView } from "@vite-hub/runtime";
 import { WorkspaceName } from "@vite-hub/workspace";
 
diff --git a/dist/test.js b/dist/test.js
index 9feafdd596698b0354cb2127defa32c5d4e79ad9..a7564cf0a3dca3ae1e51472f727dfd2e7ee49c4f 100644
--- a/dist/test.js
+++ b/dist/test.js
@@ -1,5 +1,5 @@
 import { i as resolveAgentUsageRecord } from "./agent-output-CyEZfgrQ.js";
-import { l as runAgentInline, n as defineAgent } from "./src-B3GiXaIA.js";
+import { l as runAgentInline, n as defineAgent } from "./src-CktB4unw.js";
 import { t as createAgentRuntimeContext } from "./context-KghRPYRx.js";
 import { createTraceEventLog, deriveTraceRuns } from "@vite-hub/runtime";
 import { registerWorkspace } from "@vite-hub/workspace/test";
diff --git a/dist/trace-GIqXV-YM.js b/dist/trace-GIqXV-YM.js
new file mode 100644
index 0000000000000000000000000000000000000000..0fbb23bbb8053582e1d10b344358c4f9132f908a
--- /dev/null
+++ b/dist/trace-GIqXV-YM.js
@@ -0,0 +1,557 @@
+import { o as toAgentRunResult } from "./agent-output-CyEZfgrQ.js";
+import { ViteHubError, emitTraceEvent, getViteHubErrorShape, resolveCapabilityPolicy } from "@vite-hub/runtime";
+//#region src/internal/agent-structured-output.ts
+const agentOutputErrorMessages = {
+	AGENT_OUTPUT_INVALID_JSON: "[vitehub] Agent output is not valid JSON.",
+	AGENT_OUTPUT_SCHEMA_INVALID: "[vitehub] Agent output failed schema validation."
+};
+function agentOutputValidationError(code, options) {
+	if (typeof code !== "string" || !Object.hasOwn(agentOutputErrorMessages, code)) throw new TypeError("[vitehub] Agent output errors require a known code.");
+	return new ViteHubError(code, agentOutputErrorMessages[code], { cause: readErrorCause(options) });
+}
+function isAgentOutputValidationError(value) {
+	const code = getViteHubErrorShape(value)?.code;
+	return code === "AGENT_OUTPUT_INVALID_JSON" || code === "AGENT_OUTPUT_SCHEMA_INVALID";
+}
+function readErrorCause(options) {
+	if ((typeof options !== "object" || options === null) && typeof options !== "function") return void 0;
+	try {
+		return Reflect.get(options, "cause");
+	} catch {
+		return;
+	}
+}
+function stripJsonFence(value) {
+	return value.trim().match(/^```(?:json)?\s*\n([\s\S]*?)\n```$/i)?.[1]?.trim() || value.trim();
+}
+function jsonValueFromResult(result) {
+	let text;
+	try {
+		const directText = result && typeof result === "object" && Reflect.has(result, "text") ? Reflect.get(result, "text") : void 0;
+		text = typeof directText === "string" && directText ? directText : toAgentRunResult(result).text;
+	} catch (cause) {
+		throw agentOutputValidationError("AGENT_OUTPUT_INVALID_JSON", { cause });
+	}
+	if (text === void 0) return result;
+	try {
+		return JSON.parse(stripJsonFence(text));
+	} catch (cause) {
+		throw agentOutputValidationError("AGENT_OUTPUT_INVALID_JSON", { cause });
+	}
+}
+function issuePath(issue) {
+	if (!issue.path?.length) return;
+	return issue.path.map((segment) => typeof segment === "object" ? String(segment.key) : String(segment)).join(".");
+}
+function formatIssues(issues) {
+	return issues.map((issue) => {
+		const path = issuePath(issue);
+		return path ? `${path}: ${issue.message}` : issue.message;
+	}).join("; ");
+}
+function inspectValidation(validation, allowIssues = false) {
+	try {
+		const issues = validation.issues;
+		if (issues !== void 0) {
+			if (!Array.isArray(issues)) throw new TypeError("[vitehub] Standard Schema returned invalid issues.");
+			if (issues.length > 0) {
+				if (allowIssues) return;
+				throw agentOutputValidationError("AGENT_OUTPUT_SCHEMA_INVALID", { cause: new Error(formatIssues(issues)) });
+			}
+		}
+		if (!("value" in validation)) throw new TypeError("[vitehub] Standard Schema returned no value.");
+		return { value: validation.value };
+	} catch (cause) {
+		if (isAgentOutputValidationError(cause)) throw cause;
+		throw agentOutputValidationError("AGENT_OUTPUT_SCHEMA_INVALID", { cause });
+	}
+}
+function isMaterializedObject(result) {
+	if (result === null || typeof result !== "object") return false;
+	try {
+		const prototype = Object.getPrototypeOf(result);
+		return prototype === Object.prototype || prototype === null;
+	} catch (cause) {
+		throw agentOutputValidationError("AGENT_OUTPUT_INVALID_JSON", { cause });
+	}
+}
+async function validateAgentOutput(output, result, options = {}) {
+	if (options.allowMaterializedObject && isMaterializedObject(result)) {
+		const directValidation = inspectValidation(await output.schema["~standard"].validate(result), true);
+		if (directValidation) return directValidation.value;
+	}
+	const value = jsonValueFromResult(result);
+	const validation = inspectValidation(await output.schema["~standard"].validate(value));
+	if (!validation) throw agentOutputValidationError("AGENT_OUTPUT_SCHEMA_INVALID");
+	return validation.value;
+}
+function supportsJsonSchema(schema) {
+	return typeof schema["~standard"].jsonSchema === "object" && typeof schema["~standard"].jsonSchema?.input === "function";
+}
+function agentOutputInstructions(output) {
+	if (!output) return;
+	let jsonSchema;
+	if (supportsJsonSchema(output.schema)) try {
+		jsonSchema = output.schema["~standard"].jsonSchema.input({ target: "draft-07" });
+	} catch {}
+	return ["Return only one valid JSON value for the configured Agent output. Do not wrap it in Markdown or add commentary.", ...jsonSchema ? [
+		"The JSON value must match this schema:",
+		"```json",
+		JSON.stringify(jsonSchema, null, 2),
+		"```"
+	] : []].join("\n");
+}
+//#endregion
+//#region src/tool-runtime.ts
+function isAgentToolDefinition(value) {
+	return typeof value === "object" && value !== null && "name" in value && typeof value.name === "string";
+}
+function toJsonCompatibleValue(value) {
+	if (value === void 0) return null;
+	try {
+		return JSON.parse(JSON.stringify(value));
+	} catch {
+		return String(value);
+	}
+}
+function createApprovalRequest(name, input, reason) {
+	return {
+		capability: name,
+		id: `approval_${name}_${Math.random().toString(36).slice(2, 10)}`,
+		input,
+		reason,
+		state: "awaiting-approval"
+	};
+}
+function withToolPolicy(tool) {
+	if (!tool.policy || typeof tool.execute !== "function") return tool;
+	const execute = tool.execute;
+	const policy = tool.policy;
+	return {
+		...tool,
+		async execute(input, context) {
+			const decision = typeof policy === "function" ? await policy({
+				name: tool.name,
+				input
+			}) : await resolveCapabilityPolicy(policy, {
+				capability: tool.name,
+				input,
+				operation: "tool.execute"
+			});
+			const approvalRequest = createApprovalRequest(tool.name, input);
+			if (decision === "deny") throw new ViteHubError("CAPABILITY_DENIED", `[vitehub:runtime] Capability "${tool.name}" was denied.`, { details: { capability: tool.name } });
+			if (decision === "require-approval") throw new ViteHubError("APPROVAL_REQUIRED", `[vitehub:runtime] Approval is required for "${tool.name}".`, {
+				cause: approvalRequest,
+				details: {
+					capability: tool.name,
+					requestId: approvalRequest.id
+				},
+				requestId: approvalRequest.id
+			});
+			if (decision === "retryable-failure") throw new Error(`[vitehub:agent] Tool "${tool.name}" failed with a retryable policy decision.`);
+			return await execute(input, context);
+		}
+	};
+}
+function applyAgentToolPolicies(tools) {
+	if (!tools || typeof tools !== "object") return tools;
+	return Object.fromEntries(Object.entries(tools).map(([name, tool]) => {
+		if (!isAgentToolDefinition(tool)) return [name, tool];
+		return [name, withToolPolicy(tool)];
+	}));
+}
+function withJsonCompatibleToolOutputs(tools) {
+	if (!tools || typeof tools !== "object") return tools;
+	return Object.fromEntries(Object.entries(tools).map(([name, tool]) => {
+		if (!tool || typeof tool !== "object" || typeof tool.execute !== "function") return [name, tool];
+		const execute = tool.execute;
+		return [name, {
+			...tool,
+			async execute(input, ...args) {
+				return toJsonCompatibleValue(await execute.call(tool, input, ...args));
+			}
+		}];
+	}));
+}
+function createToolCallId(name) {
+	return `${name}-${Date.now()}-${Math.random().toString(16).slice(2)}`;
+}
+function getErrorOutput(error) {
+	return error instanceof Error ? error.message : String(error);
+}
+function materializeSummary(output) {
+	if (!output || typeof output !== "object") return output;
+	const result = output;
+	const files = typeof result.files === "number" ? result.files : 0;
+	const sources = Array.isArray(result.sources) ? result.sources.map((source) => typeof source === "object" && source && "source" in source ? String(source.source) : "").filter(Boolean) : [];
+	const target = sources.length ? sources.join(" and ") : "workspace sources";
+	return {
+		...result,
+		summary: `Materialized ${target}${files ? ` (${files.toLocaleString()} file${files === 1 ? "" : "s"})` : ""}.`
+	};
+}
+async function reportWorkspaceMaterialization(tools, reportToolStep) {
+	if (!tools || typeof tools !== "object") return;
+	const materializeTool = tools.materialize_sources;
+	const execute = materializeTool && typeof materializeTool === "object" && typeof materializeTool.execute === "function" ? materializeTool.execute : void 0;
+	if (!execute) return;
+	const toolCall = {
+		input: { path: "" },
+		toolCallId: createToolCallId("materialize_sources"),
+		toolName: "materialize_sources"
+	};
+	await reportToolStep?.({ toolCalls: [toolCall] });
+	try {
+		const output = await execute.call(materializeTool, toolCall.input);
+		await reportToolStep?.({ toolResults: [{
+			...toolCall,
+			output: materializeSummary(output)
+		}] });
+	} catch (error) {
+		await reportToolStep?.({ toolErrors: [{
+			...toolCall,
+			output: getErrorOutput(error)
+		}] });
+	}
+}
+function withAgentToolStepReporting(tools, reportToolStep) {
+	if (!reportToolStep || !tools || typeof tools !== "object") return tools;
+	return Object.fromEntries(Object.entries(tools).map(([name, tool]) => {
+		if (!tool || typeof tool !== "object" || typeof tool.execute !== "function") return [name, tool];
+		if (name === "materialize_sources") return [name, tool];
+		const execute = tool.execute;
+		return [name, {
+			...tool,
+			async execute(input, ...args) {
+				const toolCall = {
+					input,
+					toolCallId: createToolCallId(name),
+					toolName: name
+				};
+				await reportToolStep({ toolCalls: [toolCall] });
+				try {
+					const output = await execute.call(tool, input, ...args);
+					await reportToolStep({ toolResults: [{
+						...toolCall,
+						output
+					}] });
+					return output;
+				} catch (error) {
+					await reportToolStep({ toolErrors: [{
+						...toolCall,
+						output: getErrorOutput(error)
+					}] });
+					throw error;
+				}
+			}
+		}];
+	}));
+}
+//#endregion
+//#region src/agent-error.ts
+function readAgentErrorProperty(error, key) {
+	if (typeof error !== "object" || error === null) return;
+	try {
+		return Reflect.get(error, key);
+	} catch {
+		return;
+	}
+}
+function isError(error) {
+	try {
+		return error instanceof Error;
+	} catch {
+		return false;
+	}
+}
+function stringifyErrorValue(value) {
+	const seen = /* @__PURE__ */ new WeakSet();
+	try {
+		return JSON.stringify(value, (_key, item) => {
+			if (typeof item === "bigint") return `${item}n`;
+			if (typeof item === "function") return `[Function${item.name ? `: ${item.name}` : ""}]`;
+			if (typeof item === "symbol") return String(item);
+			if (item && typeof item === "object") {
+				if (seen.has(item)) return "[Circular]";
+				seen.add(item);
+			}
+			return item;
+		});
+	} catch {
+		return;
+	}
+}
+function formatAgentError(error, fallback = "Unknown error.") {
+	if (error instanceof Error) return error.stack || error.message || error.name || fallback;
+	if (typeof error === "string") return error || fallback;
+	const text = stringifyErrorValue(error);
+	if (text) return text;
+	if (error === void 0) return fallback;
+	try {
+		const fallbackText = String(error);
+		return fallbackText && fallbackText !== "[object Object]" ? fallbackText : fallback;
+	} catch {
+		return fallback;
+	}
+}
+function agentErrorDetails(error, fallback = "Unknown error.") {
+	if (typeof error === "string") return { message: error || fallback };
+	if (typeof error === "object" && error !== null) {
+		const message = readAgentErrorProperty(error, "message");
+		const name = readAgentErrorProperty(error, "name");
+		if (typeof message === "string" && message) return {
+			message,
+			...typeof name === "string" && name ? { name } : {}
+		};
+		if (isError(error) && typeof name === "string" && name) return {
+			message: name,
+			name
+		};
+		return { message: fallback };
+	}
+	if (error === void 0 || error === null) return { message: fallback };
+	return { message: String(error) || fallback };
+}
+function agentErrorMessage(error, fallback) {
+	return agentErrorDetails(error, fallback).message;
+}
+function identifier(value) {
+	if (typeof value !== "string" || value.length === 0 || value.length > 128) return;
+	return /^[A-Za-z0-9@][A-Za-z0-9@._:/-]*$/.test(value) ? value : void 0;
+}
+function publicDetails(error, extra = {}) {
+	const owned = readAgentErrorProperty(error, "details");
+	const capability = identifier(readAgentErrorProperty(error, "capabilityId")) ?? identifier(readAgentErrorProperty(owned, "capabilityId")) ?? identifier(readAgentErrorProperty(owned, "capability"));
+	const details = {
+		...capability ? { capability } : {},
+		...extra
+	};
+	return Object.keys(details).length ? details : void 0;
+}
+function publicError(code, error, details) {
+	return {
+		code,
+		...details ? { details } : {},
+		error
+	};
+}
+function toAgentPublicError(error, context) {
+	try {
+		const viteHubError = getViteHubErrorShape(error);
+		if (viteHubError?.code === "AUTHENTICATION_REQUIRED") return publicError("AUTHENTICATION_REQUIRED", "Authentication required.");
+		if (viteHubError?.code === "RATE_LIMIT_REJECTED" || viteHubError?.code === "RATE_LIMIT_UNAVAILABLE") {
+			const retryAfter = viteHubError.details?.retryAfter;
+			const details = publicDetails(error, typeof retryAfter === "number" && Number.isFinite(retryAfter) && retryAfter >= 0 ? { retryAfter } : {});
+			return viteHubError.code === "RATE_LIMIT_UNAVAILABLE" ? publicError("RATE_LIMIT_UNAVAILABLE", "Rate limiting is unavailable.", details) : publicError("RATE_LIMIT_REJECTED", "Rate limit exceeded. Try again later.", details);
+		}
+		if (viteHubError?.code === "LLM_GATE_REJECTED") {
+			const category = identifier(viteHubError.details?.category);
+			return publicError("LLM_GATE_REJECTED", "Agent request was rejected.", publicDetails(error, category ? { category } : {}));
+		}
+		if (viteHubError?.code === "CAPABILITY_NOT_FOUND") return publicError("CAPABILITY_NOT_FOUND", "Capability was not found.", publicDetails(error));
+		if (viteHubError?.code === "CAPABILITY_DENIED") return publicError("CAPABILITY_DENIED", "Capability access was denied.", publicDetails(error));
+		if (viteHubError?.code === "APPROVAL_REQUIRED") {
+			const requestId = identifier(readAgentErrorProperty(error, "requestId"));
+			return {
+				...publicError("APPROVAL_REQUIRED", "Capability approval is required.", publicDetails(error)),
+				...requestId ? { requestId } : {}
+			};
+		}
+	} catch {}
+	return {
+		code: "INTERNAL",
+		error: context === "http" ? "Agent request failed." : context === "invocation" ? "Agent Invocation Stream failed." : "Agent Invocation Stream event could not be serialized."
+	};
+}
+//#endregion
+//#region src/trace.ts
+function hasAgentTraceLog(context) {
+	return Boolean(context.runtime.traceLog);
+}
+function invocationAttributes(context, extra = {}) {
+	return {
+		"agent.invoker.id": context.invoker.id,
+		"agent.invoker.kind": context.invoker.kind,
+		"agent.run.id": context.run?.runId,
+		"input.hasMessages": Boolean(context.input.messages?.length),
+		"input.hasPrompt": Boolean(context.input.prompt),
+		"runtime.name": context.runtime.runtime,
+		...extra
+	};
+}
+function eventAttributes(event) {
+	if (event.type === "tool-call" || event.type === "tool-input-start") return {
+		"step.id": event.id,
+		"tool.id": event.id,
+		"tool.name": event.name,
+		"tool.hasInput": event.input !== void 0
+	};
+	if (event.type === "tool-result") return {
+		"step.id": event.id,
+		"tool.id": event.id,
+		"tool.name": event.name,
+		"tool.hasOutput": event.output !== void 0,
+		"tool.error": event.error
+	};
+	if (event.type === "approval-request") return {
+		"approval.id": event.id,
+		"approval.name": event.name,
+		"approval.reason": event.reason,
+		"approval.hasInput": event.input !== void 0
+	};
+	if (event.type === "approval-decision") return {
+		"approval.approved": event.approved,
+		"approval.id": event.id,
+		"approval.reason": event.reason
+	};
+	if (event.type === "usage") return {
+		"usage.hasCost": event.usageRecord.cost !== void 0,
+		"usage.hasRaw": event.usageRecord.raw !== void 0,
+		"usage.totalTokens": event.usageRecord.usage?.totalTokens
+	};
+	if (event.type === "error") return {
+		"error.message": event.error,
+		"error.recoverable": event.recoverable
+	};
+	if (event.type === "finish") return { "finish.reason": event.reason };
+	return {};
+}
+async function traceAgentEvent(context, event) {
+	try {
+		const attributes = context.run?.runId ? {
+			"agent.run.id": context.run.runId,
+			...event.attributes
+		} : event.attributes;
+		await emitTraceEvent(context.runtime, {
+			...event,
+			...attributes ? { attributes } : {},
+			trace: event.trace || context.runtime.trace
+		});
+	} catch {}
+}
+async function traceAgentInvocationStart(context) {
+	await traceAgentEvent(context, {
+		attributes: invocationAttributes(context),
+		name: "agent.invocation.start",
+		type: "run"
+	});
+}
+async function traceAgentInvocationFinish(context, attributes = {}) {
+	await traceAgentEvent(context, {
+		attributes: invocationAttributes(context, attributes),
+		name: "agent.invocation.finish",
+		type: "run"
+	});
+}
+async function traceAgentInvocationError(context, error) {
+	const details = agentErrorDetails(error);
+	await traceAgentEvent(context, {
+		attributes: invocationAttributes(context, {
+			"error.message": details.message,
+			"error.name": details.name
+		}),
+		name: "agent.invocation.error",
+		type: "error"
+	});
+}
+async function traceAgentChannelDeliveryEffect(context, effect, attributes = {}) {
+	await traceAgentEvent(context, {
+		attributes: invocationAttributes(context, {
+			"channel.effect.intent": effect.intent,
+			"channel.effect.kind": effect.kind,
+			...attributes
+		}),
+		name: "agent.channel.delivery.effect",
+		type: "run"
+	});
+}
+async function traceAgentStreamEvent(context, event) {
+	if (!event || typeof event !== "object" || typeof event.type !== "string") return;
+	const streamEvent = event;
+	const names = {
+		"approval-decision": "agent.approval.decision",
+		"approval-request": "agent.approval.request",
+		error: "agent.stream.error",
+		finish: "agent.stream.finish",
+		"tool-call": "agent.tool.start",
+		"tool-input-start": "agent.tool.start",
+		"tool-result": streamEvent.type === "tool-result" && streamEvent.error ? "agent.tool.error" : "agent.tool.finish",
+		usage: "agent.usage.recorded"
+	};
+	const name = streamEvent.type in names ? names[streamEvent.type] : void 0;
+	if (!name) return;
+	await traceAgentEvent(context, {
+		attributes: eventAttributes(streamEvent),
+		name,
+		type: streamEvent.type.startsWith("approval") ? "approval" : streamEvent.type === "error" || streamEvent.type === "tool-result" && streamEvent.error ? "error" : "run"
+	});
+}
+function traceAgentStreamEvents(events, context) {
+	return (async function* () {
+		for await (const event of events) {
+			await traceAgentStreamEvent(context, event);
+			yield event;
+		}
+	})();
+}
+function valueFromPath(value, path) {
+	let current = value;
+	for (const key of path) {
+		if (!current || typeof current !== "object") return void 0;
+		current = current[key];
+	}
+	return current;
+}
+function firstString(...values) {
+	return values.find((value) => typeof value === "string" && value.length > 0);
+}
+function aiSdkTelemetryIntegration(context) {
+	const modelAttributes = (event) => ({
+		"model.id": firstString(valueFromPath(event, ["model", "modelId"]), valueFromPath(event, ["model", "id"]), valueFromPath(event, ["modelId"])),
+		"model.provider": firstString(valueFromPath(event, ["model", "provider"]), valueFromPath(event, ["provider"])),
+		"model.call.id": firstString(valueFromPath(event, ["id"]), valueFromPath(event, ["callId"]), valueFromPath(event, ["requestId"])) || "model"
+	});
+	const toolAttributes = (event) => ({
+		"step.id": firstString(valueFromPath(event, ["toolCall", "toolCallId"]), valueFromPath(event, ["toolCallId"]), valueFromPath(event, ["id"]), valueFromPath(event, ["tool", "id"])) || "tool",
+		"tool.id": firstString(valueFromPath(event, ["toolCall", "toolCallId"]), valueFromPath(event, ["toolCallId"]), valueFromPath(event, ["id"]), valueFromPath(event, ["tool", "id"])),
+		"tool.name": firstString(valueFromPath(event, ["toolCall", "toolName"]), valueFromPath(event, ["toolName"]), valueFromPath(event, ["name"]), valueFromPath(event, ["tool", "name"]))
+	});
+	return {
+		async onLanguageModelCallEnd(event) {
+			await traceAgentEvent(context, {
+				attributes: modelAttributes(event),
+				name: "agent.model.call.finish",
+				type: "run"
+			});
+		},
+		async onLanguageModelCallStart(event) {
+			await traceAgentEvent(context, {
+				attributes: modelAttributes(event),
+				name: "agent.model.call.start",
+				type: "run"
+			});
+		},
+		async onToolExecutionEnd(event) {
+			const outputType = valueFromPath(event, ["toolOutput", "type"]);
+			const error = valueFromPath(event, ["toolOutput", "error"]) ?? valueFromPath(event, ["error"]);
+			const failed = outputType === "tool-error" || error !== void 0;
+			await traceAgentEvent(context, {
+				attributes: {
+					...toolAttributes(event),
+					"error.message": error instanceof Error ? error.message : typeof error === "string" ? error : void 0
+				},
+				name: failed ? "agent.tool.error" : "agent.tool.finish",
+				type: failed ? "error" : "run"
+			});
+		},
+		async onToolExecutionStart(event) {
+			await traceAgentEvent(context, {
+				attributes: toolAttributes(event),
+				name: "agent.tool.start",
+				type: "run"
+			});
+		}
+	};
+}
+//#endregion
+export { withAgentToolStepReporting as _, traceAgentInvocationFinish as a, validateAgentOutput as b, traceAgentStreamEvents as c, formatAgentError as d, readAgentErrorProperty as f, toJsonCompatibleValue as g, reportWorkspaceMaterialization as h, traceAgentInvocationError as i, agentErrorDetails as l, applyAgentToolPolicies as m, hasAgentTraceLog as n, traceAgentInvocationStart as o, toAgentPublicError as p, traceAgentChannelDeliveryEffect as r, traceAgentStreamEvent as s, aiSdkTelemetryIntegration as t, agentErrorMessage as u, withJsonCompatibleToolOutputs as v, agentOutputInstructions as y };
diff --git a/dist/types-BZoKvU1a.d.ts b/dist/types-BZoKvU1a.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..ff1177a8b4c959d4a5e2c3812f4d97879c70be3a
--- /dev/null
+++ b/dist/types-BZoKvU1a.d.ts
@@ -0,0 +1,20 @@
+import { $r as MaybePromise, B as AgentCapabilityRuntimeContext, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
+import { WorkspaceName } from "@vite-hub/workspace";
+
+//#region src/mcp/types.d.ts
+interface McpClient {
+  close: () => MaybePromise<void>;
+  serverInfo?: unknown;
+  tools: () => MaybePromise<Record<string, unknown>>;
+}
+interface McpClientConfig {
+  transport: unknown;
+}
+type McpServerConfig<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = McpClient | McpClientConfig | ((context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<McpClient | McpClientConfig>);
+type McpToolFingerprints = Record<string, string>;
+interface McpCapabilityOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> {
+  integrity?: Record<string, McpToolFingerprints>;
+  servers: Record<string, McpServerConfig<TRuntimeConfig, Name>>;
+}
+//#endregion
+export { McpToolFingerprints as a, McpServerConfig as i, McpClient as n, McpClientConfig as r, McpCapabilityOptions as t };
\ No newline at end of file
diff --git a/dist/types-C4oyRXNr.d.ts b/dist/types-C4oyRXNr.d.ts
index 42dceaf5547844a5be77144261ccad7a1c53d3fd..27f95ac3af5d46fda73000be0fef8bfff527acc1 100644
--- a/dist/types-C4oyRXNr.d.ts
+++ b/dist/types-C4oyRXNr.d.ts
@@ -1384,6 +1384,7 @@ interface AgentUsageCost {
   amount: string;
   currency: "USD" | (string & {});
   estimated: boolean;
+  formatted?: string;
   source: "custom" | "estimated" | "provider" | "vercel-ai-gateway" | (string & {});
 }
 interface AgentUsageCredentialSource {
diff --git a/dist/types-CW2XmfE7.d.ts b/dist/types-CW2XmfE7.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6ce61a5ec6a1be055d8c3c6ea5e3e11ea513a387
--- /dev/null
+++ b/dist/types-CW2XmfE7.d.ts
@@ -0,0 +1,1484 @@
+import { t as LocalHarnessSandboxOptions } from "./local-sandbox-G03FIxI_.js";
+import { ExecutionAuthority, MaybePromise, MaybeResolvable, Resolvable, RuntimeCapabilities, RuntimeCapabilityHandle, RuntimeHostContext, RuntimeWaitUntil } from "@vite-hub/runtime";
+import { Adapter, AdapterPostableMessage, IdentityResolver, StateAdapter, TranscriptsConfig } from "chat";
+import { Box, BoxDefinition, BoxRequirement } from "@vite-hub/box";
+import { StandardJSONSchemaV1, StandardSchemaV1 } from "@standard-schema/spec";
+import { JSONSchema7 } from "json-schema";
+import { ReadonlyWorkspaceFacade, WorkspaceDefinition, WorkspaceDefinitionInput, WorkspaceName, WorkspaceRules, WorkspaceSourceInput, WritableWorkspaceFacade } from "@vite-hub/workspace";
+
+//#region src/messages.d.ts
+type MessageRole = "assistant" | "system" | "tool" | "user";
+interface MessageMetadata {
+  [key: string]: unknown;
+}
+interface TextPart {
+  id?: string;
+  text: string;
+  type: "text";
+}
+interface DataPart {
+  data: unknown;
+  id?: string;
+  type: "data" | `data-${string}`;
+}
+type AttachmentData = ArrayBuffer | Blob | string | Uint8Array;
+type AudioData = AttachmentData;
+interface AttachmentPart {
+  data?: AttachmentData;
+  fetchData?: () => Promise<AttachmentData> | AttachmentData;
+  fetchMetadata?: Record<string, string>;
+  id?: string;
+  mediaType: string;
+  name?: string;
+  size?: number;
+  type: "audio" | "file" | "image";
+  url?: string;
+}
+interface AudioPart extends AttachmentPart {
+  type: "audio";
+}
+interface FilePart extends AttachmentPart {
+  type: "file";
+}
+interface ImagePart extends AttachmentPart {
+  type: "image";
+}
+type ToolInvocationState = "approval-required" | "failed" | "proposed" | "running" | "completed";
+interface ToolInvocation {
+  error?: string;
+  id: string;
+  input?: unknown;
+  name: string;
+  output?: unknown;
+  state: ToolInvocationState;
+}
+interface ToolCallPart {
+  id: string;
+  input?: unknown;
+  name: string;
+  state: Exclude<ToolInvocationState, "completed">;
+  type: "tool-call";
+}
+interface ToolResultPart {
+  error?: string;
+  id: string;
+  name: string;
+  output?: unknown;
+  state: "completed" | "failed";
+  type: "tool-result";
+}
+interface ApprovalRequestPart {
+  id: string;
+  input?: unknown;
+  name: string;
+  reason?: string;
+  type: "approval-request";
+}
+interface ApprovalDecisionPart {
+  approved: boolean;
+  decidedAt?: string;
+  id: string;
+  reason?: string;
+  type: "approval-decision";
+}
+interface SourcePart {
+  id?: string;
+  sourceType?: string;
+  title?: string;
+  type: "source";
+  url?: string;
+}
+interface ErrorPart {
+  error: string;
+  id?: string;
+  recoverable?: boolean;
+  type: "error";
+}
+type MessagePart = ApprovalDecisionPart | ApprovalRequestPart | AudioPart | DataPart | ErrorPart | FilePart | ImagePart | SourcePart | TextPart | ToolCallPart | ToolResultPart;
+declare function isAttachmentData(value: unknown): value is AttachmentData;
+declare function isAttachmentPart(value: unknown): value is AttachmentPart;
+interface Message {
+  createdAt?: string;
+  id: string;
+  metadata?: MessageMetadata;
+  parts: MessagePart[];
+  role: MessageRole;
+}
+type AgentMessagePhase = "commentary" | "final";
+type StreamEvent = {
+  id?: string;
+  messageId?: string;
+  phase?: AgentMessagePhase;
+  role?: MessageRole;
+  text: string;
+  type: "text-delta";
+} | {
+  data: unknown;
+  id?: string;
+  messageId?: string;
+  transient?: boolean;
+  type: "data" | `data-${string}`;
+} | {
+  id: string;
+  input?: unknown;
+  messageId?: string;
+  name: string;
+  type: "tool-call" | "tool-input-start";
+} | {
+  durationMs?: number;
+  error?: string;
+  id: string;
+  messageId?: string;
+  name: string;
+  output?: unknown;
+  type: "tool-result";
+} | {
+  id: string;
+  input?: unknown;
+  messageId?: string;
+  name: string;
+  reason?: string;
+  type: "approval-request";
+} | {
+  approved: boolean;
+  decidedAt?: Date | string;
+  id: string;
+  messageId?: string;
+  reason?: string;
+  type: "approval-decision";
+} | {
+  error: string;
+  id?: string;
+  messageId?: string;
+  recoverable?: boolean;
+  type: "error";
+} | {
+  messageId?: string;
+  usageRecord: AgentUsageRecord;
+  type: "usage";
+} | {
+  messageId?: string;
+  reason?: string;
+  type: "finish";
+};
+type RunEvent = StreamEvent;
+interface CreateMessageOptions {
+  createdAt?: Date | string;
+  id?: string;
+  metadata?: MessageMetadata;
+  parts?: Array<MessagePart | string>;
+  role: MessageRole;
+  text?: string;
+}
+interface SerializedMessages {
+  messages: Message[];
+  version: 1;
+}
+declare function createMessage(options: CreateMessageOptions): Message;
+declare function getMessageText(message: Message): string;
+declare function appendMessageText(message: Message, text: string): Message;
+declare function getToolInvocations(message: Message): ToolInvocation[];
+declare function validateMessage(message: Message): void;
+declare function applyStreamEvent(messages: Message[], event: StreamEvent): Message[];
+declare function collectStreamEvents(stream: AsyncIterable<StreamEvent>): Promise<StreamEvent[]>;
+declare function serializeMessages(messages: Message[]): string;
+declare function deserializeMessages(input: string | SerializedMessages): Message[];
+//#endregion
+//#region src/run-events.d.ts
+declare const agentRunEventsBrand: unique symbol;
+interface AgentRunEvent<TData = unknown> {
+  cursor: string;
+  data?: TData;
+  runId: string;
+  timestamp: string;
+  type: string;
+}
+interface AgentRunEventInput<TData = unknown> {
+  data?: TData;
+  type: string;
+}
+interface AgentRunEventReadOptions {
+  after?: string;
+}
+interface AgentRunEventSubscribeOptions extends AgentRunEventReadOptions {
+  signal?: AbortSignal;
+}
+interface AgentRunEventStore {
+  append(runId: string, event: AgentRunEventInput): MaybePromise<AgentRunEvent>;
+  read(runId: string, options?: AgentRunEventReadOptions): MaybePromise<readonly AgentRunEvent[]>;
+  /** Replays events after the cursor before yielding live events for the same run. */
+  subscribe(runId: string, options?: AgentRunEventSubscribeOptions): AsyncIterable<AgentRunEvent>;
+}
+interface AgentRunEventStoreResolveContext {
+  operation: "publish" | "read" | "subscribe";
+  runId: string;
+  runtime?: ResolvedAgentRuntimeContext;
+}
+type AgentRunEventStoreResolver = (context: AgentRunEventStoreResolveContext) => MaybePromise<AgentRunEventStore>;
+interface AgentRunEventsOptions {
+  store: AgentRunEventStore | AgentRunEventStoreResolver;
+}
+interface AgentRunEventPublisher {
+  publish<TData = unknown>(event: AgentRunEventInput<TData>): Promise<AgentRunEvent<TData>>;
+}
+interface AgentRunEvents {
+  readonly [agentRunEventsBrand]: true;
+  publish<TData = unknown>(runId: string, event: AgentRunEventInput<TData>): Promise<AgentRunEvent<TData>>;
+  read(runId: string, cursor?: string): Promise<readonly AgentRunEvent[]>;
+  subscribe(runId: string, cursor?: string, options?: {
+    signal?: AbortSignal;
+  }): AsyncIterable<AgentRunEvent>;
+}
+declare function defineAgentRunEvents(options: AgentRunEventsOptions): AgentRunEvents;
+//#endregion
+//#region src/types.d.ts
+type AgentRuntimeName = "cloudflare-agents" | "deno" | "unknown" | "vercel" | "vite";
+type AgentRuntime = "auto" | AgentRuntimeName;
+type AgentExecution = "inline" | "sandbox" | "workflow";
+type AgentRuntimeBinding = false | AgentWorkflowRuntimeBinding;
+interface AgentWorkflowRuntimeBinding {
+  kind: "workflow";
+  name?: string;
+}
+type AgentWaitUntil = RuntimeWaitUntil;
+type AgentIntegrationOption = "auto" | boolean;
+type AgentCapabilityHandle<TKind extends string = string, TValue = unknown> = RuntimeCapabilityHandle<TKind, TValue>;
+type AgentCapabilities = RuntimeCapabilities;
+interface AgentRuntimeConfig {}
+interface AgentHostIdentity {
+  readonly name: string;
+  readonly workspace?: WorkspaceName;
+}
+interface AgentRuntimeContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends Omit<RuntimeHostContext<TRuntimeConfig>, "cloudflare" | "platform" | "runtime"> {
+  agentIdentity?: AgentHostIdentity;
+  cloudflare?: RuntimeHostContext<TRuntimeConfig>["cloudflare"];
+  toolStepReporter?: (step: AgentToolStep) => MaybePromise<void>;
+  run?: AgentRunMetadata;
+  runtime: AgentRuntimeName;
+}
+type ResolvedAgentRuntimeContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = AgentRuntimeContext<TRuntimeConfig> & {
+  runtimeConfig: TRuntimeConfig;
+};
+type AgentCallbackContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = Omit<ResolvedAgentRuntimeContext<TRuntimeConfig>, "runtimeConfig"> & {
+  runEvents?: AgentRunEventPublisher;
+};
+type AgentInvokerMeta = Record<string, unknown>;
+interface AgentInvoker<TMeta extends AgentInvokerMeta = AgentInvokerMeta> {
+  email?: {
+    address: string;
+    domain: string;
+  };
+  id: string;
+  kind?: "anonymous" | "chat" | (string & {});
+  label?: string;
+  meta?: TMeta;
+}
+type AgentActor<TMeta extends AgentInvokerMeta = AgentInvokerMeta> = AgentInvoker<TMeta>;
+type AgentInvokerProfile<TMeta extends AgentInvokerMeta = AgentInvokerMeta> = AgentInvoker<TMeta>;
+interface AgentAccessWorkspaceScopeContext<TScopeName extends string = string> {
+  all: boolean;
+  paths: string[];
+  role: "viewer" | "admin" | (string & {});
+  scope: TScopeName;
+  sources: string[];
+}
+interface AgentAccessInvocationContextValue<TScopeName extends string = string> {
+  workspaceScope?: AgentAccessWorkspaceScopeContext<TScopeName>;
+}
+declare global {
+  interface ViteHubAgentInvocationContextValues {}
+  interface ViteHubAgentFinishExtensions {}
+  interface ViteHubAgentOutputExtensions {}
+}
+interface AgentInvocationContextValues extends ViteHubAgentInvocationContextValues {
+  access: AgentAccessInvocationContextValue;
+  actor: AgentActor;
+  "agent.finishHook": boolean;
+  "channel.delivery.effects": AgentChannelDeliveryEffectIntent[];
+  "channel.delivery.finishEffects": AgentChannelDeliveryFinishEffect[];
+  "channel.delivery.supportsTitle": boolean;
+  invoker: AgentInvoker;
+}
+type AgentInvocationCallbackContextKey = { [Key in keyof ViteHubAgentInvocationContextValues]: Key extends string ? Key extends "actor" | "chat" | "invoker" | `agent.${string}` | `channel.delivery.${string}` | `chat.${string}` | `workspace.${string}` ? never : Key : never }[keyof ViteHubAgentInvocationContextValues];
+interface AgentInvocationCallbackContextValues extends Partial<Pick<ViteHubAgentInvocationContextValues, AgentInvocationCallbackContextKey>> {
+  access?: AgentAccessInvocationContextValue;
+}
+type AgentRunInputContextValues = Partial<AgentInvocationContextValues> & Record<string, unknown>;
+interface AgentInvocationContextStore<TValues extends object = AgentInvocationContextValues> {
+  entries: () => IterableIterator<[string, unknown]>;
+  get: {
+    <TKey extends keyof TValues & string>(id: TKey): TValues[TKey] | undefined;
+    <T = unknown>(id: string): T | undefined;
+  };
+  has: (id: string) => boolean;
+  set: {
+    <TKey extends keyof TValues & string>(id: TKey, value: TValues[TKey], options?: {
+      overwrite?: boolean;
+    }): void;
+    (id: string, value: unknown, options?: {
+      overwrite?: boolean;
+    }): void;
+  };
+  toJSON: () => Record<string, unknown>;
+}
+interface AgentInvokerResolveContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues> extends AgentCallbackContext<TRuntimeConfig> {
+  context: AgentInvocationContextStore<TContextValues>;
+  defaultInvoker: AgentInvoker;
+  input: AgentRunInput<CALL_OPTIONS>;
+  profiles: readonly TProfile[];
+  run?: AgentRunMetadata;
+  selectedProfile?: TProfile;
+}
+interface AgentInvokerOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues> {
+  profiles?: readonly TProfile[];
+  resolve?: (context: AgentInvokerResolveContext<TRuntimeConfig, CALL_OPTIONS, TProfile, TContextValues>) => MaybePromise<AgentInvoker | null | undefined>;
+}
+interface AgentRunInput<CALL_OPTIONS = unknown, TContext extends object = AgentRunInputContextValues> {
+  abortSignal?: AbortSignal;
+  context?: TContext;
+  message?: string | Message;
+  messages?: Message[];
+  options?: CALL_OPTIONS;
+  prompt?: string | Message[];
+  timeout?: number;
+}
+interface AgentRunMetadata<TOrigin extends string = string> {
+  channelId?: string;
+  messageId?: string;
+  origin?: TOrigin;
+  runId: string;
+  threadId?: string;
+}
+type AgentChannelDeliveryEffectKind = "reaction" | "reply" | "status" | (string & {});
+type AgentDeliveryArtifactPlacement = "inline" | "attachment" | "link";
+interface AgentDeliveryArtifact {
+  alt?: string;
+  mediaType?: string;
+  path: string;
+  placement?: AgentDeliveryArtifactPlacement;
+}
+interface PublishedAgentDeliveryArtifact extends AgentDeliveryArtifact {
+  channelAttachmentId?: string;
+  url?: string;
+}
+interface AgentChannelDeliveryEffectIntent<TKind extends AgentChannelDeliveryEffectKind = AgentChannelDeliveryEffectKind, TPayload = AgentChannelDeliveryEffectPayload<TKind>> {
+  artifacts?: readonly PublishedAgentDeliveryArtifact[];
+  intent?: string;
+  kind: TKind;
+  metadata?: Record<string, unknown>;
+  payload?: TPayload;
+}
+interface AgentChannelDeliveryEffectIntentOptions {
+  artifacts?: readonly PublishedAgentDeliveryArtifact[];
+  intent?: string;
+  metadata?: Record<string, unknown>;
+}
+interface AgentChannelDeliveryReplyPayload {
+  artifacts?: readonly PublishedAgentDeliveryArtifact[];
+  body?: string;
+  markdown?: string;
+}
+type AgentChannelDeliveryReplyStream = AsyncIterable<string>;
+type AgentChannelDeliveryReplyInput = string | AgentChannelDeliveryReplyPayload | AgentChannelDeliveryReplyStream;
+interface AgentChannelDeliveryReactionPayload {
+  action?: "remove" | (string & {});
+  content?: string;
+  emoji?: string;
+}
+type AgentChannelDeliveryReactionInput = string | AgentChannelDeliveryReactionPayload;
+type AgentChannelDeliveryStatusState = "error" | "failure" | "pending" | "success" | (string & {});
+interface AgentChannelDeliveryStatusPayload {
+  context?: string;
+  description?: string;
+  sha?: string;
+  state?: AgentChannelDeliveryStatusState;
+  target_url?: string;
+}
+type AgentChannelDeliveryStatusInput = AgentChannelDeliveryStatusState | AgentChannelDeliveryStatusPayload;
+type AgentChannelDeliveryEffectPayload<TKind extends AgentChannelDeliveryEffectKind> = TKind extends "reaction" ? AgentChannelDeliveryReactionInput : TKind extends "reply" ? AgentChannelDeliveryReplyInput : TKind extends "status" ? AgentChannelDeliveryStatusInput : unknown;
+interface AgentChannelDeliveryEffectContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  channel: AgentChannelDefinition<TRuntimeConfig>;
+  context: AgentInvocationContextStore;
+  effect: AgentChannelDeliveryEffectIntent;
+  finish?: AgentFinishEvent<TRuntimeConfig>;
+  input: AgentRunInput;
+  request?: Request;
+  run?: AgentRunMetadata;
+  trigger?: {
+    channelId: string;
+    id?: string;
+    name?: string;
+  };
+  workspace?: ReadonlyWorkspaceFacade | WritableWorkspaceFacade;
+}
+interface AgentChannelDeliveryFinishEffectContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> extends AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS> {
+  channel?: AgentChannelDefinition<any>;
+  error?: unknown;
+  errorMessage?: string;
+  event: AgentFinishEvent<TRuntimeConfig, CALL_OPTIONS>;
+  extensions: AgentFinishExtensions;
+  invocation: AgentFinishEvent<TRuntimeConfig, CALL_OPTIONS>["invocation"];
+  output?: unknown;
+  reaction: (input: AgentChannelDeliveryReactionInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"reaction">;
+  reply: (input: AgentChannelDeliveryReplyInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"reply">;
+  result?: AgentRunResult;
+  request?: Request;
+  status: (input: AgentChannelDeliveryStatusInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"status">;
+  text?: string;
+  workspace?: ReadonlyWorkspaceFacade;
+}
+type AgentChannelDeliveryEffectHandler<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = (context: AgentChannelDeliveryEffectContext<TRuntimeConfig>) => MaybePromise<void>;
+type AgentChannelDeliveryEffects<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = Partial<Record<AgentChannelDeliveryEffectKind, AgentChannelDeliveryEffectHandler<TRuntimeConfig> | readonly AgentChannelDeliveryEffectHandler<TRuntimeConfig>[]>>;
+type AgentChannelDeliveryFinishEffectResult = AgentChannelDeliveryEffectIntent | readonly AgentChannelDeliveryEffectIntent[] | false | null | undefined;
+type AgentChannelDeliveryFinishEffectCallback<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = {
+  (context: AgentChannelDeliveryFinishEffectContext<TRuntimeConfig, CALL_OPTIONS>, event?: AgentFinishEvent<TRuntimeConfig, CALL_OPTIONS>): MaybePromise<AgentChannelDeliveryFinishEffectResult>;
+  active?: (context: AgentChannelDeliveryFinishEffectContext<TRuntimeConfig, CALL_OPTIONS>) => boolean;
+  kind?: AgentChannelDeliveryEffectKind;
+};
+type AgentChannelDeliveryFinishEffect<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = AgentChannelDeliveryEffectIntent | readonly AgentChannelDeliveryEffectIntent[] | AgentChannelDeliveryFinishEffectCallback<TRuntimeConfig, CALL_OPTIONS>;
+interface AgentTriggerRunInvokeResult<CALL_OPTIONS = unknown> {
+  delivery?: {
+    effects?: AgentChannelDeliveryEffectIntent | readonly AgentChannelDeliveryEffectIntent[];
+    finishEffects?: AgentChannelDeliveryFinishEffect | readonly AgentChannelDeliveryFinishEffect[];
+  };
+  input: AgentRunInput<CALL_OPTIONS>;
+  metadata?: Record<string, unknown>;
+  run?: AgentRunMetadata;
+  webhook?: AgentWebhookInvocationOwnership;
+}
+interface AgentWebhookInvocationOwnership {
+  concurrencyKey?: string;
+  concurrencyTtlMs?: number;
+  deliveryId: string;
+}
+type AgentTriggerInvokeResult<CALL_OPTIONS = unknown> = AgentTriggerRunInvokeResult<CALL_OPTIONS> | Response;
+type AgentWebhookSecretToken<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<string | false, AgentCallbackContext<TRuntimeConfig>>;
+interface AgentWebhookRegistrationDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> {
+  adapter?: string;
+  channelId?: string;
+  id?: string;
+  method?: "POST" | (string & {});
+  path?: string;
+  provider: string;
+  signature?: "github-sha256" | (string & {});
+  secretHeader?: string;
+  secretToken?: AgentWebhookSecretToken<TRuntimeConfig>;
+  url?: string;
+}
+interface AgentChannelWebhookState {
+  details?: Record<string, boolean | number | string | null | readonly string[]>;
+  url?: string;
+}
+interface AgentChannelWebhookManager {
+  delete?: (options?: {
+    signal?: AbortSignal;
+  }) => MaybePromise<void>;
+  inspect: (options?: {
+    signal?: AbortSignal;
+  }) => MaybePromise<AgentChannelWebhookState>;
+  set: (options: {
+    signal?: AbortSignal;
+    url: string;
+  }) => MaybePromise<void>;
+}
+type AgentChannelWebhookManagerResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<AgentChannelWebhookManager | false | undefined, AgentCallbackContext<TRuntimeConfig>>;
+type AgentChannelWebhookRegistrationDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = Omit<AgentWebhookRegistrationDefinition<TRuntimeConfig>, "provider"> & {
+  provider?: string;
+};
+interface AgentTriggerContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends AgentCallbackContext<TRuntimeConfig> {
+  actor?: AgentActor;
+  capability: AgentCapabilityDefinition<TRuntimeConfig, Name>;
+  trigger: {
+    capabilityId: string;
+    id: `${string}.${string}`;
+    name: string;
+    source: "capability";
+  };
+}
+interface AgentChannelTriggerContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  actor?: AgentActor;
+  agentCapabilities: readonly AgentCapabilityDefinition<TRuntimeConfig>[];
+  channel: AgentChannelDefinition<TRuntimeConfig>;
+  trigger: {
+    channelId: string;
+    id: `${string}.${string}`;
+    name: string;
+    source: "channel";
+  };
+}
+interface AgentTriggerDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TInput = unknown, CALL_OPTIONS = unknown, TContext extends AgentCallbackContext<TRuntimeConfig> = AgentTriggerContext<TRuntimeConfig, Name>> {
+  input?: unknown;
+  invoke: (context: TContext, input: TInput) => MaybePromise<AgentTriggerInvokeResult<CALL_OPTIONS>>;
+  output?: "events" | "ui-message-stream" | (string & {});
+  webhooks?: AgentWebhookRegistrationDefinition<TRuntimeConfig>[];
+}
+interface ResolvedAgentTriggerDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, TInput = unknown, CALL_OPTIONS = unknown> {
+  capabilityId?: string;
+  channelId?: string;
+  definition: AgentTriggerDefinition<TRuntimeConfig, WorkspaceName, TInput, CALL_OPTIONS>;
+  id: `${string}.${string}`;
+  input?: unknown;
+  invoke: (input: TInput) => MaybePromise<AgentTriggerInvokeResult<CALL_OPTIONS>>;
+  name: string;
+  output?: "events" | "ui-message-stream" | (string & {});
+  source: "capability" | "channel";
+  webhooks?: AgentWebhookRegistrationDefinition<TRuntimeConfig>[];
+}
+interface AgentRunCallbackContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> extends AgentCallbackContext<TRuntimeConfig> {
+  actor: AgentActor;
+  context: AgentInvocationContextStore<TContextValues>;
+  input: AgentRunInput<CALL_OPTIONS>;
+  invoker: AgentInvoker;
+  run?: AgentRunMetadata;
+}
+interface AgentRunResult {
+  artifacts?: readonly PublishedAgentDeliveryArtifact[];
+  finishReason?: unknown;
+  raw?: unknown;
+  text?: string;
+  usage?: unknown;
+  usageRecord?: AgentUsageRecord;
+  warnings?: unknown;
+}
+interface AgentFinishExtensionValues extends ViteHubAgentFinishExtensions {}
+interface AgentOutputExtensionValues extends ViteHubAgentOutputExtensions {}
+interface AgentInvocationExtensions<TValues extends object = Record<string, unknown>> {
+  entries: () => Array<[string, unknown]>;
+  get<TKey extends keyof TValues & string>(capabilityId: TKey): TValues[TKey] | undefined;
+  get<TKey extends keyof TValues & string, TField extends keyof NonNullable<TValues[TKey]> & string>(capabilityId: TKey, key: TField): NonNullable<TValues[TKey]>[TField] | undefined;
+  get<T = unknown>(capabilityId: string): T | undefined;
+  get<T = unknown>(capabilityId: string, key: string): T | undefined;
+  toJSON: () => Record<string, unknown>;
+}
+type AgentFinishExtensions = AgentInvocationExtensions<AgentFinishExtensionValues>;
+type AgentOutputExtensions = AgentInvocationExtensions<AgentOutputExtensionValues>;
+interface AgentOutputExtensionEvent {
+  extensions: AgentOutputExtensions;
+  result: unknown;
+}
+interface AgentFinishEvent<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> {
+  actor: AgentActor;
+  error?: unknown;
+  errorMessage?: string;
+  extensions: AgentFinishExtensions;
+  input: AgentRunInput<CALL_OPTIONS>;
+  invoker: AgentInvoker;
+  invocation: {
+    durationMs: number;
+    resultKind?: string;
+    run?: AgentRunMetadata;
+    usage?: AgentUsageRecord;
+  };
+  result?: unknown;
+  runtime: ResolvedAgentRuntimeContext<TRuntimeConfig> & {
+    runEvents?: AgentRunEventPublisher;
+  };
+  text?: string;
+}
+interface AgentFinishHookEvent<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> extends AgentFinishEvent<TRuntimeConfig, CALL_OPTIONS> {
+  reaction: (input: AgentChannelDeliveryReactionInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"reaction">;
+  reply: (input: AgentChannelDeliveryReplyInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"reply">;
+  status: (input: AgentChannelDeliveryStatusInput, options?: AgentChannelDeliveryEffectIntentOptions) => AgentChannelDeliveryEffectIntent<"status">;
+}
+type AgentFinishHook<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = (event: AgentFinishHookEvent<TRuntimeConfig, CALL_OPTIONS>) => MaybePromise<void | AgentChannelDeliveryFinishEffectResult>;
+type AgentInputHook<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = (context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<void>;
+interface AgentInvocationHooks<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> {
+  "agent:finish"?: AgentFinishHook<TRuntimeConfig, CALL_OPTIONS>;
+  "agent:input"?: AgentInputHook<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+}
+type AgentHookOwner = "agent" | "capability" | "channel" | "runtime" | "integration" | (string & {});
+type AgentHookOutcome = "error" | "success";
+interface AgentHookObserverEvent {
+  durationMs: number;
+  error?: {
+    message: string;
+    name?: string;
+  };
+  ids?: Record<string, string | undefined>;
+  metadata?: Record<string, unknown>;
+  name: string;
+  outcome: AgentHookOutcome;
+  owner: AgentHookOwner;
+  phase?: string;
+}
+type AgentHookObserver = (event: Readonly<AgentHookObserverEvent>) => MaybePromise<void>;
+interface AgentHookObserverHooks {
+  "hook:observe"?: AgentHookObserver | readonly AgentHookObserver[];
+}
+interface AgentRunContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, Name extends WorkspaceName = WorkspaceName, TContextValues extends object = AgentInvocationContextValues> extends AgentCallbackContext<TRuntimeConfig> {
+  adapter?: AgentAdapter<CALL_OPTIONS>;
+  actor: AgentActor;
+  context: AgentInvocationContextStore<TContextValues>;
+  input: AgentRunInput<CALL_OPTIONS>;
+  invoker: AgentInvoker;
+  messages: Message[];
+  prompt?: string;
+  providerTools?: AgentProviderToolContribution[];
+  tools?: AgentToolSet;
+  workspace?: ReadonlyWorkspaceFacade<Name> | WritableWorkspaceFacade<Name>;
+}
+type AgentRunHandler<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = (context: AgentRunContext<TRuntimeConfig, CALL_OPTIONS, WorkspaceName, TContextValues>) => MaybePromise<Response | AgentRunResult | AsyncIterable<StreamEvent> | unknown>;
+type AgentToolResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<AgentToolSet | undefined, AgentCallbackContext<TRuntimeConfig>>;
+type AgentToolResolverWithWorkspace<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = Record<string, unknown> | undefined | ((context: AgentAdapterMetadataContext<TRuntimeConfig, Name>) => MaybePromise<unknown>);
+type AgentCapabilityMode = "read" | "write";
+type AgentDriverKind = "harness" | "model" | "run";
+interface AgentCapabilityCliStandardSchemaResultSuccess<T = unknown> {
+  issues?: undefined;
+  value: T;
+}
+interface AgentCapabilityCliStandardSchemaResultFailure {
+  issues: readonly unknown[];
+}
+interface AgentCapabilityCliStandardSchemaV1<T = unknown> {
+  "~standard": {
+    validate: (input: unknown) => AgentCapabilityCliStandardSchemaResultSuccess<T> | AgentCapabilityCliStandardSchemaResultFailure | Promise<AgentCapabilityCliStandardSchemaResultSuccess<T> | AgentCapabilityCliStandardSchemaResultFailure>;
+  };
+}
+type AgentCapabilityCliOutputFormat = "json" | "text";
+interface AgentCapabilityCliOutputDefinition<TOutput = unknown> {
+  format?: AgentCapabilityCliOutputFormat;
+  schema?: AgentCapabilityCliStandardSchemaV1<TOutput>;
+}
+interface AgentCapabilityCliRunContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TInput = unknown> {
+  argv: readonly string[];
+  context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>;
+  input: TInput;
+  json: boolean;
+}
+interface AgentCapabilityCliCommand<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TInput = unknown, TOutput = unknown> {
+  commands?: Record<string, AgentCapabilityCliCommand<TRuntimeConfig, Name>>;
+  description?: string;
+  effects?: readonly string[];
+  examples?: readonly string[];
+  input?: AgentCapabilityCliStandardSchemaV1<TInput>;
+  output?: AgentCapabilityCliStandardSchemaV1<TOutput> | AgentCapabilityCliOutputDefinition<TOutput>;
+  rest?: true;
+  run?: {
+    bivarianceHack(context: AgentCapabilityCliRunContext<TRuntimeConfig, Name, TInput>): MaybePromise<TOutput>;
+  }["bivarianceHack"];
+}
+interface AgentCapabilityCliContribution<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> {
+  commands: Record<string, AgentCapabilityCliCommand<TRuntimeConfig, Name>>;
+  description?: string;
+  name: string;
+}
+type AgentCapabilityCliResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = AgentCapabilityCliContribution<TRuntimeConfig, Name> | ((context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<AgentCapabilityCliContribution<TRuntimeConfig, Name> | undefined>);
+interface AgentCapabilityCliExecutionInput {
+  argv?: readonly string[];
+  input?: unknown;
+  json?: boolean;
+}
+interface AgentCapabilityCliExecutionResult<TOutput = unknown> {
+  argv: string[];
+  capability: string;
+  cli: string;
+  command: string;
+  durationMs: number;
+  exitCode: number;
+  json?: TOutput;
+  outputTruncated: false;
+  stderr: string;
+  stdout: string;
+}
+interface AgentCapabilityRequirement {
+  primitive?: "workspace-shell" | "blob" | "db" | "kv" | "mcp" | "sandbox" | "schedule" | "skills" | "workspace" | (string & {});
+  workspace?: {
+    mode?: AgentCapabilityMode;
+    paths?: string[];
+    required?: boolean;
+  };
+}
+interface AgentCapabilityContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends AgentAdapterMetadataContext<TRuntimeConfig, Name> {
+  abortSignal?: AbortSignal;
+  mode?: AgentCapabilityMode;
+  runtimeContext?: ResolvedAgentRuntimeContext;
+  workspaceDefinition?: WorkspaceDefinition;
+}
+type AgentCapabilityToolResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = Record<string, unknown> | ((context: AgentCapabilityContext<TRuntimeConfig, Name>) => MaybePromise<Record<string, unknown> | undefined>);
+type AgentCapabilityBashCommand = string | {
+  command: string;
+  description?: string;
+  install?: string;
+};
+interface AgentCapabilityWorkspaceContribution {
+  rules?: WorkspaceRules;
+  sources?: Record<string, WorkspaceSourceInput>;
+}
+type AgentCapabilityWorkspaceContributionResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = AgentCapabilityWorkspaceContribution | ((context: AgentCapabilityContext<TRuntimeConfig, Name>) => MaybePromise<AgentCapabilityWorkspaceContribution | false | null | undefined>);
+type AgentCapabilityPhase = "configure" | "prepare" | "bind" | "input" | "resolve" | "output" | "close";
+type AgentCapabilityHookName = `capability:${AgentCapabilityPhase}` | `capability:${AgentCapabilityPhase}:after`;
+type AgentDriverContributionKind = "Capability tools" | "provider tools";
+interface AgentDriverContribution {
+  capabilityId: string;
+  kind: AgentDriverContributionKind;
+  names?: string[];
+}
+type AgentToolTransform = (tools: AgentToolSet | undefined) => MaybePromise<AgentToolSet | undefined>;
+interface AgentProviderToolContribution {
+  args?: Record<string, unknown>;
+  id: `${string}.${string}`;
+  name: string;
+}
+type AgentOutputRenderer = (result: unknown, context: AgentCapabilityRuntimeContext) => MaybePromise<unknown>;
+type AgentOutputExtensionProvider = (event: AgentOutputExtensionEvent) => MaybePromise<unknown>;
+type AgentFinishExtensionProvider<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = {
+  bivarianceHack(event: AgentFinishEvent<TRuntimeConfig, CALL_OPTIONS>): MaybePromise<unknown>;
+}["bivarianceHack"];
+type AgentCapabilityHooks<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = Partial<Record<AgentCapabilityHookName, (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>>>;
+interface AgentCapabilityRuntimeContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends AgentCapabilityContext<TRuntimeConfig, Name> {
+  capability: AgentCapabilityDefinition<TRuntimeConfig, Name>;
+  input: {
+    get: () => AgentRunInput;
+    messages: () => Message[];
+    set: (input: AgentRunInput) => void;
+    setMessages: (messages: Message[]) => void;
+  };
+  delivery: {
+    effect: (intent: AgentChannelDeliveryEffectIntent) => void;
+    finishEffect: (effect: AgentChannelDeliveryFinishEffect) => void;
+  };
+  model: {
+    resolve: (model?: AgentModelResolver<TRuntimeConfig, Name>) => Promise<AgentModelInput>;
+  };
+  modelExecution: {
+    instrument: (instrumentation: AgentModelExecutionInstrumentation<TRuntimeConfig>) => void;
+  };
+  output: {
+    extensions: AgentOutputExtensions;
+    final: (renderer: AgentOutputRenderer, options?: {
+      order?: "last";
+    }) => void;
+    provide: (value: unknown | AgentOutputExtensionProvider) => void;
+    render: (renderer: AgentOutputRenderer) => void;
+  };
+  providerTools: {
+    add: (tool: AgentProviderToolContribution) => void;
+  };
+  finish: {
+    provide: (value: unknown | AgentFinishExtensionProvider) => void;
+  };
+  state: {
+    require: (name: string, options?: {
+      optional?: boolean;
+    }) => void;
+  };
+  tools: {
+    add: (tools: AgentToolSet | undefined) => void;
+    transform: (transform: AgentToolTransform) => void;
+  };
+}
+interface AgentCapabilityTypeContract {
+  inputContext?: object;
+  invocationContext?: object;
+  workspaceSources?: string;
+}
+interface AgentCapabilityDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, TTypeContract extends AgentCapabilityTypeContract = AgentCapabilityTypeContract> {
+  readonly __vitehubTypeContract?: TTypeContract;
+  bash?: readonly AgentCapabilityBashCommand[];
+  bind?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  capabilities?: readonly AgentCapabilityDefinition<TRuntimeConfig, Name>[];
+  cli?: AgentCapabilityCliResolver<TRuntimeConfig, Name>;
+  close?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  configure?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  finish?: AgentFinishExtensionProvider<TRuntimeConfig>;
+  hooks?: AgentCapabilityHooks<TRuntimeConfig, Name>;
+  id: string;
+  input?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<Response | void>;
+  metadata?: Record<string, unknown>;
+  mode?: AgentCapabilityMode;
+  output?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  prepare?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  requires?: AgentCapabilityRequirement[];
+  resolve?: (context: AgentCapabilityRuntimeContext<TRuntimeConfig, Name>) => MaybePromise<void>;
+  tools?: AgentCapabilityToolResolver<TRuntimeConfig, Name>;
+  triggers?: Record<string, AgentTriggerDefinition<TRuntimeConfig, Name, any, any>>;
+  workspaceSources?: WorkspaceDefinition["sources"];
+  workspace?: AgentCapabilityWorkspaceContributionResolver<TRuntimeConfig, Name>;
+}
+type AgentCapabilityInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = AgentCapabilityDefinition<TRuntimeConfig, Name>;
+type AgentCapabilitiesList<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = readonly AgentCapabilityInput<TRuntimeConfig, Name>[];
+interface AgentCapabilitiesResolverContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> extends AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS>, AgentInvocationCallbackContextValues {
+  driver: {
+    kind: AgentDriverKind;
+  };
+}
+type AgentCapabilitiesResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown, TCapabilities extends AgentCapabilitiesList<TRuntimeConfig, Name> = AgentCapabilitiesList<TRuntimeConfig, Name>> = (context: AgentCapabilitiesResolverContext<TRuntimeConfig, CALL_OPTIONS>) => MaybePromise<TCapabilities>;
+type AgentCapabilitiesInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown> = AgentCapabilitiesList<TRuntimeConfig, Name> | AgentCapabilitiesResolver<TRuntimeConfig, Name, CALL_OPTIONS>;
+type AgentAdapterInstructionsValue = string | string[];
+type AgentAdapterInstructions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = AgentAdapterInstructionsPart<TRuntimeConfig, Name> | Array<AgentAdapterInstructionsPart<TRuntimeConfig, Name>>;
+type AgentAdapterInstructionsPart<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = AgentAdapterInstructionsValue | ((context: AgentAdapterMetadataContext<TRuntimeConfig, Name>) => MaybePromise<AgentAdapterInstructionsValue | undefined>);
+type AgentModelInput = unknown;
+type AgentModelResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> = MaybeResolvable<AgentModelInput, AgentAdapterMetadataContext<TRuntimeConfig, Name>>;
+interface AgentModelInstrumentationContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  actor: AgentActor;
+  context: AgentInvocationContextStore;
+  invoker: AgentInvoker;
+  model: AgentModelInput;
+  run?: AgentRunMetadata;
+}
+type AgentModelInstrumentation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = (context: AgentModelInstrumentationContext<TRuntimeConfig>) => MaybePromise<AgentModelInput>;
+interface AgentCallSettingsInstrumentationContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> extends AgentCallbackContext<TRuntimeConfig> {
+  actor: AgentActor;
+  callSettings: Readonly<Record<string, unknown>>;
+  context: AgentInvocationContextStore;
+  input: AgentRunInput<CALL_OPTIONS>;
+  invoker: AgentInvoker;
+  model: AgentModelInput;
+  run?: AgentRunMetadata;
+  tools?: AgentToolSet;
+}
+type AgentCallSettingsInstrumentation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = (context: AgentCallSettingsInstrumentationContext<TRuntimeConfig, CALL_OPTIONS>) => MaybePromise<Record<string, unknown> | void>;
+interface AgentModelExecutionInstrumentation<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> {
+  callSettings?: AgentCallSettingsInstrumentation<TRuntimeConfig, CALL_OPTIONS>;
+  model?: AgentModelInstrumentation<TRuntimeConfig>;
+}
+interface AgentAttachmentExecutionOptions {
+  maxBytes?: number;
+}
+interface AgentModelExecutionOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> {
+  attachments?: AgentAttachmentExecutionOptions;
+  callSettings?: Record<string, unknown>;
+  instrumentation?: AgentModelExecutionInstrumentation<TRuntimeConfig, CALL_OPTIONS>;
+  stepLimit?: number;
+  workspaceFallback?: boolean | {
+    enabled?: boolean;
+    maxToolResults?: number;
+  };
+}
+interface AgentHarnessCredentialSource {
+  label?: string;
+  source?: "ambient" | "explicit" | "none" | "unknown" | (string & {});
+}
+interface CodexAuthOptions {
+  gateway?: {
+    apiKey?: string;
+    baseUrl?: string;
+  };
+  openai?: {
+    apiKey?: string;
+    baseUrl?: string;
+    organization?: string;
+    project?: string;
+  };
+  openaiCompatible?: {
+    apiKey?: string;
+    baseUrl?: string;
+    modelProviderName?: string;
+    queryParamsJson?: string;
+  };
+}
+type CodexDriverSandboxOptions<CALL_OPTIONS = unknown> = false | LocalHarnessSandboxOptions | AgentHarnessSandboxProviderInput<AgentRuntimeConfig, CALL_OPTIONS>;
+interface CodexDriverOptions<CALL_OPTIONS = unknown, TOutput = unknown> {
+  auth?: CodexAuthOptions;
+  credentials?: AgentHarnessCredentialSource;
+  env?: Record<string, string | undefined>;
+  instructions?: AgentHarnessInstructions<AgentRuntimeConfig, CALL_OPTIONS>;
+  model?: string;
+  output?: AgentOutputDefinition<TOutput>;
+  port?: number;
+  reasoningEffort?: "low" | "medium" | "high";
+  sandbox?: CodexDriverSandboxOptions<CALL_OPTIONS>;
+  startupTimeoutMs?: number;
+  webSearch?: boolean;
+  workDir?: AgentHarnessWorkDir<AgentRuntimeConfig, CALL_OPTIONS>;
+}
+interface ClaudeCodeAuthOptions {
+  anthropic?: {
+    apiKey?: string;
+    authToken?: string;
+    baseUrl?: string;
+  };
+  gateway?: {
+    apiKey?: string;
+    baseUrl?: string;
+  };
+}
+type ClaudeCodeThinkingConfig = {
+  display?: "summarized" | "omitted";
+  type: "adaptive" | "enabled";
+} | {
+  type: "disabled";
+};
+interface ClaudeCodeDriverOptions<TOutput = unknown> {
+  auth?: ClaudeCodeAuthOptions;
+  credentials?: AgentHarnessCredentialSource;
+  env?: Record<string, string | undefined>;
+  maxTurns?: number;
+  model?: string;
+  output?: AgentOutputDefinition<TOutput>;
+  port?: number;
+  sandbox?: false | LocalHarnessSandboxOptions;
+  startupTimeoutMs?: number;
+  thinking?: ClaudeCodeThinkingConfig;
+}
+type BuiltInAgentDriverName = "claude-code" | "codex";
+type BuiltInAgentDriver<CALL_OPTIONS = unknown, TOutput = unknown> = BuiltInAgentDriverName | ({
+  kind: "codex";
+} & CodexDriverOptions<CALL_OPTIONS, TOutput>) | ({
+  kind: "claude-code";
+} & ClaudeCodeDriverOptions<TOutput>);
+type AgentHarnessSessionKey<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = string | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<string | undefined>);
+type AgentHarnessInstructions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = string | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<string | undefined>);
+type AgentHarnessWorkDir<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = string | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<string | undefined>);
+type AgentHarnessDriverInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown> = object | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS>) => MaybePromise<object>);
+type AgentHarnessSandboxProvider = object;
+type AgentHarnessSandboxProviderInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = AgentHarnessSandboxProvider | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<AgentHarnessSandboxProvider | undefined>);
+interface AgentModelDriver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TOutput = unknown> {
+  credentials?: never;
+  execution?: AgentModelExecutionOptions<TRuntimeConfig, CALL_OPTIONS>;
+  harness?: never;
+  instructions?: AgentAdapterInstructions<TRuntimeConfig>;
+  kind?: never;
+  model: AgentModelResolver<TRuntimeConfig>;
+  output?: AgentOutputDefinition<TOutput>;
+  permissionMode?: never;
+  permissions?: never;
+  run?: never;
+  sandbox?: never;
+  sessionKey?: never;
+  workDir?: never;
+}
+interface AgentHarnessDriver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues, TOutput = unknown> {
+  credentials?: AgentHarnessCredentialSource;
+  execution?: never;
+  harness: AgentHarnessDriverInput<TRuntimeConfig, CALL_OPTIONS>;
+  instructions?: AgentHarnessInstructions<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  kind?: never;
+  model?: never;
+  output?: AgentOutputDefinition<TOutput>;
+  permissionMode?: never;
+  permissions?: never;
+  requires?: readonly BoxRequirement[];
+  run?: never;
+  sandbox?: AgentHarnessSandboxProviderInput<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  sessionKey?: AgentHarnessSessionKey<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  workDir?: AgentHarnessWorkDir<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+}
+interface AgentRunDriver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues, TOutput = unknown> {
+  credentials?: never;
+  execution?: never;
+  harness?: never;
+  instructions?: never;
+  kind?: never;
+  model?: never;
+  output?: AgentOutputDefinition<TOutput>;
+  permissionMode?: never;
+  permissions?: never;
+  run: AgentRunHandler<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  sandbox?: never;
+  sessionKey?: never;
+  workDir?: never;
+}
+type AgentDriver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues, TOutput = unknown> = AgentModelDriver<TRuntimeConfig, CALL_OPTIONS, TOutput> | AgentHarnessDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput> | AgentRunDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput> | BuiltInAgentDriver<CALL_OPTIONS, TOutput>;
+type CustomAgentDriver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues, TOutput = unknown> = AgentModelDriver<TRuntimeConfig, CALL_OPTIONS, TOutput> | AgentHarnessDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput> | AgentRunDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput>;
+interface AgentDefinitionCliOptions {
+  capabilities?: boolean;
+}
+interface AgentOutputDefinition<TOutput = unknown> {
+  schema: StandardSchemaV1<unknown, TOutput>;
+}
+interface AgentUIMessageStreamProjection {
+  reasoning?: "hidden" | "visible";
+  tools?: "hidden" | "full";
+}
+type AgentUIMessageStreamProjectionResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TContextValues extends object = AgentInvocationContextValues> = AgentUIMessageStreamProjection | ((context: AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>) => MaybePromise<AgentUIMessageStreamProjection>);
+type AgentSharedSettings<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues, TCapabilities extends AgentCapabilitiesInput<TRuntimeConfig, WorkspaceName, CALL_OPTIONS> | undefined = AgentCapabilitiesInput<TRuntimeConfig, WorkspaceName, CALL_OPTIONS> | undefined> = {
+  box?: BoxDefinition<AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>>;
+  capabilities?: TCapabilities;
+  channels?: AgentChannelInputs<TRuntimeConfig>;
+  cli?: AgentDefinitionCliOptions;
+  description?: string;
+  hooks?: AgentCapabilityHooks<TRuntimeConfig> & AgentHookObserverHooks & AgentInvocationHooks<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  invoker?: AgentInvokerOptions<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, TContextValues>;
+  messages?: AgentMessageChannelSettings<TRuntimeConfig>;
+  name?: string;
+  runtime?: AgentRuntimeBinding;
+  runEvents?: AgentRunEvents;
+  uiMessageStream?: AgentUIMessageStreamProjectionResolver<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  version?: string;
+  workspace?: WorkspaceAgentWorkspaceConfig;
+};
+type AgentSettings<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues, TCapabilities extends AgentCapabilitiesInput<TRuntimeConfig, WorkspaceName, CALL_OPTIONS> | undefined = AgentCapabilitiesInput<TRuntimeConfig, WorkspaceName, CALL_OPTIONS> | undefined, TOutput = unknown, TDriver extends AgentDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput> = AgentDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput>> = AgentSharedSettings<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, TContextValues, TCapabilities> & {
+  driver: TDriver;
+};
+declare const agentOutputType: unique symbol;
+interface AgentDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, CALL_OPTIONS = unknown, TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues, TOutput = unknown> {
+  [agentOutputType]?: TOutput;
+  box?: BoxDefinition<AgentRunCallbackContext<TRuntimeConfig, CALL_OPTIONS, TContextValues>>;
+  capabilities?: AgentCapabilityDefinition<TRuntimeConfig>[];
+  channels?: AgentChannels<TRuntimeConfig>;
+  chat?: AgentChatOptions<TRuntimeConfig>;
+  cli?: AgentDefinitionCliOptions;
+  description?: string;
+  hooks?: AgentCapabilityHooks<TRuntimeConfig, WorkspaceName> & AgentHookObserverHooks & AgentInvocationHooks<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  invoker?: AgentInvokerOptions<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, TContextValues>;
+  messages?: AgentMessageChannelSettings<TRuntimeConfig>;
+  name?: string;
+  runtime?: AgentRuntimeBinding;
+  runEvents?: AgentRunEvents;
+  resolve(context: AgentRuntimeContext<TRuntimeConfig>): Promise<AgentAdapter<CALL_OPTIONS>>;
+  run?(context: AgentRunContext<TRuntimeConfig, CALL_OPTIONS, WorkspaceName, TContextValues>): MaybePromise<Response | AgentRunResult | AsyncIterable<StreamEvent> | unknown>;
+  uiMessageStream?: AgentUIMessageStreamProjectionResolver<TRuntimeConfig, CALL_OPTIONS, TContextValues>;
+  version?: string;
+  workspace?: WorkspaceAgentWorkspaceConfig;
+}
+type AgentInput<TContext extends AgentRuntimeContext<any> = AgentRuntimeContext, TOutput = unknown> = AgentDefinition<TContext extends AgentRuntimeContext<infer TRuntimeConfig> ? TRuntimeConfig : AgentRuntimeConfig, any, any, any, TOutput>;
+type AgentRegistryModule<TContext extends AgentRuntimeContext<any> = AgentRuntimeContext> = {
+  default?: AgentInput<TContext>;
+} | AgentInput<TContext>;
+type AgentRegistry<TContext extends AgentRuntimeContext<any> = AgentRuntimeContext> = Record<string, () => MaybePromise<AgentRegistryModule<TContext>>>;
+interface AgentStateProviderOptions {
+  authToken?: string;
+  provider?: "auto" | "cloudflare" | "cloudflare-agents" | "libsql" | "memory" | "sqlite" | (string & {});
+  tablePrefix?: string;
+  url?: string;
+}
+type ResolvedAgentStateProviderOptions = Omit<AgentStateProviderOptions, "provider"> & {
+  provider: NonNullable<AgentStateProviderOptions["provider"]>;
+};
+interface AgentSchedulerProviderOptions {
+  provider?: "auto" | "cloudflare-agents" | "memory" | (string & {});
+}
+interface AgentSandboxProviderOptions {
+  provider?: "auto" | "cloudflare" | "vercel" | (string & {});
+}
+interface AgentIntegrationsOptions {
+  sandbox?: AgentIntegrationOption;
+  workflow?: AgentIntegrationOption;
+}
+interface AgentProvidersOptions {
+  sandbox?: AgentSandboxProviderOptions;
+  scheduler?: AgentSchedulerProviderOptions;
+  state?: AgentStateProviderOptions;
+}
+interface AgentEvalOptions {
+  cache?: boolean;
+  forceRerunTriggers?: string[];
+  hideTable?: boolean;
+  maxConcurrency?: number;
+  scoreThreshold?: number;
+  server?: {
+    port?: number;
+  };
+  setupFiles?: string[];
+  testTimeout?: number;
+  trialCount?: number;
+}
+type AgentCliOptions = Record<never, never>;
+type AgentRouteOption = boolean | string;
+interface AgentRoutesOptions {
+  chat?: AgentRouteOption;
+  discordGateway?: AgentRouteOption;
+}
+interface ResolvedAgentRoutesOptions {
+  chat: false | string;
+  discordGateway: false | string;
+  webhooks: string;
+}
+interface AgentModuleOptions {
+  cli?: false | AgentCliOptions;
+  execution?: AgentExecution;
+  eval?: AgentEvalOptions;
+  imports?: boolean;
+  integrations?: AgentIntegrationsOptions;
+  providers?: AgentProvidersOptions;
+  routes?: AgentRoutesOptions;
+  runtime?: AgentRuntime;
+}
+interface ResolvedAgentModuleOptions {
+  execution: AgentExecution;
+  imports: boolean;
+  integrations: Required<AgentIntegrationsOptions>;
+  providers: {
+    sandbox: Required<AgentSandboxProviderOptions>;
+    scheduler: Required<AgentSchedulerProviderOptions>;
+    state: ResolvedAgentStateProviderOptions;
+  };
+  routes: ResolvedAgentRoutesOptions;
+  runtime: AgentRuntime;
+}
+interface AgentRegistryHandlerOptions<TRuntimeContext extends AgentRuntimeContext = AgentRuntimeContext> {
+  agentParam?: string;
+  lifecycleHooks?: AgentRuntimeHooks<TRuntimeContext>;
+}
+interface AgentRuntimeHooks<TContext extends AgentRuntimeContext<any> = AgentRuntimeContext> {
+  error?: (error: unknown, context: TContext) => MaybePromise<void>;
+  request?: (context: TContext) => MaybePromise<void>;
+  resolved?: (context: TContext & {
+    agent: AgentAdapter;
+  }) => MaybePromise<void>;
+}
+interface DiscoveredAgentDefinition {
+  exportName?: string;
+  handler: string;
+  name: string;
+  source?: "server-agent" | "server-agent-workspace" | "server-agents" | "vite-suffix";
+  workspace?: string;
+}
+interface AgentToolStepItem {
+  id?: string;
+  input?: unknown;
+  name?: string;
+  output?: unknown;
+  toolCallId?: string;
+  toolName?: string;
+}
+interface AgentToolStep {
+  text?: string;
+  toolCalls?: AgentToolStepItem[];
+  toolErrors?: AgentToolStepItem[];
+  toolResults?: AgentToolStepItem[];
+}
+interface AgentChatAgentBindingOptions {
+  event?: "directMessage";
+}
+type AgentChatTriggerHistory = "none" | {
+  maxMessages?: number;
+  source: "thread";
+};
+interface AgentChatSessionOptions {
+  idleTimeoutMs?: number;
+  metadataKey?: string;
+  strategy?: "manual" | "idle-timeout" | "hybrid";
+}
+interface AgentChatMessageHookArgs {
+  id?: string;
+  metadata?: Record<string, unknown>;
+  text: string;
+}
+interface AgentChatAgentHookArgs<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends Record<string, unknown> {
+  history: Message[];
+  message: AgentChatMessageHookArgs;
+  run?: AgentRunMetadata;
+  session?: {
+    action?: "continue" | "new" | "switch";
+    id?: string;
+  };
+  thread: {
+    post: (message: unknown) => MaybePromise<unknown>;
+  };
+}
+interface AgentChatErrorHookArgs<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentChatAgentHookArgs<TRuntimeConfig> {
+  error: unknown;
+}
+interface AgentChatEventHookArgs<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends Record<string, unknown> {}
+interface AgentChatEventHooks<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends Record<string, unknown> {
+  onDirectMessage?: (args: {
+    message: {
+      text: string;
+    };
+  } & AgentChatEventHookArgs<TRuntimeConfig>) => MaybePromise<void>;
+}
+type AgentChatVersionBoundAdapterMethod = "fetchChannelMessages" | "fetchMessage" | "fetchMessages" | "initialize" | "listThreads" | "parseMessage";
+type AgentChatPlatformAdapter = Omit<Adapter, AgentChatVersionBoundAdapterMethod> & {
+  fetchChannelMessages?: (...args: Parameters<NonNullable<Adapter["fetchChannelMessages"]>>) => Promise<unknown>;
+  fetchMessage?: (...args: Parameters<NonNullable<Adapter["fetchMessage"]>>) => Promise<unknown>;
+  fetchMessages: (...args: Parameters<Adapter["fetchMessages"]>) => Promise<unknown>;
+  initialize: (chat: never) => Promise<void>;
+  listThreads?: (...args: Parameters<NonNullable<Adapter["listThreads"]>>) => Promise<unknown>;
+  parseMessage: (raw: never) => unknown;
+};
+type AgentChatPlatformResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<AgentChatPlatformAdapter, AgentCallbackContext<TRuntimeConfig>>;
+type AgentChatPlatformsResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<Record<string, AgentChatPlatformResolver<TRuntimeConfig>>, AgentCallbackContext<TRuntimeConfig>>;
+interface AgentChatStateContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  chat: {
+    agentName: string;
+    stateKeyPrefix: string;
+  };
+}
+type AgentChatStateResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<StateAdapter, AgentChatStateContext<TRuntimeConfig>>;
+interface AgentWebhookStateContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  webhook: {
+    agentName: string;
+    channelId?: string;
+    provider: string;
+    stateKeyPrefix: string;
+  };
+}
+type AgentWebhookStateResolver<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = MaybeResolvable<StateAdapter, AgentWebhookStateContext<TRuntimeConfig>>;
+type AgentChatMessage = AdapterPostableMessage | AgentChannelDeliveryReplyStream | {
+  text: string;
+} | ((Exclude<AdapterPostableMessage, string> | {
+  text: string;
+}) & {
+  artifacts?: readonly PublishedAgentDeliveryArtifact[];
+});
+type AgentChatSendMessage = (message: AgentChatMessage) => Promise<void>;
+type AgentMessageConcurrency = "drop" | "parallel" | "queue" | "reject" | (string & {});
+type AgentMessageDeliveryKind = "direct" | "mention" | "subscribed";
+type AgentMessageLockScope = "agent" | "channel" | "thread" | (string & {});
+interface AgentMessageFilterContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentCallbackContext<TRuntimeConfig> {
+  deliveryKind: AgentMessageDeliveryKind;
+  message: Message;
+  thread: {
+    post: AgentChatSendMessage;
+  };
+}
+type AgentMessageFilter<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = (context: AgentMessageFilterContext<TRuntimeConfig>) => MaybePromise<boolean>;
+interface AgentChatFinishExtension {
+  provider?: string;
+  run?: Partial<AgentRunMetadata>;
+  sendMessage: AgentChatSendMessage;
+}
+interface AgentMessageChannelSettings<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> {
+  commentary?: "hidden" | "message";
+  concurrency?: AgentMessageConcurrency;
+  dedupeTtlMs?: number;
+  delivery?: "automatic" | "manual";
+  errorFallbackText?: string | null | ((context: AgentChatErrorHookArgs<TRuntimeConfig>) => MaybePromise<string | null | undefined>);
+  fallbackStreamingPlaceholderText?: string | readonly string[] | null | ((context: AgentChatAgentHookArgs<TRuntimeConfig>) => MaybePromise<string | null | undefined>);
+  filter?: AgentMessageFilter<TRuntimeConfig>;
+  identity?: IdentityResolver;
+  lockScope?: AgentMessageLockScope;
+  messageHistory?: unknown;
+  sessions?: boolean | AgentChatSessionOptions;
+  state?: AgentChatStateResolver<TRuntimeConfig>;
+  stream?: boolean;
+  streamingUpdateIntervalMs?: number;
+  threadHistory?: unknown;
+  timeout?: number;
+  transcripts?: TranscriptsConfig;
+  triggerHistory?: AgentChatTriggerHistory;
+  userName?: string;
+  [key: string]: unknown;
+}
+interface AgentChannelDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> {
+  adapter?: AgentChatPlatformResolver<TRuntimeConfig>;
+  capabilities?: readonly AgentCapabilityDefinition<TRuntimeConfig>[];
+  effects?: AgentChannelDeliveryEffects<TRuntimeConfig>;
+  identity?: IdentityResolver;
+  kind: string;
+  listener?: {
+    kind: "telegram-polling";
+  };
+  messages?: false | AgentMessageChannelSettings<TRuntimeConfig>;
+  route?: unknown;
+  triggers?: Record<string, AgentTriggerDefinition<TRuntimeConfig, WorkspaceName, any, any, AgentChannelTriggerContext<TRuntimeConfig>>>;
+  webhookManager?: AgentChannelWebhookManagerResolver<TRuntimeConfig>;
+  webhooks?: boolean | AgentChannelWebhookRegistrationDefinition<TRuntimeConfig> | AgentChannelWebhookRegistrationDefinition<TRuntimeConfig>[];
+}
+type AgentChannelFactory<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = () => AgentChannelDefinition<TRuntimeConfig>;
+type AgentChannelInput<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = AgentChannelDefinition<TRuntimeConfig> | AgentChannelFactory<TRuntimeConfig>;
+type AgentChannelInputs<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = Record<string, AgentChannelInput<TRuntimeConfig>>;
+type AgentChannels<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> = Record<string, AgentChannelDefinition<TRuntimeConfig>>;
+interface AgentChatBaseOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentMessageChannelSettings<TRuntimeConfig> {
+  adapters?: never;
+  agent?: never;
+  event?: AgentChatAgentBindingOptions["event"];
+  execution?: never;
+  hooks?: AgentChatEventHooks<TRuntimeConfig>;
+  identity?: IdentityResolver;
+  lifecycleHooks?: Record<string, unknown>;
+  webhooks?: Record<string, false | AgentChannelWebhookRegistrationDefinition<TRuntimeConfig> | AgentChannelWebhookRegistrationDefinition<TRuntimeConfig>[]>;
+  workflow?: never;
+  [key: string]: unknown;
+}
+interface AgentChatCapabilityOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentChatBaseOptions<TRuntimeConfig> {
+  platforms?: never;
+  webhooks?: never;
+}
+interface AgentChatOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig> extends AgentChatBaseOptions<TRuntimeConfig> {
+  platforms?: AgentChatPlatformsResolver<TRuntimeConfig>;
+}
+type AgentToolPolicyDecision = "allow" | "deny" | "require-approval" | "retryable-failure";
+interface AgentToolPolicyContext {
+  input?: unknown;
+  name: string;
+}
+type AgentToolStandardSchema<T = unknown> = StandardSchemaV1<unknown, T> & StandardJSONSchemaV1<unknown, T>;
+type AgentToolSchema<T = unknown> = AgentToolStandardSchema<T> | (JSONSchema7 & {
+  "~standard"?: never;
+});
+interface AgentToolExecutionContext {
+  abortSignal?: AbortSignal;
+}
+interface AgentToolDefinition<TInput = unknown, TOutput = unknown> {
+  description?: string;
+  execute?: (input: TInput, context?: AgentToolExecutionContext) => MaybePromise<TOutput>;
+  inputSchema?: AgentToolSchema<TInput>;
+  metadata?: Record<string, unknown>;
+  name: string;
+  outputSchema?: AgentToolSchema<TOutput>;
+  policy?: AgentToolPolicyDecision | ((context: AgentToolPolicyContext) => MaybePromise<AgentToolPolicyDecision>);
+}
+type AgentToolSet = Record<string, AgentToolDefinition>;
+interface WorkspaceAgentWorkspaceOptions extends Omit<WorkspaceDefinitionInput, "name"> {
+  commit?: boolean | string;
+  mode?: AgentCapabilityMode;
+  name?: never;
+}
+type WorkspaceAgentWorkspaceReference<Name extends WorkspaceName = WorkspaceName> = {
+  mode?: AgentCapabilityMode;
+  name: Name;
+} & { [Key in keyof Omit<WorkspaceAgentWorkspaceOptions, "mode" | "name">]?: never };
+type WorkspaceAgentWorkspaceConfig<Name extends WorkspaceName = WorkspaceName> = Name | WorkspaceAgentWorkspaceReference<Name> | WorkspaceAgentWorkspaceOptions;
+interface AgentInspectionFileTreeItem {
+  children?: AgentInspectionFileTreeItem[];
+  kind: "directory" | "file";
+  label?: string;
+  materialize?: "build" | "lazy";
+  materialized?: boolean;
+  materializedAt?: string;
+  path: string;
+  source?: string;
+  status?: "lazy" | "updating" | "ready" | "error";
+  updatedAt?: string;
+}
+interface AgentInspectionToolDefinition {
+  category?: string;
+  commands?: string[];
+  description?: string;
+  icon?: string;
+  name: string;
+  preset?: string;
+  status?: "available" | "disabled";
+}
+type AgentInspectionConfigValue = boolean | null | number | string;
+interface AgentInspectionModelMetadata {
+  dynamic?: boolean;
+  id?: string;
+  provider?: string;
+}
+interface AgentInspectionModelExecutionMetadata {
+  callSettings?: Record<string, AgentInspectionConfigValue>;
+  stepLimit?: number;
+  workspaceFallback?: {
+    enabled?: boolean;
+    maxToolResults?: number;
+  };
+}
+interface AgentInspectionHarnessMetadata {
+  credentials?: AgentHarnessCredentialSource;
+  provider?: string;
+  sandboxProvider?: string;
+  sessionKey?: boolean;
+}
+interface AgentInspectionDriverMetadata {
+  readonly executionAuthority: ExecutionAuthority;
+  execution?: AgentInspectionModelExecutionMetadata;
+  harness?: AgentInspectionHarnessMetadata;
+  kind: "harness" | "model" | "run" | "unknown";
+  model?: AgentInspectionModelMetadata;
+}
+interface AgentInspectionConfigMetadata {
+  driver: AgentInspectionDriverMetadata;
+  uiMessageStream?: AgentUIMessageStreamProjection;
+}
+interface AgentInspectionMetadata {
+  config?: AgentInspectionConfigMetadata;
+  files?: AgentInspectionFileTreeItem[];
+  instructions?: string[];
+  invokerProfiles?: AgentInvokerProfile[];
+  name?: string;
+  tools?: AgentInspectionToolDefinition[];
+  version?: string;
+  warnings?: AgentInspectionWarning[];
+}
+interface AgentInspectionWarning {
+  id: string;
+  kind: "instruction-coverage";
+  message: string;
+  primitive: "capability" | "skill" | "source";
+  severity: "warning";
+}
+interface AgentAdapterResult {
+  finishReason?: unknown;
+  raw?: unknown;
+  text?: string;
+  usage?: unknown;
+  usageRecord?: AgentUsageRecord;
+  warnings?: unknown;
+}
+interface AgentUsage {
+  details?: Record<string, unknown>;
+  inputTokenDetails?: Record<string, number>;
+  inputTokens?: number;
+  outputTokenDetails?: Record<string, number>;
+  outputTokens?: number;
+  raw?: unknown;
+  totalTokens?: number;
+}
+interface AgentUsageCost {
+  amount: string;
+  currency: "USD" | (string & {});
+  estimated: boolean;
+  formatted?: string;
+  source: "custom" | "estimated" | "provider" | "vercel-ai-gateway" | (string & {});
+}
+interface AgentUsageCredentialSource {
+  label?: string;
+  source?: AgentHarnessCredentialSource["source"];
+}
+interface AgentUsageRecord {
+  cost?: AgentUsageCost;
+  credentialSource?: AgentUsageCredentialSource;
+  latency?: {
+    durationMs?: number;
+    timeToFirstTokenMs?: number;
+    tokensPerSecond?: number;
+  };
+  model?: {
+    id?: string;
+    provider?: string;
+  };
+  raw?: unknown;
+  response?: {
+    finishReason?: unknown;
+    id?: string;
+    timestamp?: Date | string;
+  };
+  run?: Partial<AgentRunMetadata>;
+  usage?: AgentUsage;
+}
+interface AgentAdapterMetadataContext<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends AgentCallbackContext<TRuntimeConfig>, AgentInvocationCallbackContextValues {
+  actor: AgentActor;
+  context: AgentInvocationContextStore;
+  driver?: {
+    kind: AgentDriverKind;
+  };
+  fs: ReadonlyWorkspaceFacade<Name>["fs"];
+  invoker: AgentInvoker;
+  workspace: ReadonlyWorkspaceFacade<Name>;
+}
+interface AgentGlobalSkill {
+  path: string;
+  source: WorkspaceSourceInput;
+  sourceKey: string;
+}
+interface AgentAdapterRunContext<TOptions = unknown, TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> {
+  actor: AgentActor;
+  box?: Box;
+  close?: () => Promise<void>;
+  context: AgentInvocationContextStore;
+  toolStepReporter?: AgentRuntimeContext<TRuntimeConfig>["toolStepReporter"];
+  driverContributions?: AgentDriverContribution[];
+  globalSkills?: readonly AgentGlobalSkill[];
+  hasCapabilityCleanup?: boolean;
+  harnessSandboxProvider?: object;
+  harnessWorkDir?: string;
+  input: AgentRunInput<TOptions>;
+  instructions?: string;
+  invoker: AgentInvoker;
+  messages: Message[];
+  modelExecutionInstrumentation?: AgentModelExecutionInstrumentation[];
+  output?: AgentOutputDefinition;
+  outputRenderers?: Array<(result: unknown) => MaybePromise<unknown>>;
+  prompt?: string;
+  providerTools?: AgentProviderToolContribution[];
+  runtime: ResolvedAgentRuntimeContext<TRuntimeConfig>;
+  tools?: AgentToolSet;
+  workspace?: ReadonlyWorkspaceFacade<Name>;
+  workspaceMaterializationSource?: ReadonlyWorkspaceFacade<Name>;
+  workspaceAutoCommit?: boolean | string;
+  workspaceDefinition?: WorkspaceDefinition;
+  workspaceInstructionBindings?: Record<string, unknown>;
+}
+interface AgentAdapter<TOptions = unknown, TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> {
+  generate(context: AgentAdapterRunContext<TOptions, TRuntimeConfig, Name>): MaybePromise<AgentAdapterResult | Response | AsyncIterable<StreamEvent> | unknown>;
+  metadata?(context: AgentAdapterMetadataContext<TRuntimeConfig, Name>): MaybePromise<AgentInspectionMetadata | undefined>;
+  name: string;
+  stream?(context: AgentAdapterRunContext<TOptions, TRuntimeConfig, Name>): MaybePromise<Response | AsyncIterable<StreamEvent> | AgentAdapterResult | unknown>;
+}
+type AgentAdapterFactory<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, TOptions = unknown, Name extends WorkspaceName = WorkspaceName> = (context: ResolvedAgentRuntimeContext<TRuntimeConfig>) => MaybePromise<AgentAdapter<TOptions, TRuntimeConfig, Name>>;
+//#endregion
+export { AgentChannelDeliveryFinishEffectResult as $, isAttachmentPart as $i, AgentRegistryModule as $n, MaybePromise as $r, AgentHarnessSandboxProviderInput as $t, AgentCapabilityCliStandardSchemaResultSuccess as A, FilePart as Ai, AgentMessageChannelSettings as An, AgentTriggerInvokeResult as Ar, AgentChatStateContext as At, AgentCapabilityRuntimeContext as B, TextPart as Bi, AgentModelInstrumentation as Bn, AgentWebhookRegistrationDefinition as Br, AgentDriverKind as Bt, AgentCapabilityCliExecutionInput as C, AttachmentData as Ci, AgentInvocationExtensions as Cn, AgentToolSchema as Cr, AgentChatMessageHookArgs as Ct, AgentCapabilityCliResolver as D, CreateMessageOptions as Di, AgentInvokerOptions as Dn, AgentToolTransform as Dr, AgentChatPlatformsResolver as Dt, AgentCapabilityCliOutputFormat as E, AudioPart as Ei, AgentInvokerMeta as En, AgentToolStep as Er, AgentChatPlatformResolver as Et, AgentCapabilityHookName as F, MessageRole as Fi, AgentMessageLockScope as Fn, AgentUsageCost as Fr, AgentDeliveryArtifact as Ft, AgentChannelDeliveryEffectIntent as G, appendMessageText as Gi, AgentOutputExtensionEvent as Gn, ClaudeCodeAuthOptions as Gr, AgentFinishExtensions as Gt, AgentChannelDefinition as H, ToolInvocation as Hi, AgentModelResolver as Hn, AgentWorkflowRuntimeBinding as Hr, AgentExecution as Ht, AgentCapabilityHooks as I, RunEvent as Ii, AgentModelDriver as In, AgentUsageCredentialSource as Ir, AgentDeliveryArtifactPlacement as It, AgentChannelDeliveryEffectPayload as J, createMessage as Ji, AgentOutputExtensions as Jn, CodexAuthOptions as Jr, AgentHarnessCredentialSource as Jt, AgentChannelDeliveryEffectIntentOptions as K, applyStreamEvent as Ki, AgentOutputExtensionProvider as Kn, ClaudeCodeDriverOptions as Kr, AgentFinishHook as Kt, AgentCapabilityInput as L, SerializedMessages as Li, AgentModelExecutionInstrumentation as Ln, AgentUsageRecord as Lr, AgentDriver as Lt, AgentCapabilityContext as M, Message as Mi, AgentMessageDeliveryKind as Mn, AgentUIMessageStreamProjection as Mr, AgentChatTriggerHistory as Mt, AgentCapabilityDefinition as N, MessageMetadata as Ni, AgentMessageFilter as Nn, AgentUIMessageStreamProjectionResolver as Nr, AgentDefinition as Nt, AgentCapabilityCliRunContext as O, DataPart as Oi, AgentInvokerProfile as On, AgentTriggerContext as Or, AgentChatSendMessage as Ot, AgentCapabilityHandle as P, MessagePart as Pi, AgentMessageFilterContext as Pn, AgentUsage as Pr, AgentDefinitionCliOptions as Pt, AgentChannelDeliveryFinishEffectContext as Q, isAttachmentData as Qi, AgentRegistryHandlerOptions as Qn, DiscoveredAgentDefinition as Qr, AgentHarnessSandboxProvider as Qt, AgentCapabilityMode as R, SourcePart as Ri, AgentModelExecutionOptions as Rn, AgentWaitUntil as Rr, AgentDriverContribution as Rt, AgentCapabilityCliContribution as S, ApprovalRequestPart as Si, AgentInvocationContextValues as Sn, AgentToolResolverWithWorkspace as Sr, AgentChatMessage as St, AgentCapabilityCliOutputDefinition as T, AudioData as Ti, AgentInvoker as Tn, AgentToolStandardSchema as Tr, AgentChatPlatformAdapter as Tt, AgentChannelDeliveryEffectContext as U, ToolInvocationState as Ui, AgentModuleOptions as Un, BuiltInAgentDriver as Ur, AgentFinishEvent as Ut, AgentCapabilityTypeContract as V, ToolCallPart as Vi, AgentModelInstrumentationContext as Vn, AgentWebhookStateResolver as Vr, AgentEvalOptions as Vt, AgentChannelDeliveryEffectHandler as W, ToolResultPart as Wi, AgentOutputDefinition as Wn, BuiltInAgentDriverName as Wr, AgentFinishExtensionValues as Wt, AgentChannelDeliveryFinishEffect as X, getMessageText as Xi, AgentProvidersOptions as Xn, CodexDriverSandboxOptions as Xr, AgentHarnessDriverInput as Xt, AgentChannelDeliveryEffects as Y, deserializeMessages as Yi, AgentOutputRenderer as Yn, CodexDriverOptions as Yr, AgentHarnessDriver as Yt, AgentChannelDeliveryFinishEffectCallback as Z, getToolInvocations as Zi, AgentRegistry as Zn, CustomAgentDriver as Zr, AgentHarnessInstructions as Zt, AgentCapabilitiesInput as _, AgentRunEvents as _i, AgentInspectionModelMetadata as _n, AgentStateProviderOptions as _r, AgentChatCapabilityOptions as _t, AgentAdapterFactory as a, ResolvedAgentStateProviderOptions as ai, AgentHookOutcome as an, AgentRunInputContextValues as ar, AgentChannelDeliveryStatusInput as at, AgentCapabilitiesResolverContext as b, AgentMessagePhase as bi, AgentIntegrationsOptions as bn, AgentToolPolicyDecision as br, AgentChatEventHooks as bt, AgentAdapterInstructionsValue as c, WorkspaceAgentWorkspaceOptions as ci, AgentInput as cn, AgentRuntime as cr, AgentChannelFactory as ct, AgentAdapterRunContext as d, AgentRunEventPublisher as di, AgentInspectionConfigValue as dn, AgentRuntimeContext as dr, AgentChannelTriggerContext as dt, serializeMessages as ea, MaybeResolvable as ei, AgentHarnessSessionKey as en, AgentRunCallbackContext as er, AgentChannelDeliveryReactionInput as et, AgentAttachmentExecutionOptions as f, AgentRunEventReadOptions as fi, AgentInspectionDriverMetadata as fn, AgentRuntimeHooks as fr, AgentChannelWebhookManager as ft, AgentCapabilities as g, AgentRunEventSubscribeOptions as gi, AgentInspectionModelExecutionMetadata as gn, AgentSettings as gr, AgentChatAgentHookArgs as gt, AgentCallbackContext as h, AgentRunEventStoreResolver as hi, AgentInspectionMetadata as hn, AgentSchedulerProviderOptions as hr, AgentChatAgentBindingOptions as ht, AgentAdapter as i, ResolvedAgentRuntimeContext as ii, AgentHookObserverHooks as in, AgentRunInput as ir, AgentChannelDeliveryReplyStream as it, AgentCapabilityCliStandardSchemaV1 as j, ImagePart as ji, AgentMessageConcurrency as jn, AgentTriggerRunInvokeResult as jr, AgentChatStateResolver as jt, AgentCapabilityCliStandardSchemaResultFailure as k, ErrorPart as ki, AgentInvokerResolveContext as kn, AgentTriggerDefinition as kr, AgentChatSessionOptions as kt, AgentAdapterMetadataContext as l, AgentRunEvent as li, AgentInputHook as ln, AgentRuntimeBinding as lr, AgentChannelInput as lt, AgentCallSettingsInstrumentationContext as m, AgentRunEventStoreResolveContext as mi, AgentInspectionHarnessMetadata as mn, AgentSandboxProviderOptions as mr, AgentChannels as mt, AgentAccessWorkspaceScopeContext as n, Resolvable as ni, AgentHookObserver as nn, AgentRunDriver as nr, AgentChannelDeliveryReplyInput as nt, AgentAdapterInstructions as o, ResolvedAgentTriggerDefinition as oi, AgentHookOwner as on, AgentRunMetadata as or, AgentChannelDeliveryStatusPayload as ot, AgentCallSettingsInstrumentation as p, AgentRunEventStore as pi, AgentInspectionFileTreeItem as pn, AgentRuntimeName as pr, AgentChannelWebhookRegistrationDefinition as pt, AgentChannelDeliveryEffectKind as q, collectStreamEvents as qi, AgentOutputExtensionValues as qn, ClaudeCodeThinkingConfig as qr, AgentFinishHookEvent as qt, AgentActor as r, ResolvedAgentModuleOptions as ri, AgentHookObserverEvent as rn, AgentRunHandler as rr, AgentChannelDeliveryReplyPayload as rt, AgentAdapterInstructionsPart as s, WorkspaceAgentWorkspaceConfig as si, AgentHostIdentity as sn, AgentRunResult as sr, AgentChannelDeliveryStatusState as st, AgentAccessInvocationContextValue as t, validateMessage as ta, PublishedAgentDeliveryArtifact as ti, AgentHarnessWorkDir as tn, AgentRunContext as tr, AgentChannelDeliveryReactionPayload as tt, AgentAdapterResult as u, AgentRunEventInput as ui, AgentInspectionConfigMetadata as un, AgentRuntimeConfig as ur, AgentChannelInputs as ut, AgentCapabilitiesList as v, AgentRunEventsOptions as vi, AgentInspectionToolDefinition as vn, AgentToolDefinition as vr, AgentChatErrorHookArgs as vt, AgentCapabilityCliExecutionResult as w, AttachmentPart as wi, AgentInvocationHooks as wn, AgentToolSet as wr, AgentChatOptions as wt, AgentCapabilityCliCommand as x, ApprovalDecisionPart as xi, AgentInvocationContextStore as xn, AgentToolResolver as xr, AgentChatFinishExtension as xt, AgentCapabilitiesResolver as y, defineAgentRunEvents as yi, AgentIntegrationOption as yn, AgentToolPolicyContext as yr, AgentChatEventHookArgs as yt, AgentCapabilityPhase as z, StreamEvent as zi, AgentModelInput as zn, AgentWebhookInvocationOwnership as zr, AgentDriverContributionKind as zt };
\ No newline at end of file
diff --git a/dist/vite.d.ts b/dist/vite.d.ts
index 6975e3327a5f1dcccfd1ac7f2fa673234e33fc9b..66cdb69ccaf18f1a3eb078fe80fa5e35f8f140ac 100644
--- a/dist/vite.d.ts
+++ b/dist/vite.d.ts
@@ -1,4 +1,4 @@
-import { Hn as AgentModuleOptions } from "./types-C4oyRXNr.js";
+import { Un as AgentModuleOptions } from "./types-CW2XmfE7.js";
 import { Plugin } from "vite";
 
 //#region src/vite.d.ts
diff --git a/dist/vite.js b/dist/vite.js
index d864968db5de3f5121d5f328eda2df3dbc51146e..5d6c3af5e187bd18ec996792af32c08f5f65f245 100644
--- a/dist/vite.js
+++ b/dist/vite.js
@@ -1,20 +1,15 @@
 import { Z as inheritMessageChannelInstructions } from "./capability-runtime-Be85oBbC.js";
-import { r as resolveInstructionImports } from "./instruction-composition-CBkhC2F8.js";
 import { a as streamAgentOutputToEvents } from "./agent-output-CyEZfgrQ.js";
-import { F as uiMessagesToAgentMessages } from "./channels-DNuHTrNn.js";
-import { l as runAgentInline, o as resolveAgentTriggerInvocation, p as streamAgent, s as resolveAgentTriggers, t as agentWithColocatedInstructions, y as isResolvedAgentTriggerHandledInvocation } from "./src-B3GiXaIA.js";
-import { i as withColocatedAgentSkills, r as decodeColocatedAgentSkills } from "./colocated-agent-skills-BAz3kNyV.js";
-import { a as resolveAgentInspectionMetadata, l as workspaceAgentOwnsWorkspaceDefinition, m as workspaceNameFromOptions, p as workspaceModeFromOptions, u as workspaceAgentWithSourceRoot } from "./workspace-agent-BtTRGp6h.js";
-import { n as agentInvocationStreamRoute, r as createAgentInvocationStreamResponse, t as agentInvocationStreamHeader } from "./invocation-stream-DFGK1iYd.js";
+import { F as uiMessagesToAgentMessages } from "./channels-D6wOMJN1.js";
+import { C as isResolvedAgentTriggerHandledInvocation, _ as agentInvocationStreamHeader, l as runAgentInline, o as resolveAgentTriggerInvocation, p as streamAgent, s as resolveAgentTriggers, v as agentInvocationStreamRoute, y as createAgentInvocationStreamResponse } from "./src-CktB4unw.js";
+import { a as resolveAgentInspectionMetadata, l as workspaceAgentOwnsWorkspaceDefinition, m as workspaceNameFromOptions, p as workspaceModeFromOptions } from "./workspace-agent-BtTRGp6h.js";
 import { a as installCloudflareAgentStateEntrypoint, n as defaultCloudflareAgentStateBinding, t as configureCloudflareAgentState } from "./cloudflare-vNMGeVXW.js";
-import { c as dirname$1, l as relative$1, n as resolveAgentEvalOptions, o as discoverAgentDefinitions, r as writeAgentEvaliteConfig, s as discoverAgentEvalFiles, t as removeAgentEvaliteConfig, u as resolve$1 } from "./evalite-config-D6Q9AQNQ.js";
-import { t as createAgentRuntimeContext } from "./context-KghRPYRx.js";
-import { n as withColocatedAgentHome, t as decodeColocatedAgentHome } from "./colocated-agent-home-EYZ8BwLn.js";
+import { _ as relative$1, a as createViteAgentRuntimeContext, c as writeViteResponse, d as readColocatedAgentHome, g as dirname$1, h as discoverAgentEvalFiles, i as createViteAgentDiscoveryContext, l as readColocatedAgentSkills, m as discoverAgentDefinitions, n as resolveAgentEvalOptions, o as createViteWorkspaceAgentLoader, r as writeAgentEvaliteConfig, s as loadViteAgent, t as removeAgentEvaliteConfig, u as readColocatedAgentInstructions, v as resolve$1 } from "./evalite-config-7KBtA3Zi.js";
 import { createRequire } from "node:module";
 import { access, copyFile, cp, mkdir, readFile, readdir, realpath, rm, rmdir, stat, writeFile } from "node:fs/promises";
 import { basename, dirname, isAbsolute, join, relative, resolve, sep } from "node:path";
 import { ensureWorkspaceDevToken, refreshWorkspaceDevToken, runWorkspaceDevCommand, validateWorkspaceDevToken, workspaceDevTokenServerId } from "@vite-hub/workspace/server";
-import { existsSync, lstatSync, readFileSync, readdirSync, statSync } from "node:fs";
+import { existsSync, statSync } from "node:fs";
 import { fileURLToPath, pathToFileURL } from "node:url";
 import { getWorkspaceHostedStoreLoader, setWorkspaceHostedStoreLoader, setWorkspaceRuntimeRegistry } from "@vite-hub/workspace/runtime";
 import { build } from "esbuild";
@@ -926,216 +921,6 @@ function getHostingProvider(hosting) {
 	if (preset === "cloudflare" || preset === "netlify" || preset === "vercel") return preset;
 }
 //#endregion
-//#region ../internal/src/build/colocated-agent-files.ts
-function formatSize(bytes) {
-	const mebibyte = 1024 * 1024;
-	return bytes % mebibyte === 0 ? `${bytes / mebibyte} MiB` : `${bytes} bytes`;
-}
-function isFolderAgentEntry(file) {
-	if (!/^(?:agent|index)\.(?:c|m)?[jt]s$/i.test(basename(file))) return false;
-	return !(basename(file).toLowerCase().startsWith("agent.") && basename(dirname(file)) === "agents" && basename(dirname(dirname(file))) === "server");
-}
-function readColocatedAgentFiles(handler, directoryName, options = {}) {
-	if (!isFolderAgentEntry(handler)) return;
-	const root = join(dirname(handler), directoryName);
-	if (!existsSync(root)) return;
-	if (!(options.rejectUnsupportedEntries ? lstatSync(root) : statSync(root)).isDirectory()) {
-		if (options.rejectUnsupportedEntries) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} supports regular files and directories only: ${directoryName}`);
-		return;
-	}
-	const files = {};
-	let fileCount = 0;
-	let totalSize = 0;
-	const visit = (directory) => {
-		for (const entry of readdirSync(directory, { withFileTypes: true }).sort((left, right) => left.name.localeCompare(right.name))) {
-			const file = join(directory, entry.name);
-			const target = relative(root, file).replace(/\\/g, "/");
-			if (entry.isDirectory()) {
-				visit(file);
-				continue;
-			}
-			if (!entry.isFile()) {
-				if (options.rejectUnsupportedEntries) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} supports regular files and directories only: ${target}`);
-				continue;
-			}
-			const size = statSync(file).size;
-			fileCount += 1;
-			if (options.fileCountLimit !== void 0 && fileCount > options.fileCountLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${options.fileCountLimit} files.`);
-			if (options.fileSizeLimit !== void 0 && size > options.fileSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent file"} file exceeds ${formatSize(options.fileSizeLimit)}: ${target}`);
-			totalSize += size;
-			if (options.totalSizeLimit !== void 0 && totalSize > options.totalSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${formatSize(options.totalSizeLimit)}.`);
-			const content = readFileSync(file);
-			if (content.byteLength !== size) {
-				if (options.fileSizeLimit !== void 0 && content.byteLength > options.fileSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent file"} file exceeds ${formatSize(options.fileSizeLimit)}: ${target}`);
-				totalSize += content.byteLength - size;
-				if (options.totalSizeLimit !== void 0 && totalSize > options.totalSizeLimit) throw new Error(`[vitehub] ${options.label || "Colocated Agent files"} exceeds ${formatSize(options.totalSizeLimit)}.`);
-			}
-			files[target] = {
-				content: content.toString("base64"),
-				encoding: "base64"
-			};
-		}
-	};
-	visit(root);
-	return Object.keys(files).length ? files : void 0;
-}
-//#endregion
-//#region src/vite/colocated-agent-home.ts
-const fileSizeLimit = 1024 * 1024;
-const totalSizeLimit = 4 * 1024 * 1024;
-function readColocatedAgentHome(handler) {
-	const files = readColocatedAgentFiles(handler, "home", {
-		fileCountLimit: 1024,
-		fileSizeLimit,
-		label: "Colocated Agent Home",
-		rejectUnsupportedEntries: true,
-		totalSizeLimit
-	});
-	if (!files) return;
-	return Object.fromEntries(Object.entries(files).map(([target, file]) => [target, {
-		contents: file.content,
-		encoding: file.encoding
-	}]));
-}
-//#endregion
-//#region src/vite/colocated-agent-instructions.ts
-async function readColocatedAgentInstructions(handler, options = {}) {
-	const file = join(dirname(handler), "instructions.md");
-	options.dependencies?.add(file);
-	if (!existsSync(file) || !statSync(file).isFile()) return;
-	return await resolveInstructionImports(readFileSync(file, "utf8"), {
-		file,
-		read(specifier, importer) {
-			const imported = resolve(dirname(importer), specifier);
-			options.dependencies?.add(imported);
-			return {
-				content: readFileSync(imported, "utf8"),
-				file: imported
-			};
-		}
-	});
-}
-//#endregion
-//#region src/vite/colocated-agent-skills.ts
-function readColocatedAgentSkills(handler) {
-	const files = readColocatedAgentFiles(handler, "skills");
-	if (!files) return;
-	return Object.fromEntries(Object.entries(files).map(([path, file]) => {
-		const workspacePath = `skills/${path}`;
-		return [`__vitehubAgentSkill:${workspacePath}`, {
-			...file,
-			materialize: "build",
-			mount: "",
-			workspacePath
-		}];
-	}));
-}
-//#endregion
-//#region src/vite/runtime-adapter.ts
-function isRecord$2(value) {
-	return !!value && typeof value === "object" && !Array.isArray(value);
-}
-function resolveAgentModule(module) {
-	if (isRecord$2(module) && "default" in module) return module.default;
-	return module;
-}
-function workspaceSourceRoot(file) {
-	const workspaceDirectory = join(dirname(file), "workspace");
-	return existsSync(workspaceDirectory) && statSync(workspaceDirectory).isDirectory() ? workspaceDirectory : dirname(file);
-}
-function colocatedSkills(file) {
-	return decodeColocatedAgentSkills(readColocatedAgentSkills(file));
-}
-function colocatedHome(file) {
-	return decodeColocatedAgentHome(readColocatedAgentHome(file));
-}
-async function loadViteAgent(server, definition) {
-	const agent = withColocatedAgentHome(withColocatedAgentSkills(agentWithColocatedInstructions(resolveAgentModule(await server.ssrLoadModule(pathToFileURL(definition.handler).href)), await readColocatedAgentInstructions(definition.handler)), colocatedSkills(definition.handler)), colocatedHome(definition.handler));
-	if (!agent) return;
-	return {
-		agent,
-		definition,
-		identity: {
-			name: definition.name,
-			...definition.workspace ? { workspace: definition.workspace } : {}
-		}
-	};
-}
-function createViteWorkspaceAgentLoader(server, definition) {
-	return async () => {
-		const module = await server.ssrLoadModule(pathToFileURL(definition.handler).href);
-		return {
-			...module,
-			default: withColocatedAgentHome(workspaceAgentWithSourceRoot(withColocatedAgentSkills(module.default, colocatedSkills(definition.handler)), workspaceSourceRoot(definition.handler)), colocatedHome(definition.handler))
-		};
-	};
-}
-function headersFromNode(headers) {
-	const result = new Headers();
-	for (const [name, value] of Object.entries(headers)) if (typeof value === "string") result.set(name, value);
-	else if (Array.isArray(value)) for (const item of value) result.append(name, item);
-	return result;
-}
-function createRequest(server, req, fallbackRoute) {
-	const base = server.resolvedUrls?.local?.[0] || `http://localhost:${server.config.server.port || 5173}/`;
-	return new Request(new URL(req.url || fallbackRoute, base), {
-		headers: headersFromNode(req.headers),
-		method: req.method || "GET"
-	});
-}
-function createViteAgentRuntimeContext(server, req, identity, options) {
-	return createAgentRuntimeContext({
-		agentIdentity: identity,
-		request: createRequest(server, req, options.fallbackRoute),
-		...options.run ? { run: options.run } : {},
-		runtime: "vite",
-		runtimeConfig: {},
-		waitUntil: (task) => void Promise.resolve(task).catch(() => {})
-	});
-}
-function createViteAgentDiscoveryContext(identity) {
-	return createAgentRuntimeContext({
-		agentIdentity: identity,
-		runtime: "vite",
-		runtimeConfig: {},
-		waitUntil: (task) => void Promise.resolve(task).catch(() => {})
-	});
-}
-async function writeViteResponse(res, response) {
-	res.statusCode = response.status;
-	for (const [name, value] of response.headers) res.setHeader(name, value);
-	if (!response.body) {
-		res.end();
-		return;
-	}
-	const reader = response.body.getReader();
-	let closed = false;
-	const cancel = () => {
-		if (closed) return;
-		closed = true;
-		Promise.resolve().then(() => reader.cancel()).catch(() => {});
-	};
-	res.once("close", cancel);
-	try {
-		for (;;) {
-			const { done, value } = await reader.read();
-			if (done) break;
-			res.write(value);
-		}
-		closed = true;
-		res.end();
-	} catch (error) {
-		const wasClosed = closed;
-		closed = true;
-		if (wasClosed && error instanceof Error && error.name === "AbortError") return;
-		res.destroy(error instanceof Error ? error : void 0);
-	} finally {
-		closed = true;
-		res.off("close", cancel);
-		reader.releaseLock();
-	}
-}
-//#endregion
 //#region src/vite/invocation-stream-endpoint.ts
 const capabilityCliRunSurface = Symbol.for("vitehub.capabilityCliRunSurface");
 const workspaceRegistryId = "#vitehub-workspace-registry";
@@ -3097,10 +2882,13 @@ function hubAgent(options) {
 				"./cli.js"
 );
 			if (agent === false || agent?.cli === false) return createAgentCliContributor(false);
+			const normalized = normalizeAgentOptions(agent);
 			return createAgentCliContributor({
+				configFile: resolved?.configFile,
 				eval: agent?.eval,
 				rootDir: resolved?.root ?? process.cwd(),
-				serverDirs
+				serverDirs,
+				webhookRoute: normalized ? normalized.routes.webhooks : false
 			});
 		} },
 		config(config) {
diff --git a/dist/workspace-agent-u4sFNw76.d.ts b/dist/workspace-agent-u4sFNw76.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..971b1e0dbd97d83ab4d2e22952c2f02dd91ffb35
--- /dev/null
+++ b/dist/workspace-agent-u4sFNw76.d.ts
@@ -0,0 +1,33 @@
+import { Lt as AgentDriver, Nt as AgentDefinition, On as AgentInvokerProfile, Sn as AgentInvocationContextValues, _ as AgentCapabilitiesInput, ci as WorkspaceAgentWorkspaceOptions, cn as AgentInput, dr as AgentRuntimeContext, gr as AgentSettings, hn as AgentInspectionMetadata, ii as ResolvedAgentRuntimeContext, ir as AgentRunInput, si as WorkspaceAgentWorkspaceConfig, ur as AgentRuntimeConfig } from "./types-CW2XmfE7.js";
+import { ReadonlyWorkspaceFacade, WorkspaceDefinition, WorkspaceName } from "@vite-hub/workspace";
+
+//#region src/workspace-agent.d.ts
+type WorkspaceAgentOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, _Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown, TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues, TCapabilities extends AgentCapabilitiesInput<TRuntimeConfig, _Name, CALL_OPTIONS> | undefined = AgentCapabilitiesInput<TRuntimeConfig, _Name, CALL_OPTIONS> | undefined, TOutput = unknown, TDriver extends AgentDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput> = AgentDriver<TRuntimeConfig, CALL_OPTIONS, TContextValues, TOutput>> = AgentSettings<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, TContextValues, TCapabilities, TOutput, TDriver> & {
+  name?: string;
+  workspace: WorkspaceAgentWorkspaceConfig<_Name>;
+};
+type WorkspaceAgentDefinition<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName, CALL_OPTIONS = unknown, TInvokerProfile extends AgentInvokerProfile = AgentInvokerProfile, TContextValues extends object = AgentInvocationContextValues, TCapabilities extends AgentCapabilitiesInput<TRuntimeConfig, Name, CALL_OPTIONS> | undefined = AgentCapabilitiesInput<TRuntimeConfig, Name, CALL_OPTIONS> | undefined, TOutput = unknown> = AgentDefinition<TRuntimeConfig, CALL_OPTIONS, TInvokerProfile, TContextValues, TOutput> & WorkspaceAgentWorkspaceOptions & {
+  __vitehubWorkspaceAgent: true;
+  __vitehubWorkspaceAgentOptions: WorkspaceAgentOptions<TRuntimeConfig, Name, CALL_OPTIONS, TInvokerProfile, TContextValues, TCapabilities, TOutput>;
+};
+interface WorkspaceAgentDefaults<Name extends WorkspaceName = WorkspaceName> {
+  name?: string;
+  workspace?: Name;
+}
+interface AgentInspectionMetadataResolutionOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends WorkspaceAgentDefaults<Name> {
+  input?: AgentRunInput;
+  runtime?: Partial<ResolvedAgentRuntimeContext<TRuntimeConfig>>;
+}
+interface AgentInspectionSourceMaterializationOptions<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName> extends AgentInspectionMetadataResolutionOptions<TRuntimeConfig, Name> {
+  path?: string;
+  source?: string;
+  sources?: string[];
+}
+declare function workspaceAgentOwnsWorkspaceDefinition(agent: unknown): boolean;
+declare function workspaceAgentWithSourceRoot<Agent>(agent: Agent, sourceRootDir: string, colocatedInstructions?: string): Agent;
+declare function workspaceDefinitionFromOptions<TRuntimeConfig extends AgentRuntimeConfig, Name extends WorkspaceName>(options: WorkspaceAgentOptions<TRuntimeConfig, Name>): WorkspaceAgentWorkspaceOptions;
+declare function createAgentInspectionMetadata<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName>(definition: AgentInput<AgentRuntimeContext<TRuntimeConfig>>): AgentInspectionMetadata;
+declare function resolveAgentInspectionMetadata<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName>(definition: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, defaultsOverride?: AgentInspectionMetadataResolutionOptions<TRuntimeConfig, Name>): Promise<AgentInspectionMetadata>;
+declare function materializeAgentInspectionSourceMetadata<TRuntimeConfig extends AgentRuntimeConfig = AgentRuntimeConfig, Name extends WorkspaceName = WorkspaceName>(definition: AgentInput<AgentRuntimeContext<TRuntimeConfig>>, options?: AgentInspectionSourceMaterializationOptions<TRuntimeConfig, Name>): Promise<AgentInspectionMetadata>;
+//#endregion
+export { materializeAgentInspectionSourceMetadata as a, workspaceAgentWithSourceRoot as c, createAgentInspectionMetadata as i, workspaceDefinitionFromOptions as l, WorkspaceAgentDefinition as n, resolveAgentInspectionMetadata as o, WorkspaceAgentOptions as r, workspaceAgentOwnsWorkspaceDefinition as s, WorkspaceAgentDefaults as t };
\ No newline at end of file
diff --git a/node_modules/.bin/esbuild b/node_modules/.bin/esbuild
new file mode 100755
index 0000000000000000000000000000000000000000..0614740086e303351fd97db3e9943e91b1426ed1
--- /dev/null
+++ b/node_modules/.bin/esbuild
@@ -0,0 +1,21 @@
+#!/bin/sh
+basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
+
+case `uname` in
+    *CYGWIN*|*MINGW*|*MSYS*)
+        if command -v cygpath > /dev/null 2>&1; then
+            basedir=`cygpath -w "$basedir"`
+        fi
+    ;;
+esac
+
+if [ -z "$NODE_PATH" ]; then
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/esbuild@0.27.7/node_modules/esbuild/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/esbuild@0.27.7/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules"
+else
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/esbuild@0.27.7/node_modules/esbuild/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/esbuild@0.27.7/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules:$NODE_PATH"
+fi
+if [ -x "$basedir/node" ]; then
+  exec "$basedir/node"  "$basedir/../../../../../../esbuild@0.27.7/node_modules/esbuild/bin/esbuild" "$@"
+else
+  exec node  "$basedir/../../../../../../esbuild@0.27.7/node_modules/esbuild/bin/esbuild" "$@"
+fi
diff --git a/node_modules/.bin/nft b/node_modules/.bin/nft
new file mode 100755
index 0000000000000000000000000000000000000000..22c53c855eb7945096ca9115a261fcd8f86377ef
--- /dev/null
+++ b/node_modules/.bin/nft
@@ -0,0 +1,21 @@
+#!/bin/sh
+basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
+
+case `uname` in
+    *CYGWIN*|*MINGW*|*MSYS*)
+        if command -v cygpath > /dev/null 2>&1; then
+            basedir=`cygpath -w "$basedir"`
+        fi
+    ;;
+esac
+
+if [ -z "$NODE_PATH" ]; then
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/@vercel+nft@1.10.2_rollup@4.62.2/node_modules/@vercel/nft/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/@vercel+nft@1.10.2_rollup@4.62.2/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules"
+else
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/@vercel+nft@1.10.2_rollup@4.62.2/node_modules/@vercel/nft/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/@vercel+nft@1.10.2_rollup@4.62.2/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules:$NODE_PATH"
+fi
+if [ -x "$basedir/node" ]; then
+  exec "$basedir/node"  "$basedir/../../../../../../@vercel+nft@1.10.2_rollup@4.62.2/node_modules/@vercel/nft/out/cli.js" "$@"
+else
+  exec node  "$basedir/../../../../../../@vercel+nft@1.10.2_rollup@4.62.2/node_modules/@vercel/nft/out/cli.js" "$@"
+fi
diff --git a/node_modules/.bin/vite b/node_modules/.bin/vite
new file mode 100755
index 0000000000000000000000000000000000000000..c161218e3d1d73a6561d5edef30209e6474b72ff
--- /dev/null
+++ b/node_modules/.bin/vite
@@ -0,0 +1,21 @@
+#!/bin/sh
+basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
+
+case `uname` in
+    *CYGWIN*|*MINGW*|*MSYS*)
+        if command -v cygpath > /dev/null 2>&1; then
+            basedir=`cygpath -w "$basedir"`
+        fi
+    ;;
+esac
+
+if [ -z "$NODE_PATH" ]; then
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules/vite/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules"
+else
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules/vite/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules:$NODE_PATH"
+fi
+if [ -x "$basedir/node" ]; then
+  exec "$basedir/node"  "$basedir/../../../../../../vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules/vite/bin/vite.js" "$@"
+else
+  exec node  "$basedir/../../../../../../vite@8.1.5_@types+node@25.9.2_esbuild@0.28.1_jiti@2.7.0_tsx@4.23.1_yaml@2.9.0/node_modules/vite/bin/vite.js" "$@"
+fi
diff --git a/node_modules/.bin/vitest b/node_modules/.bin/vitest
new file mode 100755
index 0000000000000000000000000000000000000000..3b305acbe25e898fbb41f9e542bd23394da30a26
--- /dev/null
+++ b/node_modules/.bin/vitest
@@ -0,0 +1,21 @@
+#!/bin/sh
+basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
+
+case `uname` in
+    *CYGWIN*|*MINGW*|*MSYS*)
+        if command -v cygpath > /dev/null 2>&1; then
+            basedir=`cygpath -w "$basedir"`
+        fi
+    ;;
+esac
+
+if [ -z "$NODE_PATH" ]; then
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules/vitest/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules"
+else
+  export NODE_PATH="/Users/maxi/vitehub/calories/node_modules/.pnpm/vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules/vitest/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules:/Users/maxi/vitehub/calories/node_modules/.pnpm/node_modules:$NODE_PATH"
+fi
+if [ -x "$basedir/node" ]; then
+  exec "$basedir/node"  "$basedir/../../../../../../vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules/vitest/vitest.mjs" "$@"
+else
+  exec node  "$basedir/../../../../../../vitest@4.1.4_@opentelemetry+api@1.9.0_@types+node@25.9.2_vite@8.1.5_@types+node@25.9.2__1fd3ec194298584d8e56dd48e470b49b/node_modules/vitest/vitest.mjs" "$@"
+fi
