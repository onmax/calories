diff --git a/dist/definition-config-DkdJxJeF.js b/dist/definition-config-DkdJxJeF.js
index c450e5771f234d547d688818df2750f0d1d013a1..f02ae10d6e6d054c35e8e48b55d95b64fb1844be 100644
--- a/dist/definition-config-DkdJxJeF.js
+++ b/dist/definition-config-DkdJxJeF.js
@@ -9,7 +9,12 @@ function defaultUrl(name) {
 	return `file:.vitehub/data/database/${name}.sqlite.db`;
 }
 function runtimeConfig(definition) {
-	const cloudflare = definition.cloudflare ?? definitionDefaults.cloudflare;
+	const remote = typeof process === "object" && process.env.VITEHUB_DATABASE_REMOTE === "true";
+	const cloudflare = definition.cloudflare || definitionDefaults.cloudflare ? {
+		...definitionDefaults.cloudflare,
+		...definition.cloudflare,
+		...remote ? { http: true } : {}
+	} : void 0;
 	return {
 		...cloudflare ? { cloudflare: {
 			...cloudflare,
diff --git a/dist/nuxt.js b/dist/nuxt.js
index 042fad1cdb9f35beb9d8783bb64b1dcb3850d461..d7b40981bb65743c80a040797c3eb8769f62b6cd 100644
--- a/dist/nuxt.js
+++ b/dist/nuxt.js
@@ -89,6 +89,7 @@ function resolveDatabaseViteOptions(options) {
 		viteOptions.local = options.local;
 		viteOptions.migrationsTable = options.migrationsTable;
 		viteOptions.previewDatabaseId = options.previewDatabaseId;
+		viteOptions.remote = options.remote;
 	}
 	return Object.keys(viteOptions).length ? viteOptions : void 0;
 }
diff --git a/dist/runtime/definition-local.js b/dist/runtime/definition-local.js
index 276aac62cfd3631c298226827f95564199e979db..d4e5358e43b44304e03c73fbf234bee6c5368ac0 100644
--- a/dist/runtime/definition-local.js
+++ b/dist/runtime/definition-local.js
@@ -12,6 +12,7 @@ function resolveLocalUrl(url) {
 }
 function createDefinitionRuntime(definition) {
 	return createDrizzleSqliteAdapter(runtimeConfig(definition), definition.schema, {
+		cloudflareD1Http: true,
 		libsql: {
 			createClient,
 			drizzle
diff --git a/dist/types-nPF7IReI.d.ts b/dist/types-nPF7IReI.d.ts
index be7782a21685f630b998396e63e3a10a3b227eff..a82d10b7ee10dab4de6ad7ecbc084e77841c2671 100644
--- a/dist/types-nPF7IReI.d.ts
+++ b/dist/types-nPF7IReI.d.ts
@@ -43,6 +43,7 @@ interface DatabaseRuntimeD1Options {
   local?: DatabaseLocalRuntimeOptions;
   migrationsTable?: string;
   previewDatabaseId?: DatabaseConfigValue;
+  remote?: boolean;
 }
 interface DatabaseIntegrationOptions {
   cli?: false | {
diff --git a/dist/vite-BhybkS79.js b/dist/vite-BhybkS79.js
index 072d46f2ff52cad05394c0536ca000ee9ba645f8..981754f0774f5e464bd9ce19019c24c1be3e43b2 100644
--- a/dist/vite-BhybkS79.js
+++ b/dist/vite-BhybkS79.js
@@ -1391,8 +1391,10 @@ function hubDb(options) {
 				provision
 			};
 		} },
-		config(config) {
+		config(config, environment) {
 			serverDirs = config["__vitehubServerDirs"] ?? serverDirs;
+			const database = config.database ?? options;
+			if (environment.command === "serve" && database && typeof database === "object" && database.remote === true) process.env.VITEHUB_DATABASE_REMOTE = "true";
 		},
 		async configResolved(config) {
 			resolved = config;
@@ -1434,10 +1436,13 @@ function hubDb(options) {
 		load(id) {
 			if (id === RESOLVED_DB_VIRTUAL_SCHEMA_ID) return renderSchemaModule(runtimeConfig);
 			if (id === RESOLVED_DB_VIRTUAL_DATABASES_ID) return renderDatabasesModule(runtimeConfig);
-			if (id === RESOLVED_DB_VIRTUAL_DEFINITION_DEFAULTS_ID) {
+			if (id === RESOLVED_DB_VIRTUAL_DEFINITION_DEFAULTS_ID || normalize(id).endsWith("/@vite-hub/database/dist/runtime/definition-defaults.js")) {
 				const options = resolvedOptions();
 				return `export default ${JSON.stringify({
-					...options && options.driver === "d1" ? { cloudflare: { binding: options.binding } } : {},
+					...options && options.driver === "d1" ? { cloudflare: {
+						binding: options.binding,
+						...options.remote === true && resolved?.command === "serve" ? { http: true } : {}
+					} } : {},
 					...options && options.connection ? { connection: options.connection } : {}
 				})}\n`;
 			}
